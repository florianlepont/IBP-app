# Deferred Items — Phase 01

Out-of-scope discoveries logged during execution, not fixed (scope boundary: only fix issues
directly caused by the current task's changes).

## Plan 01-01

- **Pre-existing `format:check` failures, unrelated to this plan.** `npm run format:check` reports
  5 files with formatting issues, all under `.planning/intel/classifications/*.json`. These were
  committed in `de92d03 docs: ingest 22 docs from docs/ and bootstrap .planning`, predate this
  phase, and are untouched by plan 01-01's changes. Not fixed here — out of scope.
