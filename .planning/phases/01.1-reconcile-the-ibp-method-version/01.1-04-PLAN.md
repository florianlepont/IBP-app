---
phase: 01.1-reconcile-the-ibp-method-version
plan: 04
type: execute
wave: 2
depends_on: [01.1-01, 01.1-02, 01.1-03]
files_modified:
  - CLAUDE.md
  - docs/README.md
  - .planning/ROADMAP.md
  - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh
  - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md
autonomous: true
requirements: [REQ-QA-ibp-version]

must_haves:
  truths:
    - "ADR-003 and the comparison document are listed in CLAUDE.md's documentation tree and in docs/README.md (D-05), and CLAUDE.md's IBP domain section says which method version the app implements and which it targets"
    - "The ROADMAP phase 01.8 entry names its input from phase 01.1: ADR-003's change list CH-1..CH-11 including the method-version dispatch (CH-6), with CH-12 left to phase 2; 01.8's success criteria are not rewritten"
    - "Every D-08 check passes mechanically: all ten factors plus the genus list have a section; every difference row has API, mobile and matrix impact; ADR-003 has a Decision line and the D-07 statement; both citation files name the implemented version; relative links in the touched docs resolve"
    - "No code, package or PDF file changed in the phase, and lint, typecheck, unit tests and format:check pass (D-08)"
    - "01.1-VALIDATION.md records each criterion, the command that proves it and its result, plus the open owner items (production SQL, '/10' display decision, review of the autonomous decision)"
  artifacts:
    - path: .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh
      provides: "Re-runnable D-08 document checks for the phase"
      contains: "ALL DOC CHECKS PASSED"
    - path: .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md
      provides: "Phase gate record"
      contains: "status: validated"
  key_links:
    - from: "CLAUDE.md documentation index"
      to: "docs/technical/adr-003-ibp-method-version-v1.md"
      via: "tree entry under technical/"
      pattern: "adr-003-ibp-method-version-v1\\.md"
    - from: ".planning/ROADMAP.md Phase 01.8"
      to: "docs/technical/adr-003-ibp-method-version-v1.md"
      via: "'Input from Phase 01.1' line"
      pattern: "Input from Phase 01\\.1"
---

<objective>
Index the new documents, hand phase 01.8 its input in the ROADMAP, then run the phase gate (D-08) and record it in 01.1-VALIDATION.md.

Purpose: D-05 requires ADR-003 to be indexed in CLAUDE.md and docs/README.md. The ROADMAP line makes sure 01.8's planner consumes CH-1..CH-11 and the version dispatch (the orchestrator asked the planner to decide; it belongs here because 01.8 is planned from the ROADMAP, and the line is only a pointer, not a criteria change). The gate proves every ROADMAP 01.1 criterion and that no code regressed.
Output: two index edits, one ROADMAP line, a re-runnable check script and the validation record. STATE.md is not edited.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-CONTEXT.md
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-RESEARCH.md
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-01-SUMMARY.md
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-02-SUMMARY.md
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-03-SUMMARY.md

<interfaces>
CLAUDE.md facts: "## Documentation index" has a fenced tree; under `├── technical/` the last two entries are `│   ├── adr-002-on-device-species-recognition-v1.md      # Species recognition go/no-go (Phase 1)` and `│   └── species-recognition-spike-measurements-v1.md     # Spike evidence log for ADR-002`. The "### IBP domain" section ends with "Factor validation matrix: `docs/technical/ibp-validation-matrix-v1.md`".
docs/README.md: bullet "`docs/technical/`" with the line "Technical contracts, ADRs, validation rules, architecture and sync behavior."
ROADMAP Phase 01.8 entry ("### Phase 01.8: Shared IBP domain package and test completeness (INSERTED)") has lines **Goal**, **Depends on** ("… Should follow Phase 01.1, so the extracted rules are the ratified method version."), **Requirements**, **Source**, then **Success Criteria**.

Phase base commit for "no code changed": `a674354` (docs(01.1): research). Code/package paths: `api/`, `mobile/`, `packages/`, `package.json`, `package-lock.json`, `.github/`, `infra/`.

Check script (`01.1-doc-checks.sh`, bash, `set -u`, run from the repo root, accumulates failures and prints `FAIL: <reason>` lines, exits 1 if any failed, otherwise prints `ALL DOC CHECKS PASSED`). It implements RESEARCH "Deliverable checks" plus the stronger per-plan checks:
1. Comparison: headings `^##+ .*Factor X\b` for A..J, a genus heading, a global heading; the 33 genera; the v3.2 SHA-256; at least 56 rows matching `^\| *(CLS|A|B|CD|E|F|G|H|I|J|GS)-[0-9]+ *\|`; the awk seven-non-empty-cells check on those rows plus BUG-/DRIFT- rows; the awk "p. N in the v3.2 cell" check; all 17 original MAT IDs; no «…» quote of 120+ characters.
2. ADR-003: a line matching `^\*\*Decision:\*\*`; "method version", "submitted", "drafts", "prepareUpsert"; CH-1..CH-12; the sql block has no write/DDL keyword.
3. Criterion 5, per file (`docs/references/README.md`, `docs/specs/ibp-form-spec.md`): a line with v3.0 and "implement"; "ADR-003"; the v3.2 URL; no "implements/conformant … v3.2" sentence.
4. Matrix: the 17 rows of `git show a674354:docs/technical/ibp-validation-matrix-v1.md` still present verbatim; the "v3.2 target cases (not implemented yet)" heading.
5. Indexes: `adr-003-ibp-method-version-v1.md` and `ibp-version-comparison-v3.0-v3.2.md` in CLAUDE.md and docs/README.md; "Input from Phase 01.1" in ROADMAP.
6. Spike doc §2 page fixes (no "(p. 2", "(p. 9", "p. 9–10" left in §2).
7. Relative Markdown links in the touched docs resolve: for each of the files above plus ADR-002 and the spike doc, extract `](target)` links that do not start with `http`, `mailto:` or `#`, strip any `#anchor`, and test that the path exists relative to the file's directory (a short `node -e` or `grep -o` + `test -e` loop).
8. Hygiene: `git diff --name-only a674354 HEAD -- api mobile packages package.json package-lock.json .github infra` is empty; `git ls-files '*.pdf'` is empty; STATE.md is not in `git diff --name-only a674354 HEAD` for commits made by this phase's plans (report only; the orchestrator may touch it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Index ADR-003 and the comparison; ROADMAP input line for phase 01.8</name>
  <files>CLAUDE.md, docs/README.md, .planning/ROADMAP.md</files>
  <read_first>
    - CLAUDE.md lines 225-240 and 360-380
    - docs/README.md
    - .planning/ROADMAP.md (Phase 01.8 entry only, found with grep "Phase 01.8:")
    - docs/technical/adr-003-ibp-method-version-v1.md (the Decision line and the change-list section, to word the pointers)
  </read_first>
  <action>Per D-05: in CLAUDE.md's documentation tree insert, between the adr-002 line and the spike line, `│   ├── adr-003-ibp-method-version-v1.md               # IBP method version: v3.0 implemented, v3.2 adopted (Phase 1.1)` and `│   ├── ibp-version-comparison-v3.0-v3.2.md            # IBP Fr v3.0 vs FR v3.2, factor by factor`, aligning the `#` comments with the neighbouring lines as closely as the tree allows. In CLAUDE.md's "### IBP domain" section, after the "Factor validation matrix" line, add one line: "Method version: the app implements IBP Fr v3.0; ADR-003 (`docs/technical/adr-003-ibp-method-version-v1.md`) adopts IBP FR v3.2, implemented in phase 01.8 (comparison: `docs/technical/ibp-version-comparison-v3.0-v3.2.md`)." Change nothing else in CLAUDE.md. In docs/README.md, under the `docs/technical/` bullet, add a sentence naming ADR-003 (IBP method version decision) and the v3.0/v3.2 comparison with relative links (`technical/adr-003-ibp-method-version-v1.md`, `technical/ibp-version-comparison-v3.0-v3.2.md`). In .planning/ROADMAP.md, Phase 01.8 entry only, insert after the **Source** line a line beginning `**Input from Phase 01.1**:` saying: ADR-003 adopts IBP FR v3.2; 01.8 implements change list CH-1..CH-11 from `docs/technical/adr-003-ibp-method-version-v1.md` (CH-12, the genus list, is phase 2), including the method-version dispatch (CH-6: missing version means v3.0, so identical replays of submitted v3.0 surveys stay valid), then flips the citation files to "implemented: v3.2" and produces `ibp-validation-matrix-v2.md` (CH-10). Do not change 01.8's Goal, Requirements or Success Criteria, the phase 01.1 entry (the orchestrator maintains its plan list), or STATE.md.</action>
  <verify>
    <automated>cd /home/user/cortege && grep -q "adr-003-ibp-method-version-v1.md" CLAUDE.md && grep -q "ibp-version-comparison-v3.0-v3.2.md" CLAUDE.md && grep -q "^Method version: the app implements IBP Fr v3.0" CLAUDE.md && grep -q "technical/adr-003-ibp-method-version-v1.md" docs/README.md && grep -q "technical/ibp-version-comparison-v3.0-v3.2.md" docs/README.md && sed -n '/^### Phase 01.8:/,/^### Phase 01.9:/p' .planning/ROADMAP.md | grep -q '^\*\*Input from Phase 01.1\*\*:.*CH-6' && test "$(git diff --numstat a674354 -- CLAUDE.md | awk '{print $2}')" -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md gains exactly three lines and loses none (numstat deletions = 0)
    - docs/README.md links both new documents
    - The 01.8 entry has the "Input from Phase 01.1" line naming CH-1..CH-11 and CH-6; its criteria are unchanged (`git diff .planning/ROADMAP.md` shows one added line)
  </acceptance_criteria>
  <done>The new docs are discoverable from both indexes and phase 01.8 knows what it must implement.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate: document checks, full suite, 01.1-VALIDATION.md</name>
  <files>.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh, .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-RESEARCH.md lines 676-692 and 737-764 (deliverable checks, validation architecture)
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md lines 1-40 (frontmatter and layout to mirror)
    - the three wave-1 SUMMARY files (deviations to report)
  </read_first>
  <action>Write `01.1-doc-checks.sh` implementing checks 1-8 of the interfaces block (make it executable; bash only, no new dependency) and run it from the repo root. If a check fails because a deliverable is wrong, fix the deliverable (the smallest edit in the plan-01/02/03 file concerned, same rules as that plan) and re-run; if the check itself is wrong, fix the script and say why in the SUMMARY. Then run the full suite `npm run lint && npm run typecheck && npm run test:unit && npm run format:check` and note the API and mobile test counts. Write `01.1-VALIDATION.md` with frontmatter `phase: 01.1`, `slug: reconcile-the-ibp-method-version`, `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, `created: 2026-09-26`, then: Test infrastructure (doc checks script + Jest suites; no E2E needed because no API code changed); a table mapping ROADMAP criteria 1-5, D-05 and D-08 to the command and its result; the D-08 checklist ticked with evidence; the suite results; the no-code-change and no-PDF evidence; "Manual-only: none (D-08: Claude-only verification)"; and "Owner items (not verification gaps)": review ADR-003's autonomous decision, run the read-only production SQL in ADR-003 so its D-07 numbers can be filled in, decide the "/10" score display (BUG-3, CH-11), optionally ask CNPF about Ficus, Pistacia and the linear C/D table.</action>
  <verify>
    <automated>cd /home/user/cortege && bash .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh | tail -1 | grep -qx "ALL DOC CHECKS PASSED" && npm run lint >/dev/null 2>&1 && npm run typecheck >/dev/null 2>&1 && npm run test:unit >/dev/null 2>&1 && npm run format:check >/dev/null 2>&1 && grep -q "^status: validated" .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md && test -z "$(git diff --name-only a674354 HEAD -- api mobile packages package.json package-lock.json .github infra)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The check script prints `ALL DOC CHECKS PASSED` and exits 0
    - lint, typecheck, test:unit and format:check all exit 0
    - 01.1-VALIDATION.md maps every ROADMAP 01.1 criterion to a passing command and lists the owner items
    - No file under api/, mobile/, packages/, .github/, infra/ or the root package files changed since a674354; no PDF is tracked
  </acceptance_criteria>
  <done>The phase is verified by re-runnable checks and the gate record exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs → phase 01.8 planner | The ROADMAP line steers what 01.8 builds |
| Repository → CI / VPS deployment | A change under api/** on main would rebuild and redeploy the API image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-09 | Tampering | ROADMAP 01.8 entry | mitigate | Only one added pointer line; criteria untouched (acceptance check on the diff) |
| T-01.1-10 | Denial of Service | Accidental code/package change triggering an image rebuild and VPS restart | mitigate | Gate asserts an empty diff since a674354 for api/, mobile/, packages/, package files, .github/, infra/ |
| T-01.1-11 | Information Disclosure | CNPF PDF committed | mitigate | Gate asserts `git ls-files '*.pdf'` is empty |
</threat_model>

<verification>
- `bash .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh` prints ALL DOC CHECKS PASSED
- Full suite green; VALIDATION.md written
</verification>

<success_criteria>
All five ROADMAP 01.1 criteria and D-05/D-08 are proven by re-runnable commands; the new docs are indexed; phase 01.8 has its input pointer; nothing outside documentation changed.
</success_criteria>

<output>
Create `.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-04-SUMMARY.md` when done
</output>
