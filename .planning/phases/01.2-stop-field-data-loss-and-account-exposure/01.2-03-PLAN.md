---
phase: 01.2-stop-field-data-loss-and-account-exposure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/package.json
  - package-lock.json
  - mobile/src/hooks/render-hook-smoke.test.ts
  - mobile/src/hooks/auth-errors.ts
  - mobile/src/hooks/auth-errors.test.ts
  - mobile/src/app/id-token.ts
  - mobile/src/app/id-token.test.ts
  - mobile/src/hooks/useAuth0Session.ts
  - mobile/src/hooks/useAuth0Session.test.ts
autonomous: true
requirements: [REQ-AUD-session-data-loss]

must_haves:
  truths:
    - "A NO_NETWORK, timeout, unknown or non-SDK error from getCredentials never ends the session, at launch or mid-session"
    - "Only NO_REFRESH_TOKEN, RENEW_FAILED while the device is online, and NO_CREDENTIALS at restore end the session"
    - "A 401 from the API triggers exactly one retry with getCredentials(undefined, undefined, undefined, true)"
    - "The hook exposes the Auth0 sub (and email) of the current session, decoded from the ID token, for the local-data owner check"
    - "renderHook from @testing-library/react-native runs under the existing mobile Jest config"
  artifacts:
    - path: mobile/src/hooks/auth-errors.ts
      provides: "Error constants and classifyCredentialsError"
      exports: ["AUTH_REQUIRED_ERROR", "AUTH_TEMPORARILY_UNAVAILABLE_ERROR", "classifyCredentialsError", "isAuthRequiredError", "isAuthTemporarilyUnavailableError"]
    - path: mobile/src/app/id-token.ts
      provides: "extractIdTokenClaims"
      exports: ["extractIdTokenClaims", "IdTokenClaims"]
    - path: mobile/src/hooks/useAuth0Session.test.ts
      provides: "renderHook regression tests for M-C1"
  key_links:
    - from: mobile/src/hooks/useAuth0Session.ts
      to: mobile/src/hooks/auth-errors.ts
      via: "classifyCredentialsError in getValidAccessToken AND in the restore effect"
      pattern: "classifyCredentialsError\\("
    - from: mobile/src/hooks/useAuth0Session.ts
      to: "react-native-auth0 credentialsManager.getCredentials"
      via: "forceRefresh as 4th positional argument on 401"
      pattern: "getCredentials\\(undefined, undefined, undefined, true\\)"
---

<objective>
Fix the root cause of audit M-C1 (lot L1) inside `useAuth0Session`: both call sites that swallow `CredentialsManagerError` (the mid-session `getValidAccessToken` and the launch-time restore effect) classify the error per D-01, network/timeout/unknown errors become "retry later" instead of "session over", and a 401 forces a real token refresh. Install the test tooling of D-13 and prove `renderHook` works in this Jest setup.

Purpose: REQ-AUD-session-data-loss — a session error must never be able to reach the purge path.
Output: RNTL + test-renderer in the mobile workspace, pure `auth-errors` and `id-token` helpers, reworked `useAuth0Session`, renderHook regression tests.

Scope boundary: this plan keeps every key of the hook's current return object (consumers are updated in plan 06). It does not touch `useSurveySync.ts` or any `survey-sync/*` file.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-CONTEXT.md
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-RESEARCH.md
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-PATTERNS.md
@mobile/src/hooks/useAuth0Session.ts
@mobile/jest.unit.config.js

<interfaces>
react-native-auth0 5.4.1 (verified in node_modules):
- Main entry exports `CredentialsManagerError`, `CredentialsManagerErrorCodes`, default `Auth0`. `Auth0User` is NOT exported.
- `CredentialsManagerError extends AuthError` with `readonly type: string` (normalized code), plus `status`, `code`, `json`.
- Codes: INVALID_CREDENTIALS, NO_CREDENTIALS, NO_REFRESH_TOKEN, RENEW_FAILED, STORE_FAILED, REVOKE_FAILED, LARGE_MIN_TTL, CREDENTIAL_MANAGER_ERROR, BIOMETRICS_FAILED, NO_NETWORK, API_ERROR, API_EXCHANGE_FAILED, INCOMPATIBLE_DEVICE, CRYPTO_EXCEPTION, UNKNOWN_ERROR.
- SDK map: `invalid_grant`, `invalid_refresh_token`, iOS `renewFailed` → RENEW_FAILED; `temporarily_unavailable` → NO_NETWORK. iOS has no separate network code for renew failures, so a network failure during refresh on iOS surfaces as RENEW_FAILED — hence the online check below.
- `credentialsManager.getCredentials(scope?, minTtl?, parameters?, forceRefresh?): Promise<Credentials>`; `Credentials { idToken: string; accessToken: string; tokenType; expiresAt; refreshToken?; scope? }`.
- `credentialsManager.hasValidCredentials(minTtl?)`, `saveCredentials(c)`, `clearCredentials()`; `webAuth.authorize(...)`, `webAuth.clearSession()`.

Existing helpers:
- mobile/src/hooks/survey-sync/utils.ts: `export const isOnlineNetworkState = (state: Network.NetworkState): boolean => Boolean(state.isConnected) && (state.isInternetReachable ?? true)`
- expo-network: `getNetworkStateAsync(): Promise<NetworkState>`
- mobile/src/api/client.ts: `ApiError` with `status`.

@testing-library/react-native 14.0.1 (inspected from the published tarball):
- `renderHook` is ASYNC: `const { result, rerender, unmount } = await renderHook(() => useX(params))`; `act` is exported and should be awaited: `await act(async () => { ... })`.
- `@testing-library/react-native` and `/pure` both require "react-native" at import time (helpers/accessibility.js). The mobile Jest config uses preset ts-jest, testEnvironment node, and does NOT map react-native, so every renderHook test file must declare `jest.mock("react-native", () => ({ ... }))` with the members the code under test touches (e.g. `Alert: { alert: jest.fn() }`, `Platform: { OS: "ios", select: (o: Record<string, unknown>) => o.ios }`, `StyleSheet: { flatten: (s: unknown) => s }`).
- Peer deps: react >=19 (19.2.3 installed), react-native >=0.78 (0.86.3), test-renderer ^1.0.0.

Package legitimacy (01.2-RESEARCH.md "Package Legitimacy Audit"): `@testing-library/react-native` [OK] Approved; `test-renderer` [OK] Approved. No [ASSUMED]/[SUS]/[SLOP] package → no blocking legitimacy checkpoint required.

Current hook facts: `getValidAccessToken` (lines 84-95) returns null on ANY error; `withAuthRetry` (97-114) re-calls it on 401 without forceRefresh; restore effect catch (182-187) calls `clearSession()` on ANY error. `clearSession` calls `onSessionCleared` (which today purges local data — removed in plan 06, not here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install RNTL + test-renderer in the mobile workspace and prove renderHook runs</name>
  <files>mobile/package.json, package-lock.json, mobile/src/hooks/render-hook-smoke.test.ts</files>
  <read_first>
    - mobile/package.json (devDependencies block)
    - mobile/jest.unit.config.js (preset ts-jest, testEnvironment node, testMatch **/*.test.ts, __DEV__ global) — do NOT edit it (Phase 1.3 owns jest config, RESEARCH Pitfall 4)
    - package.json at repo root (must not gain these packages)
    - .planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-RESEARCH.md section "Package Legitimacy Audit"
  </read_first>
  <action>
    From the repo root run exactly `npm install --workspace mobile --save-dev @testing-library/react-native@14.0.1 test-renderer@1.3.0` (D-13: mobile workspace only, never root). Do not run slopcheck install (it mutates the root lockfile). Confirm root package.json dependencies/devDependencies are unchanged.

    Create mobile/src/hooks/render-hook-smoke.test.ts that establishes the recipe every later renderHook test copies: a header comment explaining (1) renderHook/act are async in RNTL 14, (2) "react-native" must be jest.mock'ed with a factory because the ts-jest node environment cannot parse the real package, (3) import from "@testing-library/react-native". Declare `jest.mock("react-native", () => ({ Platform: { OS: "ios", select: (o: Record<string, unknown>) => o.ios }, StyleSheet: { flatten: (s: unknown) => s } }))`. Test a tiny inline hook using useState + useEffect: after `await renderHook(...)` the effect-set value is visible in `result.current`; after `await act(async () => result.current.increment())` the value increments. If importing the main entry fails because of its matcher side effects, switch the import to "@testing-library/react-native/pure" and document that in the header comment — later tasks must use the same entry point.
  </action>
  <verify>
    <automated>npm --workspace mobile run test:unit -- render-hook-smoke</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./mobile/package.json');process.exit(p.devDependencies['@testing-library/react-native']&&p.devDependencies['test-renderer']?0:1)"` exits 0 (run from repo root)
    - `node -e "const p=require('./package.json');const all={...p.dependencies,...p.devDependencies};process.exit(all['@testing-library/react-native']||all['test-renderer']?1:0)"` exits 0
    - `grep -c "node_modules/@testing-library/react-native\"" package-lock.json` returns at least 1
    - `npm --workspace mobile run test:unit -- render-hook-smoke` exits 0 with 2 passing tests
    - `git diff --quiet mobile/jest.unit.config.js` exits 0
  </acceptance_criteria>
  <done>renderHook works in the current mobile Jest setup and the recipe is documented in a committed test file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure helpers — credentials error classification and ID-token claims (TDD)</name>
  <files>mobile/src/hooks/auth-errors.ts, mobile/src/hooks/auth-errors.test.ts, mobile/src/app/id-token.ts, mobile/src/app/id-token.test.ts</files>
  <read_first>
    - mobile/src/hooks/useAuth0Session.ts (AUTH_REQUIRED_ERROR constant, isUnauthorizedError)
    - node_modules/react-native-auth0/lib/typescript/src/core/models/CredentialsManagerError.d.ts (type field, codes)
    - mobile/src/hooks/operation-status.test.ts (small pure-function test template)
    - mobile/src/hooks/useGpsCapture.test.ts (jest.mock factory + "mock" prefix hoisting pattern)
  </read_first>
  <behavior>
    classifyCredentialsError(error, { phase, isOnline }) — tests mock "react-native-auth0" with a CredentialsManagerError class carrying `type`:
    - NO_NETWORK (any phase, online or not) → "retry-later"
    - RENEW_FAILED + isOnline true → "session-ended"; RENEW_FAILED + isOnline false → "retry-later"
    - NO_REFRESH_TOKEN → "session-ended" (both phases, online or not)
    - NO_CREDENTIALS + phase "restore" → "session-ended"; NO_CREDENTIALS + phase "refresh" → "retry-later"
    - INVALID_CREDENTIALS, API_ERROR, UNKNOWN_ERROR, CRYPTO_EXCEPTION, STORE_FAILED → "retry-later"
    - plain `new Error("Network request timed out")`, a string, undefined → "retry-later"
    isAuthRequiredError(new Error("AUTH_REQUIRED")) === true; isAuthTemporarilyUnavailableError(new Error("AUTH_TEMPORARILY_UNAVAILABLE")) === true; both false for other errors and non-Errors.
    extractIdTokenClaims:
    - a token built in the test as base64url(header) + "." + base64url(JSON {sub:"auth0|abc", email:"a@b.fr"}) + ".sig" → { sub: "auth0|abc", email: "a@b.fr" }
    - payload with a non-ASCII email ("éa@b.fr") decodes correctly (UTF-8)
    - payload without email → { sub, email: null }
    - missing sub, empty sub, non-JSON payload, token with fewer than 3 segments, "", null, undefined → null
  </behavior>
  <action>
    Write both test files first (RED), then implement (GREEN).

    mobile/src/hooks/auth-errors.ts: export `AUTH_REQUIRED_ERROR = "AUTH_REQUIRED"` and `AUTH_TEMPORARILY_UNAVAILABLE_ERROR = "AUTH_TEMPORARILY_UNAVAILABLE"`; export `type CredentialsFailureKind = "session-ended" | "retry-later"`; export `classifyCredentialsError(error: unknown, context: { phase: "restore" | "refresh"; isOnline: boolean }): CredentialsFailureKind` implementing exactly D-01: only NO_REFRESH_TOKEN, RENEW_FAILED and (restore phase only) NO_CREDENTIALS end the session; everything else, including every non-CredentialsManagerError, is "retry-later". Refinement documented in a comment: RENEW_FAILED ends the session only when `isOnline` is true, because iOS reports a network failure during refresh as RENEW_FAILED (the SDK has no iOS network code) and D-01 classes network errors as retry-later. Use `error instanceof CredentialsManagerError` and compare `error.type` against `CredentialsManagerErrorCodes.*` (no message string matching). Export `isAuthRequiredError(error: unknown): boolean` and `isAuthTemporarilyUnavailableError(error: unknown): boolean` comparing `error instanceof Error && error.message === <constant>`.

    mobile/src/app/id-token.ts: export `type IdTokenClaims = { sub: string; email: string | null }`; export `extractIdTokenClaims(idToken: string | null | undefined): IdTokenClaims | null` — split on ".", require at least 3 segments, convert the payload segment from base64url to base64 (replace "-"→"+", "_"→"/", pad with "=" to a multiple of 4), decode with global `atob` (available in Hermes/RN 0.86 and Node 20), turn the binary string into UTF-8 via percent-encoding each char code then decodeURIComponent, JSON.parse, and return `{ sub, email }` only when sub is a non-empty string (email = string or null). Wrap everything in try/catch returning null. Comment: claims are read without signature verification on purpose — the token comes from the Auth0 SDK's secure store and is used only to label which account owns local data, never for authorization.
  </action>
  <verify>
    <automated>npm --workspace mobile run test:unit -- auth-errors id-token</automated>
  </verify>
  <acceptance_criteria>
    - `npm --workspace mobile run test:unit -- auth-errors id-token` exits 0 with at least 18 passing tests
    - `grep -c "CredentialsManagerErrorCodes.RENEW_FAILED" mobile/src/hooks/auth-errors.ts` returns 1 and `grep -c "CredentialsManagerErrorCodes.NO_REFRESH_TOKEN" mobile/src/hooks/auth-errors.ts` returns 1
    - `grep -c "\.message\.\(includes\|match\)\|RegExp\|/.*network.*/i" mobile/src/hooks/auth-errors.ts` returns 0 (no message string matching)
    - `grep -c "atob(" mobile/src/app/id-token.ts` returns 1
  </acceptance_criteria>
  <done>Classification and claim extraction are pure, fully tested, and ready for the hook.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Classify both credential call sites in useAuth0Session, force refresh on 401, expose session owner</name>
  <files>mobile/src/hooks/useAuth0Session.ts, mobile/src/hooks/useAuth0Session.test.ts</files>
  <read_first>
    - mobile/src/hooks/useAuth0Session.ts (entire file — getValidAccessToken 84-95, withAuthRetry 97-114, restore effect 138-194, login/register/forgot 196-305, logout 307-317, refreshSessionTokens 319-326, return 328-350)
    - mobile/src/hooks/render-hook-smoke.test.ts (renderHook recipe from Task 1)
    - mobile/src/hooks/auth-errors.ts and mobile/src/app/id-token.ts (Task 2)
    - mobile/src/hooks/survey-sync/utils.ts (isOnlineNetworkState)
    - mobile/src/app/auth0-config.ts (constants the hook imports; mock in the test if it reads env at import)
    - mobile/src/hooks/useGpsCapture.test.ts (mock-hoisting conventions)
  </read_first>
  <behavior>
    renderHook tests (mock "react-native-auth0" default class with credentialsManager {getCredentials, hasValidCredentials, saveCredentials, clearCredentials} and webAuth {authorize, clearSession}, plus CredentialsManagerError/CredentialsManagerErrorCodes; mock "expo-network" getNetworkStateAsync; mock "../api/ibp-api" getMyProfile; mock "react-native"):
    - restore: hasValidCredentials true, getCredentials rejects NO_NETWORK → onSessionCleared never called, sessionRestoring becomes false
    - restore: RENEW_FAILED with network offline → onSessionCleared never called
    - restore: RENEW_FAILED with network online → onSessionCleared called once
    - restore: NO_CREDENTIALS → onSessionCleared called once
    - restore: getCredentials rejects plain Error("timeout") → onSessionCleared never called
    - restore success with idToken for sub "auth0|abc" → result.current.sessionOwner equals { sub: "auth0|abc", email: ... }
    - withAuthRetry: getCredentials rejects NO_NETWORK → rejects with message "AUTH_TEMPORARILY_UNAVAILABLE", onSessionCleared not called
    - withAuthRetry: getCredentials rejects RENEW_FAILED while online → rejects with message "AUTH_REQUIRED"
    - withAuthRetry: operation rejects ApiError(401) once → getCredentials second call args are (undefined, undefined, undefined, true) and the operation's second call receives the refreshed token
    - withAuthRetry: operation rejects a non-401 error → rethrown, no forced refresh
  </behavior>
  <action>
    Tests first in mobile/src/hooks/useAuth0Session.test.ts using the Task 1 recipe (await renderHook, await act, jest.mock("react-native", ...)). Use `waitFor` or awaited act to flush the restore effect.

    Then change mobile/src/hooks/useAuth0Session.ts:
    - Import AUTH_REQUIRED_ERROR, AUTH_TEMPORARILY_UNAVAILABLE_ERROR, classifyCredentialsError from "./auth-errors"; re-export `AUTH_REQUIRED_ERROR` from this module (`export { AUTH_REQUIRED_ERROR } from "./auth-errors"` or equivalent) so existing imports `from "./useAuth0Session"` keep compiling. Also re-export AUTH_TEMPORARILY_UNAVAILABLE_ERROR.
    - Add module-level `async function isDeviceOnline(): Promise<boolean>` using `Network.getNetworkStateAsync()` and `isOnlineNetworkState`; any exception → false (offline is the safe side: retry later).
    - Add state `sessionOwner: IdTokenClaims | null` (initial null). Set it from `extractIdTokenClaims(credentials.idToken)` wherever credentials are obtained: restore success, handleLogin, handleRegister, handleForgotPassword (after saveCredentials), and each successful getCredentials in getValidAccessToken. Reset to null inside clearSession. Return it from the hook as `sessionOwner`.
    - getValidAccessToken becomes `(options?: { forceRefresh?: boolean }) => Promise<string>`: call `getCredentials()` with no arguments normally, and `getCredentials(undefined, undefined, undefined, true)` when forceRefresh is true. Return the access token; if credentials have no accessToken throw `new Error(AUTH_TEMPORARILY_UNAVAILABLE_ERROR)`. On exception: `classifyCredentialsError(error, { phase: "refresh", isOnline: await isDeviceOnline() })` → "session-ended" throws `new Error(AUTH_REQUIRED_ERROR)`, "retry-later" throws `new Error(AUTH_TEMPORARILY_UNAVAILABLE_ERROR)`. Never return null, never call clearSession here.
    - withAuthRetry: `const token = await getValidAccessToken()`; on an error where isUnauthorizedError is true, `const refreshed = await getValidAccessToken({ forceRefresh: true })` then `return operation(refreshed)`. Other errors rethrow unchanged.
    - Restore effect: wrap hasValidCredentials + getCredentials; in the catch, compute the kind with phase "restore". "session-ended" → `await clearSession()` and `reportStatus("session", "error", "Session expirée : reconnectez-vous. Vos relevés locaux sont conservés.")`. "retry-later" → do NOT call clearSession; `reportStatus("session", "idle", "Session non restaurée (réseau indisponible). Vos relevés locaux sont conservés.")`. Both paths setSessionRestoring(false). Errors thrown by getMyProfile keep today's "restored (offline)" handling. Remove the stale "DEV ONLY: slow down" comment.
    - refreshSessionTokens keeps its current name and return type in this plan (plan 06 changes it): implement it with `getValidAccessToken({ forceRefresh: true })`, returning `{ accessToken, refreshToken: "" }`, returning null only when the error is AUTH_REQUIRED and rethrowing AUTH_TEMPORARILY_UNAVAILABLE.
    - Remove the unused `ensureAccessToken` key from the return object (no consumer exists: `grep -rn ensureAccessToken mobile/src mobile/App.tsx` finds only this file). Keep every other return key unchanged.
    - handleLoadMyProfile needs no change: AUTH_REQUIRED still clears the session (which no longer purges after plan 06); temporary errors fall into its generic branch.
  </action>
  <verify>
    <automated>npm --workspace mobile run test:unit -- useAuth0Session && npm --workspace mobile run typecheck && npm --workspace mobile run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm --workspace mobile run test:unit -- useAuth0Session` exits 0 with the 10 behavior cases passing
    - `grep -c "classifyCredentialsError(" mobile/src/hooks/useAuth0Session.ts` returns at least 2 (refresh path and restore path)
    - `grep -c "getCredentials(undefined, undefined, undefined, true)" mobile/src/hooks/useAuth0Session.ts` returns 1
    - `sed -n '/const getValidAccessToken/,/^  }, \[/p' mobile/src/hooks/useAuth0Session.ts | grep -c "return null"` returns 0 (getValidAccessToken never returns null)
    - `grep -c "sessionOwner" mobile/src/hooks/useAuth0Session.ts` returns at least 3
    - `npm --workspace mobile run typecheck` exits 0 and `npm --workspace mobile run test:unit` (full mobile suite) exits 0
  </acceptance_criteria>
  <done>No credential error can reach clearSession unless it is a genuine session end; a 401 forces a refresh; the session's Auth0 sub is available to downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auth0 SDK → app session state | SDK error types decide whether the session ends |
| npm registry → mobile workspace | Two new dev dependencies enter the lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-11 | Denial of Service (data loss) | useAuth0Session getValidAccessToken + restore catch (M-C1) | mitigate | classifyCredentialsError at both call sites; network/unknown → AUTH_TEMPORARILY_UNAVAILABLE, never clearSession; renderHook regression tests for both paths |
| T-01.2-12 | Elevation of Privilege | Stale access token reused after a 401 | mitigate | retry uses getCredentials(..., forceRefresh=true); test asserts the 4th argument |
| T-01.2-13 | Spoofing | Unverified ID-token claims used for local ownership | accept | claims come from the SDK's secure store and only label local data ownership; no authorization decision uses them; server still verifies every access token |
| T-01.2-SC | Tampering | npm install of @testing-library/react-native, test-renderer | mitigate | both [OK]/Approved in RESEARCH Package Legitimacy Audit; exact versions pinned (14.0.1, 1.3.0); dev-only, mobile workspace only; root package.json asserted unchanged |
</threat_model>

<verification>
- `npm --workspace mobile run test:unit`, `npm --workspace mobile run typecheck`, `npm --workspace mobile run lint` green
- `npm run format:check` green
</verification>

<success_criteria>
ROADMAP criterion 1 (hook side): a network error, timeout or unknown error while refreshing never ends the session; only an explicit refresh-token rejection does. REQ-AUD-session-data-loss: "The 401 retry forces a token refresh."
</success_criteria>

<output>
Create `.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-03-SUMMARY.md` when done
</output>
