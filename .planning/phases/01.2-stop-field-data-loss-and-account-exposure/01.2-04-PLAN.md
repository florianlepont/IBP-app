---
phase: 01.2-stop-field-data-loss-and-account-exposure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/app/map-viewport.ts
  - mobile/src/app/map-viewport.test.ts
  - mobile/src/hooks/useNearbyParcels.ts
  - mobile/src/hooks/useNearbyParcels.test.ts
  - mobile/src/app/dev-tools.ts
  - mobile/src/app/dev-tools.test.ts
  - mobile/src/screens/SettingsScreen.tsx
  - mobile/App.tsx
autonomous: true
requirements: [REQ-AUD-mobile-quick-fixes]

must_haves:
  truths:
    - "The nearby-parcels request sends bbox as minLng,minLat,maxLng,maxLat"
    - "The nearby-parcels bbox and the map viewport bbox come from the same helper in map-viewport.ts"
    - "In a build where __DEV__ is false, Settings shows no 'Outils développeur' section (no API URL field, no data reset buttons)"
    - "In a build where __DEV__ is false, a previously stored API URL override is ignored at startup"
  artifacts:
    - path: mobile/src/app/map-viewport.ts
      provides: "buildBboxAroundPoint sharing the bbox formatter with computeRegionBbox"
      exports: ["buildBboxAroundPoint", "computeRegionBbox"]
    - path: mobile/src/app/dev-tools.ts
      provides: "shouldShowDevTools predicate"
      exports: ["shouldShowDevTools"]
    - path: mobile/src/hooks/useNearbyParcels.test.ts
      provides: "bbox-order regression test"
  key_links:
    - from: mobile/src/hooks/useNearbyParcels.ts
      to: mobile/src/app/map-viewport.ts
      via: "buildBboxAroundPoint({ lat, lng }, RADIUS_DEG)"
      pattern: "buildBboxAroundPoint\\("
    - from: mobile/src/screens/SettingsScreen.tsx
      to: mobile/src/app/dev-tools.ts
      via: "shouldShowDevTools() gate around the dev-tools section"
      pattern: "shouldShowDevTools\\(\\)"
    - from: mobile/App.tsx
      to: mobile/src/app/dev-tools.ts
      via: "stored API URL loaded only when shouldShowDevTools()"
      pattern: "shouldShowDevTools\\(\\)"
---

<objective>
Close audit findings M-H3 and M-H5 (lot L4): the nearby-parcels bbox is sent in the order the API expects (D-12), and developer tools — API URL override and data resets — disappear from production builds through a small helper a `*.test.ts` can cover (D-11).

Purpose: REQ-AUD-mobile-quick-fixes — a production user cannot redirect the app to another server or wipe data from Settings, and the home screen's nearby parcels are real.
Output: shared bbox helper, fixed `useNearbyParcels`, `dev-tools.ts` predicate, gated Settings section and startup override.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-CONTEXT.md
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-PATTERNS.md
@mobile/src/app/map-viewport.ts
@mobile/src/hooks/useNearbyParcels.ts

<interfaces>
mobile/src/app/map-viewport.ts (current):
- `export function computeRegionBbox(region: Region): string` — clamps lat to [-90, 90], lng to [-180, 180], returns `${minLng.toFixed(6)},${minLat.toFixed(6)},${maxLng.toFixed(6)},${maxLat.toFixed(6)}`. `Region` is a type-only import from react-native-maps. Covered by mobile/src/app/map-viewport.test.ts.

mobile/src/hooks/useNearbyParcels.ts (current bug, line 73):
- `const bbox = \`${lng - RADIUS_DEG},${lng + RADIUS_DEG},${lat - RADIUS_DEG},${lat + RADIUS_DEG}\`` — sends minLng,maxLng,minLat,maxLat. RADIUS_DEG = 0.025, ZOOM = 15, calls `fetchPublicParcelStatuses(apiUrl, { bbox, zoom: ZOOM })` from "../api/ibp-api"; location via expo-location getForegroundPermissionsAsync / requestForegroundPermissionsAsync / getCurrentPositionAsync({ accuracy: Location.Accuracy.Balanced }).

mobile/src/screens/SettingsScreen.tsx lines 218-239: unconditional `<AppCollapsibleSection title="Outils développeur" badge="DEV">` containing the "URL de l'API" AppField (onApiUrlChange) and two AppButtons ("Vider la base IBP", "Vider la base utilisateur").

mobile/App.tsx lines 30-36: an effect calls `loadStoredApiUrl()` from "./src/app/api-url-storage" and `setApiUrl(stored)` unconditionally; initial state is `process.env.EXPO_PUBLIC_API_URL ?? DEFAULT_API_URL`.

Existing __DEV__ gating analog: mobile/src/screens/AuthGateScreen.tsx lines 372 and 499 (`__DEV__ ? ... : undefined/null`). mobile/jest.unit.config.js sets `globals: { __DEV__: true }`.

Test pattern without renderHook (this plan runs in wave 1, before RNTL is installed): mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts spies on React.useState/useCallback/useEffect and calls the hook directly; mobile/src/hooks/useGpsCapture.test.ts mocks expo-location with a factory.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared bbox helper and nearby-parcels bbox order fix (TDD)</name>
  <files>mobile/src/app/map-viewport.ts, mobile/src/app/map-viewport.test.ts, mobile/src/hooks/useNearbyParcels.ts, mobile/src/hooks/useNearbyParcels.test.ts</files>
  <read_first>
    - mobile/src/app/map-viewport.ts and mobile/src/app/map-viewport.test.ts
    - mobile/src/hooks/useNearbyParcels.ts
    - mobile/src/hooks/useGpsCapture.test.ts (expo-location mock factory)
    - mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts (React hook spy pattern for calling a hook without a renderer)
    - api/src/surveys/dtos (the parcels/status query DTO) or docs/technical/api-contract-v1.md GET /public/parcels/status — confirms minLng,minLat,maxLng,maxLat
  </read_first>
  <behavior>
    - buildBboxAroundPoint({ lat: 46, lng: 2 }, 0.025) === "1.975000,45.975000,2.025000,46.025000"
    - buildBboxAroundPoint({ lat: 89.99, lng: 179.99 }, 0.025) clamps to "179.965000,89.965000,180.000000,90.000000"
    - buildBboxAroundPoint({ lat: -89.99, lng: -179.99 }, 0.025) clamps min values to -180 / -90
    - existing computeRegionBbox tests keep passing unchanged
    - useNearbyParcels.load with permission granted and position { latitude: 46, longitude: 2 } calls fetchPublicParcelStatuses with ("<apiUrl>", { bbox: "1.975000,45.975000,2.025000,46.025000", zoom: 15 })
    - useNearbyParcels.load with permission denied twice does not call fetchPublicParcelStatuses
  </behavior>
  <action>
    Tests first. In map-viewport.test.ts add a describe for buildBboxAroundPoint with the cases above. Create mobile/src/hooks/useNearbyParcels.test.ts: mock "expo-location" (factory with getForegroundPermissionsAsync, requestForegroundPermissionsAsync, getCurrentPositionAsync, Accuracy: { Balanced: 3 }) and "../api/ibp-api" (fetchPublicParcelStatuses resolving `{ items: [] }`); call the hook without a renderer by spying on React.useState (return `[initialState, jest.fn()]`) and React.useCallback (identity), the same way useSurveySyncNetwork.test.ts does; then `await hook.load()` and assert the fetch arguments.

    Implementation (D-12): in map-viewport.ts extract a private `formatBbox(minLng, minLat, maxLng, maxLat): string` that applies the existing clamping and `toFixed(6)` formatting, make computeRegionBbox call it (output unchanged), and export `buildBboxAroundPoint(center: { lat: number; lng: number }, radiusDeg: number): string` that calls it with center ± radius. In useNearbyParcels.ts replace the hand-rolled bbox with `buildBboxAroundPoint({ lat, lng }, RADIUS_DEG)` imported from "../app/map-viewport". No other behavior change.
  </action>
  <verify>
    <automated>npm --workspace mobile run test:unit -- map-viewport useNearbyParcels</automated>
  </verify>
  <acceptance_criteria>
    - `npm --workspace mobile run test:unit -- map-viewport useNearbyParcels` exits 0; the useNearbyParcels bbox assertion is "1.975000,45.975000,2.025000,46.025000"
    - `grep -c "buildBboxAroundPoint(" mobile/src/hooks/useNearbyParcels.ts` returns 1
    - `grep -c "lng - RADIUS_DEG" mobile/src/hooks/useNearbyParcels.ts` returns 0
    - `grep -c "export function buildBboxAroundPoint" mobile/src/app/map-viewport.ts` returns 1
  </acceptance_criteria>
  <done>Nearby parcels are queried with the correct bbox, from the single shared formatter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dev tools only in __DEV__ builds</name>
  <files>mobile/src/app/dev-tools.ts, mobile/src/app/dev-tools.test.ts, mobile/src/screens/SettingsScreen.tsx, mobile/App.tsx</files>
  <read_first>
    - mobile/src/screens/SettingsScreen.tsx (lines 100-245: confirmDebugReset* handlers and the Zone 3 section)
    - mobile/App.tsx (lines 20-45: apiUrl state, loadStoredApiUrl effect, handleApiUrlChange)
    - mobile/src/app/api-url-storage.ts (stored override key ibp_api_url_v1)
    - mobile/src/screens/AuthGateScreen.tsx lines 365-375 and 495-525 (existing __DEV__ pattern)
    - mobile/src/hooks/operation-status.test.ts (pure test template)
  </read_first>
  <behavior>
    - shouldShowDevTools(false) === false
    - shouldShowDevTools(true) === true
    - shouldShowDevTools() === true under the Jest config (globals.__DEV__ = true), proving the default reads __DEV__
  </behavior>
  <action>
    Create mobile/src/app/dev-tools.ts exporting `shouldShowDevTools(isDev: boolean = __DEV__): boolean` returning isDev, with a comment that production bundles have __DEV__ === false, so the API URL override and data-reset tools never render there (D-11, audit M-H5). Create mobile/src/app/dev-tools.test.ts with the three cases (plain describe/test, no mocks) — `.test.ts`, not `.test.tsx`, because testMatch only collects `.test.ts` until Phase 1.3.

    SettingsScreen.tsx: wrap the whole Zone 3 block (the comment line and the `<AppCollapsibleSection title="Outils développeur" badge="DEV">…</AppCollapsibleSection>`) in `{shouldShowDevTools() ? ( … ) : null}`. Leave the confirmDebugReset* handlers and props in place (the props are still passed by the navigator; they simply have no UI in production).

    App.tsx: at the top of the effect that calls loadStoredApiUrl, return early when `!shouldShowDevTools()` so a previously stored override (from a build where the field was visible) cannot redirect a production build to another server. The initial `process.env.EXPO_PUBLIC_API_URL ?? DEFAULT_API_URL` value is unchanged. AuthGateScreen already gates its dev modal with __DEV__ — no change there.
  </action>
  <verify>
    <automated>npm --workspace mobile run test:unit -- dev-tools && npm --workspace mobile run typecheck && npm --workspace mobile run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm --workspace mobile run test:unit -- dev-tools` exits 0 with 3 passing tests
    - `grep -c "shouldShowDevTools()" mobile/src/screens/SettingsScreen.tsx` returns 1
    - `grep -B2 "title=\"Outils développeur\"" mobile/src/screens/SettingsScreen.tsx | grep -c "shouldShowDevTools()"` returns 1
    - `grep -c "shouldShowDevTools()" mobile/App.tsx` returns 1
    - `npm --workspace mobile run typecheck` and `npm --workspace mobile run lint` exit 0
  </acceptance_criteria>
  <done>Production builds render no developer tools and ignore any stored API URL override; development builds are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device user → app configuration | A production user must not be able to change the API endpoint the app sends tokens and field data to |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-14 | Information Disclosure / Spoofing | Settings "URL de l'API" field (M-H5) — bearer tokens and surveys sent to an arbitrary server | mitigate | section rendered only when shouldShowDevTools(); stored override ignored at startup when __DEV__ is false |
| T-01.2-15 | Tampering | Settings data-reset buttons in production | mitigate | same gate; server-side endpoints are also removed in production by plan 05 |
| T-01.2-16 | Tampering (data integrity) | Nearby-parcels bbox sent in wrong order (M-H3) | mitigate | single shared formatter buildBboxAroundPoint; regression test pins the exact string |
</threat_model>

<verification>
- `npm --workspace mobile run test:unit`, `npm --workspace mobile run typecheck`, `npm --workspace mobile run lint`, `npm run format:check` green
- Manual (release build) listed in 01.2-VALIDATION.md: Settings shows no "Outils développeur" section
</verification>

<success_criteria>
ROADMAP criterion 6: developer tools are absent from production builds, and the nearby-parcels bbox is sent as minLng,minLat,maxLng,maxLat.
</success_criteria>

<output>
Create `.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-04-SUMMARY.md` when done
</output>
