---
phase: 01.2-stop-field-data-loss-and-account-exposure
plan: 05
type: execute
wave: 2
depends_on: [01.2-01]
files_modified:
  - api/src/debug/debug-gating.ts
  - api/src/app.module.ts
  - api/test/debug-surface.spec.ts
  - api/test/debug-surface.e2e-spec.ts
autonomous: true
requirements: [REQ-AUD-debug-surface]

must_haves:
  truths:
    - "With NODE_ENV=production, AppModule does not import DebugModule"
    - "With NODE_ENV=production, POST /v1/debug/test-token, /v1/debug/reset-ibp-data and /v1/debug/reset-user-data return 404"
    - "With NODE_ENV=production, AuthGuard rejects an HS256 token signed with ACCESS_TOKEN_SECRET"
    - "With NODE_ENV=test (CI), DebugModule is loaded and the whole existing E2E suite still obtains tokens from /v1/debug/test-token"
  artifacts:
    - path: api/src/debug/debug-gating.ts
      provides: "isDebugSurfaceEnabled(env)"
      exports: ["isDebugSurfaceEnabled"]
    - path: api/test/debug-surface.e2e-spec.ts
      provides: "404 proof for /v1/debug/* in production mode"
  key_links:
    - from: api/src/app.module.ts
      to: api/src/debug/debug-gating.ts
      via: "conditional spread of DebugModule in imports"
      pattern: "isDebugSurfaceEnabled\\(\\) \\? \\[DebugModule\\] : \\[\\]"
---

<objective>
Close audit finding A-H4 (lot L2): `DebugModule` and the HS256 test-token path load only when `NODE_ENV !== "production"` (D-07), so `/v1/debug/*` is 404 in production while CI (`NODE_ENV=test`, `DEBUG_DATA_RESET_ENABLED=true`) keeps working.

Purpose: REQ-AUD-debug-surface — a configuration mistake (e.g. a leaked ACCESS_TOKEN_SECRET or a mis-set flag) can no longer open token minting or data wipes in production.
Output: `isDebugSurfaceEnabled` predicate, conditional import in AppModule, unit spec and production-mode E2E spec.

Depends on plan 01 only because both edit api/src/app.module.ts.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-CONTEXT.md
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-RESEARCH.md
@api/src/app.module.ts
@api/src/debug/debug.controller.ts

<interfaces>
- api/src/debug/debug.module.ts: `@Module({ imports: [AuthModule, DatabaseModule], controllers: [DebugController], providers: [DebugService] }) export class DebugModule {}` — no change.
- api/src/debug/debug.controller.ts line 17: `if (process.env.NODE_ENV !== "test") throw new ForbiddenException()` inside POST test-token — keep untouched (defense in depth).
- api/src/debug/debug.service.ts line 72: reset endpoints also require `DEBUG_DATA_RESET_ENABLED === "true"` — keep untouched.
- api/src/auth/auth.guard.ts line 42: HS256 branch runs only when `process.env.NODE_ENV === "test"` (already stricter than D-07; it is read per request). Keep untouched; this plan adds a regression test for it.
- api/src/app.module.ts after plan 01: imports `ThrottlerModule.forRoot(buildThrottlerOptions())`, DatabaseModule, AuthModule, UsersModule, SurveysModule, ReportsModule, DebugModule; APP_GUARD ClientAwareThrottlerGuard. The decorator metadata is evaluated at import time, so tests must import AppModule in a fresh module registry after setting NODE_ENV (`jest.isolateModulesAsync` + `await import(...)`; `require` is banned by lint).
- E2E harness: api/jest.config.js (setupFiles test/setup-env.js sets NODE_ENV=test), supertest, `Test.createTestingModule({ imports: [AppModule] })`, `app.setGlobalPrefix("v1")` (or `configureApp(app)` from api/src/app.setup.ts created by plan 01).
- CI env (.github/workflows/ci.yml): NODE_ENV test, DEBUG_DATA_RESET_ENABLED true — no new env var may be required (RESEARCH Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate DebugModule on NODE_ENV and cover it with unit tests</name>
  <files>api/src/debug/debug-gating.ts, api/src/app.module.ts, api/test/debug-surface.spec.ts</files>
  <read_first>
    - api/src/app.module.ts (as left by plan 01)
    - api/src/debug/debug.module.ts, api/src/debug/debug.controller.ts, api/src/debug/debug.service.ts
    - api/src/auth/auth.guard.ts (canActivate HS256 branch — do not edit)
    - api/test/auth.guard.spec.ts (buildGuard/makeContext/makeToken helpers to copy into the new spec)
    - api/test/debug.service.spec.ts (env save/restore convention)
  </read_first>
  <behavior>
    - isDebugSurfaceEnabled({ NODE_ENV: "production" }) === false
    - isDebugSurfaceEnabled({ NODE_ENV: "test" }), ({ NODE_ENV: "development" }), ({}) all === true
    - With process.env.NODE_ENV = "production", inside jest.isolateModulesAsync: `Reflect.getMetadata("imports", AppModule)` (both imported in the same isolated registry) does not contain DebugModule
    - With process.env.NODE_ENV = "test", same check contains DebugModule
    - With process.env.NODE_ENV = "production" and ACCESS_TOKEN_SECRET set, `new AuthGuard(db).canActivate(ctx)` with an HS256 token signed by that secret rejects with UnauthorizedException and db.query is never called
  </behavior>
  <action>
    Test first: create api/test/debug-surface.spec.ts with the five cases; save and restore process.env.NODE_ENV and ACCESS_TOKEN_SECRET in beforeEach/afterEach (setup-env.js sets NODE_ENV=test for the rest of the suite). Silence the AuthGuard's console.error in the HS256 case with a jest.spyOn(console, "error").mockImplementation.

    Implementation (D-07): create api/src/debug/debug-gating.ts exporting `isDebugSurfaceEnabled(env: NodeJS.ProcessEnv = process.env): boolean` → `env.NODE_ENV !== "production"`, with a comment that CI runs E2E with NODE_ENV=test and that debug.controller's own NODE_ENV==="test" check and DEBUG_DATA_RESET_ENABLED remain as inner layers. In api/src/app.module.ts replace the bare `DebugModule` entry in `imports` with `...(isDebugSurfaceEnabled() ? [DebugModule] : [])`. Do not change DebugController, DebugService, AuthGuard or ci.yml, and introduce no new environment variable.
  </action>
  <verify>
    <automated>npm --workspace api run test:unit -- debug-surface && npm --workspace api run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm --workspace api run test:unit -- debug-surface` exits 0 with 5 passing tests
    - `grep -c "isDebugSurfaceEnabled() ? \[DebugModule\] : \[\]" api/src/app.module.ts` returns 1
    - `grep -c "process.env.NODE_ENV !== \"test\"" api/src/debug/debug.controller.ts` returns 1 (inner check kept)
    - `grep -c "process.env.NODE_ENV === \"test\"" api/src/auth/auth.guard.ts` returns 1 (HS256 gate kept)
    - `git diff --quiet .github/workflows/ci.yml` exits 0
  </acceptance_criteria>
  <done>DebugModule is only part of the application graph outside production, with unit proof for both modes and for the HS256 path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Production-mode E2E — /v1/debug/* returns 404</name>
  <files>api/test/debug-surface.e2e-spec.ts</files>
  <read_first>
    - api/test/auth-profile.e2e-spec.ts (E2E bootstrap)
    - api/src/app.setup.ts (configureApp from plan 01)
    - api/test/setup-env.js and api/jest.config.js
  </read_first>
  <behavior>
    - NODE_ENV=production app: POST /v1/debug/test-token → 404; POST /v1/debug/reset-ibp-data → 404; POST /v1/debug/reset-user-data → 404; GET /v1/health → 200
    - after the spec, process.env.NODE_ENV is restored to "test"
  </behavior>
  <action>
    Create api/test/debug-surface.e2e-spec.ts. In beforeAll: remember NODE_ENV, set `process.env.NODE_ENV = "production"`, then inside `jest.isolateModulesAsync(async () => { ... })` dynamically import "@nestjs/testing", "../src/app.module" and "../src/app.setup" (all from the same isolated registry so Nest's metadata and DI tokens stay consistent), build the testing module, `createNestApplication<NestExpressApplication>()`, `configureApp(app)`, `await app.init()`. In afterAll close the app and restore NODE_ENV. Use supertest (`import request = require("supertest")` exactly as the other E2E specs do — that form is TypeScript import-equals, allowed by lint) for the four requests. Header comment: proves ROADMAP 1.2 criterion 4 and that the E2E suite's own debug usage still works because every other spec runs with NODE_ENV=test.
  </action>
  <verify>
    <automated>npm --workspace api run test:e2e -- debug-surface</automated>
  </verify>
  <acceptance_criteria>
    - `npm --workspace api run test:e2e -- debug-surface` exits 0 with 4 passing assertions (requires PostgreSQL)
    - `npm --workspace api run test:e2e` (full suite) exits 0 — auth-profile, epic-e-search-reports, surveys-idempotency and validation-reports still obtain tokens from /v1/debug/test-token
    - `grep -c "isolateModulesAsync" api/test/debug-surface.e2e-spec.ts` returns 1
  </acceptance_criteria>
  <done>Automated proof that production exposes no debug route while CI's test mode keeps its token endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /v1/debug/* | Token minting and data-reset endpoints must not exist in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-17 | Elevation of Privilege | POST /v1/debug/test-token (HS256 minting, A-H4) | mitigate | DebugModule not imported when NODE_ENV=production; E2E asserts 404; inner NODE_ENV==="test" check kept |
| T-01.2-18 | Tampering | POST /v1/debug/reset-ibp-data, reset-user-data | mitigate | same module gate + existing AdminGuard + DEBUG_DATA_RESET_ENABLED flag kept; E2E asserts 404 |
| T-01.2-19 | Spoofing | AuthGuard HS256 branch | mitigate | branch only when NODE_ENV==="test"; unit test asserts HS256 token rejected in production without DB access |
| T-01.2-20 | Denial of Service (CI) | Over-tight gating breaks E2E token acquisition | mitigate | gate is exactly NODE_ENV !== "production"; no new env var; full E2E suite run in acceptance |
</threat_model>

<verification>
- `npm --workspace api run test:unit` and `npm --workspace api run test:e2e` green
- `npm --workspace api run build` and `npm --workspace api run lint` green
</verification>

<success_criteria>
REQ-AUD-debug-surface / ROADMAP criterion 4: DebugModule and the HS256 test-token path are not loaded in production; /v1/debug/* returns 404 there.
</success_criteria>

<output>
Create `.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-05-SUMMARY.md` when done
</output>
