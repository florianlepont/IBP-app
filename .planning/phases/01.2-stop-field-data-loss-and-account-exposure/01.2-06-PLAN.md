---
phase: 01.2-stop-field-data-loss-and-account-exposure
plan: 06
type: execute
wave: 2
depends_on: [01.2-03]
files_modified:
  - mobile/src/hooks/useAuth0Session.ts
  - mobile/src/hooks/useAuth0Session.test.ts
  - mobile/src/hooks/useSurveySync.ts
  - mobile/src/hooks/useSurveySync.test.ts
  - mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts
  - mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts
  - mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.ts
  - mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.test.ts
autonomous: true
requirements: [REQ-AUD-session-data-loss]

must_haves:
  truths:
    - "Ending a session (onSessionCleared) never calls clearLocalIbpData: local_surveys, sync_queue and local_attachments survive"
    - "An AUTH_TEMPORARILY_UNAVAILABLE error during sync, pull or report leaves the session in place and shows a French 'retry later' status"
    - "The pre-Auth0 stubs are gone: handleVerifyEmail, handleResendVerification, handleCancelEmailVerification, pendingEmailVerification, devVerificationToken, refreshToken: \"\""
    - "No caller tests accessToken || refreshToken any more"
  artifacts:
    - path: mobile/src/hooks/useSurveySync.ts
      provides: "clearSurveySessionState without storage purge"
    - path: mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts
      provides: "retry-later handling for AUTH_TEMPORARILY_UNAVAILABLE; accessToken-only auth gating"
  key_links:
    - from: mobile/src/hooks/useSurveySync.ts
      to: mobile/src/hooks/useAuth0Session.ts
      via: "onSessionCleared: clearSurveySessionState (React state reset only)"
      pattern: "onSessionCleared: clearSurveySessionState"
    - from: mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts
      to: mobile/src/hooks/auth-errors.ts
      via: "isAuthRequiredError / isAuthTemporarilyUnavailableError"
      pattern: "isAuthTemporarilyUnavailableError\\("
---

<objective>
Finish the M-C1 fix on the consumer side (lot L1): the session-cleared callback stops purging local data (D-02), sync/pull/report treat a temporary auth failure as "retry later", and the pre-Auth0 session stubs are removed from the hook and every caller (ROADMAP criterion 7).

Purpose: REQ-AUD-session-data-loss — even a genuine session end keeps the offline queue, so it syncs after re-login with the same account.
Output: updated `useAuth0Session` return shape, `useSurveySync`, `useSurveySyncNetwork`, `useSurveySyncSurveyOperations` and their tests.

Out of scope here (plan 08): logout confirmation, owner check, sync suspension. Keep `handleLogout` behavior as-is in this plan except that it no longer purges through clearSession — plan 08 adds the explicit, confirmed purge. Between plans 06 and 08 (same wave-3 delivery), logout keeps local data, which is the safe direction.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-CONTEXT.md
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-PATTERNS.md
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-03-SUMMARY.md

<interfaces>
From plan 03 (mobile/src/hooks/auth-errors.ts):
- `AUTH_REQUIRED_ERROR = "AUTH_REQUIRED"`, `AUTH_TEMPORARILY_UNAVAILABLE_ERROR = "AUTH_TEMPORARILY_UNAVAILABLE"`
- `isAuthRequiredError(error: unknown): boolean`, `isAuthTemporarilyUnavailableError(error: unknown): boolean`
- useAuth0Session re-exports AUTH_REQUIRED_ERROR and AUTH_TEMPORARILY_UNAVAILABLE_ERROR; `withAuthRetry` throws Error(AUTH_REQUIRED_ERROR) only for genuine session ends and Error(AUTH_TEMPORARILY_UNAVAILABLE_ERROR) otherwise; hook returns `sessionOwner: IdTokenClaims | null`; `refreshSessionTokens` currently returns `{ accessToken, refreshToken: "" } | null`.

Current stubs (verified by grep):
- useAuth0Session.ts return: `refreshToken: ""`, `pendingEmailVerification: null`, `devVerificationToken: null`, `handleCancelEmailVerification`, `handleVerifyEmail`, `handleResendVerification`; refreshSessionTokens returns `refreshToken: ""`.
- useSurveySync.ts destructures and re-returns all of them (lines 94-119 and 413-437) and passes `refreshToken` to useSurveySyncNetwork (line 180) and useSurveySyncSurveyOperations (line 340).
- useSurveySyncNetwork.ts: param `refreshToken: string | null`; `!(accessToken || refreshToken)` at lines 79, 231, 238, 252 and in effect deps.
- useSurveySyncSurveyOperations.ts: param `refreshToken: string`, `refreshSessionTokens: (tokenOverride?: string) => Promise<{ accessToken: string; refreshToken: string } | null>`; `(accessToken || refreshToken)` at line 237.
- No screen or App.tsx reads the stubs (grep of mobile/App.tsx and mobile/src/app, mobile/src/screens finds none).
- D-02 target: useSurveySync.ts lines 50-55 `clearSurveySessionState` calls `await clearLocalIbpData()` — remove that call. `resetLocalSurveyState` (debug reset) keeps its explicit clearLocalIbpData.

Existing tests are spy-based (React.useState/useCallback/useEffect spies): useSurveySync.test.ts mocks "./useAuth0Session" with a stub object containing the removed keys and mocks "../storage/surveys" with clearLocalIbpData; useSurveySyncNetwork.test.ts mocks "../useAuth0Session" with only AUTH_REQUIRED_ERROR — after this plan the network hook imports from "../auth-errors" instead, so that mock line must be replaced or removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the session-cleared purge and the pre-Auth0 stubs; handle retry-later</name>
  <files>mobile/src/hooks/useAuth0Session.ts, mobile/src/hooks/useSurveySync.ts, mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts, mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.ts</files>
  <read_first>
    - mobile/src/hooks/useAuth0Session.ts (return object, refreshSessionTokens — as left by plan 03)
    - mobile/src/hooks/useSurveySync.ts (whole file)
    - mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts (whole file)
    - mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.ts (params type lines 22-60, handleToggleVisibility lines 220-260)
    - mobile/src/hooks/auth-errors.ts (plan 03)
    - mobile/src/app/AuthenticatedAppNavigation.tsx (SurveySyncController type usage — confirm nothing reads removed keys)
  </read_first>
  <action>
    useAuth0Session.ts: remove from the return object `refreshToken`, `pendingEmailVerification`, `devVerificationToken`, `handleCancelEmailVerification`, `handleVerifyEmail`, `handleResendVerification`. Change `refreshSessionTokens` to `(): Promise<{ accessToken: string } | null>` (forced refresh from plan 03 kept; null only on AUTH_REQUIRED; AUTH_TEMPORARILY_UNAVAILABLE rethrown).

    useSurveySync.ts (D-02): in clearSurveySessionState delete the `await clearLocalIbpData()` line and add a comment "Session end resets UI state only — local surveys, queue and photos are never purged here (D-02, audit M-C1)". Keep the clearLocalIbpData import (still used by resetLocalSurveyState). Stop destructuring/returning the six removed keys; stop passing refreshToken to the two sub-hooks.

    useSurveySyncNetwork.ts: remove the `refreshToken` param from the type and destructuring; replace every `!(accessToken || refreshToken)` with `!accessToken` and drop refreshToken from dependency arrays. Import `isAuthRequiredError` and `isAuthTemporarilyUnavailableError` from "../auth-errors" (replacing the `AUTH_REQUIRED_ERROR` import from "../useAuth0Session"); switch the existing `(error as Error).message === AUTH_REQUIRED_ERROR` checks to isAuthRequiredError(error). In runSync, maybeAutoSync's pull branch, handlePullChanges and handleReportSurvey add a branch before the generic one: if isAuthTemporarilyUnavailableError(error) → do NOT call clearSession; set status "Synchronisation reportée : authentification momentanément indisponible. Vos relevés locaux sont conservés." (handleReportSurvey returns `{ ok: false, message }` with "Signalement non envoyé : authentification momentanément indisponible. Réessayez plus tard.").

    useSurveySyncSurveyOperations.ts: remove the `refreshToken` param; type `refreshSessionTokens: () => Promise<{ accessToken: string } | null>`; replace `(accessToken || refreshToken)` with `accessToken`; drop refreshToken from dependency arrays. handleToggleVisibility's existing catch already reports thrown temporary errors — no clearSession on that path.

    Run `grep -rnE "pendingEmailVerification|devVerificationToken|handleVerifyEmail|handleResendVerification|handleCancelEmailVerification" mobile/src mobile/App.tsx` and fix any remaining non-test reference.
  </action>
  <verify>
    <automated>npm --workspace mobile run typecheck && npm --workspace mobile run lint</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/const clearSurveySessionState/,/}, \[\])/p' mobile/src/hooks/useSurveySync.ts | grep -c "clearLocalIbpData"` returns 0
    - `grep -rn "accessToken || refreshToken" mobile/src mobile/App.tsx` prints nothing
    - `grep -rnE "pendingEmailVerification|devVerificationToken|handleVerifyEmail|handleResendVerification|handleCancelEmailVerification" mobile/src mobile/App.tsx --include=*.ts --include=*.tsx | grep -v "\.test\.ts"` prints nothing
    - `grep -rn "refreshToken: \"\"" mobile/src --include=*.ts --include=*.tsx | grep -v "\.test\.ts"` prints nothing
    - `grep -c "isAuthTemporarilyUnavailableError(" mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts` returns at least 4
    - `npm --workspace mobile run typecheck` exits 0
  </acceptance_criteria>
  <done>No session end purges data, temporary auth failures keep the session, and the dead stubs are gone from production code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update and extend the hook tests for the new contract</name>
  <files>mobile/src/hooks/useSurveySync.test.ts, mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts, mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.test.ts, mobile/src/hooks/useAuth0Session.test.ts</files>
  <read_first>
    - mobile/src/hooks/useSurveySync.test.ts (mockAuth0Session object, storage mock, spy setup)
    - mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts (useBuildHook params, "../useAuth0Session" mock)
    - mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.test.ts (params with refreshToken / refreshSessionTokens)
    - mobile/src/hooks/useAuth0Session.test.ts (plan 03 renderHook tests)
    - the four source files changed in Task 1
  </read_first>
  <behavior>
    - useSurveySync: the `onSessionCleared` function passed to useAuth0Session (captured from mockUseAuth0Session's first call argument) resolves without calling mockClearLocalIbpData (D-02 regression for both AUTH_REQUIRED and RENEW_FAILED session ends)
    - useSurveySync: the returned object has no pendingEmailVerification, devVerificationToken, handleVerifyEmail, handleResendVerification, handleCancelEmailVerification keys
    - useSurveySyncNetwork: handleSync where withAuthRetry rejects Error("AUTH_TEMPORARILY_UNAVAILABLE") → clearSession not called, setStatus called with a string starting "Synchronisation reportée"
    - useSurveySyncNetwork: handlePullChanges and handleReportSurvey with the same rejection → clearSession not called
    - useSurveySyncNetwork: existing AUTH_REQUIRED tests still assert clearSession is called
    - useSurveySyncSurveyOperations: existing tests pass without the refreshToken param; refreshSessionTokens mock resolves `{ accessToken: "new-token" }`
    - useAuth0Session: result.current has no refreshToken / pendingEmailVerification / devVerificationToken keys; refreshSessionTokens resolves `{ accessToken }` with getCredentials called with forceRefresh true
  </behavior>
  <action>
    Update the mocks: remove the six stub keys from mockAuth0Session in useSurveySync.test.ts and add `sessionOwner: null`; in useSurveySyncNetwork.test.ts delete `refreshToken: null` from params and replace the "../useAuth0Session" mock with either nothing (auth-errors is a pure module and can be imported for real) or a mock of "../auth-errors" that re-implements the two predicates; in useSurveySyncSurveyOperations.test.ts remove `refreshToken` and change the refreshSessionTokens mock values to `{ accessToken: "new-token" }`. Add the new test cases from the behavior list. Keep the existing spy-based strategy in these three files (Phase 1.3 migrates them); the useAuth0Session test stays on renderHook.
  </action>
  <verify>
    <automated>npm --workspace mobile run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `npm --workspace mobile run test:unit` exits 0 (full mobile suite)
    - `grep -c "AUTH_TEMPORARILY_UNAVAILABLE" mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts` returns at least 3
    - `grep -c "mockClearLocalIbpData).not.toHaveBeenCalled" mobile/src/hooks/useSurveySync.test.ts` returns at least 1
    - `grep -E "pendingEmailVerification|devVerificationToken|handleVerifyEmail" mobile/src/hooks/useSurveySync.test.ts | grep -vc "toHaveProperty"` returns 0 (stub names only appear in not.toHaveProperty assertions)
  </acceptance_criteria>
  <done>The new session contract — no purge on session end, retry-later on temporary auth failure, no stubs — is pinned by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session lifecycle → local SQLite | A session transition must never delete unsynced field data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-21 | Denial of Service (data loss) | useSurveySync.clearSurveySessionState → clearLocalIbpData (M-C1) | mitigate | purge removed from the session-cleared callback; regression test asserts clearLocalIbpData is not called |
| T-01.2-22 | Denial of Service | Temporary auth failure treated as logout during sync/pull/report | mitigate | AUTH_TEMPORARILY_UNAVAILABLE branch keeps the session; tests assert clearSession not called |
| T-01.2-23 | Information Disclosure | Local data of an ended session remains on device | accept (bounded by plan 08) | data belongs to the same device user; plan 08 adds the owner check so it is never sent under another account, and explicit logout offers a confirmed purge |
</threat_model>

<verification>
- `npm --workspace mobile run test:unit`, `npm --workspace mobile run typecheck`, `npm --workspace mobile run lint`, `npm run format:check` green
</verification>

<success_criteria>
ROADMAP criteria 1 and 7: a session error never deletes local_surveys, sync_queue or local_attachments, even on a genuine session end; the pre-Auth0 stubs are gone and no caller tests `accessToken || refreshToken`.
</success_criteria>

<output>
Create `.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-06-SUMMARY.md` when done
</output>
