---
phase: 01.2-stop-field-data-loss-and-account-exposure
plan: 07
type: execute
wave: 2
depends_on: [01.2-03]
files_modified:
  - mobile/src/app/local-data-owner.ts
  - mobile/src/app/local-data-owner.test.ts
  - mobile/src/storage/local-owner.ts
  - mobile/src/storage/local-owner.test.ts
  - mobile/src/storage/surveys.ts
  - mobile/src/storage.ts
  - mobile/src/hooks/useLocalDataOwner.ts
  - mobile/src/hooks/useLocalDataOwner.test.ts
autonomous: true
requirements: [REQ-AUD-session-data-loss]

must_haves:
  truths:
    - "The Auth0 sub owning local data is persisted in local_meta under session_owner_sub (email under session_owner_email)"
    - "Unsynced local work (surveys and photos) can be counted for dialogs"
    - "When the logged-in sub differs from the stored owner and unsynced work exists, the owner hook reports 'conflict' with counts and never allows sync"
    - "When the stored owner is absent (upgrade from the shipped app) the current session adopts the data; when it matches, sync is allowed"
    - "When the owner differs but nothing is unsynced, synced leftovers are purged and the new account adopts the device"
    - "An explicit purge (clearLocalIbpData) also forgets the owner"
  artifacts:
    - path: mobile/src/app/local-data-owner.ts
      provides: "resolveLocalDataOwnership, hasUnsyncedWork, formatUnsyncedWorkSummary"
      exports: ["resolveLocalDataOwnership", "hasUnsyncedWork", "formatUnsyncedWorkSummary", "LocalOwnershipDecision", "UnsyncedLocalWork"]
    - path: mobile/src/storage/local-owner.ts
      provides: "getLocalDataOwner, setLocalDataOwner, countUnsyncedLocalWork, LOCAL_OWNER_SUB_KEY, LOCAL_OWNER_EMAIL_KEY"
      exports: ["getLocalDataOwner", "setLocalDataOwner", "countUnsyncedLocalWork", "LOCAL_OWNER_SUB_KEY", "LOCAL_OWNER_EMAIL_KEY"]
    - path: mobile/src/hooks/useLocalDataOwner.ts
      provides: "Owner-check state machine with syncAllowed"
      exports: ["useLocalDataOwner", "LocalDataOwnerStatus"]
  key_links:
    - from: mobile/src/hooks/useLocalDataOwner.ts
      to: mobile/src/app/local-data-owner.ts
      via: "resolveLocalDataOwnership decision"
      pattern: "resolveLocalDataOwnership\\("
    - from: mobile/src/storage/surveys.ts
      to: mobile/src/storage/local-owner.ts
      via: "clearLocalIbpData deletes owner keys"
      pattern: "LOCAL_OWNER_SUB_KEY"
---

<objective>
Build the pieces D-03 and D-04 need, without wiring them into the app yet: a persisted owner marker for local data (Auth0 sub in `local_meta`), a counter of unsynced surveys and photos, a pure ownership decision function, and a `useLocalDataOwner` hook whose `syncAllowed` flag is false unless the logged-in account owns the local data.

Purpose: REQ-AUD-session-data-loss — "local data is never attached to a different account after re-login" and "logout with unsynced work purges only after a confirmation that counts it".
Output: pure module + storage module + hook, each with tests. Plan 08 wires them into `useSurveySync` and the UI.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-CONTEXT.md
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-PATTERNS.md
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-03-SUMMARY.md

<interfaces>
From plan 03:
- mobile/src/app/id-token.ts: `export type IdTokenClaims = { sub: string; email: string | null }`
- mobile/src/hooks/render-hook-smoke.test.ts documents the renderHook recipe (async renderHook/act, `jest.mock("react-native", factory)`).

Existing storage (verified):
- mobile/src/storage/db.ts: `getDb(): Promise<SQLite.SQLiteDatabase>`; table `local_meta (key TEXT PRIMARY KEY NOT NULL, value TEXT NOT NULL, updated_at TEXT NOT NULL)` already exists — no schema change needed.
- `local_surveys` columns include `id`, `sync_state` ('pending' | 'failed' | 'synced'), `sync_blocked` INTEGER; `sync_queue (id, survey_id, payload, status, retry_count, next_retry_at, ...)`; `local_attachments (id, survey_id, sync_state, ...)`.
- mobile/src/storage/surveys.ts `clearLocalIbpData()` runs DELETE on sync_queue, local_attachments, local_surveys and `DELETE FROM local_meta WHERE key = 'downsync_cursor'`.
- mobile/src/storage/sync.ts has private getMetaValue/setMetaValue (upsert `INSERT ... ON CONFLICT(key) DO UPDATE`) — do not import them; implement the same SQL in local-owner.ts to avoid a sync.ts ↔ surveys.ts import cycle.
- mobile/src/storage.ts is the barrel: `export * from "./storage/db"`, `"./storage/surveys"`, `"./storage/sync"`.
- expo-sqlite is mapped in Jest to mobile/test/expo-sqlite.mock.ts (jest.fn stubs; no real SQL until Phase 1.3) — storage tests mock "./db" getDb with a fake db object and assert SQL/params.

Decision table (D-04; the owner marker is the Auth0 sub):
| storedOwnerSub | sessionSub | unsynced work | decision |
| any | null | any | "unknown-session" (sync stays suspended) |
| null | sub | any | "adopt" (upgrade path: the shipped app purged on logout, so existing local data belongs to the restored session) |
| = sessionSub | sub | any | "match" |
| ≠ sessionSub | sub | yes | "conflict" (never sent, never merged) |
| ≠ sessionSub | sub | none | "purge-and-adopt" (only synced copies of another account remain) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ownership decision, unsynced-work summary and owner storage (TDD)</name>
  <files>mobile/src/app/local-data-owner.ts, mobile/src/app/local-data-owner.test.ts, mobile/src/storage/local-owner.ts, mobile/src/storage/local-owner.test.ts, mobile/src/storage/surveys.ts, mobile/src/storage.ts</files>
  <read_first>
    - mobile/src/storage/db.ts (schema, getDb)
    - mobile/src/storage/surveys.ts (clearLocalIbpData, hasPendingSyncWork query style)
    - mobile/src/storage/sync.ts lines 385-410 (meta upsert SQL to replicate)
    - mobile/src/storage.ts (barrel)
    - mobile/src/app/id-token.ts (IdTokenClaims)
    - mobile/src/hooks/operation-status.test.ts (pure test template)
  </read_first>
  <behavior>
    resolveLocalDataOwnership({ storedOwnerSub, sessionSub, unsynced }) follows the decision table exactly (one test per row, plus "match" when unsynced > 0).
    hasUnsyncedWork({ surveys: 0, attachments: 0 }) === false; true when either count > 0.
    formatUnsyncedWorkSummary (French):
    - { surveys: 1, attachments: 0 } → "1 relevé"
    - { surveys: 3, attachments: 0 } → "3 relevés"
    - { surveys: 0, attachments: 1 } → "1 photo"
    - { surveys: 2, attachments: 5 } → "2 relevés et 5 photos"
    - { surveys: 0, attachments: 0 } → "aucune donnée"
    Storage (getDb mocked):
    - getLocalDataOwner returns null when no session_owner_sub row; returns { sub, email } when rows exist (email null when absent)
    - setLocalDataOwner({ sub, email: "a@b.fr" }) upserts both keys; with email null it upserts the sub and deletes session_owner_email
    - countUnsyncedLocalWork returns { surveys, attachments } from two COUNT queries: surveys counts local_surveys rows WHERE sync_state != 'synced' OR sync_blocked = 1 OR id IN (SELECT survey_id FROM sync_queue); attachments counts local_attachments WHERE sync_state != 'synced'
    - clearLocalIbpData additionally deletes the two owner keys from local_meta
  </behavior>
  <action>
    Tests first (RED), then implement (GREEN).

    mobile/src/app/local-data-owner.ts: export `type UnsyncedLocalWork = { surveys: number; attachments: number }`, `type LocalOwnershipDecision = "adopt" | "match" | "purge-and-adopt" | "conflict" | "unknown-session"`, `hasUnsyncedWork(work)`, `resolveLocalDataOwnership(input: { storedOwnerSub: string | null; sessionSub: string | null; unsynced: UnsyncedLocalWork }): LocalOwnershipDecision` implementing the table, and `formatUnsyncedWorkSummary(work): string` with the French singular/plural rules above (UI is French; wording is Claude's discretion per CONTEXT).

    mobile/src/storage/local-owner.ts: export `LOCAL_OWNER_SUB_KEY = "session_owner_sub"` and `LOCAL_OWNER_EMAIL_KEY = "session_owner_email"` (key names per D-04 discretion); `getLocalDataOwner(): Promise<{ sub: string; email: string | null } | null>`; `setLocalDataOwner(owner: { sub: string; email: string | null }): Promise<void>` using the same `INSERT INTO local_meta (key, value, updated_at) VALUES (?, ?, ?) ON CONFLICT(key) DO UPDATE SET value = excluded.value, updated_at = excluded.updated_at` statement as sync.ts, and `DELETE FROM local_meta WHERE key = ?` for a null email; `countUnsyncedLocalWork(): Promise<UnsyncedLocalWork>` with the two COUNT queries from the behavior list (Number(row?.count ?? 0)). Import getDb from "./db" only.

    mobile/src/storage/surveys.ts: in clearLocalIbpData add `DELETE FROM local_meta WHERE key IN (?, ?)` with [LOCAL_OWNER_SUB_KEY, LOCAL_OWNER_EMAIL_KEY] imported from "./local-owner", so any explicit purge also forgets the owner. mobile/src/storage.ts: add `export * from "./storage/local-owner"`.
  </action>
  <verify>
    <automated>npm --workspace mobile run test:unit -- local-data-owner local-owner && npm --workspace mobile run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm --workspace mobile run test:unit -- local-data-owner local-owner` exits 0 with at least 16 passing tests
    - `grep -c "session_owner_sub" mobile/src/storage/local-owner.ts` returns 1
    - `grep -c "LOCAL_OWNER_SUB_KEY" mobile/src/storage/surveys.ts` returns at least 1
    - `grep -c "local-owner" mobile/src/storage.ts` returns 1
    - `grep -c "from \"./sync\"" mobile/src/storage/local-owner.ts` returns 0 (no import cycle)
    - `npm --workspace mobile run typecheck` exits 0
  </acceptance_criteria>
  <done>Owner marker persistence, unsynced counting and the pure decision are implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useLocalDataOwner hook with a default-deny syncAllowed flag</name>
  <files>mobile/src/hooks/useLocalDataOwner.ts, mobile/src/hooks/useLocalDataOwner.test.ts</files>
  <read_first>
    - mobile/src/hooks/render-hook-smoke.test.ts (renderHook recipe from plan 03)
    - mobile/src/app/local-data-owner.ts and mobile/src/storage/local-owner.ts (Task 1)
    - mobile/src/storage/surveys.ts (clearLocalIbpData)
    - mobile/src/hooks/useGpsCapture.test.ts (mock conventions)
  </read_first>
  <behavior>
    renderHook tests with "../storage/local-owner" and "../storage/surveys" mocked:
    - sessionOwner null → status "idle", syncAllowed false, no storage call
    - stored owner null, sessionOwner {sub:"a"} → setLocalDataOwner({sub:"a", email}) called, status "ok", syncAllowed true
    - stored owner "a", sessionOwner "a" → status "ok", clearLocalIbpData not called
    - stored owner "a", sessionOwner "b", unsynced {surveys:2, attachments:1} → status "conflict", foreignWork {2,1}, foreignOwnerEmail = stored email, syncAllowed false, clearLocalIbpData and setLocalDataOwner NOT called
    - stored owner "a", sessionOwner "b", unsynced {0,0} → clearLocalIbpData called, then setLocalDataOwner({sub:"b",...}), onLocalDataPurged called once, status "ok"
    - in conflict, `await act(() => result.current.discardForeignData())` → clearLocalIbpData, setLocalDataOwner({sub:"b"}), onLocalDataPurged, status "ok", syncAllowed true
    - storage rejection during the check → status "error", syncAllowed false; `recheck()` after the mock resolves → status "ok"
    - changing sessionOwner from "a" to null (session ended) → status "idle", syncAllowed false
  </behavior>
  <action>
    Tests first, then create mobile/src/hooks/useLocalDataOwner.ts:
    `export type LocalDataOwnerStatus = "idle" | "checking" | "ok" | "conflict" | "error"`;
    `export function useLocalDataOwner(params: { sessionOwner: IdTokenClaims | null; onLocalDataPurged: () => Promise<void> })` returning `{ status, syncAllowed, foreignWork, foreignOwnerEmail, discardForeignData, recheck }` where `syncAllowed = status === "ok"` (default-deny: every other status suspends sync, D-04).
    A `recheck` useCallback reads `getLocalDataOwner()` and `countUnsyncedLocalWork()`, calls resolveLocalDataOwnership, and applies: adopt → setLocalDataOwner(sessionOwner) → "ok"; match → setLocalDataOwner(sessionOwner) only if the stored email differs → "ok"; purge-and-adopt → clearLocalIbpData() → setLocalDataOwner(sessionOwner) → await onLocalDataPurged() → "ok"; conflict → store foreignWork and foreignOwnerEmail (stored email) → "conflict"; unknown-session → "idle". Wrap in try/catch → "error". Set "checking" at the start. Guard against stale async results when sessionOwner changes mid-check (compare against a ref holding the current sub).
    A useEffect keyed on `sessionOwner?.sub` runs recheck (or sets "idle" when null).
    `discardForeignData` (called by plan 08 only AFTER the user confirms): clearLocalIbpData() → setLocalDataOwner(sessionOwner) → await onLocalDataPurged() → reset foreignWork → "ok". It must never run on its own.
    The hook never calls the API and never shows UI.
  </action>
  <verify>
    <automated>npm --workspace mobile run test:unit -- useLocalDataOwner && npm --workspace mobile run typecheck && npm --workspace mobile run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm --workspace mobile run test:unit -- useLocalDataOwner` exits 0 with the 8 behavior cases passing
    - `grep -c "syncAllowed = status === \"ok\"\|syncAllowed: status === \"ok\"" mobile/src/hooks/useLocalDataOwner.ts` returns 1
    - `grep -c "ibp-api\|fetch(" mobile/src/hooks/useLocalDataOwner.ts` returns 0 (no network access)
    - `npm --workspace mobile run test:unit` (full suite) exits 0
  </acceptance_criteria>
  <done>A tested hook tells the app whether the logged-in account owns the local data, and suspends sync otherwise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device shared between accounts → server account | Locally stored surveys of account A must never be uploaded under account B |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-24 | Spoofing / Information Disclosure | Re-login with a different account on the same device (D-04) | mitigate | owner marker session_owner_sub; resolveLocalDataOwnership returns "conflict" on mismatch with unsynced work; syncAllowed default-deny; tests assert no purge and no sync permission in conflict |
| T-01.2-25 | Denial of Service (data loss) | Automatic purge on owner mismatch | mitigate | purge only when zero unsynced work (synced copies); unsynced data is purged only through discardForeignData after explicit confirmation (plan 08) |
| T-01.2-26 | Tampering | local_meta owner keys edited on a rooted device | accept | attacker with device root already has the SQLite file; the server still authorizes every write by access token |
</threat_model>

<verification>
- `npm --workspace mobile run test:unit`, `npm --workspace mobile run typecheck`, `npm --workspace mobile run lint`, `npm run format:check` green
</verification>

<success_criteria>
The mechanism behind ROADMAP criterion 2 exists and is tested: local data carries its owner, unsynced work is countable, and a different account can never obtain `syncAllowed`.
</success_criteria>

<output>
Create `.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-07-SUMMARY.md` when done
</output>
