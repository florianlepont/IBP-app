---
phase: 01.2-stop-field-data-loss-and-account-exposure
plan: 09
type: execute
wave: 4
depends_on: [01.2-01, 01.2-02, 01.2-05, 01.2-08]
files_modified:
  - .planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-VALIDATION.md
autonomous: false
requirements: [REQ-AUD-session-data-loss, REQ-AUD-rate-limit, REQ-AUD-debug-surface, REQ-AUD-identity, REQ-AUD-mobile-quick-fixes]

must_haves:
  truths:
    - "The full quality gate (lint, typecheck, unit, E2E, format) is green on the phase result"
    - "On a real device build, an expired access token with no network keeps the session and every unsynced survey"
    - "On a real device build, a revoked refresh token ends the session, keeps the queue, and re-login with the same account syncs it"
    - "On a real device build, logout with unsynced work shows the count; another account sees the conflict screen; dev tools are absent from a release build"
  artifacts:
    - path: .planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-VALIDATION.md
      provides: "Per-task statuses and manual verification results recorded"
  key_links:
    - from: 01.2-VALIDATION.md
      to: "manual-only verifications"
      via: "results recorded per row"
      pattern: "Manual-Only Verifications"
---

<objective>
Phase gate: run the complete automated suite once on the integrated result, then have the developer verify on a device the behaviors that mocks cannot prove — the real Auth0 RENEW_FAILED/NO_NETWORK classification (research assumption A1), the logout and conflict dialogs, and dev tools in a release build.

Purpose: all five phase requirements; M-C1 is a critical data-loss defect in the shipped app and a corrective mobile release follows this phase (STATE.md blocker), so the fix is confirmed on real hardware before release.
Output: green suite, VALIDATION.md statuses updated, developer sign-off.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full quality gate and record results</name>
  <files>.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-VALIDATION.md (Per-Task Verification Map)
    - CLAUDE.md ("Before committing" command list)
    - the SUMMARY files of plans 01-08 in the phase directory
  </read_first>
  <action>
    Ensure PostgreSQL is running (`docker compose -f infra/docker-compose.yml up -d`), then run from the repo root, in order: `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run test:e2e`, `npm run format:check`. Fix nothing silently: if a command fails, stop and report which plan's files are involved. Then run the phase-level source checks: `grep -rn "accessToken || refreshToken" mobile/src mobile/App.tsx` (expect no output); `grep -rnE "pendingEmailVerification|devVerificationToken|handleVerifyEmail|handleResendVerification|handleCancelEmailVerification" mobile/src mobile/App.tsx --include=*.ts --include=*.tsx | grep -v "\.test\.ts"` (expect no output); `sed -n '/const clearSurveySessionState/,/}, \[\])/p' mobile/src/hooks/useSurveySync.ts | grep -c clearLocalIbpData` (expect 0). In 01.2-VALIDATION.md set each Per-Task Verification Map row's Status to ✅ green (or ❌ red with the failing command), and set `wave_0_complete: true` in the frontmatter.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run test:unit && npm run test:e2e && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - All five root commands exit 0
    - The three phase-level grep checks produce the expected empty/0 results
    - `grep -c "⬜ pending" .planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-VALIDATION.md` returns 1 (only the 01.2-09-02 manual row, closed by Task 2)
  </acceptance_criteria>
  <done>The integrated phase passes every automated gate and VALIDATION.md reflects it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device verification of session safety, logout, owner conflict and dev tools</name>
  <files>.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-VALIDATION.md section "Manual-Only Verifications"
  </read_first>
  <action>Present the steps below to the developer, wait for the resume signal, then record each result in the "Manual-Only Verifications" table of 01.2-VALIDATION.md.</action>
  <what-built>Plans 01-08: classified Auth0 session errors (no purge on session end), forced refresh on 401, counted confirmed-purge logout, account-owner check with a blocking conflict screen, dev tools hidden outside __DEV__, fixed nearby-parcels bbox, and the API-side rate limiting, debug gating, identity and report privacy fixes.</what-built>
  <how-to-verify>
    Use a device build pointed at a local or staging API (`npm run dev:api:migrated`, `npm run ios` or `npm run android`).
    1. Offline session keep: log in, create 2 draft surveys and 1 photo while in airplane mode. Force-quit the app, wait until the access token is expired (or shorten the API token lifetime in the Auth0 tenant for the test), relaunch in airplane mode. Expected: no data loss — the 2 surveys and the photo are still listed after reconnecting and logging in if needed; nothing was wiped.
    2. Revoked refresh token: with 1 unsynced survey, revoke the user's refresh tokens in the Auth0 dashboard (User → Devices/Authorized apps → revoke), reconnect. Expected: session ends ("Session expirée…" status), the survey is still on the device; log in again with the SAME account → the survey syncs automatically.
    3. Logout with unsynced work: create 1 survey offline, go to Compte → Se déconnecter → confirm. Expected: a second alert "Données non synchronisées" states "1 relevé"; "Annuler" keeps everything; "Supprimer et se déconnecter" logs out and the list is empty on next login. Logout with everything synced shows no second alert.
    4. Other account: create 1 survey offline with account A, end the session without purging (step 2's revocation, or choose "Me reconnecter…" later), log in with account B. Expected: blocking screen "Relevés d'un autre compte" with "1 relevé", no sync happens (check API logs: no POST /v1/sync from B). "Me reconnecter avec l'autre compte" returns to login; logging in as A syncs the survey. Repeat and choose "Supprimer ces données" → confirmation alert → data deleted, B continues normally.
    5. Dev tools: in a release build (`npx expo run:ios --configuration Release` or `npx expo run:android --variant release`), open Réglages. Expected: no "Outils développeur" section.
    6. Nearby parcels: on the home screen with location allowed near known parcels, the "parcelles proches" list is populated (the API receives bbox as minLng,minLat,maxLng,maxLat — visible in API request logs).
    7. Rate limiting behind Caddy (after the API deploys to the VPS): sync from two devices at once; Caddy/API logs show no burst of 429. If unauthenticated requests all share one IP bucket, set `TRUST_PROXY=loopback,uniquelocal` in /home/ubuntu/cortege.env and restart the stack.
  </how-to-verify>
  <resume-signal>Type "approved", or list the step numbers that failed with what you saw</resume-signal>
  <verify>
    <automated>grep -c "Manual-Only Verifications" .planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Developer replied "approved", or every failed step is recorded in 01.2-VALIDATION.md with the observed behavior
  </acceptance_criteria>
  <done>Device behavior confirmed for session errors, logout, account conflict, dev tools and bbox; results recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real Auth0 tenant → device session | SDK error mapping on real iOS/Android can differ from mocked tests (assumption A1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-32 | Denial of Service (data loss) | RENEW_FAILED/NO_NETWORK mapping on real devices (A1) | mitigate | steps 1-2 exercise both paths on hardware before the corrective release |
| T-01.2-33 | Denial of Service | Proxy topology collapses client IPs | mitigate | step 7 checks production logs; TRUST_PROXY override documented by plan 01 |
</threat_model>

<verification>
Automated gate green and developer sign-off recorded in 01.2-VALIDATION.md.
</verification>

<success_criteria>
All seven ROADMAP success criteria for Phase 01.2 are demonstrated: automated for API and hook logic, on-device for Auth0 behavior, dialogs, and release-build dev tools.
</success_criteria>

<output>
Create `.planning/phases/01.2-stop-field-data-loss-and-account-exposure/01.2-09-SUMMARY.md` when done
</output>
