---
phase: 01.3-ci-and-test-safety-net
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/jest.unit.config.js
  - mobile/test/expo-sqlite.mock.ts
  - mobile/src/hooks/render-hook-wrapper.test.tsx
  - mobile/src/storage.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [REQ-AUD-test-infra]

must_haves:
  truths:
    - "Every mobile unit test that opens the local database gets a real in-memory SQLite database (node:sqlite) by default, not jest.fn() stubs"
    - "A deliberately broken SQL statement in mobile/src/storage/surveys.ts makes mobile/src/storage.test.ts fail"
    - "A *.test.tsx file under mobile/src is collected and run by the mobile unit Jest config"
    - "mobile/jest.unit.config.js has exactly one moduleNameMapper block (the shadowed duplicate is gone)"
    - "The full mobile unit suite still passes, and useSurveySync.test.ts is not rewritten (D-06)"
  artifacts:
    - path: mobile/test/expo-sqlite.mock.ts
      provides: "Default expo-sqlite mock delegating openDatabaseAsync to createNodeSqliteDb()"
      contains: "createNodeSqliteDb"
    - path: mobile/src/storage.test.ts
      provides: "Storage-layer tests asserting table state via real SQL"
      min_lines: 400
    - path: mobile/src/hooks/render-hook-wrapper.test.tsx
      provides: "Proof that .test.tsx is collected; renderHook with a JSX wrapper"
    - path: mobile/jest.unit.config.js
      provides: "testMatch including .test.tsx, single moduleNameMapper"
      contains: "**/*.test.tsx"
  key_links:
    - from: mobile/jest.unit.config.js
      to: mobile/test/expo-sqlite.mock.ts
      via: "moduleNameMapper '^expo-sqlite$'"
      pattern: "expo-sqlite\\.mock\\.ts"
    - from: mobile/test/expo-sqlite.mock.ts
      to: mobile/test/node-sqlite-db.ts
      via: "import { createNodeSqliteDb }"
      pattern: "from \"\\./node-sqlite-db\""
    - from: mobile/src/storage.test.ts
      to: mobile/src/storage/db.ts
      via: "getDb() for direct SELECT assertions"
      pattern: "getDb\\("
---

<objective>
Make real SQL the default in mobile unit tests and collect `*.test.tsx` (audit T3/T4, lot L7 core). Route the global `expo-sqlite` mock through the existing `node:sqlite` helper (D-05), rewrite `storage.test.ts` to assert on table state instead of SQL string fragments, fix the duplicated `moduleNameMapper`, add `.test.tsx` to `testMatch` (D-07) with one real `.test.tsx` file, and floor Node at 22.5 for `node:sqlite`.

Purpose: REQ-AUD-test-infra — Phases 01.4 and 01.5 depend on storage tests that fail on broken SQL. Land in wave 1.
Output: rewired mock and config, rewritten storage.test.ts, one .test.tsx proof, root `engines`.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.3-ci-and-test-safety-net/01.3-CONTEXT.md
@.planning/phases/01.3-ci-and-test-safety-net/01.3-RESEARCH.md
@.planning/phases/01.3-ci-and-test-safety-net/01.3-PATTERNS.md
@CLAUDE.md

<interfaces>
mobile/test/node-sqlite-db.ts (phase 1.2, unchanged by this plan):
  export function createNodeSqliteDb(): {
    execAsync(sql: string): Promise<void>
    runAsync(sql: string, values?: unknown[]): Promise<{ changes: number; lastInsertRowId: number }>
    getFirstAsync<T>(sql: string, values?: unknown[]): Promise<T | null>
    getAllAsync<T>(sql: string, values?: unknown[]): Promise<T[]>
    withTransactionAsync(task: () => Promise<void>): Promise<void>
    closeAsync(): Promise<void>
  }

mobile/src/storage/db.ts: `getDb()` caches one `SQLite.openDatabaseAsync("cortege-local.db")` promise per module registry (so one real DB per test file); `initLocalDb()` creates tables local_surveys, sync_queue, local_attachments (+ index), local_meta and runs tolerant `ALTER TABLE ... ADD COLUMN` upgrades (errors swallowed). Production storage code only calls execAsync, runAsync, getFirstAsync, getAllAsync.

mobile/src/storage.ts re-exports everything from ./storage/db, ./storage/surveys, ./storage/sync, ./storage/local-owner (so `getDb` and `initLocalDb` are importable from "./storage").

Existing real-SQL analog: mobile/src/storage/local-owner.sqlite.test.ts (beforeAll initLocalDb; beforeEach `DELETE FROM local_surveys; DELETE FROM sync_queue; DELETE FROM local_attachments; DELETE FROM local_meta;`). It declares its own jest.mock("expo-sqlite") factory — leave it unchanged.

renderHook recipe (mobile/src/hooks/render-hook-smoke.test.ts): jest.mock("react-native", () => ({ Platform: {...}, StyleSheet: {...} })) BEFORE importing from "@testing-library/react-native/pure"; renderHook and act are async (await them); call cleanup() in afterEach. tsconfig.jest.json sets "jsx": "react-jsx".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-SQL default mock, single moduleNameMapper, .test.tsx collection, Node engines floor</name>
  <files>mobile/jest.unit.config.js, mobile/test/expo-sqlite.mock.ts, mobile/src/hooks/render-hook-wrapper.test.tsx, package.json, package-lock.json</files>
  <read_first>
    - mobile/jest.unit.config.js (two moduleNameMapper keys today; the second one wins)
    - mobile/test/expo-sqlite.mock.ts (current jest.fn() stub being replaced)
    - mobile/test/node-sqlite-db.ts (helper the mock must delegate to)
    - mobile/src/storage/db.ts (getDb caching behaviour)
    - mobile/src/hooks/render-hook-smoke.test.ts (renderHook recipe to copy)
    - package.json (root; add engines only)
  </read_first>
  <action>
    Edit mobile/jest.unit.config.js (per D-07): set `testMatch` to `['**/*.test.ts', '**/*.test.tsx']`; delete the first `moduleNameMapper` block (the one containing only `^@expo/vector-icons$`) so exactly one `moduleNameMapper` remains, with its five existing mappings unchanged (`^@expo/vector-icons$`, `^expo-sqlite$`, `^expo-haptics$`, `^react-native-svg$`, the image extension regex). Change nothing else in this file (coverage thresholds are plan 04's job).

    Rewrite mobile/test/expo-sqlite.mock.ts (per D-05, no better-sqlite3): import `createNodeSqliteDb` from "./node-sqlite-db"; export `openDatabaseAsync = jest.fn(async (_name: string) => createNodeSqliteDb())` so each call returns a fresh real in-memory database (getDb() caches it, giving one DB per test file); export `type SQLiteDatabase = ReturnType<typeof createNodeSqliteDb>`. Add a header comment: real SQL executes by default so broken SQL fails tests; tests needing a shared handle call `getDb()` from the storage module.

    Create mobile/src/hooks/render-hook-wrapper.test.tsx (per D-07, proof of .tsx collection and JSX transform): follow the render-hook-smoke recipe (jest.mock("react-native") plain-object factory before importing from "@testing-library/react-native/pure", afterEach cleanup). Define a test-local `React.createContext<string>("default")` and a hook `useLabel()` that reads it via `useContext`. Test 1: `await renderHook(() => useLabel(), { wrapper })` where `wrapper` is a JSX component rendering `<LabelContext.Provider value="field">{children}</LabelContext.Provider>` — expect `result.current` to be "field". Test 2: without wrapper, expect "default". Type the wrapper props as `{ children: React.ReactNode }`; no `any`.

    Root package.json: add `"engines": { "node": ">=22.5.0" }` (node:sqlite DatabaseSync needs 22.5+). Then run `npm install --package-lock-only --ignore-scripts` from the repo root to sync the lockfile; the only expected lockfile change is an `engines` block in the root package entry (measured during planning: 3 added lines). If the lockfile diff is larger, revert package-lock.json and package.json engines change and report it instead of committing unrelated lockfile churn.

    Run the full mobile unit suite. The global mock now executes real SQL, so any test that imported storage without its own expo-sqlite mock now hits a real DB: if one fails, fix the test minimally (e.g. call initLocalDb() in beforeAll) — do not change production code, and do not rewrite mobile/src/hooks/useSurveySync.test.ts beyond what a mock change strictly requires (D-06). storage.test.ts keeps its own local jest.mock factory until Task 2, so it must still pass after this task.
  </action>
  <verify>
    <automated>cd mobile && npx jest --config jest.unit.config.js --listTests | grep -c "render-hook-wrapper.test.tsx" && npm --workspace mobile run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `cd mobile && npx jest --config jest.unit.config.js --listTests | grep -c '\.test\.tsx$'` returns at least 1
    - `grep -c "moduleNameMapper" mobile/jest.unit.config.js` returns 1
    - `grep -c "createNodeSqliteDb" mobile/test/expo-sqlite.mock.ts` returns at least 1 and `grep -c "mockResolvedValue" mobile/test/expo-sqlite.mock.ts` returns 0
    - `node -e "process.exit(require('./package.json').engines.node === '>=22.5.0' ? 0 : 1)"` exits 0 (run at repo root)
    - `git diff --numstat package-lock.json` shows at most 3 added and 0 deleted lines
    - `npm --workspace mobile run test:unit` exits 0 and reports 2 more tests than before this task (the two in render-hook-wrapper.test.tsx)
    - `git diff --quiet mobile/src/hooks/useSurveySync.test.ts` exits 0, or the SUMMARY names the exact mock-driven lines changed
    - `npm --workspace mobile run lint` and `npm --workspace mobile run typecheck` exit 0
  </acceptance_criteria>
  <done>Real SQL is the default for every mobile test that opens the local DB, .test.tsx files run, and Node >= 22.5 is declared.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite storage.test.ts to assert on table state and prove broken SQL fails</name>
  <files>mobile/src/storage.test.ts</files>
  <read_first>
    - mobile/src/storage.test.ts (807 lines, 48 tests; the local jest.mock("expo-sqlite") factory, mockDb and getRunAsyncPayload are removed)
    - mobile/src/storage/local-owner.sqlite.test.ts (real-SQL test pattern: initLocalDb in beforeAll, DELETE all rows in beforeEach, SELECT to assert)
    - mobile/src/storage/surveys.ts and mobile/src/storage/sync.ts (functions under test, their SQL and returned shapes)
    - mobile/src/storage/db.ts (schema: exact column names to SELECT)
    - mobile/test/expo-sqlite.mock.ts (from Task 1)
  </read_first>
  <action>
    Rewrite mobile/src/storage.test.ts (D-05; audit T3): delete the local `jest.mock("expo-sqlite", ...)` factory, the `mockDb` spy object, the `MockFn` type and `getRunAsyncPayload`, so the file uses the global real-SQL mock from Task 1. Import `getDb` together with the existing function imports from "./storage". Add `beforeAll(async () => { await initLocalDb() })` and a `beforeEach` that runs `DELETE FROM local_surveys; DELETE FROM sync_queue; DELETE FROM local_attachments; DELETE FROM local_meta;` through `(await getDb()).execAsync(...)`. Keep `jest.clearAllMocks()` only where fetch mocks need it.

    Keep every existing describe block and keep at least 48 tests. Convert each test from "which SQL string was sent / which mocked row was returned" to "what state the real database holds":
    - initLocalDb: after init, `SELECT name FROM sqlite_master WHERE type='table'` contains local_surveys, sync_queue, local_attachments, local_meta; calling initLocalDb a second time resolves (duplicate-column ALTER errors are swallowed); `PRAGMA table_info(local_surveys)` includes visibility, sync_blocked, payload_json, created_at.
    - createLocalDraft / updateLocalDraft / queueDeleteSurvey / queueLocalAttachment / queueDeleteAttachment / retrySurveyNow / discardSurveyLocalChanges / markSurveyExpiredLocally / clearLocalIbpData: perform the call, then SELECT from local_surveys, sync_queue or local_attachments and assert the resulting rows and columns (payload JSON parsed, status, sync_state, retry_count, next_retry_at, row counts). Seed prerequisite rows through the public functions (createLocalDraft, queueLocalAttachment) or a direct INSERT with the exact schema columns — never through mockResolvedValue.
    - getLocalSurveyDraft / listLocalSurveys / listLocalAttachments / hasPendingSyncWork: seed real rows (use makeSurveyRow values as INSERT parameters), call the function, assert the mapped result.
    - syncPending / pullRemoteChanges: keep the `global.fetch = jest.fn()` mocks; seed the queue with createLocalDraft instead of mocked getAllAsync rows; after pullRemoteChanges with two remote surveys, assert both rows exist in local_surveys.
    Add one describe "expo-sqlite test double" with a test asserting `(await getDb()).runAsync("INSERT INTO no_such_table VALUES (1)")` rejects — a permanent guard that the mock runs real SQL.

    Then prove broken SQL fails (record commands and exit codes in the SUMMARY): copy mobile/src/storage/surveys.ts to the scratchpad, run `sed -i 's/INSERT INTO local_surveys (id, site_name/INSERT INTO local_surveys (id, site_nam/' mobile/src/storage/surveys.ts`, run `npm --workspace mobile run test:unit -- storage.test.ts` (must exit non-zero), then restore with `git checkout -- mobile/src/storage/surveys.ts` and confirm `git diff --quiet mobile/src/storage/surveys.ts`. Formatting: double quotes, no semicolons, trailing commas, 100 cols.
  </action>
  <verify>
    <automated>npm --workspace mobile run test:unit -- storage.test.ts && npm --workspace mobile run test:unit && git diff --quiet mobile/src/storage/surveys.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "jest.mock(\"expo-sqlite\"" mobile/src/storage.test.ts` returns 0
    - `grep -c "getRunAsyncPayload\|mockDb\." mobile/src/storage.test.ts` returns 0
    - `grep -c "mock.calls" mobile/src/storage.test.ts` returns 0, or every remaining occurrence inspects `global.fetch` (check with `grep -n "mock.calls" mobile/src/storage.test.ts`)
    - `grep -cE "^\s*(test|it)\(" mobile/src/storage.test.ts` returns at least 49
    - `grep -c "no_such_table" mobile/src/storage.test.ts` returns 1
    - `npm --workspace mobile run test:unit -- storage.test.ts` exits 0 on the clean tree, and exited non-zero with the `site_nam` sabotage applied (recorded in SUMMARY)
    - `git diff --quiet mobile/src/storage/surveys.ts` exits 0 after the sabotage is reverted
    - `npm --workspace mobile run test:unit`, `npm --workspace mobile run lint` and `npx prettier --check mobile/src/storage.test.ts` exit 0
  </acceptance_criteria>
  <done>storage.test.ts verifies resulting table state against a real SQLite engine; a broken INSERT in surveys.ts fails it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → node:sqlite | Test-only in-memory database; no production data, no network |
| Developer machine → npm registry | `npm install --package-lock-only` resolves the lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3-01 | Tampering | Storage SQL regressions reaching field devices (sync data loss) | mitigate | Real-SQL default mock plus state-based storage.test.ts; sabotage run proves a broken INSERT fails the suite |
| T-01.3-02 | Tampering | package-lock.json churn from `npm install --package-lock-only` | mitigate | Acceptance caps the lockfile diff at the 3-line engines block; larger diffs are reverted, not committed |
| T-01.3-03 | Denial of Service | node:sqlite is experimental in Node 22 | accept | Test-only dependency; production uses expo-sqlite; engines floor >=22.5.0 makes an old-Node failure explicit |
| T-01.3-SC | Tampering | npm installs | accept | No new packages added (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npm --workspace mobile run test:unit`, `npm --workspace mobile run lint`, `npm --workspace mobile run typecheck` exit 0
- `cd mobile && npx jest --config jest.unit.config.js --listTests | grep '\.test\.tsx$'` lists render-hook-wrapper.test.tsx
</verification>

<success_criteria>
ROADMAP criterion 4 (mobile half): mobile unit tests execute real SQL behind the expo-sqlite mock, hooks are tested with renderHook (existing phase 1.2 pattern plus the new .tsx test), and *.test.tsx files are picked up.
</success_criteria>

<output>
Create `.planning/phases/01.3-ci-and-test-safety-net/01.3-01-SUMMARY.md` when done
</output>
