---
phase: 01.3-ci-and-test-safety-net
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/app.json
  - mobile/metro.config.js
autonomous: true
requirements: [REQ-AUD-ci-pipeline]

must_haves:
  truths:
    - "`npx expo-doctor` exits 0 in mobile/ (the newArchEnabled and disableHierarchicalLookup findings are fixed, not suppressed) (D-02)"
    - "`npx expo export --platform android` still succeeds after the change"
    - "The exported bundle contains exactly one copy of react and one of react-native (the monorepo reason for the Metro override is proven obsolete or preserved)"
    - "Whether the effective native (prebuild) config changed is recorded, so plan 06's owner device check knows what to look at"
  artifacts:
    - path: mobile/app.json
      provides: "Expo config without the invalid newArchEnabled key"
    - path: mobile/metro.config.js
      provides: "Metro config that passes expo-doctor while keeping a single React/React Native copy"
  key_links:
    - from: mobile/metro.config.js
      to: "expo/metro-config getDefaultConfig"
      via: "monorepo defaults (watchFolders, nodeModulesPaths) instead of disableHierarchicalLookup"
      pattern: "getDefaultConfig\\(__dirname\\)"
---

<objective>
Fix the two current `expo-doctor` failures before the `mobile-build` CI job is wired (D-02), so that job starts green: the `newArchEnabled` key in `mobile/app.json` fails schema validation, and `mobile/metro.config.js` sets `resolver.disableHierarchicalLookup = true` against Expo's expected default.

Purpose: REQ-AUD-ci-pipeline (mobile build check). A check that is red on day one trains everyone to ignore it.
Output: corrected app.json and metro.config.js, with proof that `expo export` still bundles a single React.

Owner device check: the Metro change affects the JS bundle and removing `newArchEnabled` touches native config input, so plan 06's human checkpoint includes a dev-build smoke test on a device (D-02). This plan records what changed so that check is targeted.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.3-ci-and-test-safety-net/01.3-CONTEXT.md
@.planning/phases/01.3-ci-and-test-safety-net/01.3-RESEARCH.md
@CLAUDE.md
@mobile/README-native.md

<interfaces>
mobile/metro.config.js today: `getDefaultConfig(__dirname)` from "expo/metro-config"; then `config.resolver.nodeModulesPaths = [mobile/node_modules, ../node_modules]`, `config.resolver.disableHierarchicalLookup = true`, `config.resolver.extraNodeModules = { react: mobile/node_modules/react, "react-native": mobile/node_modules/react-native }`, with a comment "keep Metro resolution pinned to mobile/node_modules so React / React Native versions remain consistent".
Measured during planning: `mobile/node_modules/react` and `mobile/node_modules/react-native` do NOT exist — both are hoisted to the root `node_modules` (root package.json pins expo 57.0.24, react 19.2.3, react-native 0.86.3 via dependencies + overrides). The extraNodeModules targets are therefore dead paths today.
mobile/app.json line 36: `"newArchEnabled": true` at the `expo` level (Expo SDK 57 / RN 0.86: New Architecture is always on; the key is no longer in the config schema).
expo-doctor is not a project dependency; research ran `npx expo-doctor` (resolved 1.20.4) with 19/21 checks passing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove newArchEnabled and record the prebuild config impact</name>
  <files>mobile/app.json</files>
  <read_first>
    - mobile/app.json (line 36, `newArchEnabled`)
    - mobile/README-native.md (native projects are generated by prebuild, not committed)
    - mobile/plugins/with-scene-delegate.js (config plugin that must still resolve)
  </read_first>
  <action>
    Resolve the expo-doctor version once: `npm view expo-doctor version` and use that exact version (EXPO_DOCTOR_VERSION) in every command of this plan and record it in the SUMMARY — plan 05 pins the same version in CI. Before editing, in mobile/ run `npx expo config --type prebuild --json` and save the output to the scratchpad as prebuild-before.json.

    Edit mobile/app.json (per D-02): delete the `"newArchEnabled": true,` line — nothing else. In mobile/ run `npx expo config --type prebuild --json` again to prebuild-after.json and diff the two (e.g. with `node -e` comparing JSON.parse of both after deleting `newArchEnabled`). Record in the SUMMARY a line "Native prebuild config changed: yes/no" with the diff if any. Do not run `expo prebuild` into the repo (ios/ and android/ are not committed).
  </action>
  <verify>
    <automated>cd mobile && node -e "const c=require('./app.json');process.exit('newArchEnabled' in c.expo ? 1 : 0)" && npx expo config --type prebuild --json > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "newArchEnabled" mobile/app.json` returns 0
    - `cd mobile && npx expo config --type prebuild --json` exits 0 (plugins including ./plugins/with-scene-delegate still resolve)
    - `git diff --numstat mobile/app.json` shows 0 added and 1 deleted line
    - SUMMARY records EXPO_DOCTOR_VERSION and "Native prebuild config changed: yes/no"
  </acceptance_criteria>
  <done>The invalid schema key is gone and the effect on the prebuild config is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Metro config that passes expo-doctor with a single React copy</name>
  <files>mobile/metro.config.js</files>
  <read_first>
    - mobile/metro.config.js (current overrides and their comment)
    - root package.json (react/react-native hoisted to the root node_modules)
    - node_modules/expo/metro-config (or @expo/metro-config) default monorepo handling, to confirm watchFolders/nodeModulesPaths defaults
  </read_first>
  <action>
    Per D-02, prove the Metro override obsolete rather than suppress the finding. Target state: remove `config.resolver.disableHierarchicalLookup = true` and the `config.resolver.extraNodeModules` block (its target paths do not exist — see interfaces). Keep `config.resolver.nodeModulesPaths` only if expo-doctor accepts it; if expo-doctor flags it too, remove it and rely on Expo's automatic monorepo configuration from `getDefaultConfig(__dirname)`. Replace the old comment with one stating that Expo's default config handles this npm-workspaces monorepo and that react/react-native are hoisted to the root node_modules (pinned by root overrides), so one copy is bundled — verified in phase 01.3 by the single-copy check below.

    Proof, in mobile/ (record commands and output in the SUMMARY):
    1. `npx --yes expo-doctor@EXPO_DOCTOR_VERSION` exits 0 (all checks pass).
    2. `npx expo export --platform android --output-dir <scratchpad>/expo-export --source-maps` exits 0 (if the flag name differs in this Expo CLI, find it with `npx expo export --help`).
    3. Single-copy check: with node, read every `*.map` file under `<scratchpad>/expo-export`, collect `sources`, and count distinct entries ending in `/react/index.js` and in `/react-native/index.js` — each count must be exactly 1.
    Run the same three steps on the unmodified file first (git stash or a scratch copy) to record the baseline counts, so the SUMMARY shows before/after.

    Fallback: if no config that passes expo-doctor also passes steps 2 and 3, restore the original file and stop with a checkpoint to the owner describing the failing combination — do not add `--exit-code 0` style suppression and do not wire around it.
  </action>
  <verify>
    <automated>cd mobile && ! grep -v '^\s*//' metro.config.js | grep -q "disableHierarchicalLookup = true" && npx expo export --platform android --output-dir "${TMPDIR:-/tmp}/expo-export-013" > /dev/null && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' mobile/metro.config.js | grep -c "disableHierarchicalLookup"` returns 0
    - `cd mobile && npx --yes expo-doctor@EXPO_DOCTOR_VERSION` exits 0 (output recorded in SUMMARY)
    - `cd mobile && npx expo export --platform android --output-dir <scratch>` exits 0
    - Single-copy check recorded in SUMMARY: exactly 1 distinct `/react/index.js` and 1 distinct `/react-native/index.js` source after the change
    - `npm --workspace mobile run test:unit` and `npm --workspace mobile run lint` exit 0
    - No `mobile/dist` directory is left in the working tree (`git status --porcelain mobile | grep -c dist` returns 0)
  </acceptance_criteria>
  <done>expo-doctor passes cleanly, the Android export still bundles one React and one React Native, and the change is documented for the owner device check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → npm registry | `npx expo-doctor@<version>` downloads a tool at run time |
| Metro bundle → field devices | A resolver change could bundle a different or duplicated React |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3-10 | Tampering | `npx expo-doctor` resolving an unexpected version | mitigate | Exact version resolved once and pinned (`expo-doctor@X.Y.Z`), reused by plan 05's CI job |
| T-01.3-11 | Denial of Service | Duplicate React in the bundle crashing the app on device | mitigate | Source-map single-copy check before/after; owner device smoke test in plan 06 |
| T-01.3-12 | Repudiation | Silent native config change | mitigate | Prebuild config diff recorded in SUMMARY and surfaced to the owner checkpoint |
</threat_model>

<verification>
- `npx --yes expo-doctor@EXPO_DOCTOR_VERSION` and `npx expo export --platform android` exit 0 in mobile/
- `npm --workspace mobile run typecheck`, `test:unit`, `lint` exit 0
</verification>

<success_criteria>
ROADMAP criterion 6 prerequisite: `expo-doctor` and `expo export` both pass today, so the mobile-build job in plan 05 starts green.
</success_criteria>

<output>
Create `.planning/phases/01.3-ci-and-test-safety-net/01.3-03-SUMMARY.md` when done
</output>
