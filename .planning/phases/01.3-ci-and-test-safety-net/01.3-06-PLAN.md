---
phase: 01.3-ci-and-test-safety-net
plan: 06
type: execute
wave: 3
depends_on: [01.3-01, 01.3-02, 01.3-03, 01.3-04, 01.3-05]
files_modified:
  - .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md
autonomous: false
requirements: [REQ-AUD-ci-pipeline, REQ-AUD-reproducible-image, REQ-AUD-test-infra]

must_haves:
  truths:
    - "The full local gate (lint, typecheck, unit with coverage, format, actionlint) is green on the integrated phase result"
    - "On the phase PR, every CI job ran and CI OK is green; the build job was skipped (PRs never push)"
    - "On the phase PR, E2E ran twice against the same database and the reset sentinel was gone after run 2"
    - "On the phase PR, the image built from the repo root, ran as a non-root uid and answered /v1/health with 200"
    - "The owner confirmed on a device that a dev build with the new Metro/app.json config starts and works, then merged the PR"
  artifacts:
    - path: .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md
      provides: "Per-task statuses and the PR's CI evidence (run URL, job conclusions)"
  key_links:
    - from: 01.3-VALIDATION.md
      to: "GitHub Actions run of the phase PR"
      via: "run URL and job conclusions recorded"
      pattern: "actions/runs/"
---

<objective>
Phase gate, part 1 (pre-merge): run the complete local gate on the integrated result, open the phase PR and verify from its GitHub Actions run the behaviours the sandbox cannot prove (Docker build and smoke run, E2E twice, expo-doctor/export in CI, CodeQL). Then the owner checks a device build (D-02: Metro and app.json changed) and merges.

Purpose: all three phase requirements — the CI system itself is the thing under test.
Output: VALIDATION.md statuses and CI evidence; merged PR.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md
@.planning/phases/01.3-ci-and-test-safety-net/01.3-03-SUMMARY.md
@.planning/phases/01.3-ci-and-test-safety-net/01.3-05-SUMMARY.md
@CLAUDE.md

<interfaces>
Repository: github.com/florianlepont/cortege (public). Current working branch: the phase branch checked out in the repo (e.g. claude/code-audit-complete-3sn99m); PR base: main.
GitHub access: the `gh` CLI is not installed in the agent sandbox; use the GitHub MCP server tools (create/read pull requests, list workflow runs and jobs, read job logs) if available. If neither can read Actions runs, surface an authentication/access gate and ask the owner for the run URL and job conclusions.
ci.yml job names (plan 05): "Detect changed paths", "Lint, format, typecheck", "Unit tests — API", "Unit tests — Mobile", "E2E tests — API", "Mobile build check", "Dependency audit", "Docker image check", "CI OK", "Build & push Docker image". CodeQL workflow job: "CodeQL analysis".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full local gate and validation map update</name>
  <files>.planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md (Per-Task Verification Map)
    - the SUMMARY files of plans 01-05 in the phase directory
    - CLAUDE.md ("Before committing" list)
  </read_first>
  <action>
    From the repo root run, in order: `npm run lint`, `npm run typecheck`, `npm --workspace api run test:unit:coverage`, `npm --workspace mobile run test:unit:coverage`, `npm run format:check`, and actionlint v1.7.12 on `.github/workflows/*.yml` (install with `GOBIN=<scratch> go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.12` if absent). Fix nothing silently: if a command fails, stop and report which plan's files are involved. Re-run the plan-level static checks: `cd mobile && npx jest --config jest.unit.config.js --listTests | grep -c '\.test\.tsx$'` (at least 1); `grep -E "uses: " .github/workflows/*.yml | grep -vcE "@[0-9a-f]{40} # v[0-9]"` (0). In 01.3-VALIDATION.md set each Per-Task Verification Map row whose proof is local to ✅ green (or ❌ red with the failing command); leave CI-proven rows ⬜ pending for Task 2. Set `wave_0_complete: true`.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm --workspace api run test:unit:coverage && npm --workspace mobile run test:unit:coverage && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - All five commands and actionlint exit 0
    - `grep -c "wave_0_complete: true" .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md` returns 1
    - Every locally-proven row in the Per-Task Verification Map shows ✅ or ❌ (none left ⬜ except CI-proven rows)
  </acceptance_criteria>
  <done>The integrated phase passes every local gate and the validation map reflects it.</done>
</task>

<task type="auto">
  <name>Task 2: Open the phase PR and verify its GitHub Actions run</name>
  <files>.planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md</files>
  <read_first>
    - .github/workflows/ci.yml and .github/workflows/codeql.yml (job names and expected step output)
    - .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md
  </read_first>
  <action>
    Push the phase branch and open (or reuse) a PR against main titled "Phase 01.3: CI and test safety net", body summarising the seven plans and ending with the attribution lines from the session's system reminder. Wait for the "CI" and "CodeQL" workflow runs on the PR head commit to finish (poll; do not exceed 45 minutes). Then verify, reading job conclusions and logs:
    1. Conclusion `success` for: Detect changed paths, Lint, format, typecheck, Unit tests — API, Unit tests — Mobile, E2E tests — API, Mobile build check, Dependency audit, Docker image check, CI OK, CodeQL analysis. (Every path-filtered job must have run, because the PR changes .github/workflows/**.)
    2. "Build & push Docker image" is `skipped`.
    3. E2E job log: both "E2E run 1" and "E2E run 2 (same database)" passed and "Assert the database was reset" printed `t`.
    4. Docker image check log: the `id -u` output is non-zero, the no-expo check passed, the healthcheck JSON contains /v1/health, and the /v1/health poll got 200.
    5. Unit job logs show coverage threshold enforcement ran (no "coverage threshold ... not met" errors) and the coverage-api / coverage-mobile artifacts exist.
    6. Mobile build check log shows expo-doctor with all checks passing and a completed Android export.
    If any job fails, diagnose from the log, fix it in the owning plan's files on the same branch, push, and re-verify; record each fix in the SUMMARY as a deviation. Record the run URLs and the job conclusion table in 01.3-VALIDATION.md and set the CI-proven Per-Task rows to ✅.
  </action>
  <verify>
    <automated>grep -c "actions/runs/" .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 01.3-VALIDATION.md contains the PR URL, the CI and CodeQL run URLs (`actions/runs/`) and a table of job conclusions matching points 1-2
    - Evidence for points 3-6 (quoted log lines) is recorded in the SUMMARY
    - No Per-Task Verification Map row is left ⬜ pending except those owned by plan 07 (post-merge and branch protection)
  </acceptance_criteria>
  <done>The phase PR's CI run proves every job, the E2E reset and the image smoke test; evidence is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner device check of the Metro/app.json change, then merge</name>
  <files>.planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.3-ci-and-test-safety-net/01.3-03-SUMMARY.md ("Native prebuild config changed" line and the Metro diff)
  </read_first>
  <action>Present the steps below with the PR URL and the "Native prebuild config changed" line from 01.3-03-SUMMARY.md, wait for the resume signal, then record the result in the "Manual-Only Verifications" table of 01.3-VALIDATION.md.</action>
  <what-built>Plans 01-05: real-SQL mobile tests and .test.tsx collection, E2E database reset, a root-context non-root API image, expo-doctor fixes (newArchEnabled removed from mobile/app.json, Metro override removed from mobile/metro.config.js), coverage ratchet, the restructured CI with a single "CI OK" check, and CodeQL. The PR's CI run is green (Task 2).</what-built>
  <how-to-verify>
    1. Check out the PR branch, `npm ci`, then build a development client: `npm run ios` or `npm run android` (prebuild regenerates the native projects from app.json).
    2. Expected: the app launches without a red screen (a duplicated React would fail here), the login screen appears, you can log in, open the survey list, create a draft survey in airplane mode, and it syncs after reconnecting.
    3. If 01.3-03-SUMMARY says the native prebuild config changed, also confirm on that platform that maps and the camera/photo picker still open.
    4. Review the PR, then merge it into main (the merge is the owner's action).
  </how-to-verify>
  <resume-signal>Type "approved — merged", or describe what failed on the device</resume-signal>
  <verify>
    <automated>grep -c "Manual-Only Verifications" .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Owner replied "approved — merged", or each failure is recorded in 01.3-VALIDATION.md with the observed behaviour
  </acceptance_criteria>
  <done>The device build works with the new Metro/app.json config and the phase PR is merged into main.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → GitHub | The agent pushes a branch and opens a PR; merging stays with the owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3-23 | Elevation of Privilege | Agent merging or changing protections on main | mitigate | Merge is an owner step in the blocking checkpoint; the agent never merges and never changes branch protection (D-10) |
| T-01.3-24 | Denial of Service | Metro change shipping a broken bundle to devices | mitigate | Owner device smoke test before merge; CI mobile-build job green |
| T-01.3-25 | Repudiation | CI claims without evidence | mitigate | Run URLs, job conclusions and quoted log lines recorded in VALIDATION.md and SUMMARY |
</threat_model>

<verification>
- Local gate green; PR CI and CodeQL green with evidence recorded; owner approved the device check and merged
</verification>

<success_criteria>
ROADMAP criteria 2-6 demonstrated on a real GitHub Actions run of the phase PR, and the D-02 device check passed.
</success_criteria>

<output>
Create `.planning/phases/01.3-ci-and-test-safety-net/01.3-06-SUMMARY.md` when done
</output>
