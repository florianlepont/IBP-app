---
phase: 01.3-ci-and-test-safety-net
plan: 07
type: execute
wave: 4
depends_on: [01.3-06]
files_modified:
  - .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md
autonomous: false
requirements: [REQ-AUD-ci-pipeline, REQ-AUD-reproducible-image]

must_haves:
  truths:
    - "The first main push after the merge ran CI OK green and pushed ghcr.io/florianlepont/cortege with both :latest and :sha-<merge commit>"
    - "A PR with a deliberate type error fails the check job and CI OK"
    - "A docs-only PR runs only changes, check, audit and CI OK; unit, e2e, mobile-build and image-check are skipped and CI OK is green"
    - "The VPS timer pulled the new image, the API answers https://cortege.algernon.ovh/v1/health, and the container runs as a non-root uid"
    - "Branch protection on main requires the single status check CI OK (configured by the owner, D-10)"
  artifacts:
    - path: .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md
      provides: "Post-merge evidence, throwaway PR results, VPS and branch-protection confirmation; nyquist_compliant: true"
  key_links:
    - from: "main push CI run"
      to: "ghcr.io/florianlepont/cortege:sha-<commit>"
      via: "build job tags"
      pattern: "sha-"
---

<objective>
Phase gate, part 2 (post-merge): prove the main-only push and SHA tag on the real registry, exercise the two throwaway-PR behaviours (type error fails, docs-only skips expensive jobs) that can only be tested against the new workflow on main, then hand the owner the two manual steps: VPS health after the first main push, and branch protection requiring `CI OK` (D-10). Agents never change branch protection.

Purpose: REQ-AUD-ci-pipeline criterion 1 and REQ-AUD-reproducible-image deploy continuity.
Output: VALIDATION.md completed and signed off.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md
@.planning/phases/01.3-ci-and-test-safety-net/01.3-06-SUMMARY.md
@infra/vps/README.md

<interfaces>
GitHub access as in plan 06 (GitHub MCP tools; `gh` is not installed; surface an access gate if runs cannot be read).
GHCR public tag list without credentials: fetch an anonymous pull token from `https://ghcr.io/token?scope=repository:florianlepont/cortege:pull`, then GET `https://ghcr.io/v2/florianlepont/cortege/tags/list` with `Authorization: Bearer <token>`. If the package is private, fall back to reading the build job log for the pushed tags.
VPS: timer cortege-deploy.timer polls :latest every 5 minutes; logs via `journalctl -u cortege-deploy.service -n 50`; container name cortege-api; public health URL https://cortege.algernon.ovh/v1/health.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the main push and exercise the type-error and docs-only PRs</name>
  <files>.planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md ("Manual-Only Verifications" rows for the throwaway PRs)
    - .github/workflows/ci.yml (changes filters and ci-ok logic)
    - .planning/phases/01.3-ci-and-test-safety-net/01.3-06-SUMMARY.md (merge commit)
  </read_first>
  <action>
    1. Main push: find the CI run for the merge commit on main. Verify CI OK is `success` and "Build & push Docker image" is `success`, and that the registry now lists both `latest` and `sha-<merge commit SHA>` (GHCR tag list per interfaces, or the build job log). Record run URL and tags.
    2. Type-error PR: from an up-to-date main, create branch `ci-proof/type-error` whose only change adds `export const ciProofTypeError: number = "not a number"` to a new file api/src/ci-proof.ts; push and open a draft PR titled "CI proof: deliberate type error (do not merge)". Expected: "Lint, format, typecheck" fails at the typecheck step and CI OK fails.
    3. Docs-only PR: from main, create branch `ci-proof/docs-only` whose only change appends one line to docs/README.md; push and open a draft PR titled "CI proof: docs-only change (do not merge)". Expected: Detect changed paths, Lint, format, typecheck, Dependency audit and CI OK succeed; Unit tests — API, Unit tests — Mobile, E2E tests — API, Mobile build check and Docker image check are `skipped`.
    4. Close both PRs without merging and delete both remote branches. Record PR URLs, run URLs and per-job conclusions in the "Manual-Only Verifications" table of 01.3-VALIDATION.md.
    If a result differs from the expectation, stop and report it (the fix belongs in a follow-up change to ci.yml, reviewed by the owner).
  </action>
  <verify>
    <automated>grep -c "ci-proof/" .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION.md records: main run URL with CI OK success and build success; registry tags `latest` and `sha-<merge SHA>`
    - VALIDATION.md records the type-error PR with check = failure and CI OK = failure
    - VALIDATION.md records the docs-only PR with the five expensive jobs = skipped and CI OK = success
    - Both throwaway PRs are closed unmerged and their branches deleted (no `ci-proof/` branch left on origin: `git ls-remote --heads origin 'ci-proof/*'` prints nothing)
  </acceptance_criteria>
  <done>Main-only SHA-tagged push, typecheck gate and path filtering are proven against the live workflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner: VPS health after the first main push, then branch protection on CI OK</name>
  <files>.planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md</files>
  <read_first>
    - infra/vps/README.md ("Operating it" and "Rolling back")
    - .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md
  </read_first>
  <action>Present the steps below with the main run URL and pushed SHA tag from Task 1, wait for the resume signal, record each result in the "Manual-Only Verifications" table of 01.3-VALIDATION.md, then set `nyquist_compliant: true` and `status: complete` in its frontmatter and tick the Validation Sign-Off items that hold.</action>
  <what-built>The phase is merged; main's CI OK is green and the image was pushed as :latest and :sha-(merge commit) from a root build context, running as the non-root node user with a Docker healthcheck.</what-built>
  <how-to-verify>
    VPS (within about 10 minutes of the main push):
    1. `journalctl -u cortege-deploy.service -n 50` shows "image changed, restarting the stack" followed by "API healthy after N attempt(s)".
    2. `curl -s https://cortege.algernon.ovh/v1/health` returns HTTP 200.
    3. `docker exec cortege-api id -u` prints a non-zero uid (1000), and `docker inspect --format '{{.State.Health.Status}}' cortege-api` prints `healthy`.
    4. Upload one photo from the app (MinIO path) to confirm attachments still work under the non-root user.
    If the API is unhealthy, follow "Rolling back" in infra/vps/README.md with the previous sha- tag and report.
    Branch protection (D-10 — only after step 2 passes and CI OK has been green on main once):
    5. GitHub → Settings → Branches → rule for `main` → enable "Require status checks to pass before merging" → select only `CI OK` (not the individual jobs, which are skipped by path filters) → Save.
  </how-to-verify>
  <resume-signal>Type "approved", or list the failed step numbers with what you saw</resume-signal>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/01.3-ci-and-test-safety-net/01.3-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Owner replied "approved", or every failed step is recorded in 01.3-VALIDATION.md with the observed behaviour
    - 01.3-VALIDATION.md frontmatter has `nyquist_compliant: true` once approved
  </acceptance_criteria>
  <done>Production still deploys from :latest and is healthy under the new image; main is protected by the single CI OK check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → GitHub | The agent opens and closes throwaway draft PRs; it never merges them |
| Owner → repository settings | Branch protection is a human-only change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3-26 | Tampering | Throwaway PR content reaching main | mitigate | Draft PRs titled "do not merge", closed unmerged, branches deleted; acceptance checks no ci-proof/ branch remains |
| T-01.3-27 | Elevation of Privilege | Unreviewed changes to main bypassing CI | mitigate | Owner sets branch protection requiring CI OK (D-10); agents never touch protection settings |
| T-01.3-28 | Denial of Service | New non-root image failing on the VPS | mitigate | Owner health check within one timer cycle; documented sha- rollback that stops the timer first |
</threat_model>

<verification>
- Main push, SHA tag, type-error failure and docs-only skip recorded; owner approved VPS health and branch protection
</verification>

<success_criteria>
ROADMAP criterion 1 (typecheck failure and docs-only behaviour on real PRs) and criterion 3 (pushed only from main with a SHA tag, VPS still deploying :latest); D-10 completed.
</success_criteria>

<output>
Create `.planning/phases/01.3-ci-and-test-safety-net/01.3-07-SUMMARY.md` when done
</output>
