---
phase: 01.4-api-sync-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/database/database.service.ts
  - api/test/database.service.spec.ts
  - api/test/e2e-fault-injection.ts
  - api/test/database-transaction.e2e-spec.ts
  - api/src/users/users.service.ts
  - api/test/users.service.spec.ts
autonomous: true
requirements: [REQ-AUD-transactions]

must_haves:
  truths:
    - "DatabaseService.transaction(fn) runs fn on one dedicated client between BEGIN and COMMIT, rolls back and rethrows when fn throws, and always releases the client (D-06)"
    - "A statement run through the transaction handle is rolled back with the rest when a later statement fails (proved against real PostgreSQL)"
    - "E2E specs can make INSERT INTO survey_events fail for one survey and one event type with a temporary trigger, and remove it afterwards (D-15)"
    - "Account deletion commits the database transaction before calling Auth0; a database failure never deletes the Auth0 user (D-10, success criterion 5)"
    - "When the Auth0 deletion fails after the commit, the server logs a structured error with the user id and Auth0 sub and the request still succeeds (D-10)"
  artifacts:
    - path: api/src/database/database.service.ts
      provides: "Queryable type and transaction<T>(fn) helper"
      contains: "async transaction<"
    - path: api/test/database.service.spec.ts
      provides: "Unit spec of commit, rollback, rollback failure and release paths (pg mocked)"
    - path: api/test/e2e-fault-injection.ts
      provides: "installEventInsertFailure / removeEventInsertFailures helpers for E2E fault injection"
      exports: ["installEventInsertFailure", "removeEventInsertFailures"]
    - path: api/test/database-transaction.e2e-spec.ts
      provides: "Real-PostgreSQL proof of the helper and of the fault-injection trigger"
    - path: api/src/users/users.service.ts
      provides: "deleteAccount: db.transaction first, Auth0 after commit, Logger.error on Auth0 failure"
  key_links:
    - from: api/src/users/users.service.ts
      to: api/src/database/database.service.ts
      via: "this.db.transaction(async (db) => ...)"
      pattern: "this\\.db\\.transaction\\("
    - from: api/test/database-transaction.e2e-spec.ts
      to: api/test/e2e-fault-injection.ts
      via: "import of installEventInsertFailure"
      pattern: "installEventInsertFailure"
---

<objective>
Create the one transaction primitive every later plan uses, the E2E fault-injection helper that proves "an injected failure on the event insert leaves nothing committed", and fix the account-deletion order (A-M9).

Purpose: REQ-AUD-transactions (ARCH-3 foundation, A-M9). Plans 03 and 04 wrap every multi-statement write in `DatabaseService.transaction` and use `installEventInsertFailure` in their E2E specs.
Output: `Queryable` + `transaction()` in DatabaseService with unit and E2E proofs; reusable trigger helper; reordered `deleteAccount` with spec.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.4-api-sync-integrity/01.4-CONTEXT.md
@.planning/phases/01.4-api-sync-integrity/01.4-PATTERNS.md
@CLAUDE.md

<interfaces>
From api/src/database/database.service.ts (current, 32 lines):
- imports `Pool, PoolClient, QueryResult, QueryResultRow` from "pg"
- `query<T extends QueryResultRow = QueryResultRow>(text: string, values?: unknown[]): Promise<QueryResult<T>>` wraps `this.pool.query`
- `connect(): Promise<PoolClient>` — KEEP IT: `api/src/debug/debug.service.ts` and `api/test/debug.service.spec.ts` depend on it.

Transaction pattern to generalise: `api/src/users/users.service.ts` deleteAccount (client = await this.db.connect(); BEGIN; work; COMMIT; catch → ROLLBACK with `.catch(() => undefined)` then rethrow; finally release). Today it calls `this.auth0Management.deleteUser(user.auth0_sub)` BEFORE `BEGIN` (A-M9). Storage cleanup (profile picture `rm`, `cleanupAttachmentStorage`) runs after the transaction — keep that best-effort placement (D-07).

Logger pattern: `import { Logger } from "@nestjs/common"`; `private readonly logger = new Logger(UsersService.name)` (analog: api/src/surveys/cadastre-provider.service.ts).

pg error fabrication in specs: `Object.assign(new Error("unique violation"), { code: "23505" })` (api/test/users.service.spec.ts).

E2E app setup to copy (api/test/rate-limit.e2e-spec.ts): `Test.createTestingModule({ imports: [AppModule] }).compile()`, `moduleFixture.createNestApplication<NestExpressApplication>()`, `configureApp(app)` from "../src/app.setup" (sets the v1 prefix and the production ValidationPipe), `await app.init()`, `db = moduleFixture.get(DatabaseService)`.

Local PostgreSQL for E2E (the sandbox has PostgreSQL 16 binaries and a Debian cluster config at /etc/postgresql/16/main, server not running): start it with `pg_ctlcluster 16 main start` (or `service postgresql start`); create the login role once with `su postgres -c "psql -c \"CREATE ROLE ibp LOGIN SUPERUSER PASSWORD 'ibp'\""` (ignore "already exists"). `api/test/global-setup.js` creates `ibp_test` if missing, drops its schema and re-migrates on every run; env comes from `api/.env.test` or the committed `api/.env.test.example` (localhost:5432, ibp/ibp, ibp_test). Targeted run: `npm --workspace api run test:e2e -- database-transaction`. If the cluster cannot be started, do not fake it: record "E2E deferred to CI" in the SUMMARY; plan 06 proves it on the PR.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Queryable type and DatabaseService.transaction helper with unit spec</name>
  <files>api/src/database/database.service.ts, api/test/database.service.spec.ts</files>
  <read_first>
    - api/src/database/database.service.ts
    - api/src/users/users.service.ts (deleteAccount BEGIN/COMMIT/ROLLBACK/release block)
    - api/test/debug.service.spec.ts (mock client with query/release jest.fn)
    - api/test/users.service.spec.ts (first lines: jest.mock("fs/promises") module-mock precedent)
  </read_first>
  <behavior>
    - fn resolves: client.query called with "BEGIN", then fn(handle), then "COMMIT"; transaction resolves with fn's value; release called exactly once
    - fn rejects: "ROLLBACK" issued, the original error is rethrown (same object), "COMMIT" never issued, release called once
    - ROLLBACK itself rejects: the original fn error is still the one rethrown, release still called once
    - BEGIN rejects: error rethrown, release called once
    - the handle passed to fn forwards query(text, values) to the same client (not the pool)
  </behavior>
  <action>
    In database.service.ts export a type-only contract (per CLAUDE.md "export type"): `export type Queryable = { query<T extends QueryResultRow = QueryResultRow>(text: string, values?: unknown[]): Promise<QueryResult<T>> }`. DatabaseService already satisfies it through its `query` method, so services can pass `this.db` to helpers outside a transaction. Add `async transaction<T>(fn: (db: Queryable) => Promise<T>): Promise<T>` per D-06: `const client = await this.pool.connect()`; build a small adapter object `{ query: (text, values) => client.query(text, values) }` typed as Queryable (the adapter avoids PoolClient overload-assignability issues and gives E2E specs one object to spy on, D-15); `BEGIN`, `const result = await fn(adapter)`, `COMMIT`, return result; in catch issue `ROLLBACK` with `.catch(() => undefined)` and rethrow the original error; `client.release()` in finally. Keep `query()` and `connect()` unchanged. No nesting support, no savepoints.
    Create api/test/database.service.spec.ts (unit specs live in api/test/ by convention): `jest.mock("pg", () => ({ Pool: jest.fn().mockImplementation(() => ({ connect, query, end })) }))` with module-level jest.fn()s so the spec controls `connect` resolving a fake client `{ query: jest.fn(), release: jest.fn() }`. Write the behaviours above as failing tests first, then implement. Assert call order with `toHaveBeenNthCalledWith`.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- database.service && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type Queryable" api/src/database/database.service.ts` returns 1
    - `grep -c "async transaction<T>" api/src/database/database.service.ts` returns 1
    - `grep -c "connect(): Promise<PoolClient>" api/src/database/database.service.ts` returns 1 (kept for DebugService)
    - The spec has at least 5 `it(` cases and passes; `npm --workspace api run test:unit` passes overall
  </acceptance_criteria>
  <done>transaction() commits, rolls back, rethrows and releases exactly as specified, proven by unit tests with pg mocked.</done>
</task>

<task type="auto">
  <name>Task 2: E2E fault-injection helper and real-PostgreSQL proof of the helper</name>
  <files>api/test/e2e-fault-injection.ts, api/test/database-transaction.e2e-spec.ts</files>
  <read_first>
    - api/test/rate-limit.e2e-spec.ts (lines 1-40: app setup with configureApp)
    - api/test/surveys-idempotency.e2e-spec.ts (lines 1-60: test-token login, db = moduleFixture.get(DatabaseService))
    - api/migrations/ (grep the survey_events table definition: columns id, survey_id, actor_id, event_type, payload, created_at)
    - api/jest.config.js and api/jest.unit.config.js (testMatch: the helper must not be collected as a test)
  </read_first>
  <action>
    Create api/test/e2e-fault-injection.ts (plain module, not a spec; the name does not match `*.spec.ts` or `*.e2e-spec.ts`). Export `installEventInsertFailure(db: Queryable, options: { surveyId: string; eventType?: string }): Promise<void>` which creates (CREATE OR REPLACE) a plpgsql function and a `BEFORE INSERT ON survey_events FOR EACH ROW` trigger named `e2e_fail_event_insert` whose WHEN clause matches `NEW.survey_id = <surveyId>` and, when given, `NEW.event_type = <eventType>`, and raises `RAISE EXCEPTION 'e2e injected event failure' USING ERRCODE = 'P0001'` (class P0: not 22/23, so the sync path treats it as retryable and REST returns 500 — this simulates a transient failure of the event insert, the exact ARCH-3 scenario). Values in DDL cannot be bind parameters: accept only `/^[A-Za-z0-9_.:-]+$/` for surveyId/eventType and throw otherwise, then inline them as quoted literals. Export `removeEventInsertFailures(db: Queryable): Promise<void>` which runs `DROP TRIGGER IF EXISTS e2e_fail_event_insert ON survey_events` and drops the function. Only one injected trigger exists at a time (install replaces it).
    Create api/test/database-transaction.e2e-spec.ts (production app via configureApp; `db = moduleFixture.get(DatabaseService)`; `afterEach` calls removeEventInsertFailures; `afterAll` closes the app). Cases:
    (1) `db.transaction` commits: create a probe table `e2e_tx_probe (id text primary key)` in `beforeAll` (drop it in `afterAll`), insert row "c" inside `db.transaction`, and assert it is visible through `db.query` afterwards.
    (2) Rollback: inside `db.transaction`, insert probe row "a", then throw; assert the transaction rejects with the same error and row "a" is absent.
    (3) Fault helper: log in via `POST /v1/debug/test-token`, create a draft survey through `POST /v1/surveys` (payload without `location`: the production pipe forbids unknown fields) to get a real survey id; call `installEventInsertFailure(db, { surveyId, eventType: "e2e_probe" })`; assert a direct `db.query("INSERT INTO survey_events (id, survey_id, actor_id, event_type, payload) VALUES (...,'e2e_probe','{}')")` rejects with code `P0001`, and that an insert with a different event_type for the same survey succeeds; after `removeEventInsertFailures`, the `e2e_probe` insert succeeds.
    Start local PostgreSQL as described in the interfaces block and run the spec. If the cluster cannot start, record "E2E deferred to CI" in the SUMMARY instead of claiming a pass.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm run typecheck && npm run lint && (pg_isready -h localhost -p 5432 && npm --workspace api run test:e2e -- database-transaction || echo "E2E deferred to CI: no local PostgreSQL")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function installEventInsertFailure" api/test/e2e-fault-injection.ts` returns 1 and `grep -c "export async function removeEventInsertFailures" api/test/e2e-fault-injection.ts` returns 1
    - `grep -c "P0001" api/test/e2e-fault-injection.ts` returns at least 1
    - `cd api && npx jest --config jest.unit.config.js --listTests | grep -c e2e-fault-injection` returns 0 (not collected as a unit test)
    - With local PostgreSQL running, `npm --workspace api run test:e2e -- database-transaction` passes; otherwise the SUMMARY says "E2E deferred to CI"
  </acceptance_criteria>
  <done>A reusable, scoped, removable event-insert failure exists and the transaction helper is proven on real PostgreSQL (or explicitly deferred to CI).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: deleteAccount commits in the database before deleting the Auth0 user</name>
  <files>api/src/users/users.service.ts, api/test/users.service.spec.ts</files>
  <read_first>
    - api/src/users/users.service.ts (deleteAccount and its storage cleanup; constructor)
    - api/test/users.service.spec.ts (buildService mocks, existing deleteAccount test around the `deleteUser` assertions)
    - api/src/users/users.controller.ts (DELETE /me returns 204)
  </read_first>
  <behavior>
    - Happy path: every DB statement (BEGIN ... COMMIT) is issued before `auth0Management.deleteUser` (compare `mock.invocationCallOrder`); deleteUser called once with the user's auth0_sub
    - DB failure mid-transaction: ROLLBACK issued, deleteAccount rejects with the DB error, deleteUser NOT called
    - Auth0 failure after commit: deleteAccount resolves; `Logger.prototype.error` spy called once with a message containing the user id and the auth0_sub; storage cleanup still attempted
    - Missing user row: NotFoundException as today, deleteUser NOT called
  </behavior>
  <action>
    Per D-10 and success criterion 5: replace the hand-rolled connect/BEGIN/COMMIT block in `deleteAccount` with `await this.db.transaction(async (db) => { ... })` running the same statements in the same order on `db.query` (collect `storageKeysToDelete` inside, return them). Only after the transaction resolves, call `this.auth0Management.deleteUser(user.auth0_sub)` inside try/catch; on failure call `this.logger.error(...)` with a single structured message such as `Auth0 user deletion failed after local account deletion (user_id=<id>, auth0_sub=<sub>): <error message>` and do not rethrow — the request still succeeds. No new column, status or retry job (D-10; the retry job is a deferred idea). Keep profile-picture and attachment storage cleanup after commit and best-effort (D-07). Add `private readonly logger = new Logger(UsersService.name)` and the `Logger` import. Do not touch other UsersService methods.
    Update api/test/users.service.spec.ts: add `transaction: jest.fn(async (fn) => { await client.query("BEGIN"); try { const r = await fn(client); await client.query("COMMIT"); return r } catch (e) { await client.query("ROLLBACK"); throw e } })` to the db mock built in buildService (so existing statement-order assertions keep working), rename the existing deleteAccount test to reflect the new order, and add the behaviour cases above. Spy the logger with `jest.spyOn(Logger.prototype, "error").mockImplementation(() => undefined)` and restore it.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- users.service && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - In api/src/users/users.service.ts the line calling `auth0Management.deleteUser` appears after the `this.db.transaction(` line: `awk '/this.db.transaction\(/{t=NR} /auth0Management.deleteUser/{d=NR} END{exit !(t>0 && d>t)}' api/src/users/users.service.ts` exits 0
    - `grep -c "client.query(\"BEGIN\")" api/src/users/users.service.ts` returns 0
    - `grep -c "new Logger(UsersService.name)" api/src/users/users.service.ts` returns 1
    - users.service spec contains the DB-failure case asserting `deleteUser` not called and the Auth0-failure case asserting resolve + logger call; all pass
  </acceptance_criteria>
  <done>Account deletion can no longer leave a DB user without an Auth0 account; an Auth0 failure after commit is logged with user id and sub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → PostgreSQL | Multi-statement writes must be atomic |
| API → Auth0 Management API | Irreversible external deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.4-01 | Tampering | DatabaseService.transaction | mitigate | ROLLBACK on any throw, original error rethrown, client always released (unit spec covers ROLLBACK failure) |
| T-01.4-02 | Denial of Service | Pool client leak | mitigate | `release()` in finally; spec asserts exactly one release on every path |
| T-01.4-03 | Repudiation | deleteAccount Auth0 failure | mitigate | Structured `Logger.error` with user id + auth0_sub (D-10); no silent failure |
| T-01.4-04 | Tampering | deleteAccount ordering (A-M9) | mitigate | DB transaction committed before `deleteUser`; DB failure never reaches Auth0 |
| T-01.4-05 | Elevation of Privilege | e2e-fault-injection DDL | mitigate | Test-only module under api/test, identifiers restricted to `[A-Za-z0-9_.:-]`, runs only against the `*_test` DB guarded by globalSetup |
</threat_model>

<verification>
- `npm run lint && npm run typecheck && npm run test:unit` green
- database-transaction E2E green locally, or explicitly deferred to CI in the SUMMARY
</verification>

<success_criteria>
The transaction primitive and fault-injection helper exist and are proven; ROADMAP success criterion 5 (account deletion commits before Auth0) holds and is unit-tested.
</success_criteria>

<output>
Create `.planning/phases/01.4-api-sync-integrity/01.4-01-SUMMARY.md` when done
</output>
