---
phase: 01.4-api-sync-integrity
plan: 04
type: execute
wave: 2
depends_on: [01.4-01]
files_modified:
  - api/src/surveys/surveys-attachments.service.ts
  - api/src/reports/reports.service.ts
  - api/test/attachments-reports-transactions.e2e-spec.ts
autonomous: true
requirements: [REQ-AUD-transactions]

must_haves:
  truths:
    - "Attachment create, upload confirmation and delete each run in one transaction with their survey_events insert (D-06)"
    - "Report creation runs in one transaction with its 'reported' event (D-06)"
    - "An injected failure on attachment_created, attachment_uploaded, attachment_deleted or reported leaves the attachments/reports rows as they were"
    - "Two concurrent attachment creates cannot push a survey past the 10-attachment limit (count and insert are serialised on the survey row)"
    - "Object-storage deletion of an attachment happens only after the commit, best-effort (D-07)"
  artifacts:
    - path: api/src/surveys/surveys-attachments.service.ts
      provides: "createAttachment, uploadAttachment, deleteAttachment in db.transaction; helpers take db: Queryable"
      contains: "this.db.transaction("
    - path: api/src/reports/reports.service.ts
      provides: "createReport in db.transaction"
      contains: "this.db.transaction("
    - path: api/test/attachments-reports-transactions.e2e-spec.ts
      provides: "Injected-failure E2E for attachments and reports"
  key_links:
    - from: api/src/surveys/surveys-attachments.service.ts
      to: api/src/database/database.service.ts
      via: "this.db.transaction(async (db) => ...)"
      pattern: "this\\.db\\.transaction\\("
    - from: api/src/reports/reports.service.ts
      to: api/src/database/database.service.ts
      via: "this.db.transaction(async (db) => ...)"
      pattern: "this\\.db\\.transaction\\("
---

<objective>
Apply the transaction primitive to the remaining multi-statement writes outside SurveysService: attachment create / upload confirmation / delete and report creation, each atomic with its event.

Purpose: REQ-AUD-transactions (ARCH-3), ROADMAP success criterion 3 for "attachment writes and report creation".
Output: refactored attachments and reports services, one E2E spec. Runs in parallel with plan 03 (disjoint files).
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.4-api-sync-integrity/01.4-CONTEXT.md
@.planning/phases/01.4-api-sync-integrity/01.4-PATTERNS.md
@.planning/phases/01.4-api-sync-integrity/01.4-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 01: `Queryable` type and `DatabaseService.transaction<T>(fn: (db: Queryable) => Promise<T>)` in api/src/database/database.service.ts; `installEventInsertFailure(db, { surveyId, eventType? })` / `removeEventInsertFailures(db)` in api/test/e2e-fault-injection.ts (SQLSTATE P0001 → REST 500, /sync retryable_error).

api/src/surveys/surveys-attachments.service.ts (read 2026-09-24):
- private copies: getSurveyForUserOrThrow (~51), getSurveyForUser (~59-72), insertEvent (~74-90), all on `this.db.query`
- createAttachment (~152-224): getSurveyForUserOrThrow, COUNT active attachments (limit 10), body checks (mime_type, size_bytes, 25 MB, isAllowedMimeType), ids + storageKey, `buildUploadUrl` (presign, no DB), INSERT attachments, insertEvent "attachment_created"
- uploadAttachment (~226-310): token check, getSurveyForUserOrThrow, SELECT attachment, file write to storage (not transactional), UPDATE uploaded_at, insertEvent "attachment_uploaded"
- deleteAttachment (~312-359): getSurveyForUserOrThrow, SELECT attachment (allowMissing path), UPDATE deleted_at, cleanupAttachmentStorage (inside the flow today), insertEvent "attachment_deleted"

api/src/reports/reports.service.ts createReport (~24-67): body checks, findSurveyForReport, canReportSurvey (moderator/admin always; owner never; others only when visibility is public), INSERT reports, INSERT survey_events 'reported' (actor_id not set), both on `this.db.query`. Constructor takes only DatabaseService.

E2E fixtures: REST attachment create `POST /v1/surveys/:id/attachments` with `{ mime_type: "image/jpeg", size_bytes: 1000 }` returns `{ attachment_id, storage_key, upload_url, confirm_url }`; confirm with `PUT /v1/surveys/:id/attachments/:attachment_id/upload?token=...` (read the controller for the multipart field name). Reports: `POST /v1/reports` `{ survey_id, reason }` by a second user on a survey whose visibility is `public`. Do not rely on client `status: "submitted"` in fixtures (plan 05 makes upsert ignore it); if a submitted survey is ever needed, set it with a direct `db.query` UPDATE. Build the app with `configureApp(app)`; do not send `location` in REST bodies.

Local PostgreSQL for E2E: plan 01 interfaces. If it cannot start, record "E2E deferred to CI".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transactions in SurveysAttachmentsService and ReportsService</name>
  <files>api/src/surveys/surveys-attachments.service.ts, api/src/reports/reports.service.ts</files>
  <read_first>
    - api/src/surveys/surveys-attachments.service.ts (whole file)
    - api/src/reports/reports.service.ts (createReport, findSurveyForReport)
    - api/src/database/database.service.ts (after plan 01)
  </read_first>
  <action>
    Attachments (D-06, D-07): change the private getSurveyForUser / getSurveyForUserOrThrow / insertEvent copies to take `db: Queryable` first (read-only callers pass `this.db`), and add a `forUpdate` option to getSurveyForUser that appends `FOR UPDATE`.
    createAttachment: keep the body checks and the id/storage-key/presign computation (no DB) before the transaction, but move the ownership check, the COUNT and the INSERT + "attachment_created" event into `this.db.transaction(async (db) => ...)`, reading the survey with `forUpdate: true` first so two concurrent creates serialise on the survey row and the 10-attachment limit holds. Keep the order of user-visible errors sensible: ownership (404) is checked inside the transaction before the count; body validation errors (400) may be raised before it.
    uploadAttachment: keep the token check, ownership check, attachment SELECT and the storage write outside or before the transaction (the file write cannot be transactional); run the `UPDATE ... uploaded_at` and the "attachment_uploaded" event in one transaction.
    deleteAttachment: run ownership check, attachment SELECT, UPDATE deleted_at and the "attachment_deleted" event in one transaction returning the storage key (allowMissing path returns early without writes); call `cleanupAttachmentStorage` only after the transaction resolves.
    Reports: in createReport keep the input checks and the reportability checks as they are; run the INSERT into reports and the 'reported' event INSERT in one `this.db.transaction(async (db) => ...)`.
    Every statement inside a callback uses `db.query` (a leftover `this.db.query` would autocommit on another connection). Keep all user_id / survey_id predicates. Public signatures and return shapes unchanged.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm run typecheck && npm run lint && npm --workspace api run test:unit && test "$(grep -c 'this.db.transaction(' api/src/surveys/surveys-attachments.service.ts)" -ge 3 && test "$(grep -c 'this.db.transaction(' api/src/reports/reports.service.ts)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "this.db.transaction(" api/src/surveys/surveys-attachments.service.ts` returns at least 3 and `grep -c "this.db.transaction(" api/src/reports/reports.service.ts` returns at least 1
    - In api/src/surveys/surveys-attachments.service.ts, `grep -A2 "private async insertEvent(" | grep -c "db: Queryable"` returns 1 and `grep -A2 "private async getSurveyForUser(" | grep -c "db: Queryable"` returns 1
    - In deleteAttachment, `cleanupAttachmentStorage` is called after the transaction: `awk '/async [a-zA-Z]+\(/ && !/async deleteAttachment\(/{f=0} /async deleteAttachment\(/{f=1} f&&/this.db.transaction\(/{t=NR} f&&/cleanupAttachmentStorage\(/{c=NR} END{exit !(t>0&&c>t)}' api/src/surveys/surveys-attachments.service.ts` exits 0
    - lint, typecheck and unit tests pass
  </acceptance_criteria>
  <done>Attachment and report writes are atomic with their events; storage cleanup is post-commit; the attachment limit is race-free.</done>
</task>

<task type="auto">
  <name>Task 2: E2E — injected event failures on attachments and reports</name>
  <files>api/test/attachments-reports-transactions.e2e-spec.ts</files>
  <read_first>
    - api/test/e2e-fault-injection.ts and api/test/database-transaction.e2e-spec.ts (plan 01)
    - api/src/surveys/surveys.controller.ts (attachment routes, upload multipart field name and token query)
    - api/test/validation-reports.e2e-spec.ts and api/test/epic-e-search-reports.e2e-spec.ts (report fixtures: owner + reporter users)
  </read_first>
  <action>
    Create api/test/attachments-reports-transactions.e2e-spec.ts with `configureApp(app)`, `db = moduleFixture.get(DatabaseService)`, `afterEach(() => removeEventInsertFailures(db))`. For each case create a fresh draft survey through `POST /v1/surveys` (sync_version 1, site_name, visibility, region_version "ACA", vegetation_stage "collineen", factors {}), unique IDs from Date.now().
    (1) attachment_created: install failure (survey S, "attachment_created"); REST create → 500; `SELECT count(*) FROM attachments WHERE survey_id = S` is 0. Same through `POST /v1/sync` with an `attachment.create` operation shaped like the mobile app (`survey_id` on the envelope, payload `{ mime_type: "image/jpeg", size_bytes: 1000, captured_at: null, metadata: {} }`) → result `retryable_error`, still 0 rows; after removing the failure the same operation → `synced`, 1 row.
    (2) attachment_uploaded: create an attachment (no failure), install failure on "attachment_uploaded", confirm the upload with a small JPEG buffer → 500; `uploaded_at` is still null.
    (3) attachment_deleted: create an attachment, install failure on "attachment_deleted", `DELETE /v1/surveys/S/attachments/A` → 500; the attachment's `deleted_at` is still null.
    (4) Concurrent creates respect the limit: create 9 attachments, then fire 3 creates with `Promise.all`; exactly one succeeds (201) and the others get 400; active attachment count is 10.
    (5) reported: owner creates a public survey; a second user logs in; install failure (S, "reported"); `POST /v1/reports` → 500; `SELECT count(*) FROM reports WHERE survey_id = S` is 0; after removal the report → 201 and one 'reported' event exists.
    Run the spec and then the full E2E suite with local PostgreSQL; otherwise record "E2E deferred to CI".
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm run typecheck && npm run lint && (pg_isready -h localhost -p 5432 && npm --workspace api run test:e2e || echo "E2E deferred to CI: no local PostgreSQL")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "attachment_created|attachment_uploaded|attachment_deleted|\"reported\"" api/test/attachments-reports-transactions.e2e-spec.ts` returns at least 4
    - `grep -c "configureApp(app)" api/test/attachments-reports-transactions.e2e-spec.ts` returns 1
    - With local PostgreSQL the full `npm --workspace api run test:e2e` passes; otherwise the SUMMARY says "E2E deferred to CI"
  </acceptance_criteria>
  <done>Injected event failures on every attachment and report write leave nothing committed, proven on real PostgreSQL (or deferred to CI).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → attachment / report endpoints and /sync attachment operations | Untrusted, possibly concurrent requests |
| API → object storage | Non-transactional side effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.4-17 | Repudiation | Attachment/report writes without events (ARCH-3) | mitigate | Row write + event in one db.transaction; E2E injected failures |
| T-01.4-18 | Denial of Service | Attachment limit race | mitigate | Survey row read FOR UPDATE before COUNT + INSERT in the same transaction |
| T-01.4-19 | Tampering | Storage deleted but DB rollback | mitigate | cleanupAttachmentStorage only after commit (D-07) |
| T-01.4-20 | Elevation of Privilege | Ownership checks moved into transactions | mitigate | getSurveyForUser keeps `user_id = $2`; reportability rules unchanged |
</threat_model>

<verification>
- `npm run lint && npm run typecheck && npm run test:unit` green
- Full E2E suite green locally or deferred to CI in the SUMMARY
</verification>

<success_criteria>
ROADMAP criterion 3 holds for attachment writes and report creation.
</success_criteria>

<output>
Create `.planning/phases/01.4-api-sync-integrity/01.4-04-SUMMARY.md` when done
</output>
