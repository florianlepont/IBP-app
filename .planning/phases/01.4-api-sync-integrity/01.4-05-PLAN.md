---
phase: 01.4-api-sync-integrity
plan: 05
type: execute
wave: 3
depends_on: [01.4-02, 01.4-03]
files_modified:
  - api/src/surveys/surveys-normalize.utils.ts
  - api/test/surveys-normalize.utils.spec.ts
  - api/src/surveys/surveys.service.ts
  - api/test/surveys-idempotency.e2e-spec.ts
  - api/test/epic-e-search-reports.e2e-spec.ts
  - api/test/sync-installed-app-compat.e2e-spec.ts
  - docs/technical/api-contract-v1.md
autonomous: true
requirements: [REQ-AUD-sync-validation]

must_haves:
  truths:
    - "A sync or REST upsert can no longer submit a survey: client status is accepted and ignored; new surveys are created as draft and an update never changes status (D-03)"
    - "A client can no longer move expires_at: it is computed server-side at creation (now + 7 days) and never changed by an upsert (D-03)"
    - "An upsert that changes a read-only field of a submitted survey gets 409 survey_submitted_read_only (fatal_error on /sync) with the changed field names; resending identical values is accepted; scores are excluded from the comparison (D-04, D-13)"
    - "An accepted upsert on a submitted survey never rewrites its factors, factor_results, scores or parcels"
    - "PATCH /surveys/:id keeps its 422 submitted_read_only_fields contract (D-13)"
    - "A batch replaying exactly what the installed mobile app sends today (all five operation types, status/expires_at included, pulled-survey payloads included) syncs without any rejection (backward compatibility)"
  artifacts:
    - path: api/src/surveys/surveys-normalize.utils.ts
      provides: "getChangedSubmittedReadOnlyFields(body, existing, existingParcelIds) value comparison"
      exports: ["getChangedSubmittedReadOnlyFields"]
    - path: api/test/surveys-normalize.utils.spec.ts
      provides: "Unit spec of the value comparison"
    - path: api/test/sync-installed-app-compat.e2e-spec.ts
      provides: "Installed-app payload replay E2E (mobile/src/storage/sync.ts shapes)"
  key_links:
    - from: api/src/surveys/surveys.service.ts
      to: api/src/surveys/surveys-normalize.utils.ts
      via: "getChangedSubmittedReadOnlyFields in upsertForUser"
      pattern: "getChangedSubmittedReadOnlyFields\\("
    - from: api/test/sync-installed-app-compat.e2e-spec.ts
      to: "POST /v1/sync"
      via: "operations built exactly like mobile/src/storage/sync.ts"
      pattern: "/v1/sync"
---

<objective>
Stop the upsert path from being a back door (A-H2): ignore client `status` and `expires_at`, protect a submitted survey's read-only fields by value, and prove with a replay of today's mobile payloads that installed apps keep syncing without an update.

Purpose: REQ-AUD-sync-validation, ROADMAP success criterion 2 and the backward-compatibility constraint (D-03, D-12, D-13).
Output: value-comparison helper with unit spec, upsertForUser changes, fixed E2E fixtures that relied on client status/expires_at, installed-app replay E2E, contract docs.
Runs after plan 03 (same file api/src/surveys/surveys.service.ts, already transaction-wrapped) and plan 02 (per-operation validation, whitelisting).
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.4-api-sync-integrity/01.4-CONTEXT.md
@.planning/phases/01.4-api-sync-integrity/01.4-PATTERNS.md
@.planning/phases/01.4-api-sync-integrity/01.4-02-SUMMARY.md
@.planning/phases/01.4-api-sync-integrity/01.4-03-SUMMARY.md
@CLAUDE.md

<interfaces>
api/src/surveys/surveys-normalize.utils.ts (existing):
- `getSubmittedReadOnlyFields(body: SurveyPatchBody): string[]` — key-presence check over readonlyFields `site_name, parcel_id, parcel_ids, observation_year, version_number, previous_survey_id, region_version, vegetation_stage, factors, scores`; used by patchSurvey (422 `submitted_read_only_fields`) — do not change it or patch's contract.
- `normalizeParcelId`, `normalizeParcelIds` (trim, upper-case, dedupe), `normalizeObservationYear`, `normalizeVersionNumber`, `normalizePreviousSurveyId`.
- `SurveyRow` (surveys.types.ts): status, parcel_id, observation_year, version_number, previous_survey_id, region_version, vegetation_stage, factors (JSONB object), factor_results, scores, expires_at, sync_version, ...

upsertForUser after plan 03 (re-read it; line numbers moved): runs inside `this.db.transaction(async (db) => ...)`, reads the existing row FOR UPDATE, INSERT uses `ON CONFLICT (id) DO NOTHING`, the UPDATE is guarded by `AND sync_version < $n`, zero-row outcome → idempotent result or `sync_version_conflict`. Today it still writes `body.status ?? "draft"` on INSERT, `body.status ?? existing.status` and `expiresAt` (from `body.expires_at ?? now + 7d`) on UPDATE.

Exactly what installed apps send (mobile/src/storage/sync.ts + mobile/src/storage/surveys.ts), body `{ operations }`, client_ref = String(sync_queue row id):
- survey.upsert (no survey_id on the envelope), payload from local saves: `{ id, sync_version, site_name, status: "draft", visibility, parcel_ids: [upper-cased], region_version, vegetation_stage, factors }` plus keys spread from the previous local payload (may include `scores`, `expires_at`)
- survey.upsert built from a pulled survey (buildSurveyPayloadFromRemote): `{ id, sync_version: remote.sync_version, site_name, status: remote.status (e.g. "submitted"), visibility, parcel_ids, region_version?, vegetation_stage?, factors, scores: remote.scores, expires_at: remote.expires_at }` where expires_at is PostgreSQL text such as `2026-10-01 10:00:00.123+00`
- attachment.create: envelope survey_id; payload `{ mime_type, size_bytes, captured_at: null, metadata: {} }`
- attachment.delete: envelope survey_id; payload `{ attachment_id }`
- survey.delete: envelope survey_id; payload `{ id }`
- survey.visibility_update: envelope survey_id; payload `{ visibility }`
The pulled survey is read from `GET /v1/sync/changes` (`surveys[]` items carry sync_version, status, parcel_ids, factors, scores, expires_at).

E2E fixtures that rely on the old behaviour and must be fixed here:
- api/test/surveys-idempotency.e2e-spec.ts ~180-225 ("submit expired"): sends `expires_at: expiredAt` on POST /v1/surveys and expects 422 + status 'expired'
- api/test/epic-e-search-reports.e2e-spec.ts lines ~73, 211, 228, 377: create surveys with `status: "submitted"` through POST /v1/surveys; this spec already has `db = moduleFixture.get(DatabaseService)`
Grep all of api/test for other upsert/sync payloads relying on client status or expires_at before finishing.

Local PostgreSQL for E2E: plan 01 interfaces. If it cannot start, record "E2E deferred to CI".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Value comparison of submitted read-only fields</name>
  <files>api/src/surveys/surveys-normalize.utils.ts, api/test/surveys-normalize.utils.spec.ts</files>
  <read_first>
    - api/src/surveys/surveys-normalize.utils.ts (getSubmittedReadOnlyFields, normalize* helpers)
    - api/src/surveys/surveys.types.ts (SurveyUpsertBody, SurveyRow)
  </read_first>
  <behavior>
    - identical values in every field → []
    - body missing a field, or the field `undefined`/`null` → not reported (absent means "unchanged")
    - `site_name` differs → ["site_name"]
    - `parcel_ids` same set, different order / lower-case / whitespace → []; different set → ["parcel_ids"]; compared against existingParcelIds, falling back to `[existing.parcel_id]` when existingParcelIds is empty
    - `parcel_id` compared after normalizeParcelId; `observation_year`, `version_number`, `previous_survey_id` compared after their normalize helpers
    - `region_version`, `vegetation_stage` differ → reported
    - `factors` with the same content but different key order → []; a changed value (`A: 1` → `A: 2`), an added or removed key → ["factors"]; nested objects compared recursively; arrays compared in order
    - `scores` different → never reported (D-13)
    - several differences → all names reported, in readonly-list order
  </behavior>
  <action>
    Per D-04/D-13 add `export function getChangedSubmittedReadOnlyFields(body: SurveyUpsertBody, existing: SurveyRow, existingParcelIds: string[]): string[]` next to getSubmittedReadOnlyFields, reusing the same field list minus `scores`. Implement a small local `jsonDeepEqual(a: unknown, b: unknown): boolean` (plain objects key-order independent, arrays ordered, primitives strict) — no new dependency. Leave getSubmittedReadOnlyFields untouched. Write the spec (api/test/surveys-normalize.utils.spec.ts) first with a `makeRow(overrides)` SurveyRow factory, then implement.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- surveys-normalize.utils && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function getChangedSubmittedReadOnlyFields" api/src/surveys/surveys-normalize.utils.ts` returns 1
    - `grep -c "export function getSubmittedReadOnlyFields" api/src/surveys/surveys-normalize.utils.ts` returns 1 (unchanged)
    - The spec covers every behaviour bullet and passes
  </acceptance_criteria>
  <done>A pure helper reports exactly the read-only fields whose values differ, ignoring scores and key order.</done>
</task>

<task type="auto">
  <name>Task 2: upsertForUser ignores client status/expires_at and protects submitted surveys; fix dependent fixtures</name>
  <files>api/src/surveys/surveys.service.ts, api/test/surveys-idempotency.e2e-spec.ts, api/test/epic-e-search-reports.e2e-spec.ts</files>
  <read_first>
    - api/src/surveys/surveys.service.ts upsertForUser (whole method, after plan 03) and patchSurvey's read-only block
    - api/test/surveys-idempotency.e2e-spec.ts ~170-230
    - api/test/epic-e-search-reports.e2e-spec.ts (each survey created with status "submitted" and what the test then asserts)
  </read_first>
  <action>
    In upsertForUser (inside the existing transaction; keep plan 03's locking, ON CONFLICT and sync_version guard intact):
    D-03 status: the INSERT always writes 'draft'; the UPDATE no longer sets `status` at all (remove it from SET; renumber the parameters and keep the `sync_version < $n` guard pointing at the sync_version parameter). Status changes only through submitSurvey (submitted/expired).
    D-03 expires_at: the INSERT writes `now + 7 days` computed server-side; the UPDATE no longer sets `expires_at`. Never read `body.status` or `body.expires_at` anywhere in the service (they stay on the DTO so validation accepts them).
    D-04/D-13 submitted surveys: after the `sync_version < existing` conflict and the equal-version idempotent return, when `existing.status === "submitted"` compute `getChangedSubmittedReadOnlyFields(body, existing, existingParcelIds)`; if non-empty throw `new ConflictException({ code: "survey_submitted_read_only", message: "Submitted survey fields are read-only", details: { survey_id: body.id, fields } })` (mapSyncError turns it into fatal_error / http_status 409 with code and details). If empty, run a restricted UPDATE that only sets `visibility`, `sync_version`, `updated_at` (same guard), skip ensureParcelIds/syncSurveyParcels, keep factors, factor_results and scores as stored, and insert the usual "updated" event. Draft/expired surveys keep today's update behaviour (minus status/expires_at).
    Leave patchSurvey (422 submitted_read_only_fields) unchanged.
    Fixtures: in surveys-idempotency.e2e-spec.ts "submit expired" test, keep the upsert (it may still send expires_at — ignored) and add `await db.query("UPDATE surveys SET expires_at = $2 WHERE id = $1", [surveyId, expiredAt])` before submitting. In epic-e-search-reports.e2e-spec.ts, after each POST /v1/surveys that sent `status: "submitted"`, set it directly with `await db.query("UPDATE surveys SET status = 'submitted', submitted_at = NOW() WHERE id = $1", [id])` (remove the misleading `status: "submitted"` from those payloads). Grep api/test for any other payload whose outcome depends on client status or expires_at and fix it the same way.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm run typecheck && npm run lint && npm --workspace api run test:unit && node -e "const s=require('fs').readFileSync('api/src/surveys/surveys.service.ts','utf8');const m=s.slice(s.indexOf('async upsertForUser('),s.indexOf('async patchSurvey('));if(/body\.status|body\.expires_at/.test(m)){console.error('upsert still reads body.status/expires_at');process.exit(1)}if(!/survey_submitted_read_only/.test(m)){console.error('missing survey_submitted_read_only');process.exit(1)}console.log('ok')" && (pg_isready -h localhost -p 5432 && npm --workspace api run test:e2e -- "surveys-idempotency|epic-e-search-reports|surveys-transactions" || echo "E2E deferred to CI: no local PostgreSQL")</automated>
  </verify>
  <acceptance_criteria>
    - The node check prints `ok` (upsertForUser never reads body.status or body.expires_at and raises survey_submitted_read_only)
    - `grep -c "submitted_read_only_fields" api/src/surveys/surveys.service.ts` returns 1 (patch contract unchanged)
    - `grep -c "status: \"submitted\"" api/test/epic-e-search-reports.e2e-spec.ts` returns 0
    - With local PostgreSQL the idempotency, epic-e and transactions specs pass; otherwise the SUMMARY says "E2E deferred to CI"
  </acceptance_criteria>
  <done>Upsert cannot submit, cannot move expires_at and cannot overwrite a submitted survey's read-only values, while identical resends pass.</done>
</task>

<task type="auto">
  <name>Task 3: Installed-app replay E2E and contract docs</name>
  <files>api/test/sync-installed-app-compat.e2e-spec.ts, docs/technical/api-contract-v1.md</files>
  <read_first>
    - mobile/src/storage/sync.ts (operation construction ~695-770, buildSurveyPayloadFromRemote ~372-386)
    - mobile/src/storage/surveys.ts (~325-345: local save payload) and mobile/src/storage/types.ts (SurveyQueuePayload)
    - api/test/sync-validation.e2e-spec.ts (plan 02: configureApp setup, login)
    - docs/technical/api-contract-v1.md ("### POST /surveys", "### POST /surveys/{id}/submit", "### POST /sync", "## Standard Error Codes")
  </read_first>
  <action>
    Create api/test/sync-installed-app-compat.e2e-spec.ts with `configureApp(app)` (production pipe) and `db`. Add a header comment naming the mobile source of each payload shape. Build operations with a local `mobileOps` helper that mirrors mobile/src/storage/sync.ts field-for-field (client_ref strings "1", "2", ...; no extra envelope fields). Scenario on a resolved synthetic parcel P:
    (1) One batch as a fresh install would send: survey.upsert S `{ id, sync_version: 1, site_name, status: "draft", visibility: "private", parcel_ids: [P], region_version: "ACA", vegetation_stage: "collineen", factors: validFactors, expires_at: <ISO one year ahead> }` (installed apps omit observation_year/version_number; the server derives them from the parcel), attachment.create for S, survey.visibility_update S to "public", survey.upsert T (second survey) and survey.delete T → every result `synced`. Assert S is 'draft', its expires_at is within [created_at + 7d - 1 min, created_at + 7d + 1 min] (the client value was ignored), survey_parcels has P.
    (2) attachment.delete of the created attachment → `synced`.
    (3) Upsert S at sync_version 2 with `status: "submitted"` → `synced`; status still 'draft'; no 'submitted' event exists.
    (4) Submit S through `POST /v1/surveys/S/submit` → 201 (P is a fresh parcel, so the server-derived version_number is the next one; if the submit returns 422/409, print the body and fix the fixture — never loosen the assertion).
    (5) Pulled-survey replay: `GET /v1/sync/changes` and take S from `surveys[]`; build the payload exactly like buildSurveyPayloadFromRemote (status "submitted", server `expires_at` text, server `scores`, server `factors`, server `parcel_ids`). (a) Same sync_version → `synced`. (b) sync_version + 1, identical values → `synced`; factors, scores, factor_results, parcels and status unchanged in the DB. (c) sync_version + 2 with `scores: {}` → `synced` (scores excluded). (d) sync_version + 3 with factors.A changed → `fatal_error`, `error.code === "survey_submitted_read_only"`, `error.http_status === 409`, `error.details.fields` contains "factors"; stored factors unchanged.
    (6) `expires_at: "abc"` on a draft upsert → `synced` (ignored, never rejected, D-03).
    Run it with local PostgreSQL, then the full E2E suite; otherwise record "E2E deferred to CI".
    Docs (api-contract-v1.md): under "### POST /surveys" and "### POST /sync" state that `status` and `expires_at` are accepted for compatibility and ignored; `expires_at` is set by the server at creation (7 days) and status changes only through submit/expiry; an upsert on a submitted survey that changes `site_name, parcel_id(s), observation_year, version_number, previous_survey_id, region_version, vegetation_stage` or `factors` returns 409 `survey_submitted_read_only` with `details.fields` (identical values and `scores` are accepted); under "### POST /surveys/{id}/submit" document that concurrent submits on a parcel return 409 `parcel_version_conflict` for the loser; add `survey_submitted_read_only`, `survey_id_conflict` and `parcel_version_conflict` (if missing) to "## Standard Error Codes".
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm run typecheck && npm run lint && npm run format:check && (pg_isready -h localhost -p 5432 && npm --workspace api run test:e2e || echo "E2E deferred to CI: no local PostgreSQL")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mobile/src/storage/sync.ts" api/test/sync-installed-app-compat.e2e-spec.ts` returns at least 1
    - `grep -cE "\"(upsert|create|delete|visibility_update)\"" api/test/sync-installed-app-compat.e2e-spec.ts` returns at least 4 (all operation types replayed)
    - `grep -c "survey_submitted_read_only" api/test/sync-installed-app-compat.e2e-spec.ts docs/technical/api-contract-v1.md` returns at least 1 for each file
    - With local PostgreSQL the full `npm --workspace api run test:e2e` passes; otherwise the SUMMARY says "E2E deferred to CI"
  </acceptance_criteria>
  <done>Today's installed-app payloads sync unchanged, status/expires_at are provably ignored, and a submitted survey is protected by value; the contract documents it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile client → upsert (/sync, POST /surveys) | Client-controlled status, expires_at and observation fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.4-21 | Elevation of Privilege | upsert status (client sets "submitted") | mitigate | INSERT always 'draft'; UPDATE never writes status; submission only via submitSurvey |
| T-01.4-22 | Tampering | upsert expires_at (deadline moved) | mitigate | Server computes expires_at at creation; UPDATE never writes it |
| T-01.4-23 | Tampering | Submitted survey overwrite | mitigate | Value comparison of read-only fields → 409 survey_submitted_read_only; accepted resends only touch visibility/sync_version |
| T-01.4-24 | Denial of Service | Installed apps broken by the change | mitigate | Fields still accepted by the DTO and ignored; identical resends pass; E2E replays exact mobile payloads |
</threat_model>

<verification>
- `npm run lint && npm run typecheck && npm run test:unit && npm run format:check` green
- Full E2E suite green locally or deferred to CI in the SUMMARY
</verification>

<success_criteria>
ROADMAP criterion 2 holds and installed-app backward compatibility (D-03, D-12, D-13) is proven by an E2E replaying mobile/src/storage/sync.ts payload shapes.
</success_criteria>

<output>
Create `.planning/phases/01.4-api-sync-integrity/01.4-05-SUMMARY.md` when done
</output>
