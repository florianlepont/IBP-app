---
phase: 01.4-api-sync-integrity
plan: 06
type: execute
wave: 4
depends_on: [01.4-01, 01.4-02, 01.4-03, 01.4-04, 01.4-05]
files_modified:
  - api/jest.unit.config.js
  - .planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md
autonomous: false
requirements: [REQ-AUD-sync-validation, REQ-AUD-transactions]

must_haves:
  truths:
    - "The full local gate (lint, typecheck, unit with coverage, format) is green on the integrated phase result, and the full E2E suite is green locally when PostgreSQL can be started"
    - "Coverage thresholds in api/jest.unit.config.js are raised to the new measured floors where they went up and no value is lowered"
    - "On the phase PR, every CI job is green and the E2E job (run twice on ibp_test) executed the concurrent-submit, injected-failure and installed-app replay tests"
    - "01.4-VALIDATION.md records the PR URL, the run URL, the job conclusions and the named E2E tests that passed"
    - "The owner reviewed and merged the PR, and an unchanged installed app still syncs against the deployed API"
  artifacts:
    - path: .planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md
      provides: "Per-task statuses and CI evidence (run URL, job conclusions, E2E test names)"
      contains: "actions/runs/"
    - path: api/jest.unit.config.js
      provides: "Ratcheted coverage thresholds"
  key_links:
    - from: 01.4-VALIDATION.md
      to: "GitHub Actions run of the phase PR"
      via: "run URL, job table and E2E test names recorded"
      pattern: "actions/runs/"
---

<objective>
Phase gate: run the complete local gate on the integrated result, ratchet API coverage upward, open the phase PR, and record from its GitHub Actions run the E2E evidence the sandbox may not have been able to produce (concurrent submits, injected event failures, installed-app replay). Then the owner merges and confirms an installed app still syncs.

Purpose: both phase requirements; ROADMAP success criteria 1-5 demonstrated on CI.
Output: ratcheted thresholds, VALIDATION.md with CI evidence, merged PR.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md
@.planning/phases/01.4-api-sync-integrity/01.4-01-SUMMARY.md
@.planning/phases/01.4-api-sync-integrity/01.4-02-SUMMARY.md
@.planning/phases/01.4-api-sync-integrity/01.4-03-SUMMARY.md
@.planning/phases/01.4-api-sync-integrity/01.4-04-SUMMARY.md
@.planning/phases/01.4-api-sync-integrity/01.4-05-SUMMARY.md
@CLAUDE.md

<interfaces>
Repository: github.com/florianlepont/cortege (public). Working branch: the phase branch currently checked out (e.g. claude/code-audit-complete-3sn99m); PR base: main.
GitHub access: the `gh` CLI may be absent in the sandbox; use the GitHub MCP server tools (create/read pull requests, list workflow runs and jobs, read job logs) if available. If neither can read Actions runs, surface an access gate and ask the owner for the run URL, job conclusions and the E2E job log.
ci.yml job names: "Detect changed paths", "Lint, format, typecheck", "Unit tests — API", "Unit tests — Mobile", "E2E tests — API" (steps "E2E run 1", "E2E run 2 (same database)", reset assertion), "Mobile build check", "Dependency audit", "Docker image check", "CI OK", "Build & push Docker image" (main pushes only, skipped on PRs). CodeQL workflow job: "CodeQL analysis". Mobile jobs may be skipped by path filters because this phase changes only api/** and docs — "skipped" is acceptable for path-filtered mobile jobs, "failure" is not.
Coverage ratchet (phase 01.3 convention): `npm --workspace api run test:unit:coverage` writes api/coverage/unit/coverage-summary.json; `node scripts/coverage-by-directory.js api` prints floor-rounded per-directory values. Thresholds may only go up.
Local PostgreSQL: `pg_ctlcluster 16 main start`; role ibp/ibp superuser (`su postgres -c "psql -c \"CREATE ROLE ibp LOGIN SUPERUSER PASSWORD 'ibp'\""`); `npm run test:e2e` resets and migrates ibp_test.
E2E spec files of this phase: api/test/database-transaction.e2e-spec.ts (01), sync-validation.e2e-spec.ts (02), surveys-transactions.e2e-spec.ts (03), attachments-reports-transactions.e2e-spec.ts (04), sync-installed-app-compat.e2e-spec.ts (05).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full local gate, coverage ratchet and validation map update</name>
  <files>api/jest.unit.config.js, .planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md</files>
  <read_first>
    - api/jest.unit.config.js (coverageThreshold block and ratchet comment)
    - scripts/coverage-by-directory.js (usage)
    - .planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md (Per-Task Verification Map)
    - the SUMMARY files of plans 01-05 (which E2E runs were deferred to CI)
  </read_first>
  <action>
    From the repo root run, in order: `npm run lint`, `npm run typecheck`, `npm --workspace api run test:unit:coverage`, `npm --workspace mobile run test:unit`, `npm run format:check`. Then `node scripts/coverage-by-directory.js api` and, for every directory/metric in `coverageThreshold` whose measured floor is now higher than the configured value, raise the configured value to the measured floor; never lower any value (`./src/surveys/` and `./src/database/` are expected to rise); update the date in the ratchet comment. Re-run `npm --workspace api run test:unit:coverage` to prove the new thresholds pass. Start local PostgreSQL (interfaces) and run `npm run test:e2e` twice in a row; if the cluster cannot start, note it and rely on Task 2. Fix nothing silently: if a command fails, stop and report which plan's files are involved. In 01.4-VALIDATION.md set each locally-proven Per-Task row to ✅ green (or ❌ red with the failing command), leave CI-only rows ⬜ for Task 2, and set `wave_0_complete: true`.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm run lint && npm run typecheck && npm --workspace api run test:unit:coverage && npm --workspace mobile run test:unit && npm run format:check && git diff -U0 api/jest.unit.config.js | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const rm=d.split('\n').filter(l=>l.startsWith('-')&&!l.startsWith('---')&&/statements/.test(l));const add=d.split('\n').filter(l=>l.startsWith('+')&&!l.startsWith('+++')&&/statements/.test(l));const parse=l=>{const k=(l.match(/'([^']+)'|global/)||[''])[0];const v={};for(const m of l.matchAll(/(statements|branches|functions|lines): (\d+)/g))v[m[1]]=+m[2];return [k,v]};const old=Object.fromEntries(rm.map(parse));for(const l of add){const [k,v]=parse(l);const o=old[k];if(!o)continue;for(const x in v){if(v[x]<o[x]){console.error('lowered '+k+' '+x);process.exit(1)}}}console.log('ratchet ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - All five gate commands exit 0 and the ratchet check prints `ratchet ok`
    - `grep -c "wave_0_complete: true" .planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md` returns 1
    - Every locally-proven row of the Per-Task Verification Map shows ✅ or ❌
  </acceptance_criteria>
  <done>The integrated phase passes every local gate, thresholds only went up, and the validation map reflects it.</done>
</task>

<task type="auto">
  <name>Task 2: Open the phase PR and record its CI evidence</name>
  <files>.planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md</files>
  <read_first>
    - .github/workflows/ci.yml (job and step names)
    - .planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md
  </read_first>
  <action>
    Push the phase branch and open (or reuse) a PR against main titled "Phase 01.4: API sync integrity", body summarising plans 01-05, calling out the behaviour changes for the owner (client status/expires_at ignored, 409 survey_submitted_read_only, 22/23 SQLSTATE fatal, parcel_ids ≤ 50) and ending with the attribution lines from the session's system reminder. Wait for the CI and CodeQL runs on the PR head commit to finish (poll; at most 45 minutes). Verify from job conclusions and logs:
    1. `success` for Detect changed paths, Lint/format/typecheck, Unit tests — API, E2E tests — API, Dependency audit, Docker image check, CI OK, CodeQL analysis; mobile jobs `success` or `skipped` (path filter); "Build & push Docker image" `skipped`.
    2. The E2E job log shows both "E2E run 1" and "E2E run 2 (same database)" passing, and lists as passed the five phase specs, including the concurrent-submit test (surveys-transactions), the injected-failure tests (surveys-transactions, attachments-reports-transactions), the deterministic-error test (sync-validation) and the installed-app replay (sync-installed-app-compat). Quote the matching log lines.
    3. Unit tests — API log shows the coverage thresholds enforced with no "threshold ... not met".
    If a job fails, diagnose from the log, fix in the owning plan's files on the same branch, push, re-verify, and record each fix as a deviation in the SUMMARY. Record the PR URL, run URLs, the job conclusion table and the quoted E2E lines in 01.4-VALIDATION.md ("CI Evidence" section) and set the CI-proven Per-Task rows to ✅.
  </action>
  <verify>
    <automated>grep -c "actions/runs/" /home/user/cortege/.planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 01.4-VALIDATION.md contains the PR URL, at least one `actions/runs/` URL and a job-conclusion table matching point 1
    - The quoted E2E log lines name the concurrent-submit, injected-failure, deterministic-error and installed-app replay tests as passed
    - No Per-Task row is left ⬜ except the owner checkpoint row
  </acceptance_criteria>
  <done>The phase PR's CI proves every ROADMAP criterion end to end on real PostgreSQL, with evidence recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner review, merge and installed-app sync check</name>
  <files>.planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md (CI Evidence)
  </read_first>
  <action>Present the steps below with the PR URL and the CI run URL, wait for the resume signal, then record the result in the "Manual-Only Verifications" table of 01.4-VALIDATION.md.</action>
  <what-built>Plans 01-05: transaction helper and account-deletion order, per-operation DTO validation of /v1/sync with generic fatal errors, atomic writes with events and one-winner concurrent submits, attachments/reports transactions, and an upsert that ignores client status/expires_at and protects submitted surveys. The PR's CI run is green (Task 2).</what-built>
  <how-to-verify>
    1. Review the PR, in particular the behaviour changes listed in its body, then merge it into main (the merge is the owner's action).
    2. Wait for the VPS timer to deploy the new image (about 5 minutes after the main build; `curl https://cortege.algernon.ovh/v1/health` returns 200).
    3. On a phone running the currently installed app (no update): in airplane mode create a draft survey on a parcel, add a photo, reconnect and let it sync. Expected: the survey and photo reach the server with no sync error shown.
    4. Submit that survey from the app. Expected: it is marked submitted; editing it afterwards is refused as before.
  </how-to-verify>
  <resume-signal>Type "approved — merged, installed app syncs", or describe what failed</resume-signal>
  <verify>
    <automated>grep -c "Manual-Only Verifications" /home/user/cortege/.planning/phases/01.4-api-sync-integrity/01.4-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Owner replied with approval, or each failure is recorded in 01.4-VALIDATION.md with the observed behaviour
  </acceptance_criteria>
  <done>The phase is merged and an unchanged installed app keeps syncing against the deployed API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → GitHub | The agent pushes a branch and opens a PR; merging stays with the owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.4-25 | Elevation of Privilege | Agent merging to main | mitigate | Merge is an owner step in the blocking checkpoint; the agent never merges |
| T-01.4-26 | Repudiation | CI claims without evidence | mitigate | Run URLs, job conclusions and quoted E2E log lines recorded in VALIDATION.md and SUMMARY |
| T-01.4-27 | Denial of Service | Deployed API rejecting installed apps | mitigate | Installed-app replay E2E on CI plus the owner's device sync check after deploy |
| T-01.4-28 | Tampering | Coverage thresholds lowered to pass | mitigate | Ratchet check in Task 1 fails on any lowered value |
</threat_model>

<verification>
- Local gate green; PR CI green with E2E evidence recorded; owner merged and confirmed the installed app syncs
</verification>

<success_criteria>
ROADMAP success criteria 1-5 demonstrated on a real GitHub Actions run of the phase PR, and backward compatibility confirmed on a device.
</success_criteria>

<output>
Create `.planning/phases/01.4-api-sync-integrity/01.4-06-SUMMARY.md` when done
</output>
