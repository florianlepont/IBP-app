---
phase: 01.5-mobile-sync-engine-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/package.json
  - package-lock.json
  - mobile/app.json
  - mobile/jest.unit.config.js
  - mobile/test/expo-crypto.mock.ts
  - mobile/test/expo-file-system-legacy.mock.ts
  - mobile/test/expo-image-manipulator.mock.ts
  - mobile/test/expo-image.mock.ts
  - mobile/src/storage/attachment-files.ts
  - mobile/src/storage/attachment-files.test.ts
autonomous: true
requirements: [REQ-AUD-photos]

must_haves:
  truths:
    - "expo-crypto, expo-file-system, expo-image and expo-image-manipulator are recorded in mobile/package.json at SDK 57 versions and in the root package-lock.json, installed with npx expo install (D-02, C12)"
    - "expo-doctor 1.20.4 (EXPO_DOCTOR_SKIP_DEPENDENCY_VERSION_CHECK=1) and expo export --platform android both pass from mobile/ with the new modules"
    - "Jest resolves all four modules to deterministic doubles under mobile/test/ through the global moduleNameMapper; the expo-crypto double returns real unique v4 UUIDs"
    - "The expo-file-system/legacy double is an in-memory file system (documentDirectory, getInfoAsync with size, copyAsync, makeDirectoryAsync, deleteAsync, downloadAsync, createUploadTask) that tests can seed and inspect"
    - "One module owns every photo path rule: files live in documentDirectory/attachments/, a stored URI from an older app container is re-based onto the current documentDirectory, and deletes never leave that directory (D-09, D-12)"
  artifacts:
    - path: mobile/src/storage/attachment-files.ts
      provides: "Photo file path and file I/O helpers over expo-file-system/legacy"
      exports: ["ATTACHMENTS_SUBDIR", "getAttachmentsDirUri", "ensureAttachmentsDir", "buildAttachmentFileUri", "resolveAttachmentUri", "isInAttachmentsDir", "getLocalFileSize", "localFileExists", "deleteAttachmentFile", "deleteAllAttachmentFiles", "extensionForMimeType"]
    - path: mobile/test/expo-file-system-legacy.mock.ts
      provides: "In-memory legacy FileSystem double with __resetMockFileSystem / __setMockFile / __getMockFile helpers"
    - path: mobile/test/expo-crypto.mock.ts
      provides: "randomUUID backed by node:crypto"
    - path: mobile/jest.unit.config.js
      provides: "moduleNameMapper entries for the four new modules; thresholds unchanged"
      contains: "expo-file-system/legacy"
  key_links:
    - from: mobile/jest.unit.config.js
      to: mobile/test/expo-file-system-legacy.mock.ts
      via: "moduleNameMapper '^expo-file-system/legacy$'"
      pattern: "expo-file-system/legacy"
    - from: mobile/src/storage/attachment-files.ts
      to: expo-file-system/legacy
      via: "import * as FileSystem from \"expo-file-system/legacy\""
      pattern: "expo-file-system/legacy"
---

<objective>
Land the four native modules of the phase once, early, so every later plan builds on them (D-02), give Jest deterministic doubles for them, and create the single module that owns where photo files live and how their paths are resolved.

Purpose: REQ-AUD-photos foundation. Plans 05, 06, 07, 09 and 11 import these modules and doubles; no other plan edits package.json, the lockfile or jest.unit.config.js before the phase gate.
Output: updated package.json + lockfile, four mocks wired in jest.unit.config.js, `storage/attachment-files.ts` with unit tests.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-CONTEXT.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-PATTERNS.md
@CLAUDE.md

<interfaces>
CLAUDE.md is outdated (C11-C13): the app is on Expo SDK 57 (`expo ~57.0.24`, `react-native 0.86.3`); mocks live in `mobile/test/*.mock.ts`; the key/value table is `local_meta`.

C12: `expo-crypto 57.0.3`, `expo-image 57.0.5`, `expo-image-manipulator 57.0.20` sit in root `node_modules/` from the researcher's slopcheck run but are NOT in package.json / package-lock.json; `expo-file-system 57.0.7` is a transitive dependency of `expo`. A green local tsc is not proof of installation.

Package legitimacy (RESEARCH "Package Legitimacy Audit"): all four are first-party Expo SDK packages, slopcheck `[OK]`, disposition Approved. No human legitimacy checkpoint is required.

Verified SDK 57 declarations (node_modules, 2026-09-25):
- expo-file-system/legacy (`build/legacy/FileSystem.d.ts`): `documentDirectory: string | null`, `cacheDirectory`, `getInfoAsync(uri, { size?: boolean })` returning `{ exists: false }` or `{ exists: true, size, isDirectory, uri, ... }`, `copyAsync({ from, to })`, `moveAsync`, `makeDirectoryAsync(uri, { intermediates })`, `deleteAsync(uri, { idempotent })`, `downloadAsync(url, fileUri, { headers })` resolving `{ status, uri, headers, mimeType, md5? }`, `uploadAsync(url, fileUri, options)`, `createUploadTask(url, fileUri, options, callback?)` returning `UploadTask` with `uploadAsync(): Promise<FileSystemUploadResult | undefined | null>` and `cancelAsync(): Promise<void>`. Enum `FileSystemUploadType { BINARY_CONTENT = 0, MULTIPART = 1 }`. Upload options: `httpMethod`, `uploadType`, `fieldName`, `mimeType`, `headers`, `parameters`. Result: `{ status, body, headers }`.
- expo-image-manipulator (`build/index.d.ts`): exports `ImageManipulator` (with `manipulate(source: string | SharedRef<'image'>): ImageManipulatorContext`), deprecated `manipulateAsync`, `useImageManipulator`, `SaveFormat` (`JPEG = "jpeg"`, `PNG = "png"`, `WEBP = "webp"`), `FlipType`. Context: `resize({ width?, height? })`, `rotate`, `renderAsync(): Promise<ImageRef>`. `ImageRef`: `width`, `height`, `saveAsync({ format?, compress?, base64? }): Promise<{ uri, width, height, base64? }>`.
- expo-crypto: `randomUUID(): string`.
- expo-image: `Image` component (props `source`, `contentFit`, `cachePolicy`, `recyclingKey`, `allowDownscaling`, `placeholder`).

Mock analogs: `mobile/test/expo-haptics.mock.ts` (plain jest.fn exports + enums), `mobile/test/expo-sqlite.mock.ts` (functional fake with header comment). Mapper today (`mobile/jest.unit.config.js:19-25`): `@expo/vector-icons`, `expo-sqlite`, `expo-haptics`, `react-native-svg`, image assets. `mobile/test/` is typechecked by `npm run typecheck` (tsconfig has no include) and prettier-checked by the root `format:check`, but not linted.

Test-helper access rule: tests must import mock helpers (`__setMockFile` etc.) from the mock file by relative path (e.g. `../../test/expo-file-system-legacy.mock`), never from "expo-file-system/legacy" (the real type declarations do not have them). Jest keys modules by resolved path, so the relative import and the mapped module are the same instance.

CI mobile-build job (`.github/workflows/ci.yml:264-308`): `npx --yes expo-doctor@1.20.4` with `EXPO_DOCTOR_SKIP_DEPENDENCY_VERSION_CHECK=1`, `npx expo install --check` (informational), `npx expo export --platform android --output-dir "$RUNNER_TEMP/expo-export"`, all from `mobile/`. Dependency audit job: `npm audit --audit-level=high` at the root.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the four SDK 57 modules with expo install and prove expo-doctor / expo export</name>
  <files>mobile/package.json, package-lock.json, mobile/app.json</files>
  <read_first>
    - mobile/package.json (dependencies block)
    - mobile/app.json (plugins array)
    - .github/workflows/ci.yml (lines 264-330: mobile-build and audit jobs)
    - .planning/phases/01.5-mobile-sync-engine-reliability/01.5-RESEARCH.md (Package Legitimacy Audit, Pitfall 1)
  </read_first>
  <action>
    Per D-02, from `mobile/` run `npx expo install expo-crypto expo-file-system expo-image expo-image-manipulator` so Expo picks the SDK 57 versions and writes `mobile/package.json` and the root `package-lock.json` (npm workspaces). Expected pins are `~57.0.3`, `~57.0.7`, `~57.0.5`, `~57.0.20` or whatever `expo install` selects for SDK 57; do not hand-edit versions. Then run `npm install` at the root once so the lockfile is consistent, and confirm with `npm ls expo-crypto expo-file-system expo-image expo-image-manipulator --workspace mobile` (no "missing"/"invalid").
    None of the four needs config-plugin options in this phase (PATTERNS "Native config (CNG)"), so do not touch `mobile/app.json` unless expo-doctor reports that a plugin must be listed; in that case add the bare plugin name to the `plugins` array (CNG, never `ios/`/`android/`, which are not committed).
    Run from `mobile/`: `EXPO_DOCTOR_SKIP_DEPENDENCY_VERSION_CHECK=1 npx --yes expo-doctor@1.20.4` and `npx expo export --platform android --output-dir "$(mktemp -d)"` (a temporary directory, never a tracked path; delete it afterwards). Run `npm audit --audit-level=high` at the root and record the result; if a high finding comes from one of the four packages, stop and report it rather than forcing a version.
    Do not run `expo prebuild` here; the owner rebuilds once at the phase gate (plan 12).
  </action>
  <verify>
    <automated>cd /home/user/cortege && node -e "const p=require('./mobile/package.json').dependencies;const l=require('./package-lock.json').packages;for(const n of ['expo-crypto','expo-file-system','expo-image','expo-image-manipulator']){if(!/^~?57\./.test(p[n]||''))throw new Error('package.json '+n+'='+p[n]);if(!l['node_modules/'+n]&&!l['mobile/node_modules/'+n])throw new Error('lockfile missing '+n)}console.log('deps ok')" && cd mobile && EXPO_DOCTOR_SKIP_DEPENDENCY_VERSION_CHECK=1 npx --yes expo-doctor@1.20.4 && OUT=$(mktemp -d) && npx expo export --platform android --output-dir "$OUT" && rm -rf "$OUT"</automated>
  </verify>
  <acceptance_criteria>
    - The node check prints `deps ok` (all four in package.json at a 57.x range and in package-lock.json)
    - expo-doctor exits 0 and expo export exits 0 from mobile/
    - `git status --porcelain mobile/ios mobile/android` prints nothing (no native folders committed)
    - `npm audit --audit-level=high` result recorded in the SUMMARY
  </acceptance_criteria>
  <done>The four modules are part of the dependency graph exactly as CI will install them, and the mobile-build CI checks pass locally.</done>
</task>

<task type="auto">
  <name>Task 2: Jest doubles for the four modules and global moduleNameMapper wiring</name>
  <files>mobile/test/expo-crypto.mock.ts, mobile/test/expo-file-system-legacy.mock.ts, mobile/test/expo-image-manipulator.mock.ts, mobile/test/expo-image.mock.ts, mobile/jest.unit.config.js</files>
  <read_first>
    - mobile/test/expo-haptics.mock.ts
    - mobile/test/expo-sqlite.mock.ts
    - mobile/jest.unit.config.js
    - node_modules/expo-file-system/build/legacy/FileSystem.d.ts and FileSystem.types.d.ts (signatures to mirror)
    - node_modules/expo-image-manipulator/build/index.d.ts, ImageManipulatorContext.d.ts, ImageRef.d.ts
  </read_first>
  <action>
    Create four mocks under `mobile/test/`, each with a short header comment in the style of expo-sqlite.mock.ts, using `jest.fn` so tests can override behaviour:
    (a) `expo-crypto.mock.ts`: `randomUUID` is a jest.fn whose default implementation returns `randomUUID()` from `node:crypto` (ES import, not require), so multi-insert tests never collide on a PRIMARY KEY.
    (b) `expo-file-system-legacy.mock.ts`: an in-memory file system keyed by URI (Map of uri to `{ size: number }`, plus a Set of directories). Exports `documentDirectory = "file:///mock/documents/"`, `cacheDirectory = "file:///mock/cache/"`, enum `FileSystemUploadType { BINARY_CONTENT = 0, MULTIPART = 1 }`, and jest.fn implementations of `getInfoAsync` (returns `{ exists: false, isDirectory: false, uri }` or `{ exists: true, isDirectory, size, uri, modificationTime: 0 }`), `copyAsync` (throws when the source is missing, copies the size), `moveAsync`, `makeDirectoryAsync`, `deleteAsync` (honours `idempotent`, deletes every file under a directory URI), `readDirectoryAsync`, `downloadAsync` (default: creates the destination file with size 1234 and resolves `{ status: 200, uri, headers: {}, mimeType: "image/jpeg" }`), `uploadAsync` (default resolves `{ status: 200, body: "", headers: {} }`), and `createUploadTask` returning an object with jest.fn `uploadAsync` (default resolves `{ status: 200, body: "", headers: {} }`) and `cancelAsync`. Export test helpers `__resetMockFileSystem()` (clears state and resets the jest.fn implementations to defaults), `__setMockFile(uri, size)`, `__getMockFile(uri)` and `__listMockFiles()`.
    (c) `expo-image-manipulator.mock.ts`: `SaveFormat` enum with the real string values, and `ImageManipulator.manipulate` as a jest.fn returning a chainable context whose `resize`/`rotate` are jest.fns returning the context and whose `renderAsync` resolves an ImageRef `{ width, height, saveAsync }`. Default dimensions 4032x3024 (overridable through an exported `__setMockImageSize(width, height)`); after `resize({ width })` or `resize({ height })` the rendered ref reports proportionally scaled dimensions. `saveAsync` registers a new file `file:///mock/cache/manipulated-<n>.jpg` in the file-system mock (import `__setMockFile` from `./expo-file-system-legacy.mock`) with size 250000 and resolves `{ uri, width, height }`. Also export `manipulateAsync` as a jest.fn for completeness.
    (d) `expo-image.mock.ts`: `export function Image(): null { return null }` plus `ImageBackground` the same way.
    In `mobile/jest.unit.config.js` add moduleNameMapper entries `'^expo-crypto$'`, `'^expo-file-system/legacy$'`, `'^expo-image-manipulator$'`, `'^expo-image$'` pointing at those files (keep `^expo-image$` anchored so it does not capture `expo-image-picker`, which tests mock inline). Do not change `coverageThreshold`. Run `npx prettier --write` on the new files.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit && npm run typecheck && npm run format:check && grep -c "expo-file-system/legacy\|expo-image-manipulator\|expo-crypto\|'\^expo-image\$'" mobile/jest.unit.config.js</automated>
  </verify>
  <acceptance_criteria>
    - The grep count is at least 4 and the existing mobile suite still passes (no mapper regression; `expo-image-picker` inline mocks still apply)
    - `git diff mobile/jest.unit.config.js` shows no change inside `coverageThreshold`
    - typecheck and format:check pass (mock files included)
  </acceptance_criteria>
  <done>Every later plan can import the four modules in src/ and get working, controllable doubles in Jest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: attachment-files.ts, the single owner of photo paths</name>
  <files>mobile/src/storage/attachment-files.ts, mobile/src/storage/attachment-files.test.ts</files>
  <read_first>
    - mobile/src/storage/local-owner.ts (small storage module shape)
    - mobile/src/storage/sync.ts (lines 134-173: current upload helpers that read local_uri)
    - mobile/test/expo-file-system-legacy.mock.ts (created in Task 2)
  </read_first>
  <behavior>
    - getAttachmentsDirUri() returns "file:///mock/documents/attachments/"; buildAttachmentFileUri("a.jpg") returns that dir plus "a.jpg"
    - ensureAttachmentsDir() creates the directory once and is idempotent (second call does not throw)
    - resolveAttachmentUri(uri) returns the uri unchanged when it already starts with the current attachments dir or is not an attachments-dir path (e.g. an ImagePicker cache uri); an old-container uri such as "file:///var/mobile/Containers/Data/Application/OLD-UUID/Documents/attachments/x.jpg" resolves to "file:///mock/documents/attachments/x.jpg"; "" resolves to ""
    - isInAttachmentsDir is true for current and old-container attachments paths, false for cache paths
    - getLocalFileSize returns the size for an existing file, null for a missing one or an empty uri; localFileExists mirrors it
    - deleteAttachmentFile deletes a file inside the attachments dir, is a no-op for a missing file, and refuses (no delete call) for a uri outside the attachments dir
    - deleteAllAttachmentFiles removes every file under the attachments dir and leaves cache files alone
    - extensionForMimeType: image/jpeg -> jpg, image/png -> png, image/heic -> heic, image/webp -> webp, unknown -> jpg
  </behavior>
  <action>
    Write the tests first (reset the fs mock in beforeEach via `__resetMockFileSystem` imported from `../../test/expo-file-system-legacy.mock`), then implement. Per D-12 import `* as FileSystem from "expo-file-system/legacy"` only (never the top-level "expo-file-system", Pitfall 2). Export `ATTACHMENTS_SUBDIR = "attachments/"`. `getAttachmentsDirUri()` throws a clear Error when `FileSystem.documentDirectory` is null. `resolveAttachmentUri` re-bases on the current document directory when the uri contains `/attachments/` followed by a single file name and does not already start with the current dir; this protects durable photos against the iOS container path change after app updates (D-09 "stored durably"). `getLocalFileSize` uses `getInfoAsync(uri, { size: true })` on the resolved uri and swallows errors to null. `deleteAttachmentFile` resolves the uri, returns early unless `isInAttachmentsDir`, then `deleteAsync(uri, { idempotent: true })`. `deleteAllAttachmentFiles` calls `deleteAsync(getAttachmentsDirUri(), { idempotent: true })`. No database access in this module (later plans own the SQL). Explicit return types, double quotes, no semicolons, no `any`.
  </action>
  <verify>
    <automated>cd /home/user/cortege/mobile && npx jest --config jest.unit.config.js src/storage/attachment-files.test.ts && cd .. && npm run lint && npm run typecheck && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - Every behaviour above is an `it(` case and passes
    - `grep -c "from \"expo-file-system\"" mobile/src/storage/attachment-files.ts` returns 0 and `grep -c "expo-file-system/legacy" mobile/src/storage/attachment-files.ts` returns 1
    - lint, typecheck and format:check pass
  </acceptance_criteria>
  <done>Photo paths have one tested owner that later plans reuse for capture, upload, download and purge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> repo | Four new native packages enter the dependency graph |
| app -> device file system | Photo files are written, resolved and deleted by path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-SC | Tampering | npm installs (expo-crypto, expo-file-system, expo-image, expo-image-manipulator) | mitigate | First-party Expo packages verified by slopcheck [OK] in RESEARCH; installed only through `npx expo install` (SDK-pinned); lockfile committed; `npm audit --audit-level=high` run and recorded |
| T-01.5-01 | Tampering | deleteAttachmentFile / deleteAllAttachmentFiles | mitigate | Deletes refuse any uri outside documentDirectory/attachments/ (tested); no user-supplied path reaches deleteAsync |
| T-01.5-02 | Denial of Service | Build pipeline (expo-doctor / expo export) | mitigate | Both run locally in Task 1 with the CI flags before any later plan depends on the modules |
</threat_model>

<verification>
- deps recorded in package.json + lockfile; expo-doctor and expo export green; mocks wired; attachment-files tests green; lint/typecheck/unit/format green
</verification>

<success_criteria>
The phase's native modules are installed once, CI's mobile build checks pass with them, and photo path handling has a single tested module.
</success_criteria>

<output>
Create `.planning/phases/01.5-mobile-sync-engine-reliability/01.5-01-SUMMARY.md` when done
</output>
