---
phase: 01.5-mobile-sync-engine-reliability
plan: 05
type: execute
wave: 2
depends_on: [01.5-01, 01.5-03]
files_modified:
  - mobile/src/storage/surveys.ts
  - mobile/src/storage/surveys.transactions.sqlite.test.ts
  - mobile/src/storage.test.ts
autonomous: true
requirements: [REQ-AUD-local-storage, REQ-AUD-sync-engine]

must_haves:
  truths:
    - "createLocalDraft, queueLocalAttachment, queueDeleteAttachment, queueDeleteSurvey, updateLocalDraft, clearLocalIbpData, retrySurveyNow and discardSurveyLocalChanges each run their statements in one runInTransaction; an injected failure on any later statement leaves the database exactly as before the call (D-08, ROADMAP criterion 4)"
    - "New survey ids and new local attachment ids are v4 UUIDs from expo-crypto randomUUID; existing ids are untouched and still work (D-08)"
    - "Every INSERT INTO sync_queue in surveys.ts writes op_type (survey_upsert, survey_delete, attachment_upload, attachment_delete)"
    - "Deleting an attachment or a survey locally, and the explicit logout purge, delete the corresponding photo files from documentDirectory/attachments/ after the transaction commits, never before (D-09)"
  artifacts:
    - path: mobile/src/storage/surveys.ts
      provides: "Transactional local writers, UUID ids, op_type on inserts, post-commit file cleanup"
      contains: "runInTransaction"
    - path: mobile/src/storage/surveys.transactions.sqlite.test.ts
      provides: "Trigger-based fault injection proving rollback for every multi-statement writer, UUID format, op_type"
  key_links:
    - from: mobile/src/storage/surveys.ts
      to: mobile/src/storage/transaction.ts
      via: "runInTransaction(async (tx) => ...)"
      pattern: "runInTransaction\\("
    - from: mobile/src/storage/surveys.ts
      to: expo-crypto
      via: "randomUUID()"
      pattern: "randomUUID\\(\\)"
    - from: mobile/src/storage/surveys.ts
      to: mobile/src/storage/attachment-files.ts
      via: "deleteAttachmentFile / deleteAllAttachmentFiles after commit"
      pattern: "deleteA(ttachmentFile|llAttachmentFiles)"
---

<objective>
Make every multi-statement local write in surveys.ts atomic through the plan 03 helper, switch new ids to UUIDs, tag queue rows with their op_type at insert time, and remove photo files when their rows are deleted.

Purpose: REQ-AUD-local-storage (transactions, op_type) and the "new IDs are UUIDs" clause of REQ-AUD-sync-engine.
Output: refactored surveys.ts and a fault-injection test file.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-CONTEXT.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-PATTERNS.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-03-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 03 (mobile/src/storage/transaction.ts): `runInTransaction<T>(fn: (tx: TxHandle) => Promise<T>, parentTx?: TxHandle): Promise<T>` — module-level mutex, one BEGIN/COMMIT owner; code already inside a transaction passes `tx` (to helpers typed `DbExecutor`, or as `parentTx` to join); a call without the handle from inside a callback rejects with `Error("runInTransaction called reentrantly")` after 30 s instead of hanging, and a handle used after commit throws.
From plan 03 (mobile/src/storage/types.ts): `QueueOpType = "survey_upsert" | "survey_delete" | "survey_visibility" | "attachment_upload" | "attachment_delete" | "unknown"`; `LocalAttachment.file_state: "local" | "remote" | "missing"`; `sync_queue.op_type` column exists after `initLocalDb()`.
From plan 01 (mobile/src/storage/attachment-files.ts): `deleteAttachmentFile(uri): Promise<void>` (no-op outside documentDirectory/attachments/ or for a missing file), `deleteAllAttachmentFiles(): Promise<void>`. Jest maps `expo-crypto` to a node:crypto-backed `randomUUID` and `expo-file-system/legacy` to an in-memory double (helpers `__setMockFile`, `__getMockFile`, `__resetMockFileSystem` imported from `../../test/expo-file-system-legacy.mock` or `../test/...` from src/).

mobile/src/storage/surveys.ts writers (line numbers from 2026-09-25):
- `createLocalDraft` (:25-83): `const id = \`survey-${Date.now()}\`` (:28); INSERT local_surveys + INSERT sync_queue
- `queueLocalAttachment` (:85-146): `attachment-${Date.now()}-${random}` id (:97); SELECT survey, INSERT local_attachments, INSERT sync_queue
- `queueDeleteAttachment` (:148-215): SELECT attachment + queue rows, DELETE matching queue rows, INSERT attachment_delete row, DELETE local_attachments
- `queueDeleteSurvey` (:218-252): DELETE queue rows, INSERT survey_delete row, DELETE local_attachments, DELETE local_surveys
- `updateLocalDraft` (:289-390): `deleteQueuedSurveyUpserts(db, id)` + INSERT sync_queue + UPDATE local_surveys
- `clearLocalIbpData` (:432-442): 5 DELETEs (queue, attachments, surveys, downsync_cursor, owner keys)
- `retrySurveyNow` (:457-500), `discardSurveyLocalChanges` (:502-534): several UPDATE/DELETE
- `deleteQueuedSurveyUpserts(db, surveyId)` in utils.ts:288-305 already takes the handle: pass `tx`.
Callers of `clearLocalIbpData`: useSurveySync.ts:93 (purgeWhileSyncSuspended) and :153 (logout) — signature unchanged.

Existing assertion to update: src/storage.test.ts:518 `expect(result.id).toMatch(/^attachment-/)`. storage.test.ts header/helpers :1-134 (`insertSurveyRow`, `insertQueueRow`, `selectQueueRowsForSurvey`). Fault injection in node:sqlite: a test-local `CREATE TRIGGER ... BEFORE INSERT ON sync_queue WHEN NEW.survey_id = '<id>' BEGIN SELECT RAISE(ABORT, 'injected'); END` (drop it in afterEach), the mobile analog of phase 01.4's event-insert trigger.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fault-injection tests for every multi-statement writer, UUIDs and op_type</name>
  <files>mobile/src/storage/surveys.transactions.sqlite.test.ts, mobile/src/storage.test.ts</files>
  <read_first>
    - mobile/src/storage/surveys.ts (whole file)
    - mobile/src/storage.test.ts (:1-134 setup/helpers, :500-530)
    - mobile/src/storage/transaction.ts and mobile/src/storage/transaction.sqlite.test.ts (plan 03)
    - mobile/test/expo-file-system-legacy.mock.ts (plan 01 helpers)
  </read_first>
  <behavior>
    - createLocalDraft with a trigger aborting the sync_queue INSERT: rejects, no local_surveys row exists
    - queueLocalAttachment with the same trigger: rejects, no local_attachments row exists
    - updateLocalDraft on a survey with one queued upsert, trigger aborting the new queue INSERT: rejects, the old queued upsert row still exists and local_surveys.payload_json/updated_at are unchanged
    - queueDeleteAttachment with a trigger aborting DELETE FROM local_attachments (BEFORE DELETE trigger): rejects, the attachment_upload queue row and the attachment row still exist
    - queueDeleteSurvey with a trigger aborting DELETE FROM local_surveys: rejects, queue rows and attachments of that survey are unchanged
    - discardSurveyLocalChanges and retrySurveyNow: with a trigger aborting their last statement, nothing they touched changed
    - clearLocalIbpData with a trigger aborting DELETE FROM local_surveys: rejects and all tables keep their rows
    - createLocalDraft().id and queueLocalAttachment().id match /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/
    - queue rows created by createLocalDraft, updateLocalDraft, queueLocalAttachment, queueDeleteAttachment (remote attachment) and queueDeleteSurvey have op_type survey_upsert, survey_upsert, attachment_upload, attachment_delete, survey_delete
    - an existing survey with a legacy id "survey-1700000000000" can still be updated and deleted
    - reentrancy: every writer in surveys.ts completes under `jest.useFakeTimers()` without advancing timers (proves no writer waits on its own mutex); run them sequentially in one test and assert each resolves
    - file cleanup: after queueDeleteAttachment of an attachment whose local_uri is a seeded file under file:///mock/documents/attachments/, the file is gone; after a rejected queueDeleteAttachment (trigger) the file still exists; after clearLocalIbpData the attachments directory is empty
  </behavior>
  <action>
    Create surveys.transactions.sqlite.test.ts with the global expo-sqlite mock (`initLocalDb()` in beforeAll, clean tables and `__resetMockFileSystem()` in beforeEach, drop test triggers in afterEach). Write every behaviour above; they fail against the current surveys.ts (no transaction, timestamp ids, no op_type, no file cleanup). In src/storage.test.ts change the :518 assertion to the UUID regex and keep every other assertion; if other storage.test.ts assertions depend on the `survey-`/`attachment-` prefixes, switch them to the UUID regex too. Commit the red tests (RED).
  </action>
  <verify>
    <automated>cd /home/user/cortege/mobile && npx jest --config jest.unit.config.js src/storage/surveys.transactions.sqlite.test.ts 2>&1 | grep -E "Tests:.*failed" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The new file has at least 11 `it(` cases and reports failures against the unchanged surveys.ts
    - `grep -c "RAISE(ABORT" mobile/src/storage/surveys.transactions.sqlite.test.ts` returns at least 5
  </acceptance_criteria>
  <done>Atomicity, id format, op_type and file cleanup are specified as executable tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap surveys.ts writers in runInTransaction, UUID ids, op_type, post-commit file cleanup</name>
  <files>mobile/src/storage/surveys.ts</files>
  <read_first>
    - mobile/src/storage/surveys.ts (whole file)
    - mobile/src/storage/utils.ts (deleteQueuedSurveyUpserts, deriveQueueOpType)
    - mobile/src/storage/attachment-files.ts (plan 01)
    - mobile/src/storage/surveys.transactions.sqlite.test.ts (Task 1)
  </read_first>
  <action>
    Per D-08/D-17, wrap the full read-check-write body of each listed writer in `runInTransaction(async (tx) => { ... })`, replacing `db` with `tx` inside and passing `tx` to `deleteQueuedSurveyUpserts`; keep return values and thrown messages identical. Inside a callback never call another exported writer without the handle: pass `tx` to helpers (typed `DbExecutor`) or as `parentTx`. This is enforced at runtime by plan 03's guard (reentrant call without the handle rejects, dead handle throws), so no manual grep audit is needed; Task 1 adds one test that proves it for this file (see behaviour "reentrancy"). Replace the id generators with `randomUUID()` from "expo-crypto" (survey id without prefix; local attachment id without prefix); do not rewrite existing ids anywhere.
    Add `op_type` to every `INSERT INTO sync_queue` column list in surveys.ts with the matching literal (survey_upsert, survey_delete, attachment_upload, attachment_delete). Photo file cleanup (D-09): in `queueDeleteAttachment`, `queueDeleteSurvey` and `discardSurveyLocalChanges` (only if it deletes attachment rows), select the `local_uri` values of the attachment rows about to be deleted inside the transaction, and after `runInTransaction` resolves call `deleteAttachmentFile` for each (best-effort, errors ignored; attachment-files refuses paths outside documentDirectory/attachments/). In `clearLocalIbpData`, after the transaction commits, call `deleteAllAttachmentFiles()` (best-effort). A rejected transaction must never delete a file.
    Run Task 1's file to GREEN, then the whole mobile suite (hook tests mock `../storage/surveys` and are unaffected).
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && test "$(grep -c "runInTransaction(" mobile/src/storage/surveys.ts)" -ge 8 && test "$(grep -c "Date.now()" mobile/src/storage/surveys.ts)" -eq 0 && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "runInTransaction(" mobile/src/storage/surveys.ts` returns at least 8
    - `grep -c "Date.now()" mobile/src/storage/surveys.ts` returns 0 and `grep -c "randomUUID()" mobile/src/storage/surveys.ts` returns 2
    - `grep -c "INSERT INTO sync_queue (survey_id, op_type" mobile/src/storage/surveys.ts` equals `grep -c "INSERT INTO sync_queue" mobile/src/storage/surveys.ts`
    - surveys.transactions.sqlite.test.ts fully green; full mobile suite and coverage thresholds green
  </acceptance_criteria>
  <done>No local write in surveys.ts can leave a half-applied state, new rows get UUIDs and an explicit op_type, and deleted photos free their files only after the delete is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app process -> SQLite | A crash or error can interrupt a multi-statement write |
| SQLite rows -> device files | Row deletion drives file deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-15 | Tampering | Partial writes (e.g. survey without its queue row) | mitigate | Every multi-statement writer in one runInTransaction; trigger-based tests prove rollback per writer |
| T-01.5-16 | Tampering | Id collisions between rapid inserts / devices | mitigate | expo-crypto randomUUID (platform CSPRNG) for new ids; legacy ids kept and tested |
| T-01.5-17 | Denial of Service (data loss) | File deleted but row kept after a failed delete | mitigate | File deletion only after the transaction commits; test asserts the file survives a rejected delete |
</threat_model>

<verification>
- Fault-injection file green; storage.test.ts green; full mobile suite and coverage thresholds green; lint/typecheck/format green
</verification>

<success_criteria>
Multi-statement local writes are transactional, new ids are UUIDs, queue rows carry their operation type from insertion, and photo files follow their rows.
</success_criteria>

<output>
Create `.planning/phases/01.5-mobile-sync-engine-reliability/01.5-05-SUMMARY.md` when done
</output>
