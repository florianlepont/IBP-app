---
phase: 01.5-mobile-sync-engine-reliability
plan: 09
type: execute
wave: 3
depends_on: [01.5-01, 01.5-05, 01.5-06]
files_modified:
  - mobile/src/screens/survey-screen-helpers.ts
  - mobile/src/screens/survey-screen-helpers.test.ts
  - mobile/src/screens/SurveyDetailScreen.tsx
  - mobile/src/screens/SurveyListScreen.tsx
  - mobile/src/app/AuthenticatedAppNavigation.tsx
autonomous: true
requirements: [REQ-AUD-photos]

must_haves:
  truths:
    - "Survey list thumbnails, the detail hero, the detail carousel and the debug previews render photos through expo-image (contentFit cover, recyclingKey = attachment id) from the resized local file, never through react-native Image (D-11, ROADMAP criterion 6)"
    - "Photo attachments pulled from the server are no longer hidden: while file_state is 'remote' they show a loading placeholder and the screen asks for their download; once cached they display (D-11)"
    - "An attachment whose file is missing shows a visible message 'Photo introuvable sur cet appareil' (with the option to delete it that already exists) instead of disappearing (D-10, ROADMAP criterion 5)"
    - "The detail screen requests previews for its attachments on open and when the list changes; the list screen requests them for each survey's first photo"
    - "In dev builds only (shouldShowDevTools), the detail debug tab offers 'Simuler un fichier manquant' per attachment so the owner can reproduce the missing-file state on the device"
    - "Logos and illustrations loaded with require(...) stay on react-native Image"
  artifacts:
    - path: mobile/src/screens/survey-screen-helpers.ts
      provides: "resolveAttachmentPreview, isPhotoAttachment, MISSING_PHOTO_MESSAGE, LOADING_PHOTO_MESSAGE"
      contains: "resolveAttachmentPreview"
    - path: mobile/src/screens/SurveyDetailScreen.tsx
      provides: "expo-image carousel/hero/debug previews with loading and missing states; ensure-previews effect; dev-only simulate button"
      contains: "expo-image"
    - path: mobile/src/screens/SurveyListScreen.tsx
      provides: "expo-image thumbnails with states; ensure-previews for first photos"
      contains: "expo-image"
    - path: mobile/src/app/AuthenticatedAppNavigation.tsx
      provides: "passes surveySync.handleEnsureAttachmentPreviews and (dev only) handleSimulateMissingAttachmentFile"
  key_links:
    - from: mobile/src/app/AuthenticatedAppNavigation.tsx
      to: mobile/src/hooks/useSurveySync.ts
      via: "onEnsureAttachmentPreviews={surveySync.handleEnsureAttachmentPreviews}"
      pattern: "handleEnsureAttachmentPreviews"
    - from: mobile/src/screens/SurveyDetailScreen.tsx
      to: mobile/src/screens/survey-screen-helpers.ts
      via: "resolveAttachmentPreview(attachment)"
      pattern: "resolveAttachmentPreview\\("
---

<objective>
Show every photo the user owns, wherever it came from: render thumbnails and the detail carousel through expo-image from the downsized local files, trigger on-demand download of server photos, and tell the user when a photo file is missing instead of hiding it (D-10, D-11).

Purpose: REQ-AUD-photos, ROADMAP criterion 6 (display) and the user-visible half of criterion 5.
Output: a pure, tested preview-state helper and the screen/navigation wiring.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-CONTEXT.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-PATTERNS.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-06-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 03/05: `LocalAttachment` has `file_state: "local" | "remote" | "missing"` and `listLocalAttachments` selects it. From plan 01: `resolveAttachmentUri(uri)` in mobile/src/storage/attachment-files.ts (re-bases old-container paths). From plan 06: `useSurveySync()` returns `handleEnsureAttachmentPreviews(attachments: LocalAttachment[]): Promise<void>` and `handleSimulateMissingAttachmentFile(localAttachmentId: string): Promise<void>`; missing rows carry `last_sync_error_code = "local_file_missing"`.

expo-image (SDK 57): `import { Image } from "expo-image"`; props `source={{ uri }}`, `style`, `contentFit="cover"` (not resizeMode), `cachePolicy` ("none" | "disk" | "memory" | "memory-disk"), `recyclingKey`, `allowDownscaling` (default true: decodes at the rendered size, which is what "downsized source" needs together with the 2048 px files from plan 07), `transition`. Jest maps "expo-image" to a null component (plan 01).

Render sites (2026-09-25):
- mobile/src/screens/SurveyDetailScreen.tsx: RN `Image` imported from "react-native" (:2-12); props type (:65-89) incl. `selectedSurveyAttachments: LocalAttachment[]`, `onDeleteAttachment`; `photoAttachments = selectedSurveyAttachments.filter(a => Boolean(a.local_uri?.trim()))` (:176-178) hides remote rows; carousel `<Image source={{ uri: slide.attachment.local_uri ?? undefined }} style={styles.detailHeroPhotoImage} resizeMode="cover" />` (:762-766); hero (:814-816); debug tab (surveyDetailTab === "debug", :1176+) with per-attachment previews and a "No local preview" placeholder (:1206-1240) to reuse for loading/missing.
- mobile/src/screens/SurveyListScreen.tsx: `attachmentsBySurvey: Record<string, LocalAttachment[]>` prop; `firstAttachmentWithPreview = attachments.find(a => Boolean(a.local_uri?.trim()))` (:994), `hasPhoto` (:1000), thumbnail `<Image source={{ uri: firstAttachmentWithPreview!.local_uri! }} style={styles.surveyCardPreview} />` (:1063-1066). Keep RN Image for the logo/illustrations at :577 and :1110 (require(...) assets).
- mobile/src/app/AuthenticatedAppNavigation.tsx: mounts SurveyListScreen (:383) and SurveyDetailScreen (:441) with `surveySync.*` handlers (e.g. `onDeleteAttachment={surveySync.handleDeleteAttachment}` :454).
- mobile/src/app/dev-tools.ts: `shouldShowDevTools(isDev = __DEV__)`.
Screens are not rendered in Jest (coverage ./src/screens/ floor 9/3/6/9); keep logic in survey-screen-helpers.ts, which is unit-tested (mobile/src/screens/survey-screen-helpers.test.ts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure preview-state helper with tests</name>
  <files>mobile/src/screens/survey-screen-helpers.ts, mobile/src/screens/survey-screen-helpers.test.ts</files>
  <read_first>
    - mobile/src/screens/survey-screen-helpers.ts and its test
    - mobile/src/storage/types.ts (LocalAttachment)
    - mobile/src/storage/attachment-files.ts (resolveAttachmentUri)
  </read_first>
  <behavior>
    - isPhotoAttachment: true for mime_type starting with "image/" (image/jpeg, image/png, image/heic), false for "application/pdf" and "application/octet-stream"
    - resolveAttachmentPreview({ file_state: "local", local_uri: "file:///mock/documents/attachments/a.jpg" }) → { kind: "image", uri: "file:///mock/documents/attachments/a.jpg" }
    - an old-container local uri → { kind: "image", uri: re-based uri }
    - file_state "local" with an empty local_uri → { kind: "loading", message: LOADING_PHOTO_MESSAGE }
    - file_state "remote" → { kind: "loading", message: LOADING_PHOTO_MESSAGE }
    - file_state "missing" → { kind: "missing", message: MISSING_PHOTO_MESSAGE }
    - selectPreviewCandidates(attachments) returns image attachments that are 'remote' or 'local' (for ensure calls), excluding 'missing'
  </behavior>
  <action>
    Add to survey-screen-helpers.ts: `MISSING_PHOTO_MESSAGE = "Photo introuvable sur cet appareil. Supprimez-la ou reprenez la photo."`, `LOADING_PHOTO_MESSAGE = "Photo en cours de chargement…"`, `isPhotoAttachment(a)`, `type AttachmentPreview = { kind: "image"; uri: string } | { kind: "loading"; message: string } | { kind: "missing"; message: string }`, `resolveAttachmentPreview(a: Pick<LocalAttachment, "file_state" | "local_uri">): AttachmentPreview` (uses resolveAttachmentUri) and `selectPreviewCandidates(attachments)`. French user-facing strings, consistent with the phase 01.2 screens. `isPhotoAttachment` is strictly `mime_type.startsWith("image/")`. Tests first.
  </action>
  <verify>
    <automated>cd /home/user/cortege/mobile && npx jest --config jest.unit.config.js src/screens/survey-screen-helpers.test.ts && cd .. && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All helper behaviours pass; `grep -c "export function resolveAttachmentPreview\|export const resolveAttachmentPreview" mobile/src/screens/survey-screen-helpers.ts` returns 1
  </acceptance_criteria>
  <done>Which photo state to show is decided by one tested pure function.</done>
</task>

<task type="auto">
  <name>Task 2: Screens render through expo-image with loading/missing states; navigation wiring</name>
  <files>mobile/src/screens/SurveyDetailScreen.tsx, mobile/src/screens/SurveyListScreen.tsx, mobile/src/app/AuthenticatedAppNavigation.tsx</files>
  <read_first>
    - mobile/src/screens/SurveyDetailScreen.tsx (:1-120, :160-260, :740-830, :1170-1245)
    - mobile/src/screens/SurveyListScreen.tsx (:1-120 props, :985-1075)
    - mobile/src/screens/SurveyDetailScreen.styles.ts (existing preview/placeholder styles)
    - mobile/src/app/AuthenticatedAppNavigation.tsx (:375-470)
    - mobile/src/app/dev-tools.ts
  </read_first>
  <action>
    SurveyDetailScreen (D-10, D-11): add optional props `onEnsureAttachmentPreviews?: (attachments: LocalAttachment[]) => Promise<void> | void` and `onSimulateMissingAttachmentFile?: (localAttachmentId: string) => Promise<void> | void`. Replace the `photoAttachments` filter with `selectedSurveyAttachments.filter(isPhotoAttachment)` so remote and missing photos stay visible. Add a `useEffect` keyed on the attachment ids + file_states that calls `onEnsureAttachmentPreviews?.(selectPreviewCandidates(selectedSurveyAttachments))`. For the carousel, hero and debug previews compute `resolveAttachmentPreview(attachment)`: kind "image" → `<Image>` from "expo-image" with `source={{ uri }}`, the existing style, `contentFit="cover"`, `cachePolicy="memory"` (local files need no disk cache), `recyclingKey={attachment.id}`; kind "loading" → the existing placeholder view with an ActivityIndicator and the message; kind "missing" → the placeholder with the message (and keep the existing delete action reachable for that attachment). Import expo-image's Image under a distinct name (e.g. `import { Image as ExpoImage } from "expo-image"`) if RN Image is still needed elsewhere in the file. In the debug tab, when `shouldShowDevTools()` and the prop is provided, add a small button "Simuler un fichier manquant" per attachment calling `onSimulateMissingAttachmentFile(attachment.id)`.
    SurveyListScreen: add optional prop `onEnsureAttachmentPreviews`; pick the first `isPhotoAttachment` per survey (not the first with a non-empty uri); render its thumbnail through expo-image when the preview kind is "image", a small neutral placeholder for "loading", and a small warning icon placeholder for "missing" (so the list also reveals a missing photo); in a `useEffect` over the visible surveys, call `onEnsureAttachmentPreviews` once with the first photo of each visible survey (candidates only). Leave the require(...) logo/illustration images on RN Image.
    AuthenticatedAppNavigation: pass `onEnsureAttachmentPreviews={surveySync.handleEnsureAttachmentPreviews}` to both screens and `onSimulateMissingAttachmentFile={shouldShowDevTools() ? surveySync.handleSimulateMissingAttachmentFile : undefined}` to the detail screen.
    Keep diffs focused; no layout redesign. Run the mobile coverage command: ./src/screens/ and ./src/app/ thresholds must still hold (add helper-level tests rather than lowering anything).
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && cd mobile && OUT=$(mktemp -d) && npx expo export --platform android --output-dir "$OUT" && rm -rf "$OUT"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from \"expo-image\"" mobile/src/screens/SurveyDetailScreen.tsx mobile/src/screens/SurveyListScreen.tsx` reports 1 per file
    - `grep -c "resizeMode=\"cover\"" mobile/src/screens/SurveyDetailScreen.tsx` returns 0 for photo sites (expo-image uses contentFit)
    - `grep -c "local_uri?.trim()" mobile/src/screens/SurveyDetailScreen.tsx mobile/src/screens/SurveyListScreen.tsx` returns 0 per file (remote rows no longer filtered out)
    - `grep -c "handleEnsureAttachmentPreviews" mobile/src/app/AuthenticatedAppNavigation.tsx` returns 2
    - Coverage thresholds hold; expo export succeeds
  </acceptance_criteria>
  <done>Every owned photo is visible (image, loading or missing message), rendered through expo-image from the downsized file, and server photos download when a screen needs them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local file URIs -> image renderer | Stored URIs are rendered by expo-image |
| dev build UI -> file deletion | A dev-only control deletes a local photo file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-32 | Tampering (data loss) | "Simuler un fichier manquant" reachable in release builds | mitigate | Prop passed only when shouldShowDevTools() (false in release, phase 01.2 D-11); helper deletes the file only, never the row |
| T-01.5-33 | Repudiation (silent loss) | Photos silently hidden | mitigate | Filter by image mime type, not by non-empty uri; missing state rendered with a message in list and detail |
| T-01.5-34 | Denial of Service | Decoding 2048 px images at full size in lists | mitigate | expo-image allowDownscaling (default) decodes at the rendered size; recyclingKey per attachment |
</threat_model>

<verification>
- helper tests green; mobile coverage thresholds hold; lint/typecheck/format green; expo export green
</verification>

<success_criteria>
Thumbnails and the detail carousel render through expo-image from downsized sources, server photos display once fetched, and a missing photo is shown to the user.
</success_criteria>

<output>
Create `.planning/phases/01.5-mobile-sync-engine-reliability/01.5-09-SUMMARY.md` when done
</output>
