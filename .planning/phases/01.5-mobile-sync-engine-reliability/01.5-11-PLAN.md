---
phase: 01.5-mobile-sync-engine-reliability
plan: 11
type: execute
wave: 5
depends_on: [01.5-06, 01.5-07, 01.5-08, 01.5-10]
files_modified:
  - mobile/src/storage/sync.ts
  - mobile/src/storage/sync.attachments.sqlite.test.ts
  - mobile/src/storage/utils.ts
autonomous: true
requirements: [REQ-AUD-photos, REQ-AUD-sync-engine]

must_haves:
  truths:
    - "Attachment uploads stream from the file through uploadAttachmentFile (createUploadTask, 120 s cancellation); the fetch(local_uri).blob() path and the FormData fetch are gone (D-09, D-15, ROADMAP criterion 5)"
    - "The API upload route gets MULTIPART with field 'file' and the bearer token; a presigned target gets BINARY_CONTENT with Content-Type and no bearer (D-15, C7)"
    - "When the local photo file is missing at upload time, the queue row is removed (fatal), the local_attachments row is kept with file_state 'missing', sync_state 'failed' and last_sync_error_code 'local_file_missing', and the survey is not blocked; nothing is deleted silently (D-10)"
    - "Upload network errors, upload timeouts, UPLOAD_HTTP 5xx and 429 never consume the retry cap; UPLOAD_HTTP 401/403 count; UPLOAD_HTTP 400/404/409/410/413/415/422 are fatal (D-14, REQ-AUD-photos)"
    - "Photo files are deleted after commit when the server confirms a survey deletion or a pull removes a survey or attachment"
  artifacts:
    - path: mobile/src/storage/sync.ts
      provides: "uploadAttachmentAndMarkSynced on uploadAttachmentFile; missing-file handling; post-commit file cleanup"
      contains: "uploadAttachmentFile"
    - path: mobile/src/storage/sync.attachments.sqlite.test.ts
      provides: "Real-SQL proofs for streaming targets, missing file, non-counted upload failures, file cleanup"
  key_links:
    - from: mobile/src/storage/sync.ts
      to: mobile/src/storage/attachments.ts
      via: "uploadAttachmentFile({ uploadTarget, isApiUploadTarget, localUri, mimeType, accessToken })"
      pattern: "uploadAttachmentFile\\("
    - from: mobile/src/storage/sync.ts
      to: mobile/src/storage/attachment-cache.ts
      via: "markAttachmentFileMissing(tx, id) in the same transaction as the fatal failure"
      pattern: "markAttachmentFileMissing\\("
---

<objective>
Plug the streaming upload (plan 07) and the missing-file state (plan 06) into the sync engine, so photos upload from disk with a timeout, transient upload failures never burn the retry cap, a lost file is surfaced instead of silently dropped, and deleted photos free their files.

Purpose: REQ-AUD-photos (ROADMAP criterion 5, upload and missing-file halves) and the photo clause of REQ-AUD-sync-engine's retry-cap rule.
Output: sync.ts upload path rewritten, real-SQL attachment sync tests.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-CONTEXT.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-PATTERNS.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-07-SUMMARY.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-10-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 07 (mobile/src/storage/attachments.ts): `uploadAttachmentFile({ uploadTarget, isApiUploadTarget, localUri, mimeType, accessToken }): Promise<{ status: number }>` — resolves the uri, throws `LocalFileMissingError` (code "local_file_missing") when the file is absent, `UploadTimeoutError` after `UPLOAD_TIMEOUT_MS` (120 s, task cancelled); MULTIPART/fieldName "file"/bearer for the API route, BINARY_CONTENT/Content-Type for presigned.
From plan 06 (mobile/src/storage/attachment-cache.ts): `markAttachmentFileMissing(tx, localAttachmentId)` sets file_state 'missing', last_sync_error_code 'local_file_missing', message, timestamps; `LOCAL_FILE_MISSING_CODE`.
From plan 08 (mobile/src/storage/utils.ts): `classifyUploadFailure(error)` → LocalFileMissingError fatal, UploadTimeoutError retryable, "UPLOAD_HTTP n"/"CONFIRM_HTTP n" and ApiError from the confirm call mapped per D-14; network TypeError retryable.
From plan 01 (attachment-files.ts): `deleteAttachmentFile(uri)` (no-op outside the attachments dir).
From plan 10 (sync.ts): `uploadAttachmentAndMarkSynced(db, row, payload, target, apiUrl, accessToken)` computes `uploadTarget = resolveUploadTarget(apiUrl, target.upload_url)`, `isApiUploadTarget` (includes "/surveys/", "/attachments/", "/upload?token="), calls `uploadFileViaApi` or `uploadFileDirect` (the two raw fetch helpers to delete), throws `UPLOAD_HTTP <status>` when not ok, then the confirm apiRequest and the transactional DB tail; failures reach `handleAttachmentSyncFailure(db, row, payload, message, { classification: classifyUploadFailure(error), errorCode })` from two places (after a synced attachment-create result, and for upload-only rows). `markSurveyDeleteRowSynced` and the pull's remote-delete branches delete local_attachments rows inside transactions.
Test doubles: fs mock `createUploadTask` (override `uploadAsync` per test to resolve `{ status }`, reject, or never settle), `__setMockFile` to create/remove photo files; global expo-sqlite mock with `initLocalDb()`; fetch mocked with `{ ok, status, text }` for /sync and /sync/changes, the response data for an attachment create carrying `attachment_id`, `storage_key`, `upload_url`, `confirm_url`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Real-SQL tests for the streaming upload path</name>
  <files>mobile/src/storage/sync.attachments.sqlite.test.ts</files>
  <read_first>
    - mobile/src/storage/sync.ts (upload path after plan 10)
    - mobile/src/storage/attachments.ts (plan 07) and mobile/src/storage/attachment-cache.ts (plan 06)
    - mobile/src/storage/sync.retry.sqlite.test.ts (plan 08 setup)
    - mobile/test/expo-file-system-legacy.mock.ts
  </read_first>
  <behavior>
    - API-route target ("/surveys/s/attachments/a/upload?token=t"): createUploadTask called with the absolute target, the file uri, MULTIPART, fieldName "file", bearer; on status 200 the queue row is deleted and the attachment is sync_state 'synced' with remote_attachment_id set
    - presigned target ("https://minio.example/bucket/key?X-Amz-Signature=..."): BINARY_CONTENT, Content-Type image/jpeg, no Authorization; then the confirm PUT goes through fetch with a signal (apiRequest)
    - global.fetch is never called with a "file://" uri (no blob loading)
    - file missing: queue row deleted, attachment row still present with file_state 'missing', sync_state 'failed', last_sync_error_code 'local_file_missing'; the survey's sync_blocked stays 0; createUploadTask never called; same for an upload-only row (remote id + upload_url already saved)
    - upload rejects with TypeError("Network request failed") 20 times (next_retry_at reset between runs): retry_count stays 0
    - upload never settles: after 120 s (fake timers) cancelAsync called, retry_count stays 0
    - UPLOAD_HTTP 503 and 429: retry_count stays 0
    - UPLOAD_HTTP 401 eight times: row deleted at the 8th, attachment failed with retry_cap_reached
    - UPLOAD_HTTP 404: fatal on the first attempt
    - server confirms a survey_delete: the photo files of that survey's attachments are deleted after commit; a pull deleting a remote attachment deletes its cached file
  </behavior>
  <action>
    Create the test file with the plan 08 setup plus the fs double (seed photo files with `__setMockFile("file:///mock/documents/attachments/<id>.jpg", 1000)`), queueing attachments through `createLocalDraft` + `queueLocalAttachment` with those uris. Use `jest.useFakeTimers()` only in the timeout case (advance by UPLOAD_TIMEOUT_MS). These tests are expected to fail on the fetch/blob path still present after plan 10 (RED).
  </action>
  <verify>
    <automated>cd /home/user/cortege/mobile && npx jest --config jest.unit.config.js src/storage/sync.attachments.sqlite.test.ts 2>&1 | grep -E "Tests:.*failed" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - At least 10 `it(` cases exist and some fail against the current upload path
  </acceptance_criteria>
  <done>The streaming, missing-file and retry rules for photos are executable specifications.</done>
</task>

<task type="auto">
  <name>Task 2: Stream uploads through uploadAttachmentFile, surface missing files, clean files after deletions</name>
  <files>mobile/src/storage/sync.ts, mobile/src/storage/utils.ts</files>
  <read_first>
    - mobile/src/storage/sync.ts (whole file)
    - mobile/src/storage/utils.ts (deriveAttachmentErrorCode)
    - mobile/src/storage/sync.attachments.sqlite.test.ts (Task 1)
  </read_first>
  <action>
    In `uploadAttachmentAndMarkSynced` replace the `uploadFileViaApi` / `uploadFileDirect` calls with `uploadAttachmentFile({ uploadTarget, isApiUploadTarget, localUri: payload.local_uri, mimeType: payload.mime_type, accessToken })` (D-15) and throw `new Error(\`UPLOAD_HTTP ${status}\`)` when the status is not 2xx. Delete `uploadFileViaApi` and `uploadFileDirect` (no raw fetch and no blob remain in sync.ts). In both failure sites (after a synced create result and for upload-only rows), when the classified error is a `LocalFileMissingError`, handle it as fatal with errorCode `local_file_missing` and, in the same `runInTransaction` as the failure handler's writes, call `markAttachmentFileMissing(tx, payload.local_attachment_id)` (D-10) — extend `handleAttachmentSyncFailure` with an optional `markFileMissing: true` flag rather than opening a second transaction. The local_attachments row is never deleted by this path and the survey is not blocked.
    File cleanup (D-09): in `markSurveyDeleteRowSynced` and in the pull's survey/attachment remote-delete branches, collect the deleted rows' `local_uri` values inside the transaction and call `deleteAttachmentFile` for each after it commits (best-effort, errors ignored).
    In utils.ts remove the `LOCAL_FILE_HTTP 404` branch of `deriveAttachmentErrorCode` (that message no longer exists) and map an error named LocalFileMissingError to `local_file_missing` if the derivation is reached without an explicit errorCode.
    Run Task 1's file to GREEN and the whole suite.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && test "$(grep -c "fetch(" mobile/src/storage/sync.ts)" -eq 0 && test "$(grep -c "blob()" mobile/src/storage/sync.ts)" -eq 0 && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fetch(" mobile/src/storage/sync.ts` returns 0 and `grep -c "blob()" mobile/src/storage/sync.ts` returns 0
    - `grep -c "uploadAttachmentFile(" mobile/src/storage/sync.ts` returns at least 1 and `grep -c "markAttachmentFileMissing(" mobile/src/storage/sync.ts` returns at least 1
    - `grep -c "LOCAL_FILE_HTTP" mobile/src/storage/sync.ts mobile/src/storage/utils.ts` returns 0 per file (the old silent-delete code path is gone)
    - All Task 1 cases pass; full mobile suite + coverage thresholds green
  </acceptance_criteria>
  <done>Photos upload by streaming with a timeout, transient failures never burn the cap, a missing file is recorded for the user, and deleted photos free their files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device file -> upload target | Photo bytes leave the device |
| server delete confirmations -> device files | Server results trigger local file deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-40 | Denial of Service | Memory spikes loading photos as blobs | mitigate | Blob path removed (grep gate); native streaming via createUploadTask |
| T-01.5-41 | Repudiation (silent loss) | Missing file deleting the queue and attachment silently | mitigate | Row kept as 'missing' with code local_file_missing, rendered by plan 09 |
| T-01.5-42 | Information Disclosure | Bearer token sent to object storage | mitigate | Presigned target uses BINARY_CONTENT without Authorization (tested) |
| T-01.5-43 | Tampering (data loss) | File deleted for a row that still exists | mitigate | File deletion only after the deleting transaction commits; attachment-files refuses paths outside the attachments dir |
</threat_model>

<verification>
- attachment sync tests green; full mobile suite + coverage thresholds; lint/typecheck/format green
</verification>

<success_criteria>
Photos are uploaded by streaming, network errors do not consume the retry cap, and a missing local file is shown to the user instead of being deleted silently.
</success_criteria>

<output>
Create `.planning/phases/01.5-mobile-sync-engine-reliability/01.5-11-SUMMARY.md` when done
</output>
