---
phase: 01.5-mobile-sync-engine-reliability
plan: 12
type: execute
wave: 6
depends_on: [01.5-01, 01.5-02, 01.5-03, 01.5-04, 01.5-05, 01.5-06, 01.5-07, 01.5-08, 01.5-09, 01.5-10, 01.5-11]
files_modified:
  - mobile/jest.unit.config.js
  - api/jest.unit.config.js
  - .planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md
autonomous: false
requirements: [REQ-AUD-sync-engine, REQ-AUD-local-storage, REQ-AUD-photos, REQ-AUD-offline-start]

must_haves:
  truths:
    - "The full local gate (lint, typecheck, unit with coverage for api and mobile, format, the full API E2E suite, expo-doctor 1.20.4 and expo export) is green on the integrated phase result"
    - "Coverage thresholds in mobile/jest.unit.config.js and api/jest.unit.config.js are raised to the new measured floors where they went up and no value is lowered (./src/storage/ and ./src/hooks/ expected to rise)"
    - "On the phase PR every CI job is green, including Mobile build check (expo-doctor + expo export), E2E tests - API (run twice) with attachments-download, and Dependency audit"
    - "01.5-VALIDATION.md records the PR URL, run URLs, job conclusions and the named tests proving each ROADMAP criterion"
    - "The owner merged the PR, rebuilt a fresh dev build with npx expo prebuild --clean over the existing app, and confirmed on one device: data from the old version kept, offline cold start, many queued operations syncing, photo capture/resize/upload, a server photo displayed after reinstall, and a missing local file shown (D-02)"
  artifacts:
    - path: .planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md
      provides: "Per-task statuses, CI evidence and the owner device check result"
      contains: "actions/runs/"
    - path: mobile/jest.unit.config.js
      provides: "Ratcheted mobile coverage thresholds"
    - path: api/jest.unit.config.js
      provides: "Ratcheted api coverage thresholds"
  key_links:
    - from: 01.5-VALIDATION.md
      to: "GitHub Actions run of the phase PR"
      via: "run URL, job table and test names recorded"
      pattern: "actions/runs/"
---

<objective>
Phase gate: run the complete local gate on the integrated result, ratchet coverage upward, open the phase PR and record its CI evidence, then have the owner merge, rebuild once on a fresh dev build and check the phone end to end (D-02).

Purpose: all four phase requirements; ROADMAP criteria 1-7 demonstrated on CI and on a device.
Output: ratcheted thresholds, 01.5-VALIDATION.md with CI and device evidence, merged PR.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-CONTEXT.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md
@CLAUDE.md

<interfaces>
Repository: github.com/florianlepont/cortege. Working branch: the phase branch checked out (e.g. claude/code-audit-complete-3sn99m); PR base: main. GitHub access: `gh` may be absent; use the GitHub MCP tools (pull requests, workflow runs, jobs, job logs). If runs cannot be read, surface an access gate and ask the owner for the run URL and job conclusions.

ci.yml jobs: "Detect changed paths", "Lint, format, typecheck", "Unit tests — API", "Unit tests — Mobile", "E2E tests — API" (steps "E2E run 1", "E2E run 2 (same database)"), "Mobile build check" (expo-doctor 1.20.4 with EXPO_DOCTOR_SKIP_DEPENDENCY_VERSION_CHECK=1, informational `expo install --check`, `expo export --platform android`), "Dependency audit" (`npm audit --audit-level=high`), "Docker image check", "CI OK", "Build & push Docker image" (main pushes only). CodeQL: "CodeQL analysis". This phase changes api/** and mobile/**, so no job should be path-skipped except "Build & push Docker image" on the PR.

Coverage ratchet (phase 01.3 convention): `npm --workspace <ws> run test:unit:coverage` writes <ws>/coverage/unit/coverage-summary.json; `node scripts/coverage-by-directory.js <ws>` prints floor-rounded per-directory values; thresholds may only go up. The 01.4-06 ratchet check (node one-liner over `git diff -U0`) is reused below for both config files.

E2E: PostgreSQL 16 runs in the sandbox; the orchestrator provides a dedicated *_test database and a CI-equivalent env file; `npm --workspace api run test:e2e` resets and migrates it on each run.

Deployment is pull-based: after a merge to main touching api/**, CI pushes the image and the VPS timer redeploys within about 5 minutes; `https://cortege.algernon.ovh/v1/health` returns 200. The new route answers 401 without a token once deployed (404 means the old image is still running).

Named tests per ROADMAP criterion (for the evidence table): 1 single flight → mobile/src/storage/sync-flight.test.ts, sync.engine.sqlite.test.ts; 2 batches → sync.engine.sqlite.test.ts "250"; 3 retry cap / timeouts / pull guard → sync.retry.sqlite.test.ts, sync-classification.test.ts, client.test.ts, sync.pull.sqlite.test.ts; 4 transactions / user_version / UUIDs → transaction.sqlite.test.ts, db.migration*.sqlite.test.ts, surveys.transactions.sqlite.test.ts; 5 photos → attachments.test.ts, attachments.sqlite.test.ts, sync.attachments.sqlite.test.ts; 6 server photos / expo-image → attachments-download.e2e-spec.ts, surveys-attachments-download.spec.ts, attachment-cache.sqlite.test.ts, useAttachmentPreviews.test.ts, survey-screen-helpers.test.ts; 7 offline start → useAuth0Session.test.ts, profile-cache.sqlite.test.ts; autosave → useEditingDraft.autosave.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full local gate, E2E, mobile build checks, coverage ratchet and validation map</name>
  <files>mobile/jest.unit.config.js, api/jest.unit.config.js, .planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md</files>
  <read_first>
    - mobile/jest.unit.config.js and api/jest.unit.config.js (coverageThreshold blocks and ratchet comments)
    - scripts/coverage-by-directory.js (usage)
    - .planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md
    - the SUMMARY files of plans 01-11 (deviations, anything left open)
  </read_first>
  <action>
    From the repo root run in order: `npm run lint`, `npm run typecheck`, `npm --workspace api run test:unit:coverage`, `npm --workspace mobile run test:unit:coverage`, `npm run format:check`, `npm --workspace api run test:e2e` twice in a row (same database, like CI), then from mobile/ `EXPO_DOCTOR_SKIP_DEPENDENCY_VERSION_CHECK=1 npx --yes expo-doctor@1.20.4` and `npx expo export --platform android --output-dir "$(mktemp -d)"`, and `npm audit --audit-level=high` at the root. Then `node scripts/coverage-by-directory.js mobile` and `node scripts/coverage-by-directory.js api`; for every directory/metric whose measured floor is higher than the configured value, raise the value to the measured floor; never lower one; update the date in each ratchet comment (mention phase 01.5). Re-run both coverage commands to prove the new thresholds pass. If any command fails, stop and report which plan's files are involved instead of patching silently. In 01.5-VALIDATION.md set each locally proven Per-Task row to ✅ green (or ❌ red with the failing command), leave CI-only rows ⬜ for Task 2 and the device row ⬜ for Task 3, and set `wave_0_complete: true` and `nyquist_compliant: true` once every task row has an automated command that ran.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm run lint && npm run typecheck && npm --workspace api run test:unit:coverage && npm --workspace mobile run test:unit:coverage && npm run format:check && npm --workspace api run test:e2e && for f in mobile/jest.unit.config.js api/jest.unit.config.js; do git diff -U0 "$f" | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const rm=d.split('\n').filter(l=>l.startsWith('-')&&!l.startsWith('---')&&/statements/.test(l));const add=d.split('\n').filter(l=>l.startsWith('+')&&!l.startsWith('+++')&&/statements/.test(l));const parse=l=>{const k=(l.match(/'([^']+)'|global/)||[''])[0];const v={};for(const m of l.matchAll(/(statements|branches|functions|lines): (\d+)/g))v[m[1]]=+m[2];return [k,v]};const old=Object.fromEntries(rm.map(parse));for(const l of add){const [k,v]=parse(l);const o=old[k];if(!o)continue;for(const x in v){if(v[x]<o[x]){console.error('lowered '+k+' '+x);process.exit(1)}}}console.log('ratchet ok')})" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Every gate command exits 0 (E2E twice, expo-doctor, expo export included) and both ratchet checks print `ratchet ok`
    - `git diff mobile/jest.unit.config.js` shows `./src/storage/` and `./src/hooks/` values at or above 56/35/68/58 and 82/65/80/83
    - `grep -c "wave_0_complete: true" .planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md` returns 1
  </acceptance_criteria>
  <done>The integrated phase passes every local gate, thresholds only went up, and the validation map reflects it.</done>
</task>

<task type="auto">
  <name>Task 2: Open the phase PR and record its CI evidence</name>
  <files>.planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md</files>
  <read_first>
    - .github/workflows/ci.yml (job and step names)
    - .planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md
  </read_first>
  <action>
    Push the phase branch and open (or reuse) a PR against main titled "Phase 01.5: Mobile sync engine reliability". Body: summary of plans 01-11; the behaviour changes for the owner (four new native modules, so a fresh dev build is required after `npx expo prebuild --clean`; existing queue rows get a fresh 8 attempts, D-06; photos are resized to 2048 px JPEG 0.7 and stored in the app's documents; the new API routes download-url and content; offline start with the last known profile); and the attribution lines from the session's system reminder. Wait for the CI and CodeQL runs on the head commit (poll, at most 45 minutes). Verify: `success` for Detect changed paths, Lint/format/typecheck, Unit tests — API, Unit tests — Mobile, E2E tests — API (both runs, attachments-download passing — quote the log line), Mobile build check (expo-doctor and expo export steps green — quote them), Dependency audit, Docker image check, CI OK, CodeQL analysis; "Build & push Docker image" skipped. Check the unit job logs show no "coverage threshold ... not met". If a job fails, diagnose from the log, fix on the same branch in the owning plan's files, push, re-verify, and record the fix as a deviation. Record the PR URL, run URLs, the job table, the quoted lines and the per-criterion test table (interfaces) in a "CI Evidence" section of 01.5-VALIDATION.md, and set CI-proven rows to ✅.
  </action>
  <verify>
    <automated>grep -c "actions/runs/" /home/user/cortege/.planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 01.5-VALIDATION.md contains the PR URL, at least one `actions/runs/` URL and a job-conclusion table matching the list above
    - Quoted log lines show the attachments-download E2E spec passing in both runs and the expo-doctor / expo export steps passing
    - No Per-Task row is left ⬜ except the owner device-check row
  </acceptance_criteria>
  <done>The phase PR's CI proves the whole phase on real PostgreSQL and on the mobile build pipeline, with evidence recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner merge, fresh dev build and one device check</name>
  <files>.planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md (CI Evidence)
  </read_first>
  <action>Present the steps below with the PR URL and the CI run URL, wait for the resume signal, then record each step's result (pass / what was seen) in the "Manual-Only Verifications" table of 01.5-VALIDATION.md and set `status: complete` when all pass.</action>
  <what-built>Plans 01-11: one-at-a-time sync in batches of at most 100 with a working retry limit and timeouts; pulls that never overwrite unsynced work; a versioned local database that migrates existing data; UUIDs; photos resized to 2048 px JPEG, stored in the app's documents and uploaded by streaming; server photos downloadable through a new API route and shown with expo-image; missing photos shown with a message; the app opens offline with the last known profile. CI on the PR is green (Task 2).</what-built>
  <how-to-verify>
    Before you start: use the phone that already has the current Cortege app with your data. Do NOT delete the app before step 3: installing over it is what proves your existing surveys survive the update.
    1. With the app you have today: turn on airplane mode, create one survey and add one photo. Leave it unsynced. Note how many surveys you see in the list.
    2. Review the PR, then merge it into main. Wait about 5 minutes, then open https://cortege.algernon.ovh/v1/health in a browser: it must show a healthy response.
    3. Rebuild the app on your computer, phone plugged in by cable. In a terminal, one line at a time: `cd` into your cortege folder, then `git checkout main`, `git pull`, `npm install`, `cd mobile`, `npx expo prebuild --clean`, then `npx expo run:ios --device` for an iPhone or `npx expo run:android --device` for an Android phone. Your `mobile/.env` stays as it is (API URL https://cortege.algernon.ovh/v1). Pick your phone when asked. Keep the terminal open while testing.
    4. The app opens: all your previous surveys are still there (same count as step 1), including the unsynced survey and its photo.
    5. Offline start: keep airplane mode on, close the app completely (swipe it away), open it again. Expected: you land directly on your signed-in screens with your name, not on the login screen.
    6. Many changes offline: still in airplane mode, create 5 surveys and add 2 photos to each (camera). Each photo appears immediately in the survey. Open one survey, go to its "debug" tab: the photo size shown should be well under 2 MB and the photo is the right way up.
    7. Missing file: in the survey from step 6, debug tab, press "Simuler un fichier manquant" on one photo, then go back to the survey. Expected: that photo now shows "Photo introuvable sur cet appareil..." instead of vanishing.
    8. Sync: turn airplane mode off. Within a minute (or press the sync button), everything syncs: no error message, surveys show as synced; the photo from step 7 stays listed as missing (it could not be uploaded) while its survey syncs. Delete that missing photo from the survey.
    9. Server photos: make sure everything is synced (no pending item), then delete the app from the phone and install it again with the same command as in step 3 (`npx expo run:ios --device` or `npx expo run:android --device`). Log in. Open one of the surveys from step 6. Expected: its photos appear after a short loading placeholder (they are downloaded from the server).
  </how-to-verify>
  <resume-signal>Type "approved — merged, device check passed", or give the step number and what you saw</resume-signal>
  <verify>
    <automated>grep -c "Manual-Only Verifications" /home/user/cortege/.planning/phases/01.5-mobile-sync-engine-reliability/01.5-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The owner replied with approval, or each failing step is recorded in 01.5-VALIDATION.md with the observed behaviour
  </acceptance_criteria>
  <done>The phase is merged and proven on a real device built from a clean prebuild, with the owner's existing data intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> GitHub | The agent pushes a branch and opens a PR; merging stays with the owner |
| New app version -> owner's existing on-device data | The upgrade runs the migration on real field data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-44 | Elevation of Privilege | Agent merging to main | mitigate | Merge is an owner step in the blocking checkpoint; the agent never merges |
| T-01.5-45 | Repudiation | CI claims without evidence | mitigate | Run URLs, job conclusions and quoted log lines recorded in VALIDATION.md and the SUMMARY |
| T-01.5-46 | Tampering | Coverage thresholds lowered to pass | mitigate | Ratchet check over both config diffs fails on any lowered value |
| T-01.5-47 | Denial of Service (data loss) | Migration on the owner's real data | mitigate | Seeded-v0 migration tests (plan 03) pass first; the device steps install over the existing app and check the survey count and the unsynced survey before anything else; the reinstall step only after everything is synced |
</threat_model>

<verification>
- Local gate green; PR CI green with evidence; owner merged and confirmed the device check
</verification>

<success_criteria>
ROADMAP criteria 1-7 demonstrated by named tests on a real GitHub Actions run and by the owner on one device built from a clean prebuild, with existing local data preserved.
</success_criteria>

<output>
Create `.planning/phases/01.5-mobile-sync-engine-reliability/01.5-12-SUMMARY.md` when done
</output>
