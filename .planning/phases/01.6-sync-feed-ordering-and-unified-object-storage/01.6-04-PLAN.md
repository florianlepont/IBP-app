---
phase: 01.6-sync-feed-ordering-and-unified-object-storage
plan: 04
type: execute
wave: 2
depends_on: [01.6-02]
files_modified:
  - api/src/users/users.service.ts
  - api/src/users/users.controller.ts
  - api/src/users/users.module.ts
  - api/test/users.service.spec.ts
  - api/test/auth-profile.e2e-spec.ts
autonomous: true
requirements: [REQ-AUD-object-storage]

must_haves:
  truths:
    - "Profile pictures are written, read and deleted through StorageService, so in minio mode they live in the bucket and survive a container restart (D-05, D-06)"
    - "GET /v1/me/profile-picture still streams the bytes through the API with Bearer auth (200 + image/*), never a redirect (D-15)"
    - "A user whose stored object is missing gets profile_picture_url: null from GET /v1/me, and GET /v1/me/profile-picture answers 404 and clears the stale profile_picture_* columns (D-06, D-15)"
    - "An unsupported image type (image/gif) or a prototype-key MIME answers 400, never 500 (D-15, D-09)"
    - "No old picture is migrated; UsersService no longer owns an S3 client or builds paths itself (D-05, D-06)"
  artifacts:
    - path: api/src/users/users.service.ts
      provides: "Profile pictures via StorageService; stale-picture handling"
      contains: "this.storage.getObject("
    - path: api/test/auth-profile.e2e-spec.ts
      provides: "Round trip, missing-object and MIME cases in both storage modes"
  key_links:
    - from: api/src/users/users.module.ts
      to: api/src/storage/storage.module.ts
      via: "imports: [..., StorageModule]"
      pattern: "StorageModule"
    - from: api/src/users/users.service.ts
      to: api/src/storage/storage.service.ts
      via: "constructor injection, last parameter"
      pattern: "private readonly storage: StorageService"
---

<objective>
Move profile pictures onto the shared `StorageService`: uploads, reads, deletes and account-deletion cleanup all go through it, so in production (minio mode) pictures land in the bucket instead of the container's ephemeral disk. The picture route keeps streaming bytes through the API; a missing object shows as "no picture" instead of a broken image; unsupported types get a 400.

Purpose: REQ-AUD-object-storage, ROADMAP success criterion 3 (users half). Decisions D-05, D-06, D-09 (4xx path), D-15.
Output: rewired UsersService/controller/module, updated unit spec, extended auth-profile E2E.
Old pictures are not recovered (D-06, owner decision); no migration script.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-CONTEXT.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-PATTERNS.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-02-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 02 (api/src/storage/storage.service.ts, module api/src/storage/storage.module.ts exports StorageService):
- `readonly mode: "local" | "minio"`
- `buildProfilePictureKey(userId: string, mimeType: string): string` → `profiles/${userId}/avatar${ext}`; throws BadRequestException for unsafe ids or unsupported MIME
- `putObject(key, body: Buffer, contentType): Promise<void>`
- `headObject(key): Promise<{ contentLength: number } | null>`
- `getObject(key): Promise<Buffer | null>` (null when missing)
- `deleteObject(key): Promise<void>` (best-effort, never throws)
From api/src/common/file.utils.ts: `isAllowedMimeType(mime): boolean` (own-property check).

Current UsersService (api/src/users/users.service.ts): S3 config :48-51 and :57-77 (default bucket "ibp-surveys", drifted); `uploadProfilePicture` :125-189 (accepts any `image/*`, then `extensionFromMime` throws a plain Error for image/gif → 500; writes with `writeFile(storagePathForKey(key))` regardless of mode; old key removed with rm :153-157; DB update :159-178 unchanged); `getProfilePicture` :191-214 (readFile, 404 "Profile picture not found" / "Profile picture file not found"); `removeProfilePicture` :216-254; `deleteAccount` :335-343 (removes the picture and the user's attachment objects via `cleanupAttachmentStorage`; keep the A-M9 ordering comment :276-278: DB commit first, storage cleanup after); `storagePathForKey` :396-398 and `cleanupAttachmentStorage` :400-416 to delete; `getMe(userId)` :80-85 uses the sync `toMeResponse(row)` :418. Columns: `profile_picture_url`, `profile_picture_storage_key`, `profile_picture_mime_type`.

Controller (api/src/users/users.controller.ts :55-86): PUT uses `FileInterceptor("file", { storage: memoryStorage(), limits: { fileSize: 10 MB } })`; GET returns `new StreamableFile(picture.buffer)` with Content-Type and `Cache-Control: private, max-age=60`. Keep both as they are (installed apps load the picture with a Bearer header, so a presigned redirect would break them: PATTERNS C-3).

Unit spec (api/test/users.service.spec.ts :1-66): constructs `new UsersService(db as never, auth0Management as never)` and mocks fs/promises at :5. Append a mocked storage `{ mode: "local", buildProfilePictureKey: jest.fn(), putObject: jest.fn(), headObject: jest.fn(), getObject: jest.fn(), deleteObject: jest.fn() }` as the new last argument; drop the fs mock if nothing else needs it.

E2E (api/test/auth-profile.e2e-spec.ts :47-87): upload PNG stub → 200 with string profile_picture_url; GET /v1/me string url; GET picture 200 image/*; DELETE 204; GET 404. Must keep passing (D-15). MinIO-only assertions use `const itMinio = process.env.OBJECT_STORAGE_MODE === "minio" ? it : it.skip`; the StorageService instance is available as `moduleFixture.get(StorageService)`.

Local E2E: PostgreSQL 16 (`pg_ctlcluster 16 main start`, role ibp/ibp). This plan's database: prefix E2E commands with `POSTGRES_DB=ibp_p04_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p04`. MinIO is not available in the sandbox: minio-only cases are skipped locally and run in the CI MinIO job (plan 08). If PostgreSQL cannot start, stop and report.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UsersService, controller and module on StorageService</name>
  <files>api/src/users/users.service.ts, api/src/users/users.controller.ts, api/src/users/users.module.ts, api/test/users.service.spec.ts</files>
  <read_first>
    - api/src/users/users.service.ts (full file)
    - api/src/users/users.controller.ts (:55-86)
    - api/src/users/users.module.ts
    - api/src/storage/storage.service.ts (from plan 02)
    - api/test/users.service.spec.ts
  </read_first>
  <behavior>
    Unit cases in api/test/users.service.spec.ts (storage mocked):
    - upload with mimetype "image/gif" or "constructor" → BadRequestException, putObject not called
    - upload "image/png" → putObject called with the key from buildProfilePictureKey, the buffer and "image/png"; when the previous key differs, deleteObject is called with the previous key after the DB update
    - getProfilePicture with a stored key and getObject → Buffer: returns `{ buffer, mimeType }`
    - getProfilePicture with a stored key and getObject → null: throws NotFoundException and issues an UPDATE that sets the three profile_picture_* columns to NULL for that user and that key only
    - getMe with a stored key and headObject → null: `profile_picture_url === null`; with headObject → `{ contentLength: 9 }`: url unchanged; with no stored key: headObject not called
    - removeProfilePicture and deleteAccount call deleteObject for the picture key (deleteAccount also for each attachment key) only after the DB work succeeded (existing A-M9 order)
  </behavior>
  <action>
    Per D-05, inject `private readonly storage: StorageService` as the last constructor parameter of UsersService, delete its S3 fields/constructor block, `storagePathForKey`, `cleanupAttachmentStorage` and the unused S3/fs/path imports, and add `StorageModule` to `imports` in users.module.ts. Per D-15 and D-09, in `uploadProfilePicture` replace the `image/*` prefix check with `isAllowedMimeType` (400 "Unsupported profile picture type" otherwise), build the key with `storage.buildProfilePictureKey(user.id, mime)`, write with `storage.putObject`, and delete a different previous key with `storage.deleteObject` after the DB update. In `getProfilePicture` read with `storage.getObject`; on null, clear the stale columns with `UPDATE users SET profile_picture_url = NULL, profile_picture_storage_key = NULL, profile_picture_mime_type = NULL, updated_at = NOW() WHERE id = $1 AND profile_picture_storage_key = $2` and throw NotFoundException (D-06: no error surfaced to the user beyond the 404 the app already handles). In `getMe`, when a storage key is set, call `storage.headObject` and return `profile_picture_url: null` if it is missing (one HEAD per /me; do not clear columns there). `removeProfilePicture` and `deleteAccount` use `storage.deleteObject`, keeping the DB-first ordering and its comment. The controller keeps the StreamableFile passthrough unchanged; only adjust it if types require. No migration of existing files (D-06). Update the unit spec first, then implement.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- users.service && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "S3Client|writeFile|readFile|storagePathForKey|\"ibp-surveys\"" api/src/users/users.service.ts` returns 0
    - `grep -c "this.storage.getObject(" api/src/users/users.service.ts` returns 1 and `grep -c "this.storage.headObject(" api/src/users/users.service.ts` returns 1
    - `grep -c "StorageModule" api/src/users/users.module.ts` returns at least 1
    - `grep -c "new StreamableFile" api/src/users/users.controller.ts` returns 1
    - Every behaviour bullet is covered and `npm --workspace api run test:unit` passes
  </acceptance_criteria>
  <done>Profile pictures go through StorageService in both modes, missing objects read as "no picture", and bad types get a 400.</done>
</task>

<task type="auto">
  <name>Task 2: Profile-picture E2E in both storage modes</name>
  <files>api/test/auth-profile.e2e-spec.ts</files>
  <read_first>
    - api/test/auth-profile.e2e-spec.ts (:1-90)
    - api/src/storage/storage.service.ts (public contract)
  </read_first>
  <action>
    Per D-06, D-10 and D-15, keep the existing "uploads, serves and deletes profile picture" test unchanged and add, using `storage = moduleFixture.get(StorageService)`:
    (1) "rejects unsupported picture types with 400": PUT with contentType "image/gif" → 400; PUT with contentType "constructor" → 400 (never 500).
    (2) "a missing stored picture reads as no picture": upload a PNG, read `profile_picture_storage_key` from `users` for that email, `await storage.deleteObject(key)`; GET /v1/me → 200 with `profile_picture_url === null`; GET /v1/me/profile-picture → 404; the three profile_picture_* columns are now NULL in the database; GET /v1/me still null.
    (3) "replacing a picture removes the old object": upload PNG then JPEG; `storage.headObject(oldKey)` is null and `storage.headObject(newKey)` is not null.
    (4) `itMinio("stores the picture in the bucket, not on local disk")`: after upload, `storage.headObject(key)` returns contentLength equal to the uploaded byte count, and `fs.existsSync(path.join(process.env.ATTACHMENTS_UPLOAD_DIR ?? "/tmp/ibp-uploads", key))` is false.
    Use unique emails per test. Then run the full E2E suite on this plan's database.
  </action>
  <verify>
    <automated>cd /home/user/cortege && POSTGRES_DB=ibp_p04_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p04 npm --workspace api run test:e2e && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "itMinio(" api/test/auth-profile.e2e-spec.ts` returns at least 1
    - `grep -c "image/gif" api/test/auth-profile.e2e-spec.ts` returns at least 1
    - `grep -c "profile_picture_url).toBeNull()" api/test/auth-profile.e2e-spec.ts` returns at least 1
    - The full E2E suite passes on ibp_p04_test in local mode (the itMinio case reported as skipped)
  </acceptance_criteria>
  <done>The picture round trip, the stale-picture path and the MIME 400 are proven end to end; the minio assertion is ready for the CI MinIO job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile client → PUT /v1/me/profile-picture | Untrusted file bytes and MIME |
| API → object store | Pictures now live in the shared bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.6-13 | Tampering | Profile picture MIME | mitigate | isAllowedMimeType (own-property) before the key builder; 400 on anything else |
| T-01.6-14 | Information Disclosure | Picture access | mitigate | Bytes streamed only through the authenticated /me route for the caller's own key; no presigned URL is exposed |
| T-01.6-15 | Denial of Service | Stale key causing repeated 404/500 | mitigate | getObject null → 404 and conditional column clear (`AND profile_picture_storage_key = $2`, no race with a new upload); /me returns null url |
| T-01.6-16 | Denial of Service | Upload size | accept | Existing 10 MB multer limit unchanged |
</threat_model>

<verification>
- Unit and full E2E green on ibp_p04_test (local mode); `npm run lint && npm run typecheck && npm run format:check` green
</verification>

<success_criteria>
Profile pictures are stored through StorageService (object storage in production), served as bytes by the API, and a missing object shows as no picture.
</success_criteria>

<output>
Create `.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-04-SUMMARY.md` when done
</output>
