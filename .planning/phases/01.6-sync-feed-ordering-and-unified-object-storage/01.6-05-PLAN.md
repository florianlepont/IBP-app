---
phase: 01.6-sync-feed-ordering-and-unified-object-storage
plan: 05
type: execute
wave: 2
depends_on: [01.6-02]
files_modified:
  - api/src/common/safe-id.pipe.ts
  - api/test/safe-id.pipe.spec.ts
  - api/src/surveys/surveys.controller.ts
  - api/src/surveys/dtos/survey-upsert.dto.ts
  - api/src/surveys/dtos/sync-batch.dto.ts
  - api/src/surveys/dtos/sync-payloads.dto.ts
  - api/src/surveys/surveys.module.ts
  - api/test/safe-ids.e2e-spec.ts
autonomous: true
requirements: [REQ-AUD-object-storage]

must_haves:
  truths:
    - "Every :id and :attachmentId route parameter on the surveys controller is checked against ^[A-Za-z0-9_-]{1,128}$ and answers 400 when it does not match (D-07, D-14)"
    - "SurveyUpsertDto.id, the sync envelope survey_id, SurveyDeletePayloadDto.id and AttachmentDeletePayloadDto.attachment_id carry the same pattern, so /sync operations with unsafe ids fail alone with fatal_error 400 invalid_sync_operation (D-14)"
    - "Every id format the mobile app has ever generated (survey-<ms>, UUID) and the E2E fixture ids keep working: no existing id is rejected (D-14)"
    - "SurveysModule imports StorageModule so the surveys services can inject StorageService (D-05)"
  artifacts:
    - path: api/src/common/safe-id.pipe.ts
      provides: "SafeIdPipe (PipeTransform) using isSafeId"
      exports: ["SafeIdPipe"]
    - path: api/test/safe-ids.e2e-spec.ts
      provides: "Boundary proof on REST routes and /sync with the production pipe"
  key_links:
    - from: api/src/surveys/surveys.controller.ts
      to: api/src/common/safe-id.pipe.ts
      via: "@Param(\"id\", SafeIdPipe) / @Param(\"attachmentId\", SafeIdPipe)"
      pattern: "@Param\\(\"(id|attachmentId)\", SafeIdPipe\\)"
    - from: api/src/surveys/dtos/survey-upsert.dto.ts
      to: api/src/common/safe-id.ts
      via: "@Matches(SAFE_ID_PATTERN)"
      pattern: "@Matches\\(SAFE_ID_PATTERN"
---

<objective>
Validate every client-chosen id at the API boundary with the shared safe-id rule, so an unsafe id is refused with a 400 before it can reach a storage key (the key builder in StorageService checks again as the last line). Also wire `StorageModule` into `SurveysModule` for plans 06 and 07.

Purpose: REQ-AUD-object-storage, ROADMAP success criterion 4 (validated identifiers). Decisions D-07 and D-14 (D-14 replaces D-07's UUID pipe: ids stay client-chosen TEXT), D-05 (module wiring).
Output: SafeIdPipe, DTO patterns, controller params, module import, unit + E2E proof.
This plan does not touch surveys-sync.service.ts (plan 03), surveys.service.ts (plan 06) or surveys-attachments.service.ts (plan 07).
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-CONTEXT.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-PATTERNS.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-02-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 02 (api/src/common/safe-id.ts): `SAFE_ID_PATTERN = /^[A-Za-z0-9_-]{1,128}$/`, `isSafeId(value: unknown): value is string`. StorageModule (api/src/storage/storage.module.ts) exports StorageService.

Id formats (derived from mobile git history, D-14): survey ids `survey-${Date.now()}` (early builds) and `randomUUID()` (current); attachment ids server `randomUUID()`; E2E fixtures `e2e-survey-${Date.now()}` and similar. All match the pattern.

Controller params (api/src/surveys/surveys.controller.ts): `@Param("id") id: string` at lines ~59, 66, 75, 82, 88, 96, 103, 110, 119, 139, 152, 159 and `@Param("attachmentId") attachmentId: string` at ~111, 120, 140, 153. No ParseUUIDPipe exists in the repo (and D-14 rules it out). Nest param pipes throw BadRequestException → 400 through the default filter.

DTOs:
- api/src/surveys/dtos/survey-upsert.dto.ts: `@IsOptional() @IsString() id?: string` (one decorator per line; `Matches` already imported; `PARCEL_ID_PATTERN` used via `@Matches` at ~35-44 is the analog)
- api/src/surveys/dtos/sync-batch.dto.ts: SyncOperationEnvelopeDto `@IsOptional() @IsString() @MaxLength(128) survey_id?: string`
- api/src/surveys/dtos/sync-payloads.dto.ts: `SurveyDeletePayloadDto { @IsOptional() @IsString() id? }`, `AttachmentDeletePayloadDto { @IsString() @IsNotEmpty() attachment_id! }`
`previous_survey_id` is a reference, not a storage key: leave it unchanged.
/sync validates envelopes and payloads per operation with `validateSyncDto` (phase 01.4); a violation gives `{ status: "fatal_error", error: { code: "invalid_sync_operation", http_status: 400, details: { fields } } }` for that operation only.

Module (api/src/surveys/surveys.module.ts): `imports: [AuthModule]` → add `StorageModule`.

E2E analogs: api/test/sync-validation.e2e-spec.ts (configureApp production pipe, `/v1/sync` batches, result assertions); login `POST /v1/debug/test-token`.

Local E2E: PostgreSQL 16 (`pg_ctlcluster 16 main start`, role ibp/ibp). This plan's database: prefix E2E commands with `POSTGRES_DB=ibp_p05_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p05` and always run them through `flock /tmp/ibp-e2e.lock` (cluster-wide snapshot xmin: parallel E2E runs interfere). If PostgreSQL cannot start, stop and report.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SafeIdPipe, DTO patterns, controller params and StorageModule import</name>
  <files>api/src/common/safe-id.pipe.ts, api/test/safe-id.pipe.spec.ts, api/src/surveys/surveys.controller.ts, api/src/surveys/dtos/survey-upsert.dto.ts, api/src/surveys/dtos/sync-batch.dto.ts, api/src/surveys/dtos/sync-payloads.dto.ts, api/src/surveys/surveys.module.ts</files>
  <read_first>
    - api/src/common/safe-id.ts (plan 02)
    - api/src/surveys/surveys.controller.ts
    - api/src/surveys/dtos/survey-upsert.dto.ts, sync-batch.dto.ts, sync-payloads.dto.ts
    - api/src/surveys/surveys.module.ts
  </read_first>
  <behavior>
    api/test/safe-id.pipe.spec.ts:
    - `new SafeIdPipe().transform("survey-1712345678901", meta)` and a UUID → returned unchanged
    - `"../x"`, `"a/b"`, `"a.b"`, `""`, `"a".repeat(129)`, `undefined` → BadRequestException whose message is "Invalid identifier" (the raw value is not echoed)
  </behavior>
  <action>
    Per D-14 (which replaces D-07's UUID pipe; ids stay client-chosen TEXT), create api/src/common/safe-id.pipe.ts: `@Injectable() export class SafeIdPipe implements PipeTransform<unknown, string>` returning the value when `isSafeId(value)` is true and otherwise throwing `new BadRequestException("Invalid identifier")`. Apply it to every `@Param("id")` and `@Param("attachmentId")` in surveys.controller.ts as `@Param("id", SafeIdPipe)` / `@Param("attachmentId", SafeIdPipe)`. Add `@Matches(SAFE_ID_PATTERN)` after `@IsString()` on SurveyUpsertDto.id, SyncOperationEnvelopeDto.survey_id (keep @MaxLength(128)), SurveyDeletePayloadDto.id and AttachmentDeletePayloadDto.attachment_id. Leave previous_survey_id and every other field unchanged. Per D-05, add `StorageModule` to `imports` in surveys.module.ts (no provider changes). Comment the pipe with the D-14 derivation (formats ever generated by the app). Write the pipe spec first.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- safe-id.pipe && npm --workspace api run test:unit && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "@Param\(\"(id|attachmentId)\"\)" api/src/surveys/surveys.controller.ts` returns 0
    - `grep -cE "@Param\(\"(id|attachmentId)\", SafeIdPipe\)" api/src/surveys/surveys.controller.ts` returns at least 16
    - `grep -c "@Matches(SAFE_ID_PATTERN" api/src/surveys/dtos/survey-upsert.dto.ts api/src/surveys/dtos/sync-batch.dto.ts api/src/surveys/dtos/sync-payloads.dto.ts` totals at least 4
    - `grep -c "StorageModule" api/src/surveys/surveys.module.ts` returns at least 1
    - Pipe spec passes; `npm --workspace api run test:unit` passes
  </acceptance_criteria>
  <done>Unsafe ids are refused at every survey route and in every /sync payload that names a survey or attachment.</done>
</task>

<task type="auto">
  <name>Task 2: Boundary E2E and no-regression run of every fixture</name>
  <files>api/test/safe-ids.e2e-spec.ts</files>
  <read_first>
    - api/test/sync-validation.e2e-spec.ts (bootstrap with configureApp, /v1/sync assertions)
  </read_first>
  <action>
    Per D-14, create api/test/safe-ids.e2e-spec.ts with the production pipe (`configureApp(app)`). Cases: (1) `POST /v1/surveys` with a valid draft body and id `survey-${Date.now()}` → 201, and with `randomUUID()` → 201. (2) `POST /v1/surveys` with id "../../x", "a.b", "a".repeat(129) → 400 and no row in `surveys`. (3) `GET /v1/surveys/a.b` → 400; `GET /v1/surveys/%2E%2E%2Fx` → 400 or 404 but never 200/500; `GET /v1/surveys/<valid id>/attachments/a.b/download-url` → 400. (4) `/v1/sync` batch [attachment.create with survey_id "../x", survey.upsert with a valid legacy-format id, attachment.delete with attachment_id "../y"] → HTTP 200, results [fatal_error http_status 400 invalid_sync_operation, synced, fatal_error http_status 400]. Then run the whole E2E suite on this plan's database: every existing fixture id must still be accepted (if one fails, fix the pattern reasoning in the SUMMARY; do not loosen the pattern beyond D-14 without reporting).
  </action>
  <verify>
    <automated>cd /home/user/cortege && POSTGRES_DB=ibp_p05_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p05 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "configureApp(app)" api/test/safe-ids.e2e-spec.ts` returns 1
    - `grep -c "invalid_sync_operation" api/test/safe-ids.e2e-spec.ts` returns at least 1
    - The full E2E suite passes on ibp_p05_test
  </acceptance_criteria>
  <done>Unsafe ids get a 400 at the boundary and no existing id format is rejected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → survey routes and /v1/sync | Client-chosen survey ids and attachment ids cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.6-17 | Tampering | Route params :id/:attachmentId | mitigate | SafeIdPipe on every param of surveys.controller.ts |
| T-01.6-18 | Tampering | /sync envelope and payload ids (bypass route pipes, PATTERNS C-2) | mitigate | @Matches(SAFE_ID_PATTERN) on SurveyUpsertDto.id, envelope survey_id, delete payload ids; StorageService key builder re-checks |
| T-01.6-19 | Denial of Service | Installed apps rejected | mitigate | Pattern derived from every id the app has generated; full E2E suite with existing fixtures |
| T-01.6-20 | Information Disclosure | Error echo | mitigate | Pipe message is fixed ("Invalid identifier"), no value echoed |
</threat_model>

<verification>
- Unit and full E2E green on ibp_p05_test; `npm run lint && npm run typecheck && npm run format:check` green
</verification>

<success_criteria>
Storage-bound identifiers are validated at the boundary with the D-14 pattern; SurveysModule can inject StorageService.
</success_criteria>

<output>
Create `.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-05-SUMMARY.md` when done
</output>
