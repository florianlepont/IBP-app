---
phase: 01.6-sync-feed-ordering-and-unified-object-storage
plan: 06
type: execute
wave: 3
depends_on: [01.6-01, 01.6-03, 01.6-05]
files_modified:
  - api/src/surveys/surveys-normalize.utils.ts
  - api/test/surveys-normalize.utils.spec.ts
  - api/src/surveys/surveys.service.ts
  - api/test/surveys-same-version.e2e-spec.ts
autonomous: true
requirements: [REQ-AUD-changes-feed, REQ-AUD-object-storage]

must_haves:
  truths:
    - "An upsert carrying the sync_version the server already stored, with the same read-only fields and visibility, is still answered synced (idempotent replay) (D-04)"
    - "The same sync_version with a different site_name, factors, parcel ids, other read-only field or visibility is answered 409 sync_version_conflict on REST and fatal_error sync_version_conflict (http_status 409) in /v1/sync, at all three call sites (D-04, D-16)"
    - "The comparison is by value (JSONB key order and parcel-id order ignored, scores/status/expires_at excluded), computed on the fly with no stored column (D-04, D-16)"
    - "SurveysService no longer owns an S3 client; survey deletion cleans attachment objects through StorageService after commit, best-effort (D-05)"
  artifacts:
    - path: api/src/surveys/surveys-normalize.utils.ts
      provides: "hasSameVersionContentChanged(body, existing, existingParcelIds)"
      exports: ["hasSameVersionContentChanged"]
    - path: api/src/surveys/surveys.service.ts
      provides: "Same-version conflict at three sites; StorageService-based cleanup"
      contains: "Same sync_version with different content"
    - path: api/test/surveys-same-version.e2e-spec.ts
      provides: "REST + /sync proof of the same-version rule and delete cleanup"
  key_links:
    - from: api/src/surveys/surveys.service.ts
      to: api/src/surveys/surveys-normalize.utils.ts
      via: "hasSameVersionContentChanged at the three same-version sites"
      pattern: "hasSameVersionContentChanged\\("
    - from: api/src/surveys/surveys.service.ts
      to: api/src/storage/storage.service.ts
      via: "this.storage.deleteObject after the delete transaction commits"
      pattern: "this\\.storage\\.deleteObject\\("
---

<objective>
Turn same-`sync_version` writes with different content into a `sync_version_conflict` instead of a silent replay, by comparing the existing read-only fields plus visibility by value at the three call sites in `SurveysService`. In the same file (hotspot, one owner per wave), drop the service's own S3 client and route survey-deletion cleanup through `StorageService`.

Purpose: REQ-AUD-changes-feed (ROADMAP criterion 2, same-version half) and REQ-AUD-object-storage (criterion 3, surveys half). Decisions D-04, D-05, D-16.
Output: comparison helper with unit tests, rewired SurveysService, E2E proof.
This plan runs in parallel with plan 07, which owns surveys-attachments.service.ts; do not edit that file here.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-CONTEXT.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-PATTERNS.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Existing helper (api/src/surveys/surveys-normalize.utils.ts ~95-160): `getChangedSubmittedReadOnlyFields(body: SurveyUpsertBody, existing: SurveyRow, existingParcelIds: string[]): string[]` compares by value the fields site_name, parcel_id, parcel_ids, observation_year, version_number, previous_survey_id, region_version, vegetation_stage, factors; skips fields absent/undefined/null in body; uses `jsonDeepEqual` (key order ignored) and `parcelIdSetEqual`; excludes scores. Unit spec fixture: api/test/surveys-normalize.utils.spec.ts `makeRow(overrides)`.

Same-version sites in api/src/surveys/surveys.service.ts (upsertForUser, inside `db.transaction`, row locked `forUpdate: true` at ~218):
- Site 1 ~318: `if (syncVersion === existing.sync_version) { return { id, server_status: "synced", updated_at: existing.updated_at, warnings: draftValidation.warnings, factor_results: ... } }`; `existingParcelIds` is loaded at ~304.
- Site 2 ~368 (submitted path, guarded UPDATE matched 0 rows, `reRead`) and Site 3 ~467 (draft path re-read): `if (reRead.sync_version === syncVersion) return { ...synced }`. They need the parcel ids of `reRead` (`await this.getSurveyParcelIds(db, reRead.id)`).
Conflict shape to copy (~306-316): `throw new ConflictException({ code: "sync_version_conflict", message: "Older sync_version received", details: { survey_id: surveyId, server_sync_version: existing.sync_version, client_sync_version: syncVersion } })`. `mapSyncError` (api/src/surveys/sync-error.utils.ts) maps a 409 HttpException to fatal_error with code, details and http_status 409; the installed mobile app treats it as fatal and keeps its local data (checked, CONTEXT D-04).

S3 in SurveysService: fields and constructor block ~47-51, ~63-83; `cleanupAttachmentStorage` ~1558-1575; imports `DeleteObjectCommand, S3Client` (~8), `rm` (~10), `join` (~11). Delete path caller ~1026-1030 with the comment "D-07: object-storage cleanup is best-effort and runs only after commit". Constructor ~58-62: append `private readonly storage: StorageService` as the last parameter (SurveysModule already imports StorageModule, plan 05). No unit spec constructs SurveysService directly (grep `new SurveysService(` in api/test returns nothing).
StorageService (plan 02): `deleteObject(key): Promise<void>` best-effort, never throws; `headObject(key)`.

Known edge (record in the SUMMARY, documented by plan 08): if an upsert of version N reached the server but its response was lost, and the user then changes visibility before the retry, the mobile app rewrites the pending upsert's visibility without bumping sync_version, so the retry now gets 409 (D-16 includes visibility by decision). This is the intended "different content" signal; the app keeps its local data.

E2E analogs: api/test/surveys-idempotency.e2e-spec.ts :48-92 (same-version replay, keep green), :1402-1466 (/sync conflict result shape); local-mode attachment upload in api/test/attachments-reports-transactions.e2e-spec.ts ~165-193.

Local E2E: PostgreSQL 16 (`pg_ctlcluster 16 main start`, role ibp/ibp). This plan's database: prefix E2E commands with `POSTGRES_DB=ibp_p06_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p06`. If PostgreSQL cannot start, stop and report.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Same-version content comparison helper</name>
  <files>api/src/surveys/surveys-normalize.utils.ts, api/test/surveys-normalize.utils.spec.ts</files>
  <read_first>
    - api/src/surveys/surveys-normalize.utils.ts (getChangedSubmittedReadOnlyFields, jsonDeepEqual, parcelIdSetEqual)
    - api/test/surveys-normalize.utils.spec.ts (makeRow)
  </read_first>
  <behavior>
    `hasSameVersionContentChanged(body, existing, existingParcelIds)`:
    - body identical to the stored row (same site_name, factors, parcel_ids, visibility) → false
    - factors with keys in a different order but equal values → false; parcel_ids in a different order or case → false
    - body.site_name differs → true; one factor value differs → true; parcel_ids set differs → true
    - body.visibility "public" vs existing "private" → true; body.visibility absent → compares existing to itself → false
    - body.scores, body.status or body.expires_at differ → false (excluded)
    - fields absent or null in body are not treated as changes (same rule as the read-only helper)
  </behavior>
  <action>
    Per D-04 and D-16, add exported `hasSameVersionContentChanged(body: SurveyUpsertBody, existing: SurveyRow, existingParcelIds: string[]): boolean` to surveys-normalize.utils.ts: true when `getChangedSubmittedReadOnlyFields(body, existing, existingParcelIds).length > 0` or when `(body.visibility ?? existing.visibility) !== existing.visibility`. No hashing and no stored column (D-04: computed on the fly; D-16: value comparison, never raw JSON). Comment the D-04/D-16 rationale (JSONB reorders keys). Write the spec cases first.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- surveys-normalize.utils && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function hasSameVersionContentChanged" api/src/surveys/surveys-normalize.utils.ts` returns 1
    - `grep -cE "createHash|JSON.stringify\(existing" api/src/surveys/surveys-normalize.utils.ts` returns 0
    - Every behaviour bullet is covered and passes
  </acceptance_criteria>
  <done>A value-based same-version comparison exists and ignores key order, parcel order and excluded fields.</done>
</task>

<task type="auto">
  <name>Task 2: Conflict at the three same-version sites; SurveysService storage through StorageService</name>
  <files>api/src/surveys/surveys.service.ts</files>
  <read_first>
    - api/src/surveys/surveys.service.ts (:1-100 imports/constructor/S3, :200-500 upsertForUser, :1000-1040 delete path, :1540-1580 insertEvent and cleanupAttachmentStorage)
    - api/src/storage/storage.service.ts (deleteObject)
    - api/src/surveys/sync-error.utils.ts (409 mapping)
  </read_first>
  <action>
    Per D-04, at site 1 call `hasSameVersionContentChanged(body, existing, existingParcelIds)` when `syncVersion === existing.sync_version`: false → return the existing synced response unchanged; true → `throw new ConflictException({ code: "sync_version_conflict", message: "Same sync_version with different content", details: { survey_id: surveyId, server_sync_version: existing.sync_version, client_sync_version: syncVersion } })`. At sites 2 and 3 apply the same check against `reRead` with `await this.getSurveyParcelIds(db, reRead.id)`. Order at site 1 stays: older-version conflict first, then the same-version check, then the existing submitted read-only logic. Per D-05, delete the S3 fields, constructor block, `cleanupAttachmentStorage` and now-unused imports; inject `private readonly storage: StorageService` as the last constructor parameter; replace the post-commit cleanup loop with `for (const storageKey of storageKeys) await this.storage.deleteObject(storageKey)` keeping the "best-effort, only after commit" comment. Do not touch insertEvent (column defaults from migration 014 supply seq and xid8) or any other logic.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit && npm run typecheck && npm run lint && node -e "const s=require('fs').readFileSync('api/src/surveys/surveys.service.ts','utf8');const n=(s.match(/hasSameVersionContentChanged\(/g)||[]).length;if(n<3){console.error('sites:',n);process.exit(1)}if(/S3Client|DeleteObjectCommand|cleanupAttachmentStorage/.test(s)){console.error('S3 left');process.exit(1)}console.log('ok',n)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hasSameVersionContentChanged(" api/src/surveys/surveys.service.ts` returns at least 3
    - `grep -c "Same sync_version with different content" api/src/surveys/surveys.service.ts` returns at least 1
    - `grep -cE "S3Client|DeleteObjectCommand|cleanupAttachmentStorage" api/src/surveys/surveys.service.ts` returns 0
    - `grep -c "this.storage.deleteObject(" api/src/surveys/surveys.service.ts` returns at least 1
  </acceptance_criteria>
  <done>Racing writes on the same version surface as conflicts at every site, and SurveysService owns no storage client.</done>
</task>

<task type="auto">
  <name>Task 3: Same-version E2E and survey-deletion cleanup E2E</name>
  <files>api/test/surveys-same-version.e2e-spec.ts</files>
  <read_first>
    - api/test/surveys-idempotency.e2e-spec.ts (:48-92, :1402-1466)
    - api/test/attachments-reports-transactions.e2e-spec.ts (bootstrap, loginAsNewUser, createDraftSurvey, local upload ~165-193)
  </read_first>
  <action>
    Per D-04 and D-16, create api/test/surveys-same-version.e2e-spec.ts (configureApp; login via debug test-token; ids from randomUUID). Cases: (1) REST `POST /v1/surveys` version 1, then the identical body again → 201 synced. (2) Same version, `site_name` changed → 409 with `body.code === "sync_version_conflict"` (or the nested error shape used by the existing 409 assertions) and the stored site_name unchanged in the database. (3) Same version, only `visibility` changed → 409. (4) Same version, factors object with keys re-ordered and parcel_ids re-ordered → 201 synced. (5) /v1/sync batch [same-version different-content upsert, valid new survey upsert] → results [fatal_error with `error.code === "sync_version_conflict"`, `error.http_status === 409`, details.server_sync_version and client_sync_version present; synced]. (6) Submitted survey: submit, then resend the same version with a changed factor → 409 (sync_version_conflict or the existing survey_submitted_read_only, whichever the site order yields; assert the code is one of the two and the row is unchanged). (7) Deletion cleanup (D-05): create a draft, create an attachment with the exact byte size it uploads, upload it through the local route, record `storage_key`, delete the survey → the file `path.join(process.env.ATTACHMENTS_UPLOAD_DIR, storage_key)` no longer exists. Then run the full E2E suite (surveys-idempotency :48-92 and sync-installed-app-compat must stay green).
  </action>
  <verify>
    <automated>cd /home/user/cortege && POSTGRES_DB=ibp_p06_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p06 npm --workspace api run test:e2e && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sync_version_conflict" api/test/surveys-same-version.e2e-spec.ts` returns at least 3
    - `grep -cE "\bit\(" api/test/surveys-same-version.e2e-spec.ts` returns at least 7
    - The full E2E suite passes on ibp_p06_test
  </acceptance_criteria>
  <done>The same-version rule is proven on REST and /sync, and survey deletion removes stored objects through StorageService.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile clients → POST /v1/surveys and /v1/sync | Concurrent writers can send the same sync_version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.6-21 | Tampering | Same-version silent overwrite loss (Case B) | mitigate | Value comparison of read-only fields + visibility at all three sites; 409 sync_version_conflict on difference |
| T-01.6-22 | Repudiation | Losing device unaware its write was dropped | mitigate | Explicit 409 with server/client versions in details; mobile keeps local data |
| T-01.6-23 | Denial of Service | False conflicts from JSONB key order | mitigate | jsonDeepEqual/parcel set comparison, no raw JSON hashing; E2E case 4 |
| T-01.6-24 | Denial of Service | Retry-after-visibility-change edge (interfaces) | accept | Intended by D-16; documented in sync-conflict-resolution-v1.md by plan 08 |
| T-01.6-25 | Information Disclosure | Orphaned objects after survey delete | mitigate | Post-commit best-effort StorageService.deleteObject; E2E case 7 |
</threat_model>

<verification>
- Unit and full E2E green on ibp_p06_test; `npm run lint && npm run typecheck && npm run format:check` green
</verification>

<success_criteria>
Same-version replays with different content are conflicts at every site (criterion 2), and SurveysService uses StorageService (criterion 3).
</success_criteria>

<output>
Create `.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-06-SUMMARY.md` when done
</output>
