---
phase: 01.6-sync-feed-ordering-and-unified-object-storage
plan: 07
type: execute
wave: 3
depends_on: [01.6-02, 01.6-03, 01.6-05]
files_modified:
  - api/src/surveys/surveys-attachments.service.ts
  - api/test/surveys-attachments-download.spec.ts
  - api/test/attachments-upload-size.e2e-spec.ts
  - api/test/surveys-idempotency.e2e-spec.ts
  - api/test/attachments-reports-transactions.e2e-spec.ts
autonomous: true
requirements: [REQ-AUD-object-storage]

must_haves:
  truths:
    - "SurveysAttachmentsService owns no S3 client, bucket or path logic; every key comes from StorageService.buildAttachmentKey and every read/write/delete goes through StorageService (D-05, D-14)"
    - "The presigned PUT returned by attachment create signs Content-Length = size_bytes (D-08)"
    - "Confirming an upload whose stored size differs from size_bytes answers 422, deletes the object, and leaves the attachment not uploaded, in both modes (D-08)"
    - "Local-mode content reads stay contained under the upload root; minio-mode content route keeps answering 404 as documented (D-07)"
    - "E2E fixtures declare the byte size they actually upload (D-16)"
  artifacts:
    - path: api/src/surveys/surveys-attachments.service.ts
      provides: "Attachments on StorageService with the size check"
      contains: "UnprocessableEntityException"
    - path: api/test/attachments-upload-size.e2e-spec.ts
      provides: "Size-mismatch 422 in local mode; presigned ContentLength and 422 + delete in minio mode (itMinio)"
  key_links:
    - from: api/src/surveys/surveys-attachments.service.ts
      to: api/src/storage/storage.service.ts
      via: "presignPut(key, mime, body.size_bytes) and headObject at confirm"
      pattern: "presignPut\\([^)]*size_bytes"
---

<objective>
Move survey attachments onto `StorageService` and enforce the declared size: the presigned PUT signs `Content-Length`, and confirmation compares the stored size with `size_bytes`, answering 422 and deleting the object on a mismatch. Fix the E2E fixtures that declared a size different from what they uploaded.

Purpose: REQ-AUD-object-storage, ROADMAP success criteria 3 (attachments half) and 4 (containment, ContentLength, 422). Decisions D-05, D-07, D-08, D-14, D-16 (fixtures).
Output: rewired SurveysAttachmentsService, updated unit spec, new size E2E spec, corrected fixtures.
This plan runs in parallel with plan 06, which owns surveys.service.ts; do not edit that file here.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-CONTEXT.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-PATTERNS.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-02-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 02 (api/src/storage/storage.service.ts; SurveysModule imports StorageModule since plan 05):
- `readonly mode: "local" | "minio"`
- `buildAttachmentKey(surveyId, attachmentId, mimeType): string` → `surveys/${surveyId}/${attachmentId}${ext}` (400 on unsafe id or MIME)
- `putObject(key, body: Buffer, contentType): Promise<void>`
- `presignPut(key, contentType, contentLength): Promise<string>` (minio only)
- `presignGet(key): Promise<string>` (minio only), exported `DOWNLOAD_URL_TTL_SECONDS`
- `headObject(key): Promise<{ contentLength: number } | null>`
- `getObject(key): Promise<Buffer | null>`
- `deleteObject(key): Promise<void>` (best-effort)
- `resolveLocalPath(key): string` (BadRequestException when the key escapes the root)

Current SurveysAttachmentsService (api/src/surveys/surveys-attachments.service.ts): config/S3 :29-57; `buildUploadUrl` :104-127 (presign without ContentLength; local mode returns the relative `/surveys/:id/attachments/:attachmentId/upload?token=` route); `cleanupAttachmentStorage` :129-146; `ensureS3Bucket` :148-167; `createAttachment` :169-243 (validation order: mime required, size integer, max 25 MB, isAllowedMimeType; key built at :198 with `randomUUID()` attachment id; keep the order); `uploadAttachment` :245-333 (minio branch HEADs for existence only → 400 "uploaded object not found in storage" when missing; local branch writes the multipart buffer with join() and no containment; then a transaction sets uploaded_at and inserts `attachment_uploaded` with `object_storage_mode` and `bytes_written`); private `insertEvent` :90-102 (unchanged; seq/xid8 come from column defaults); `buildDownloadUrl` :455-479 (minio → presigned GET, local → `/surveys/${surveyId}/attachments/${attachmentId}/content` with requires_auth true); `getAttachmentContent` :502-530 (minio → 404 by contract; local → containment check then readFile). `size_bytes` is read from the attachments row; compare numerically (`Number(...)`) in case the driver returns a string.
Error shape for 422 (surveys.service.ts ~196-201): `new UnprocessableEntityException({ code, message })`. Use code "attachment_size_mismatch", message "Uploaded file size does not match declared size".

Unit spec api/test/surveys-attachments-download.spec.ts :58-68 builds `new SurveysAttachmentsService(db as never)` with env-driven mode and mocks S3/presigner/fs; switch it to a mocked storage object passed as the new last constructor argument and move storage-internal assertions (S3 commands, fs) out, since plan 02's storage spec covers them.

Fixtures to fix (PATTERNS C-8): api/test/surveys-idempotency.e2e-spec.ts ~1024 declares `size_bytes: 2048000` and uploads `Buffer.from("fake-jpeg-binary")` (16 B) at ~1040-1046 (presigned) and ~1061 (local); ~1329 declares 1024 and uploads 16 B (~1343-1360). api/test/attachments-reports-transactions.e2e-spec.ts ~174 declares 1000 and attaches 4 B, then expects 500 from an injected event failure (~182-192): fix the declared size so the size check passes and the injected failure still gives 500. Fixtures that only create (never upload) are unaffected.

Mode-agnostic upload branch to copy (api/test/attachments-download.e2e-spec.ts :60-91): `uploadUrl.startsWith("http")` → fetch PUT then confirm via `confirm_url`; else multipart PUT to `/v1${uploadUrl}`. MinIO-only cases: `const itMinio = process.env.OBJECT_STORAGE_MODE === "minio" ? it : it.skip`; `storage = moduleFixture.get(StorageService)`.

Local E2E: PostgreSQL 16 (`pg_ctlcluster 16 main start`, role ibp/ibp). This plan's database: prefix E2E commands with `POSTGRES_DB=ibp_p07_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p07` and always run them through `flock /tmp/ibp-e2e.lock` (cluster-wide snapshot xmin: parallel E2E runs interfere). MinIO is not available in the sandbox: itMinio cases are skipped locally and run in the CI MinIO job (plan 08). If PostgreSQL cannot start, stop and report.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SurveysAttachmentsService on StorageService with the size check</name>
  <files>api/src/surveys/surveys-attachments.service.ts, api/test/surveys-attachments-download.spec.ts</files>
  <read_first>
    - api/src/surveys/surveys-attachments.service.ts (full file)
    - api/src/storage/storage.service.ts
    - api/test/surveys-attachments-download.spec.ts
  </read_first>
  <behavior>
    Unit cases (storage mocked, db mocked):
    - createAttachment in minio mode calls `storage.presignPut(key, "image/jpeg", 2048)` for size_bytes 2048, with key from buildAttachmentKey
    - createAttachment in local mode returns the relative upload route and does not call presignPut
    - uploadAttachment minio: headObject → `{ contentLength: 2048 }` with size_bytes 2048 → uploaded; headObject → `{ contentLength: 16 }` → UnprocessableEntityException code attachment_size_mismatch, deleteObject called with the key, no UPDATE/event query issued; headObject → null → BadRequestException "uploaded object not found in storage"
    - uploadAttachment local: buffer length 16 with size_bytes 16 → putObject called then uploaded; buffer length 16 with size_bytes 2048 → 422, putObject not called
    - getAttachmentContent local: getObject null → NotFoundException; minio → NotFoundException (contract)
    - the existing download-url cases still pass (minio presignGet, local /content route)
  </behavior>
  <action>
    Per D-05, inject `private readonly storage: StorageService` as the last constructor parameter; delete the config/S3 block, `buildUploadUrl`, `cleanupAttachmentStorage`, `ensureS3Bucket`, the local DOWNLOAD_URL_TTL_SECONDS copy and the unused S3/fs/path imports. Per D-14, build keys only with `storage.buildAttachmentKey(surveyId, attachmentId, mime)`. Per D-08, in createAttachment call `storage.presignPut(key, mime, body.size_bytes)` in minio mode (local mode keeps the relative upload route). In uploadAttachment, minio: `storage.headObject(key)`; null → keep the existing 400; `contentLength !== Number(size_bytes)` → `await storage.deleteObject(key)` then throw the 422 above, before the transaction. Local: after the existing required/25 MB checks, `file.buffer.length !== Number(size_bytes)` → 422 without writing; otherwise `storage.putObject(key, file.buffer, mime)`. Keep the transaction and the attachment_uploaded event unchanged (use `storage.mode` for object_storage_mode). Per D-07, getAttachmentContent reads through `storage.getObject` (null → NotFoundException) and keeps the minio → 404 rule; downloads use `storage.presignGet` in minio mode. Delete paths call `storage.deleteObject` after commit. Update the unit spec first.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- surveys-attachments && npm --workspace api run test:unit:coverage && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "S3Client|HeadObjectCommand|getSignedUrl|writeFile|readFile|join\(this.uploadsRootDir" api/src/surveys/surveys-attachments.service.ts` returns 0
    - `grep -c "attachment_size_mismatch" api/src/surveys/surveys-attachments.service.ts` returns at least 1
    - `grep -cE "presignPut\([^)]*size_bytes" api/src/surveys/surveys-attachments.service.ts` returns 1
    - `grep -c "buildAttachmentKey(" api/src/surveys/surveys-attachments.service.ts` returns at least 1
    - `grep -c "new S3Client" api/src/surveys/surveys-attachments.service.ts` returns 0 (the repo-wide single-client check runs in plan 08 Task 1, after plan 06 has also landed)
    - Every behaviour bullet is covered and `npm --workspace api run test:unit` passes
  </acceptance_criteria>
  <done>Attachments use StorageService only, presigned uploads carry the declared length, and size mismatches are refused with 422 and cleaned up.</done>
</task>

<task type="auto">
  <name>Task 2: Size E2E in both modes and fixture corrections</name>
  <files>api/test/attachments-upload-size.e2e-spec.ts, api/test/surveys-idempotency.e2e-spec.ts, api/test/attachments-reports-transactions.e2e-spec.ts</files>
  <read_first>
    - api/test/attachments-download.e2e-spec.ts (:1-100, mode branch)
    - api/test/surveys-idempotency.e2e-spec.ts (~1015-1070, ~1320-1365)
    - api/test/attachments-reports-transactions.e2e-spec.ts (~165-200)
  </read_first>
  <action>
    Per D-08, D-10 and D-16, create api/test/attachments-upload-size.e2e-spec.ts (configureApp; debug test-token login; draft survey; `storage = moduleFixture.get(StorageService)`). Cases:
    (1) "accepts an upload of exactly the declared size" (both modes, mode branch copied from attachments-download): declare N = JPEG_BYTES.length, upload, confirm → 200, then download works.
    (2) `it` local-only guard (`process.env.OBJECT_STORAGE_MODE !== "minio" ? it : it.skip`) "rejects a local upload whose size differs": declare N + 10, multipart upload N bytes → 422 with code attachment_size_mismatch; `attachments.uploaded_at` still NULL; no file at `path.join(ATTACHMENTS_UPLOAD_DIR, storage_key)`; no attachment_uploaded event.
    (3) `itMinio` "presigned PUT enforces Content-Length": declare N, fetch PUT N + 10 bytes to upload_url → status 403 (signature mismatch).
    (4) `itMinio` "confirm rejects and deletes a wrong-size object": declare N, `await storage.putObject(storage_key, Buffer.alloc(N + 10), "image/jpeg")` directly, PUT confirm_url → 422 attachment_size_mismatch; `storage.headObject(storage_key)` is null; uploaded_at still NULL.
    Fix the fixtures: in surveys-idempotency.e2e-spec.ts set the declared `size_bytes` to the byte length of the buffer each test actually uploads (use a named const buffer and `.length`); in attachments-reports-transactions.e2e-spec.ts set the declared size to the attached buffer's length so the injected event failure still yields 500, and make every upload in that file mode-aware so it also passes in the CI MinIO job: in local mode keep the multipart PUT to the local route; in minio mode (`process.env.OBJECT_STORAGE_MODE === "minio"`) write the object with `await storage.putObject(storage_key, buffer, "image/jpeg")` at exactly the declared size (read storage_key from the create response or the attachments row), then PUT the confirm_url, expecting the same status the local branch expects (the "injected failure leaves uploaded_at null" test: 500 and uploaded_at still NULL). Change nothing else in those files. Run the whole E2E suite.
  </action>
  <verify>
    <automated>cd /home/user/cortege && POSTGRES_DB=ibp_p07_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p07 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "itMinio(" api/test/attachments-upload-size.e2e-spec.ts` returns at least 2
    - `grep -c "attachment_size_mismatch" api/test/attachments-upload-size.e2e-spec.ts` returns at least 2
    - `grep -cE "size_bytes: (2048000|1024)," api/test/surveys-idempotency.e2e-spec.ts` is lower than before for the upload tests (the two uploading fixtures now use the buffer length)
    - The full E2E suite passes on ibp_p07_test in local mode (itMinio cases skipped)
  </acceptance_criteria>
  <done>Size enforcement is proven locally and ready for the MinIO CI pass, and no fixture lies about its size.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile client → object store (presigned PUT) | Client uploads bytes directly with a URL the API signed |
| Mobile client → confirm / local upload route | Client claims the upload is complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.6-26 | Denial of Service | Oversized uploads to presigned URLs (A-M4) | mitigate | ContentLength signed into the presigned PUT; confirm-time headObject compare with delete + 422 as the authoritative backstop |
| T-01.6-27 | Tampering | Local-mode path traversal (A-M3) | mitigate | Keys only from buildAttachmentKey (safe ids) and every local access through resolveLocalPath |
| T-01.6-28 | Denial of Service | Real devices rejected by the signed length | mitigate | Mobile sends the prepared file's size (mobile/src/storage/attachments.ts:103) with a binary upload task; owner device check in plan 09 |
| T-01.6-28b | Denial of Service | Pre-01.5 installed builds declare a wrong size_bytes (asset.fileSize or a 500_000 fallback), so their photo uploads now get 403 (MinIO signature) or 422 | accept | No mobile change (owner decision); these builds must be updated to the current app. Called out in the plan 09 PR body and the owner checkpoint; the queued survey data itself still syncs |
| T-01.6-29 | Information Disclosure | Cross-user attachment access | mitigate | Ownership check (getSurveyForUserOrThrow) unchanged before every storage call |
</threat_model>

<verification>
- Unit and full E2E green on ibp_p07_test (local); `npm run lint && npm run typecheck && npm run format:check` green
</verification>

<success_criteria>
Attachments go through StorageService, the presigned PUT carries Content-Length, and a size mismatch is refused with 422 and the object removed (criterion 4).
</success_criteria>

<output>
Create `.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-07-SUMMARY.md` when done
</output>
