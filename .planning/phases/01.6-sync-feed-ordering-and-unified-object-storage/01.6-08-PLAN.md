---
phase: 01.6-sync-feed-ordering-and-unified-object-storage
plan: 08
type: execute
wave: 4
depends_on: [01.6-03, 01.6-04, 01.6-06, 01.6-07]
files_modified:
  - .github/workflows/ci.yml
  - docs/technical/api-contract-v1.md
  - docs/technical/sync-conflict-resolution-v1.md
  - docs/technical/data-contract-v1.md
  - docs/technical/technical-architecture-v1.md
  - CLAUDE.md
autonomous: true
requirements: [REQ-AUD-changes-feed, REQ-AUD-object-storage]

must_haves:
  truths:
    - "CI has a separate MinIO-mode E2E job that starts pinned MinIO with a docker run step, waits for its health endpoint, and runs the full E2E suite with OBJECT_STORAGE_MODE=minio; the existing local-mode E2E job is unchanged (D-10, D-17)"
    - "The CI OK gate fails when the MinIO E2E job fails (D-10)"
    - "api-contract-v1.md and sync-conflict-resolution-v1.md describe the v2 cursor, legacy cursor acceptance, the snapshot rule with its cluster-wide delay note, the removed fallback and the same-version rule (D-11, D-12, D-13, D-04)"
    - "The contract docs describe the storage changes: signed Content-Length, 422 on size mismatch, safe-id rule, profile pictures in object storage served through the API, missing picture reads as null (D-08, D-14, D-15)"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "e2e-minio job wired into ci-ok"
      contains: "quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z"
    - path: docs/technical/sync-conflict-resolution-v1.md
      provides: "Case B split, changes-feed ordering section"
      contains: "pg_snapshot_xmin"
    - path: docs/technical/api-contract-v1.md
      provides: "Cursor, same-version, attachment size and profile picture contract"
      contains: "v2:"
  key_links:
    - from: ".github/workflows/ci.yml ci-ok job"
      to: "e2e-minio job"
      via: "needs list and result check"
      pattern: "needs\\.e2e-minio\\.result"
---

<objective>
Close the phase's CI and documentation gaps: add a MinIO-mode E2E job (so presigned PUT enforcement, the 422 size path and the profile-picture round trip run against real MinIO on every API change), and update the contract and architecture docs to the behaviour plans 01-07 shipped.

Purpose: ROADMAP success criterion 6 and the CI proof for criteria 3-4. Decisions D-10, D-11, D-17 (and documentation of D-04, D-08, D-12, D-13, D-14, D-15).
Output: ci.yml e2e-minio job, updated docs, CLAUDE.md module table.
MinIO does not run in the sandbox: the new job is validated statically here and proven on the PR's CI run in plan 09.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-CONTEXT.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-PATTERNS.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-03-SUMMARY.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-04-SUMMARY.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-06-SUMMARY.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-07-SUMMARY.md
@CLAUDE.md

<interfaces>
ci.yml today: job `e2e` ("E2E tests — API", ~178-262): runs-on ubuntu-latest, timeout-minutes 20, `needs: changes`, `if: needs.changes.outputs.api == 'true' || needs.changes.outputs.shared == 'true'`, postgres:16 service (ibp_test, ibp/ibp, pg_isready health), job env (NODE_ENV test, POSTGRES_*, PGPASSWORD, OBJECT_STORAGE_MODE local, OBJECT_STORAGE_BUCKET ibp-media, ATTACHMENTS_UPLOAD_DIR /tmp/ibp-uploads, token secrets, AUTH_* flags, SMTP_ENABLED false, DEBUG_DATA_RESET_ENABLED true, CADASTRE_PROVIDER synthetic, CADASTRE_PROVIDER_ALLOW_FALLBACK true), steps checkout / setup-node / cache / `npm ci` on cache miss, all actions pinned by SHA with a version comment, then "E2E run 1", sentinel, "E2E run 2 (same database)", reset assertion. Job `ci-ok` ("CI OK", ~429-470): `needs: [changes, check, unit-api, unit-mobile, e2e, mobile-build, audit, image-check]`, env `*_RESULT: ${{ needs.<job>.result }}`, echo lines, and a loop that accepts success or skipped for the path-filtered jobs.

MinIO in CI (PATTERNS C-9, D-17): `services:` cannot pass `server /data`; use a step: `docker run -d --name minio -p 9000:9000 -e MINIO_ROOT_USER=minio -e MINIO_ROOT_PASSWORD=minio123 quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z server /data` (tag pinned like infra/docker-compose.yml:18), then wait on `curl -fsS http://localhost:9000/minio/health/live` (loop, e.g. 30 tries x 2 s, fail with `docker logs minio` on timeout). StorageService env names: OBJECT_STORAGE_MODE, OBJECT_STORAGE_ENDPOINT, OBJECT_STORAGE_REGION, OBJECT_STORAGE_ACCESS_KEY, OBJECT_STORAGE_SECRET_KEY, OBJECT_STORAGE_BUCKET (bucket created lazily by StorageService).

What shipped (read the SUMMARYs of plans 03, 04, 06, 07 for exact wording and any deviation):
- Feed: events with `xid8 < pg_snapshot_xmin(pg_current_snapshot())`, ordered/paged by `(xid8, seq)`; cursor `v2:<xid8>:<seq>` (opaque); legacy `<timestamp>|<id>` accepted and translated with `(created_at, id) <= cursor`, translated v2 cursor returned when nothing is new; event-less-survey fallback removed; migration 014 inserted one 'backfilled' event per event-less owned survey; a long-running writing transaction anywhere in the cluster delays (never skips) the feed.
- Same version: identical read-only fields + visibility → synced; different → 409 `sync_version_conflict` (message "Same sync_version with different content"; /sync fatal_error http_status 409); scores/status/expires_at excluded; known edge from plan 06 (retry after a visibility change on a pending upsert).
- Storage: one StorageService; ids `^[A-Za-z0-9_-]{1,128}$` at routes, DTOs and the key builder (400 "Invalid identifier"); presigned PUT signs Content-Length = size_bytes; confirm (minio) and local upload compare size → 422 `attachment_size_mismatch` and delete; profile pictures in object storage, served as bytes via GET /me/profile-picture with Bearer auth; missing object → `/me` profile_picture_url null and 404 that clears the columns; unsupported picture type → 400; unified default bucket ibp-media.

Doc anchors (PATTERNS "Docs"): sync-conflict-resolution-v1.md :27-29 (Case B), api-contract-v1.md :707 (same-version phrasing style), :709-723 (GET /sync/changes cursor documented as `{timestamp}|{event_id}`), :480-497 (attachment create rules), :150-182 (profile picture); data-contract-v1.md :30 (`profile_picture_storage_key` "local storage key"), no survey_events section yet; technical-architecture-v1.md :19, :105-106 (object storage, upload flow); CLAUDE.md "Module layout" table (api/src modules) and "Architecture > API".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinIO-mode E2E job in CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (e2e job ~178-262, ci-ok job ~429-475)
    - infra/docker-compose.yml (MinIO image tag, credentials)
  </read_first>
  <action>
    Per D-10 and D-17, add a new job `e2e-minio` named "E2E tests — API (MinIO mode)" after the `e2e` job, as a separate job so neither job approaches its timeout: same `runs-on`, `timeout-minutes: 20`, `needs: changes`, same `if`, same postgres:16 service block, same job env as `e2e` except `OBJECT_STORAGE_MODE: minio` plus `OBJECT_STORAGE_ENDPOINT: http://localhost:9000`, `OBJECT_STORAGE_REGION: us-east-1`, `OBJECT_STORAGE_ACCESS_KEY: minio`, `OBJECT_STORAGE_SECRET_KEY: minio123` (keep OBJECT_STORAGE_BUCKET ibp-media and ATTACHMENTS_UPLOAD_DIR, which the profile test uses to prove nothing is written locally). Steps: the same SHA-pinned checkout, setup-node and cache steps and `npm ci` on cache miss (copy them verbatim); "Start MinIO" (the docker run from the interfaces block); "Wait for MinIO" (curl health loop with docker logs on failure); "E2E run (MinIO mode)" running `npm run test:e2e`. Leave the `e2e` job untouched. Wire `e2e-minio` into `ci-ok`: add it to `needs`, add `E2E_MINIO_RESULT: ${{ needs.e2e-minio.result }}`, an echo line, and include it in the success-or-skipped loop.
  </action>
  <verify>
    <automated>cd /home/user/cortege && python3 -c "import yaml,sys;d=yaml.safe_load(open('.github/workflows/ci.yml'));j=d['jobs'];m=j['e2e-minio'];assert m['env']['OBJECT_STORAGE_MODE']=='minio';assert j['e2e']['env']['OBJECT_STORAGE_MODE']=='local';assert 'e2e-minio' in j['ci-ok']['needs'];s=' '.join(str(x.get('run','')) for x in m['steps']);assert 'quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z' in s and 'minio/health/live' in s and 'test:e2e' in s;print('ci ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The python check prints `ci ok` (ci.yml is not prettier-formatted today and is outside `format:check`; do not reformat the rest of the file)
    - `git diff .github/workflows/ci.yml` shows no change inside the existing `e2e` job's steps or env
    - `grep -c "needs.e2e-minio.result" .github/workflows/ci.yml` returns 1
    - Every `uses:` line in the new job is SHA-pinned (`grep -A40 "e2e-minio:" .github/workflows/ci.yml | grep "uses:" | grep -vc "@[0-9a-f]\{40\}"` returns 0)
  </acceptance_criteria>
  <done>Every API change now runs the full E2E suite against real MinIO in CI, and CI OK depends on it.</done>
</task>

<task type="auto">
  <name>Task 2: Contract, conflict, data and architecture docs; CLAUDE.md</name>
  <files>docs/technical/api-contract-v1.md, docs/technical/sync-conflict-resolution-v1.md, docs/technical/data-contract-v1.md, docs/technical/technical-architecture-v1.md, CLAUDE.md</files>
  <read_first>
    - docs/technical/sync-conflict-resolution-v1.md
    - docs/technical/api-contract-v1.md (:140-190, :470-500, :700-730, "## Standard Error Codes")
    - docs/technical/data-contract-v1.md (users table, events)
    - docs/technical/technical-architecture-v1.md (:1-40, :95-115)
    - CLAUDE.md ("Module layout", "Architecture > API", "Key files")
    - the SUMMARYs of plans 03, 04, 06 and 07 (deviations change what is documented)
  </read_first>
  <action>
    Per D-11, document exactly what shipped (interfaces block, corrected by any SUMMARY deviation):
    sync-conflict-resolution-v1.md: split Case B into B1 (same id, same sync_version, identical read-only fields and visibility → synced, idempotent replay) and B2 (different content → 409 sync_version_conflict, fatal_error in /sync, client keeps local data), naming the compared fields and the excluded ones (scores, status, expires_at), and the retry-after-visibility-change edge from plan 06. Add a "Changes feed ordering" section: xid8 + seq columns from migration 014, the `xid8 < pg_snapshot_xmin(pg_current_snapshot())` rule and why paging on (xid8, seq) cannot skip, the cluster-wide delay note (a long or idle-in-transaction writer delays, never skips; read-only transactions do not count), legacy cursor translation, removal of the event-less fallback and the one-time 'backfilled' events.
    api-contract-v1.md: GET /sync/changes cursor becomes opaque `v2:<xid8>:<seq>` (clients must store and replay it verbatim), legacy `{timestamp}|{id}` still accepted, updated example cursor_in/cursor_out, 400 "Invalid sync cursor"; POST /surveys and POST /sync same-version rule in the :707 phrasing style; attachment create: presigned PUT signs Content-Length = size_bytes (clients must send exactly that many bytes), confirm or local upload with a different size → 422 `attachment_size_mismatch` and the object is deleted; id rule `^[A-Za-z0-9_-]{1,128}$` for survey and attachment ids on routes and /sync (400 "Invalid identifier" / invalid_sync_operation); profile picture: stored in object storage, GET streams bytes with Bearer auth, `/me` returns `profile_picture_url: null` when the stored object is missing, unsupported types 400. Add `attachment_size_mismatch` to "## Standard Error Codes".
    data-contract-v1.md: `profile_picture_storage_key` is an object storage key; add a short survey_events section listing seq (bigint identity), xid8 (xid8, default pg_current_xact_id()) and index (xid8, seq).
    technical-architecture-v1.md: one StorageService owns object storage (minio/S3 in production, local disk in dev), profile pictures included; upload flow mentions the signed Content-Length and the size check.
    CLAUDE.md: add a `storage` row to the API "Module layout" table (`api/src/storage/`, "StorageService: single owner of object storage (S3/MinIO or local), key builder, size and MIME checks"), and add `api/src/storage/storage.service.ts` to "Key files". Keep every other line unchanged.
  </action>
  <verify>
    <automated>cd /home/user/cortege && grep -q "pg_snapshot_xmin" docs/technical/sync-conflict-resolution-v1.md && grep -q "v2:" docs/technical/api-contract-v1.md && grep -q "attachment_size_mismatch" docs/technical/api-contract-v1.md && grep -q "Same sync_version with different content\|same sync_version" docs/technical/api-contract-v1.md && grep -q "xid8" docs/technical/data-contract-v1.md && grep -q "api/src/storage/" CLAUDE.md && echo docs ok</automated>
  </verify>
  <acceptance_criteria>
    - Each grep in the verify command matches and it prints `docs ok`
    - `grep -c "{timestamp}|{event_id}" docs/technical/api-contract-v1.md` is 0 outside a "legacy" sentence (the legacy form is described as legacy)
    - `grep -ci "local storage key" docs/technical/data-contract-v1.md` returns 0
  </acceptance_criteria>
  <done>The contract docs describe the new cursor, the same-version rule and the storage rules (ROADMAP criterion 6).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → third-party image | The MinIO container image is pulled in CI |
| Docs → client implementers | The contract tells clients how to send sizes and cursors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.6-30 | Tampering | MinIO image in CI | mitigate | Exact release tag pinned (same as infra/docker-compose.yml); test-only credentials; no secrets exposed to the job |
| T-01.6-31 | Tampering | Workflow actions | mitigate | All actions in the new job SHA-pinned, copied from the existing job |
| T-01.6-32 | Repudiation | MinIO behaviour claimed without proof | mitigate | Full E2E suite in minio mode on every API change; CI OK fails on its failure |
| T-01.6-33 | Denial of Service | Clients sending mismatched sizes after the change | mitigate | Contract states the exact-size requirement and the 422 code |
</threat_model>

<verification>
- ci.yml parses, the new job and gate wiring are present; docs greps match
</verification>

<success_criteria>
CI runs the E2E suite in MinIO mode on every API change, and the docs describe the new cursor, same-version and storage rules.
</success_criteria>

<output>
Create `.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-08-SUMMARY.md` when done
</output>
