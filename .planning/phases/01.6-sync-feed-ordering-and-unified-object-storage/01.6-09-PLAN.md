---
phase: 01.6-sync-feed-ordering-and-unified-object-storage
plan: 09
type: execute
wave: 5
depends_on: [01.6-01, 01.6-02, 01.6-03, 01.6-04, 01.6-05, 01.6-06, 01.6-07, 01.6-08]
files_modified:
  - api/jest.unit.config.js
  - .planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md
autonomous: false
requirements: [REQ-AUD-changes-feed, REQ-AUD-object-storage]

must_haves:
  truths:
    - "The full local gate (lint, typecheck, unit with coverage, mobile unit, tracked-file prettier, full E2E on a dedicated _test database) is green on the integrated phase result"
    - "Coverage thresholds in api/jest.unit.config.js are raised to the new measured floors where they went up, and no value is lowered"
    - "On the phase PR every CI job is green, including the local-mode E2E job (run twice) and the new MinIO-mode E2E job, whose log shows the presigned Content-Length, 422 size-mismatch and profile-picture-in-bucket cases passing"
    - "01.6-VALIDATION.md records the PR URL, run URLs, job conclusions and quoted E2E lines"
    - "The owner merged the PR, and after deploy two devices stay in sync, a photo uploads, and a profile picture uploads, displays and survives an API restart"
  artifacts:
    - path: .planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md
      provides: "Per-task statuses, CI evidence, owner device check"
      contains: "actions/runs/"
    - path: api/jest.unit.config.js
      provides: "Ratcheted coverage thresholds"
  key_links:
    - from: 01.6-VALIDATION.md
      to: "GitHub Actions run of the phase PR"
      via: "run URL, job table and quoted E2E lines"
      pattern: "actions/runs/"
---

<objective>
Phase gate: run the complete local gate on the integrated result, ratchet API coverage upward, open the phase PR and record from its CI run the evidence the sandbox cannot produce (the MinIO-mode E2E job). Then the owner merges and runs a few short device checks on a DEV build after deploy.

Purpose: both phase requirements; ROADMAP success criteria 1-6 demonstrated on CI and on a device.
Output: ratcheted thresholds, completed 01.6-VALIDATION.md, merged PR.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-01-SUMMARY.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-02-SUMMARY.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-03-SUMMARY.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-04-SUMMARY.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-05-SUMMARY.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-06-SUMMARY.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-07-SUMMARY.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-08-SUMMARY.md
@CLAUDE.md

<interfaces>
Repository: github.com/florianlepont/cortege. Working branch: the phase branch currently checked out; PR base: main. GitHub access: the `gh` CLI may be absent; use the GitHub MCP tools (pull requests, workflow runs, jobs, job logs) if available; if nothing can read Actions runs, surface an access gate and ask the owner for the run URL, job conclusions and the two E2E job logs.
ci.yml jobs: "Detect changed paths", "Lint, format, typecheck", "Unit tests — API", "Unit tests — Mobile", "E2E tests — API" (E2E run 1, sentinel, E2E run 2, reset assertion), "E2E tests — API (MinIO mode)" (new, plan 08), "Mobile build check", "Dependency audit", "Docker image check", "CI OK", "Build & push Docker image" (main only, skipped on PRs). CodeQL: "CodeQL analysis". Mobile jobs are path-filtered: skipped is acceptable, failure is not.
Coverage ratchet: `npm --workspace api run test:unit:coverage` writes api/coverage/unit/coverage-summary.json; `node scripts/coverage-by-directory.js api` prints floor-rounded values. Thresholds only go up (the `./src/storage/` row was added by plan 02).
Local E2E: PostgreSQL 16 (`pg_ctlcluster 16 main start`, role ibp/ibp); gate database `ibp_p09_test`, local storage mode. MinIO is not available in the sandbox; MinIO-mode proof comes from CI only.
Tracked-file prettier: `git ls-files -z '*.ts' '*.tsx' '*.json' | xargs -0 npx prettier --check --ignore-path .prettierignore` (avoids untracked local artifacts), in addition to `npm run format:check`.
Presigned Content-Length on MinIO (RESEARCH A1) is proven only by the MinIO job's "presigned PUT enforces Content-Length" case (plan 07). If that case fails because MinIO accepts a mismatched body, D-08 applies ("signed header where the SDK and MinIO allow it"): keep the signed header, change that case to assert the upload is then refused at confirm with 422 and the object deleted, record the deviation in VALIDATION.md and the SUMMARY, and tell the owner in the checkpoint that criterion 4's enforcement rests on the confirm check.
VPS: deploy is pull-based, about 5 minutes after the main image build; `curl https://cortege.algernon.ovh/v1/health` returns 200 when up. API restart on the VPS: `docker compose -f infra/docker-compose.vps.yml --env-file /home/ubuntu/cortege.env restart api` (infra/vps/README.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full local gate, coverage ratchet and validation map update</name>
  <files>api/jest.unit.config.js, .planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md</files>
  <read_first>
    - api/jest.unit.config.js (coverageThreshold)
    - scripts/coverage-by-directory.js (usage)
    - .planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md
    - the SUMMARY files of plans 01-08 (deviations, anything deferred)
  </read_first>
  <action>
    From the repo root run, in order: `npm run lint`, `npm run typecheck`, `npm --workspace api run test:unit:coverage`, `npm --workspace mobile run test:unit`, `npm run format:check`, the tracked-file prettier command from the interfaces block, then `node scripts/coverage-by-directory.js api` and raise every coverageThreshold value whose measured floor is higher (`./src/surveys/`, `./src/users/`, `./src/common/`, `./src/storage/` and global are expected to move); never lower any value; update the date in the ratchet comment; re-run the coverage command to prove the new thresholds pass. Then run the full E2E suite twice in a row on `ibp_p09_test` in local mode (`POSTGRES_DB=ibp_p09_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p09 npm --workspace api run test:e2e`), and run `sync-changes-ordering` three extra times to check for flakiness. Fix nothing silently: if a command fails, stop and report which plan's files are involved. In 01.6-VALIDATION.md set each locally proven Per-Task row to ✅ green (or ❌ red with the failing command), leave the MinIO-only and CI rows ⬜ for Task 2, and set `wave_0_complete: true`.
  </action>
  <verify>
    <automated>cd /home/user/cortege && npm run lint && npm run typecheck && npm --workspace api run test:unit:coverage && npm --workspace mobile run test:unit && npm run format:check && git ls-files -z '*.ts' '*.tsx' '*.json' | xargs -0 npx prettier --check --ignore-path .prettierignore > /dev/null && git diff -U0 api/jest.unit.config.js | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const rm=d.split('\n').filter(l=>l.startsWith('-')&&!l.startsWith('---')&&/statements/.test(l));const add=d.split('\n').filter(l=>l.startsWith('+')&&!l.startsWith('+++')&&/statements/.test(l));const parse=l=>{const k=(l.match(/'([^']+)'|global/)||[''])[0];const v={};for(const m of l.matchAll(/(statements|branches|functions|lines): (\d+)/g))v[m[1]]=+m[2];return [k,v]};const old=Object.fromEntries(rm.map(parse));for(const l of add){const [k,v]=parse(l);const o=old[k];if(!o)continue;for(const x in v){if(v[x]<o[x]){console.error('lowered '+k+' '+x);process.exit(1)}}}console.log('ratchet ok')})" && POSTGRES_DB=ibp_p09_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p09 npm --workspace api run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - Every gate command exits 0 and the ratchet check prints `ratchet ok`
    - The full E2E suite passed twice on ibp_p09_test and sync-changes-ordering passed three extra times
    - `grep -c "wave_0_complete: true" .planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md` returns 1
  </acceptance_criteria>
  <done>The integrated phase passes every local gate, thresholds only went up, and the validation map reflects it.</done>
</task>

<task type="auto">
  <name>Task 2: Open the phase PR and record its CI evidence</name>
  <files>.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md</files>
  <read_first>
    - .github/workflows/ci.yml (job and step names, including e2e-minio)
    - .planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md
  </read_first>
  <action>
    Push the phase branch and open (or reuse) a PR against main titled "Phase 01.6: Sync feed ordering and unified object storage". The body summarises plans 01-08 and calls out for the owner: migration 014 (adds columns to survey_events and one synthetic event per event-less survey; short lock at deploy), the new opaque cursor (installed apps need no update), the same-version 409 rule, the removed fallback, profile pictures moving to object storage with old pictures not recovered (D-06: users see initials until they upload again), the 400 on unsafe ids and unsupported picture types, the 422 on size mismatch, and the new MinIO CI job. End the body with the attribution lines from the session's system reminder. Wait for the CI and CodeQL runs on the PR head to finish (poll, at most 45 minutes). Verify: (1) `success` for Detect changed paths, Lint/format/typecheck, Unit tests — API, E2E tests — API, E2E tests — API (MinIO mode), Dependency audit, Docker image check, CI OK, CodeQL analysis; mobile jobs success or skipped; Build & push skipped. (2) The local-mode E2E log shows both runs passing with sync-changes-ordering, migration-014-survey-events-seq, surveys-same-version, safe-ids, attachments-upload-size and auth-profile listed as PASS. (3) The MinIO job log shows "Wait for MinIO" succeeding and the suite passing with the itMinio cases executed, not skipped (presigned Content-Length 403, confirm 422 + object deleted, picture stored in the bucket); quote the lines. (4) Unit tests — API shows no "threshold ... not met". If a job fails, diagnose from the log, fix in the owning plan's files on the same branch (Content-Length exception: follow the D-08 rule in the interfaces block), push, re-verify, and record each fix as a deviation. Record the PR URL, run URLs, the job table and the quoted lines in the "CI Evidence" section of 01.6-VALIDATION.md and set the CI-proven rows to ✅.
  </action>
  <verify>
    <automated>grep -c "actions/runs/" /home/user/cortege/.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 01.6-VALIDATION.md contains the PR URL, at least one `actions/runs/` URL and a job table matching point 1, including "E2E tests — API (MinIO mode)"
    - The quoted MinIO job lines show the itMinio cases ran and passed
    - No Per-Task row is left ⬜ except the owner checkpoint row
  </acceptance_criteria>
  <done>The PR's CI proves the phase end to end on real PostgreSQL and real MinIO, with evidence recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner review, merge and short device checks</name>
  <files>.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md (CI Evidence)
  </read_first>
  <action>Present the steps below with the PR URL and the CI run URL, wait for the resume signal, then record the result in the "Manual-Only Verifications" table of 01.6-VALIDATION.md.</action>
  <what-built>Commit-safe changes feed with a new opaque cursor and legacy cursor support, same-version conflict detection, removal of the event-less fallback, one StorageService for attachments and profile pictures (pictures now in MinIO in production), safe ids, signed upload size with a 422 check, the MIME fix, and a MinIO-mode CI job. The PR's CI run is green (Task 2).</what-built>
  <how-to-verify>
    1. Review the PR (behaviour changes are listed in its body) and merge it into main.
    2. Wait for the deploy: `curl https://cortege.algernon.ovh/v1/health` returns 200 (about 5 minutes after the main build).
    3. Two devices, same account, DEV build (a phone plus a simulator is fine): on device A edit a survey's site name and let it sync. On device B pull to refresh or reopen the survey list. Expected: the new name appears on B within a few seconds, with no sync error on either device.
    4. On device A add a photo to a draft and let it sync. Expected: no sync error; the photo shows on the survey.
    5. On the Account screen upload a profile picture. Expected: it displays. Close and reopen the app: still displayed.
    6. Restart the API on the VPS (`docker compose -f infra/docker-compose.vps.yml --env-file /home/ubuntu/cortege.env restart api`), wait for /v1/health, reopen the Account screen. Expected: the picture is still displayed. (If an old picture existed before this deploy, it is expected to show initials until you upload again.)
  </how-to-verify>
  <resume-signal>Type "approved — merged, devices in sync, picture survives restart", or describe what failed</resume-signal>
  <verify>
    <automated>grep -c "Manual-Only Verifications" /home/user/cortege/.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The owner replied with approval, or each failure is recorded in 01.6-VALIDATION.md with the observed behaviour
  </acceptance_criteria>
  <done>The phase is merged, installed DEV builds keep syncing across devices, and profile pictures survive an API restart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → GitHub | The agent pushes a branch and opens a PR; merging stays with the owner |
| Deployed API → installed apps | New cursor and rules meet apps that were not updated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.6-34 | Elevation of Privilege | Agent merging to main | mitigate | Merge is an owner step in the blocking checkpoint; the agent never merges |
| T-01.6-35 | Repudiation | CI claims without evidence | mitigate | Run URLs, job table and quoted E2E lines (both modes) recorded in VALIDATION.md |
| T-01.6-36 | Tampering | Coverage thresholds lowered to pass | mitigate | Ratchet check in Task 1 fails on any lowered value |
| T-01.6-37 | Denial of Service | Deployed API breaking installed apps | mitigate | Legacy-cursor and installed-app E2E on CI plus the owner's two-device, photo and picture checks after deploy |
</threat_model>

<verification>
- Local gate green; PR CI green in both E2E modes with evidence recorded; owner merged and approved the device checks
</verification>

<success_criteria>
ROADMAP success criteria 1-6 demonstrated on a real GitHub Actions run (PostgreSQL and MinIO) and confirmed on devices after deploy.
</success_criteria>

<output>
Create `.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-09-SUMMARY.md` when done
</output>
