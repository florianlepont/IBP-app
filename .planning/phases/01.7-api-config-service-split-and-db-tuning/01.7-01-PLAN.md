---
phase: 01.7-api-config-service-split-and-db-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/package.json
  - package-lock.json
  - api/src/config/env.schema.ts
  - api/src/config/production-rules.ts
  - api/src/config/app-config.ts
  - api/src/config/config.types.ts
  - api/src/config/check-config.ts
  - api/src/app.module.ts
  - api/src/main.ts
  - api/test/env.schema.spec.ts
  - api/test/check-config.spec.ts
  - api/test/config-helper.ts
  - api/test/debug-surface.e2e-spec.ts
autonomous: false
requirements: [REQ-AUD-config]

must_haves:
  truths:
    - "The API reads its configuration through @nestjs/config 4.0.4: ConfigModule.forRoot({ isGlobal: true, ignoreEnvFile: true, cache: true, validate: validateEnv, load: [appConfig] }) is the first import of AppModule (D-01)"
    - "With NODE_ENV=production, validation refuses a default or empty POSTGRES_PASSWORD, a default or empty OBJECT_STORAGE_SECRET_KEY in minio mode, an empty AUTH0_DOMAIN or AUTH0_AUDIENCE, and a missing or empty CORS_ORIGIN; errors name variables, never values (D-02, D-03, D-18)"
    - "Default detection matches ibp, minio, minio123, empty, and any value starting with change-me / change_me / changeme (case-insensitive); OBJECT_STORAGE_ACCESS_KEY=minio stays allowed (D-18, pattern map C-2)"
    - "CORS_ORIGIN=none and an explicit comma-separated origin list are accepted in production; an entry containing CHANGE_ME is refused (D-03)"
    - "Outside production every variable keeps today's default, so local dev, unit tests and E2E need no new variable (D-02)"
    - "node api/dist/config/check-config.js prints one plain French line per problem naming the variable, exits 1 on any problem and 0 when valid; AUTH0_MGMT_* gaps print a warning only (D-05, D-02)"
    - "The production-mode debug-surface E2E boots with a production-valid fake env and restores every key it sets"
  artifacts:
    - path: api/src/config/env.schema.ts
      provides: "EnvironmentVariables (class-validator) + validateEnv"
      contains: "validateSync"
    - path: api/src/config/production-rules.ts
      provides: "findProductionProblems, findProductionWarnings, assertProductionSafety"
    - path: api/src/config/app-config.ts
      provides: "loadAppConfig, currentNodeEnv, appConfig (registerAs), appConfigOf"
    - path: api/src/config/check-config.ts
      provides: "VPS pre-flight CLI, compiled to api/dist/config/check-config.js"
    - path: api/test/config-helper.ts
      provides: "buildTestConfig / buildTestConfigService for specs that construct services"
  key_links:
    - from: api/src/app.module.ts
      to: api/src/config/env.schema.ts
      via: "ConfigModule.forRoot validate hook"
      pattern: "validate: validateEnv"
    - from: api/src/config/check-config.ts
      to: api/src/config/production-rules.ts
      via: "same rule set outside Nest"
      pattern: "findProductionProblems\\("
---

<objective>
Install `@nestjs/config@4.0.4` and `lru-cache` (both, per the pattern map C-8, after the owner confirms their legitimacy), build the typed and validated configuration module under `api/src/config/`, register it in `AppModule`, move `main.ts` to the Nest Logger, and ship the pre-flight CLI `check-config`.

Purpose: REQ-AUD-config, ROADMAP criterion 1 (schema validated at startup; production refuses defaults, an empty AUTH0_AUDIENCE and a missing CORS_ORIGIN). Decisions D-01, D-02, D-03 (rule side), D-05 (check entry point), D-18 (default detection).
Output: the `src/config/` module every later config plan reads from, the test config helper, and a green E2E suite with ConfigModule registered.
This plan does not migrate any service's `process.env` reads (plans 05 and 06) and does not touch CI, infra or docs (plan 07). No later plan edits `api/src/config/**`: every key those plans need is defined here.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-RESEARCH.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-PATTERNS.md
@CLAUDE.md

<interfaces>
Package facts (RESEARCH Standard Stack, Pitfall 1): `@nestjs/config@12.x` is ESM-only and cannot load under the CommonJS build and Jest 29, so pin exactly `4.0.4` (peer `@nestjs/common ^10 || ^11`, no postinstall). `lru-cache` 11.x has dual CJS/ESM exports and is already in the tree through jwks-rsa; it must become a direct `api` dependency (used by plan 09). Never run `slopcheck install` (it performs a real install at the repo root).

Env variables read by api/src today (all must exist in the schema with today's defaults; source file in brackets):
- NODE_ENV (development|test|production, default development); PORT (3000) [main.ts]; TRUST_PROXY ("loopback,uniquelocal") [app.setup.ts]; CORS_ORIGIN (optional) [app.setup.ts]
- POSTGRES_HOST (localhost), POSTGRES_PORT (5432), POSTGRES_USER (ibp), POSTGRES_PASSWORD (ibp), POSTGRES_DB (ibp) [database.service.ts]
- AUTH0_DOMAIN (""), AUTH0_PUBLIC_DOMAIN (falls back to AUTH0_DOMAIN), AUTH0_AUDIENCE ("") [auth.guard.ts]; AUTH0_MGMT_CLIENT_ID, AUTH0_MGMT_CLIENT_SECRET, AUTH0_APP_CLIENT_ID [auth0-management.service.ts]
- OBJECT_STORAGE_MODE (local), OBJECT_STORAGE_BUCKET (ibp-media), ATTACHMENTS_UPLOAD_DIR (/tmp/ibp-uploads), OBJECT_STORAGE_ENDPOINT (http://localhost:9000), OBJECT_STORAGE_REGION (us-east-1), OBJECT_STORAGE_ACCESS_KEY (minio), OBJECT_STORAGE_SECRET_KEY (minio123) [storage.service.ts]
- CADASTRE_PROVIDER (synthetic), CADASTRE_PROVIDER_ALLOW_FALLBACK (true), CADASTRE_PROVIDER_TIMEOUT_MS (2500), CADASTRE_IGN_REVERSE_URL, CADASTRE_IGN_APICARTO_PARCEL_URL [cadastre-provider.service.ts]; CADASTRE_IGN_WFS_URL, CADASTRE_IGN_WFS_TYPENAME, CADASTRE_IGN_WFS_COUNT (1200, capped at 3000) [surveys.service.ts ~54-71]; copy the exact default URLs and typename from those files
- SMTP_ENABLED, SMTP_HOST, SMTP_PORT, SMTP_SECURE, SMTP_USER, SMTP_PASSWORD, SMTP_FROM, EMAIL_CHANGE_CONFIRM_URL_TEMPLATE [users/email.service.ts]
- DEBUG_DATA_RESET_ENABLED [debug/debug.service.ts]
- ACCESS_TOKEN_SECRET [auth.guard.ts, debug.controller.ts]: NOT part of the schema (D-04: removed; plan 05 replaces it with a per-process secret). Do not add REFRESH_TOKEN_SECRET, *_EXPIRES_IN or AUTH_* flags either.
New variables defined here for later plans (Claude's discretion on numbers, D-06): PG_POOL_MAX (10), PG_IDLE_TIMEOUT_MS (30000), PG_CONNECTION_TIMEOUT_MS (5000), PG_STATEMENT_TIMEOUT_MS (10000), PG_IDLE_IN_TRANSACTION_TIMEOUT_MS (60000), AUTH0_HTTP_TIMEOUT_MS (5000). Application name constant "cortege-api".

Contract to create (later plans code against it; keep these names):
- `api/src/config/config.types.ts`: `export type NodeEnv = "development" | "test" | "production"`; `export type AppConfig = { nodeEnv: NodeEnv; isProduction: boolean; http: { port: number; trustProxy: string }; cors: { mode: "any" | "none" | "list"; origins: string[] }; database: { host: string; port: number; user: string; password: string; database: string; poolMax: number; idleTimeoutMs: number; connectionTimeoutMs: number; statementTimeoutMs: number; idleInTransactionTimeoutMs: number; applicationName: string }; auth0: { domain: string; publicDomain: string; audience: string; mgmtClientId: string; mgmtClientSecret: string; appClientId: string; httpTimeoutMs: number }; storage: { mode: "local" | "minio"; bucket: string; uploadsDir: string; endpoint: string; region: string; accessKey: string; secretKey: string }; cadastre: { provider: string; allowFallback: boolean; timeoutMs: number; reverseUrl: string; apiCartoParcelUrl: string; wfsUrl: string; wfsTypename: string; wfsCount: number }; smtp: { enabled: boolean; host: string; port: number; secure: boolean; user: string; password: string; from: string; emailChangeConfirmUrlTemplate: string }; debug: { dataResetEnabled: boolean } }`. Adjust a field only if the source file reads something not listed; keep the section names.
- `cors.mode`: "none" when CORS_ORIGIN is exactly `none` (case-insensitive, trimmed); "list" when non-empty otherwise (origins = split on comma, trimmed, empties dropped); "any" when empty or absent (only reachable outside production).
- `api/src/config/env.schema.ts`: `export class EnvironmentVariables` (class-validator decorators, one property per variable, defaults as class field initialisers); `export function validateEnv(raw: Record<string, unknown>): EnvironmentVariables` = `plainToInstance(..., { enableImplicitConversion: true })` + `validateSync`; on type errors throw `Error("Configuration invalide : " + names.join(", "))` listing property names only; then, if NODE_ENV is production, call `assertProductionSafety`.
- `api/src/config/production-rules.ts`: `export type ConfigProblem = { variable: string; reason: string }` (reason is plain French, never contains the value); `export function isKnownDefaultSecret(value: string | undefined): boolean` (empty/whitespace, `ibp`, `minio`, `minio123`, or `/^change[-_]?me/i`); `export function findProductionProblems(env: Record<string, string | undefined>): ConfigProblem[]`; `export function findProductionWarnings(env): ConfigProblem[]` (AUTH0_MGMT_CLIENT_ID / AUTH0_MGMT_CLIENT_SECRET empty or change-me); `export function assertProductionSafety(env): void` (throws `Error("Configuration de production refusée : " + variable names)`).
- Production rules (RESEARCH Pattern 1 table, CONTEXT D-02/D-03/D-18): POSTGRES_HOST/PORT/USER/DB non-empty; POSTGRES_PASSWORD not a known default; OBJECT_STORAGE_MODE is local|minio; when minio: OBJECT_STORAGE_ENDPOINT non-empty and OBJECT_STORAGE_SECRET_KEY not a known default; OBJECT_STORAGE_ACCESS_KEY is NOT checked against defaults (`minio` allowed); AUTH0_DOMAIN and AUTH0_AUDIENCE non-empty; CORS_ORIGIN present and non-empty, and either `none` or a list whose every entry matches `^https?://[^/\s]+$` and does not contain `change_me`/`change-me` (case-insensitive).
- `api/src/config/app-config.ts`: `export function currentNodeEnv(env: NodeJS.ProcessEnv = process.env): NodeEnv` (reads NODE_ENV only, no validation: used at decorator time by plan 05 so a bad config fails inside ConfigModule, not at import); `export function loadAppConfig(env: NodeJS.ProcessEnv = process.env): AppConfig` (calls validateEnv, maps to AppConfig); `export const appConfig = registerAs("app", () => loadAppConfig())`; `export function appConfigOf(config: ConfigService): AppConfig` returning `config.getOrThrow<AppConfig>("app")`.
- `api/src/config/check-config.ts`: `export function runConfigCheck(env: Record<string, string | undefined>, argv: string[]): { exitCode: 0 | 1; lines: string[] }`: rules run as production when env.NODE_ENV is production or argv contains `--production`; type errors from validateEnv also become lines; output lines are French: `OK : la configuration de production est valide.`, `ERREUR : <VARIABLE> : <raison>`, `ATTENTION : <VARIABLE> : <raison>`. CLI guard `if (require.main === module)` writes lines with process.stdout.write / process.stderr.write (no console, D-06) and sets process.exitCode. It compiles to api/dist/config/check-config.js (tsconfig outDir, pattern map C-8).
- `api/test/config-helper.ts`: `export function buildTestConfig(overrides: Record<string, string | undefined> = {}): AppConfig` = `loadAppConfig({ ...process.env, NODE_ENV: "test", ...overrides })`; `export function buildTestConfigService(overrides = {}): ConfigService` = `new ConfigService({ app: buildTestConfig(overrides) })`, so `appConfigOf(service)` works in unit specs.

Current AppModule (api/src/app.module.ts:15-27): imports ThrottlerModule.forRoot(buildThrottlerOptions()), DatabaseModule, AuthModule, UsersModule, SurveysModule, ReportsModule, and DebugModule when isDebugSurfaceEnabled(). Leave those two decorator-time calls unchanged in this plan (plan 05 rewires them through currentNodeEnv).
main.ts (:1-23): `import "dotenv/config"`, NestFactory.create, configureApp, listen on PORT, `console.log` at :17 and `console.error` + `process.exit(1)` at :21.
@nestjs/config 4.0.4 behaviour (RESEARCH Pattern 1): forRoot is async; validate runs synchronously on process.env inside it; a throw rejects NestFactory.create, so main.ts's catch logs and exits 1.
debug-surface.e2e-spec.ts (:15-43) sets NODE_ENV=production inside jest.isolateModulesAsync and restores only NODE_ENV. Multi-key snapshot/restore pattern: api/test/storage.service.spec.ts:54-72.
Unit test analog for pure env functions: api/test/rate-limit.config.spec.ts.

Local E2E: PostgreSQL 16 (`pg_ctlcluster 16 main start`, role ibp/ibp). This plan's database is `ibp_p17_01_test`. Until plan 05 lands, the test-token route still needs a secret, so prefix E2E commands with `ACCESS_TOKEN_SECRET=local-e2e-only` (ignored once plan 05 removes the variable). Every E2E command runs under `flock /tmp/ibp-e2e.lock`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy check for @nestjs/config@4.0.4 and lru-cache@11</name>
  <files>(none: read-only registry checks)</files>
  <read_first>
    - .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-RESEARCH.md (Package Legitimacy Audit: both packages tagged [ASSUMED])
  </read_first>
  <action>Without installing anything, run `npm view @nestjs/config@4.0.4 name version repository.url maintainers scripts peerDependencies dependencies time.4.0.4` and `npm view lru-cache@11 name version repository.url maintainers scripts` and show the output. Present the checkpoint below and wait. This checkpoint is never auto-approved (auto_advance is ignored for package legitimacy).</action>
  <what-built>Registry metadata for the two packages this phase installs: `@nestjs/config@4.0.4` (official NestJS configuration module; 12.x is ESM-only and unusable here) and `lru-cache@11` (isaacs, already present indirectly through jwks-rsa).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@nestjs/config/v/4.0.4: publisher is the NestJS organisation, repository github.com/nestjs/config, no install script.
    2. Open https://www.npmjs.com/package/lru-cache: publisher isaacs, repository github.com/isaacs/node-lru-cache, very high weekly downloads.
    3. Compare with the `npm view` output shown above (same repository URLs, no `postinstall`/`preinstall` script).
  </how-to-verify>
  <resume-signal>Type "approved" to install both packages, or name the package you reject</resume-signal>
  <verify>
    <automated>npm view @nestjs/config@4.0.4 version && npm view lru-cache@11 version | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - The owner replied "approved" (or the rejected package is recorded and the plan stops)
  </acceptance_criteria>
  <done>Both packages are confirmed legitimate by the owner before any install.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install both packages and build the validated config module</name>
  <files>api/package.json, package-lock.json, api/src/config/config.types.ts, api/src/config/env.schema.ts, api/src/config/production-rules.ts, api/src/config/app-config.ts, api/src/config/check-config.ts, api/test/env.schema.spec.ts, api/test/check-config.spec.ts, api/test/config-helper.ts</files>
  <read_first>
    - api/src/database/database.service.ts, api/src/storage/storage.service.ts, api/src/auth/auth.guard.ts:1-50, api/src/auth/auth0-management.service.ts, api/src/surveys/cadastre-provider.service.ts:1-40, api/src/surveys/surveys.service.ts:50-75, api/src/users/email.service.ts, api/src/debug/debug.service.ts, api/src/app.setup.ts, api/src/main.ts (every current default to copy)
    - api/src/common/rate-limit.config.ts and api/test/rate-limit.config.spec.ts (pure-function-of-env style)
    - api/src/surveys/dtos/public-parcel-statuses-query.dto.ts (class-validator decorator style)
    - infra/vps/env.example, api/.env.production.example, api/.env.example (placeholder values the default detector must catch: CHANGE_ME_64_CHAR_HEX, change-me-access-secret, https://CHANGE_ME_YOUR_DOMAIN)
  </read_first>
  <behavior>
    api/test/env.schema.spec.ts (write first):
    - validateEnv({}) returns today's defaults (POSTGRES_PASSWORD "ibp", OBJECT_STORAGE_MODE "local", PORT 3000 as a number, CADASTRE_IGN_WFS_COUNT 1200, PG_STATEMENT_TIMEOUT_MS 10000)
    - validateEnv({ NODE_ENV: "test", PORT: "abc" }) throws and the message contains "PORT"
    - production with a full valid env (non-default password, AUTH0_DOMAIN, AUTH0_AUDIENCE, CORS_ORIGIN "none", OBJECT_STORAGE_MODE "minio", endpoint, secret "s3cr3t-value", OBJECT_STORAGE_ACCESS_KEY "minio") does not throw
    - production refuses, each case naming the variable and never the value: POSTGRES_PASSWORD "ibp", "", "CHANGE_ME_64_CHAR_HEX", "change-me-access-secret", "ChangeMe123"; OBJECT_STORAGE_SECRET_KEY "minio123" and "minio" in minio mode; AUTH0_AUDIENCE ""; AUTH0_DOMAIN ""; CORS_ORIGIN absent; CORS_ORIGIN ""; CORS_ORIGIN "https://CHANGE_ME_YOUR_DOMAIN"; CORS_ORIGIN "not a url"
    - production accepts CORS_ORIGIN "NONE", "https://a.example", "https://a.example, https://b.example"
    - OBJECT_STORAGE_SECRET_KEY "minio123" is accepted in production when OBJECT_STORAGE_MODE is local
    - findProductionWarnings flags empty AUTH0_MGMT_CLIENT_ID and AUTH0_MGMT_CLIENT_SECRET, and findProductionProblems does not
    - loadAppConfig maps CORS_ORIGIN: "none" → { mode: "none", origins: [] }; "https://a, https://b" → { mode: "list", origins: ["https://a", "https://b"] }; absent in test → { mode: "any", origins: [] }
    - currentNodeEnv({ NODE_ENV: "production" }) is "production" and currentNodeEnv({}) is "development", even when the rest of the env is invalid
    - a thrown error message never contains the rejected secret value (assert with a unique marker value)
    api/test/check-config.spec.ts:
    - runConfigCheck(validProdEnv, []) → exitCode 0 and a single line starting "OK :"
    - runConfigCheck(defaultsEnv, ["--production"]) → exitCode 1, one "ERREUR :" line per failing variable (POSTGRES_PASSWORD, AUTH0_DOMAIN, AUTH0_AUDIENCE, CORS_ORIGIN at least), no line contains the value "ibp" as a word
    - missing AUTH0_MGMT_* only → exitCode 0 with "ATTENTION :" lines
    - buildTestConfigService() from api/test/config-helper.ts returns a ConfigService for which appConfigOf(service).nodeEnv is "test"
  </behavior>
  <action>After the checkpoint is approved, from the repo root run `npm install --workspace api --save-exact @nestjs/config@4.0.4` and `npm install --workspace api lru-cache@^11` (pattern map C-8: both in this plan so later plans never touch the lockfile). Confirm `node -e "console.log(require('@nestjs/config/package.json').version)"` prints 4.0.4 and that its package.json has no `"type": "module"`. Then write the two specs above, run them red, and create the five files under api/src/config/ exactly as the interfaces block specifies (per D-01 class-validator only, no Joi or zod; per D-02/D-03/D-18 the production rules; per D-05 the CLI). Error and problem text names variables only, never values (secrets reach docker logs). Comments cite the decision ids. Only files under api/src/config/ may read process.env (plan 07 adds the lint rule). Create api/test/config-helper.ts per the contract.</action>
  <verify>
    <automated>cd /home/user/cortege && node -e "const p=require('./node_modules/@nestjs/config/package.json');if(p.version!=='4.0.4'||p.type==='module')process.exit(1)" && npm --workspace api run test:unit -- "env.schema|check-config" && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@nestjs/config": "4.0.4"' api/package.json` returns 1 and `grep -c '"lru-cache"' api/package.json` returns 1
    - `grep -rln "Joi\|from \"zod\"" api/src/config` returns nothing
    - `grep -c "validateSync" api/src/config/env.schema.ts` returns at least 1
    - `grep -cE "change\[-_\]\?me" api/src/config/production-rules.ts` returns at least 1
    - `grep -rn "console\." api/src/config` returns nothing
    - Every behavior bullet above is a passing test case
  </acceptance_criteria>
  <done>A typed, validated configuration exists with the production rules and a pre-flight CLI, both packages are installed at the pinned versions, and the test helper is ready for plans 05 and 06.</done>
</task>

<task type="auto">
  <name>Task 3: Register ConfigModule, move main.ts to the Logger, give the production E2E a valid env</name>
  <files>api/src/app.module.ts, api/src/main.ts, api/test/debug-surface.e2e-spec.ts</files>
  <read_first>
    - api/src/app.module.ts, api/src/main.ts
    - api/test/debug-surface.e2e-spec.ts (whole file)
    - api/test/storage.service.spec.ts:54-72 (multi-key env snapshot/restore)
    - api/src/surveys/surveys-sync.service.ts:34 (Logger convention)
  </read_first>
  <action>Per D-01, add `ConfigModule.forRoot({ isGlobal: true, ignoreEnvFile: true, cache: true, validate: validateEnv, load: [appConfig] })` as the first entry of AppModule's imports (ignoreEnvFile because main.ts already loads dotenv and the E2E loader controls precedence). Leave the ThrottlerModule and DebugModule decorator-time calls as they are. Per D-06 and pattern map C-8, in main.ts replace both console calls with `const logger = new Logger("Bootstrap")`: read the port with `appConfigOf(app.get(ConfigService)).http.port`, log the listening line with logger.log, and in the catch log only the error message (`error instanceof Error ? error.message : String(error)`) with logger.error, then keep `process.exit(1)`. In debug-surface.e2e-spec.ts, inside beforeAll before the isolated import, snapshot and then set a production-valid fake env: NODE_ENV=production, POSTGRES_PASSWORD=e2e-not-a-default-password, AUTH0_DOMAIN=e2e.example.auth0.com, AUTH0_AUDIENCE=https://e2e.example/api, CORS_ORIGIN=none, OBJECT_STORAGE_MODE=local; restore every snapshotted key (delete keys that were absent) in afterAll. Keep all its existing assertions unchanged. Then run the full E2E suite on this plan's database.</action>
  <verify>
    <automated>cd /home/user/cortege && npm run typecheck && npm run lint && npm run format:check && ACCESS_TOKEN_SECRET=local-e2e-only POSTGRES_DB=ibp_p17_01_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-01 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ConfigModule.forRoot" api/src/app.module.ts` returns 1 and `grep -c "validate: validateEnv" api/src/app.module.ts` returns 1
    - `grep -c "console\." api/src/main.ts` returns 0 and `grep -c 'new Logger("Bootstrap")' api/src/main.ts` returns 1
    - `grep -c "CORS_ORIGIN" api/test/debug-surface.e2e-spec.ts` returns at least 1
    - `cd api && npm run build && NODE_ENV=production POSTGRES_PASSWORD=ibp node dist/main.js; test $? -ne 0` succeeds (production refuses defaults) and its output names POSTGRES_PASSWORD
    - The full E2E suite passes on ibp_p17_01_test
  </acceptance_criteria>
  <done>The running API validates its configuration at startup through @nestjs/config, refuses production defaults with a plain message, and every existing E2E suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deployment env file → API process | Operator-supplied configuration, possibly left at defaults |
| npm registry → repository | Two new third-party packages |
| API logs → operator / log store | Validation errors printed at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.7-01 | Elevation of Privilege | Production start with default Postgres or MinIO secrets | mitigate | findProductionProblems refuses ibp/minio/minio123/empty/change-me* at bootstrap; unit matrix in env.schema.spec.ts |
| T-01.7-02 | Spoofing | Empty AUTH0_AUDIENCE or AUTH0_DOMAIN weakening token checks | mitigate | Both required non-empty in production |
| T-01.7-03 | Information Disclosure | Secret values echoed in startup errors | mitigate | Messages name variables only; spec asserts a unique marker value never appears |
| T-01.7-04 | Spoofing | Permissive CORS (`origin: true`) in production | mitigate | CORS_ORIGIN required in production: `none` or an explicit origin list; CHANGE_ME entries refused |
| T-01.7-SC | Tampering | npm installs of @nestjs/config and lru-cache | mitigate | Blocking human legitimacy checkpoint (Task 1) before install; exact version pin for @nestjs/config; no install scripts verified |
</threat_model>

<verification>
- Unit specs env.schema and check-config green; typecheck, lint, format green; full E2E green on ibp_p17_01_test; built API exits non-zero in production with defaults
</verification>

<success_criteria>
ROADMAP criterion 1, config half: configuration is read through @nestjs/config with a schema validated at startup, and production refuses default credentials, an empty AUTH0_AUDIENCE and a missing CORS_ORIGIN.
</success_criteria>

<output>
Create `.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-01-SUMMARY.md` when done
</output>
