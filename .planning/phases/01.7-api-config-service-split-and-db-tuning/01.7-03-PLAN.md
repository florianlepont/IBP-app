---
phase: 01.7-api-config-service-split-and-db-tuning
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - api/migrations/015_public_indexes_centroid_columns.sql
  - api/scripts/migrate.js
  - api/test/migration-015-public-indexes.e2e-spec.ts
  - api/test/migrate-lock.e2e-spec.ts
  - api/test/sync-query-budget.e2e-spec.ts
  - docs/technical/data-contract-v1.md
autonomous: true
requirements: [REQ-AUD-db-tuning, REQ-AUD-surveys-split]

must_haves:
  truths:
    - "Migration 015 creates the partial index idx_surveys_public_submitted on public, submitted, non-deleted surveys (D-13)"
    - "parcels has STORED generated columns centroid_lat/centroid_lng guarded by a nested CASE, so non-numeric or out-of-range centroids become NULL and the migration never fails on them, plus the btree idx_parcels_centroid_lat_lng; no PostGIS (D-13)"
    - "idx_survey_events_actor_id exists; idx_users_auth0_sub, idx_surveys_parcel_id and idx_survey_parcels_survey_id are gone; auth_sessions is absent after a DROP TABLE IF EXISTS safety drop (D-13)"
    - "scripts/migrate.js holds a session-level pg_advisory_lock around the migration loop and unlocks in finally; two concurrent runs on an empty database both succeed (D-14)"
    - "The query-budget E2E spec exists before any sync-path change, records the measured baseline (1 001 / 901 / 1 301 / 1 101) on the current code and asserts every result is synced (D-09: the spec is written first)"
  artifacts:
    - path: api/migrations/015_public_indexes_centroid_columns.sql
      provides: "Indexes, generated centroid columns, safety drop"
      contains: "GENERATED ALWAYS AS"
    - path: api/scripts/migrate.js
      provides: "Advisory-locked migration runner"
      contains: "pg_advisory_lock"
    - path: api/test/sync-query-budget.e2e-spec.ts
      provides: "Statement counter over one 100-op POST /v1/sync"
  key_links:
    - from: api/test/migrate-lock.e2e-spec.ts
      to: api/scripts/migrate.js
      via: "runMigrations(config) and MIGRATION_LOCK_KEY"
      pattern: "MIGRATION_LOCK_KEY"
    - from: api/test/sync-query-budget.e2e-spec.ts
      to: "pg.Client.prototype.query"
      via: "jest.spyOn"
      pattern: "spyOn\\(Client\\.prototype, \"query\"\\)"
---

<objective>
Ship migration 015 with the verified DDL, serialise the migration runner with an advisory lock, and commit the query-budget spec that measures today's statement counts for a 100-operation sync.

Purpose: REQ-AUD-db-tuning (ROADMAP criterion 5, schema half) and the D-09 "spec written first" rule for REQ-AUD-surveys-split criterion 4. Decisions D-09 (baseline), D-13 (DDL), D-14 (lock).
Output: migration 015 with a seeded-bad-row test, the lock with a concurrency test, the baseline spec, data-contract update.
The query rewrites that use the new index and columns come later (plans 09 and 10); plan 12 tightens the budget assertions to 3x.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-RESEARCH.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-PATTERNS.md
@CLAUDE.md

<interfaces>
Verified DDL (RESEARCH Pattern 6, PG 16.13) to use verbatim as the body of 015: CREATE INDEX IF NOT EXISTS idx_surveys_public_submitted ON surveys (submitted_at DESC) WHERE status = 'submitted' AND visibility = 'public' AND deleted_at IS NULL; ALTER TABLE parcels ADD COLUMN IF NOT EXISTS centroid_lat / centroid_lng double precision GENERATED ALWAYS AS (nested CASE: outer CASE on the numeric regex `'^\s*-?[0-9]+(\.[0-9]+)?([eE][-+]?[0-9]+)?\s*$'`, inner CASE on the range -90..90 / -180..180) STORED; CREATE INDEX IF NOT EXISTS idx_parcels_centroid_lat_lng ON parcels (centroid_lat, centroid_lng); CREATE INDEX IF NOT EXISTS idx_survey_events_actor_id ON survey_events (actor_id) WHERE actor_id IS NOT NULL; DROP INDEX IF EXISTS idx_users_auth0_sub, idx_surveys_parcel_id, idx_survey_parcels_survey_id (each with a comment naming what makes it redundant); DROP TABLE IF EXISTS auth_sessions CASCADE (already dropped by 011:21; idempotent safety). No `CONCURRENTLY` (the runner wraps each file in BEGIN/COMMIT). PostgreSQL does not guarantee AND evaluation order; only CASE orders the cast (RESEARCH anti-patterns).
Header style: 014_survey_events_seq_xid8.sql:1-4 ("-- Migration 015: …" plus 2-3 lines of why).
Migration test analog: api/test/migration-014-survey-events-seq.e2e-spec.ts:15-91 (scratch schema `SET search_path`, apply earlier files `< target` sorted, seed, run the target file in BEGIN/COMMIT). Its pre-migration list now ends at 014_survey_events_seq_xid8.sql. Do not edit the 014 spec.
migrate.js (:1-61, CommonJS, single quotes and semicolons: keep that style, Prettier does not check it): `runMigrations(config)` connects a Client, creates schema_migrations, loops files (re-checks applied per file, BEGIN/SQL/INSERT/COMMIT), `finally { client.end() }`; exported as `module.exports = { runMigrations }` with a `require.main === module` guard. Insert the lock right after connect and before CREATE TABLE schema_migrations; unlock inside the existing finally before client.end(), swallowing unlock errors. Export `MIGRATION_LOCK_KEY` (a constant bigint-range integer, e.g. 7017015).
Lock test design (pattern map: no analog): create a fresh database named from POSTGRES_DB by replacing the trailing `_test` with `_lock_test` (must still end in _test) through a maintenance client on `postgres`, run `Promise.allSettled([runMigrations(cfg), runMigrations(cfg)])` and assert both fulfilled and schema_migrations has every file exactly once; second case: hold `pg_advisory_lock(MIGRATION_LOCK_KEY)` on a separate client, start runMigrations on another fresh database, assert it has not finished after 300 ms, unlock, assert it completes; drop the scratch databases in afterAll (terminate their connections first).
Budget spec (RESEARCH Code Example 4, Pattern 4): boilerplate from api/test/sync-validation.e2e-spec.ts:15-75 (AppModule, configureApp, test-token login, `{ operations: [{ client_ref, entity: "survey", action: "upsert", payload }] }`, max 100 ops). Instrument: `import { Client } from "pg"`; `const spy = jest.spyOn(Client.prototype, "query")` immediately before the request and `spy.mockRestore()` in finally; count = spy.mock.calls.length (includes the one auth lookup). Four cases: 100 creates with 1 parcel each, 100 creates with 3 parcels each, then 100 updates (sync_version 2, same parcels) of each set. Baselines constant: `BASELINE = { creates1: 1001, updates1: 901, creates3: 1301, updates3: 1101 }` with a comment "measured 2026-09-25 before phase 01.7 (RESEARCH Pattern 4)". Assertions in this plan: every result status is `synced`; `count <= BASELINE[case]`; and the count is printed with `process.stdout.write` so the SUMMARY can quote it. Plan 12 tightens to `Math.floor(BASELINE[case] / 3)`.
Payload builder: copy the valid survey upsert payload from sync-validation.e2e-spec.ts (parcel ids go in the field that code path reads; check surveys.service.ts resolveSelectedParcelIds ~516-540 for the field name and the parcel id format accepted by normalizeParcelId).
Local E2E database for this plan: `ibp_p17_03_test` (the lock test creates `ibp_p17_03_lock_test` and `ibp_p17_03_lock2_test`); prefix `ACCESS_TOKEN_SECRET=local-e2e-only`; always under `flock /tmp/ibp-e2e.lock`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 015 with a seeded bad-row test</name>
  <files>api/migrations/015_public_indexes_centroid_columns.sql, api/test/migration-015-public-indexes.e2e-spec.ts, docs/technical/data-contract-v1.md</files>
  <read_first>
    - api/test/migration-014-survey-events-seq.e2e-spec.ts (whole file)
    - api/migrations/014_survey_events_seq_xid8.sql:1-4, 011_auth0_migration.sql (idx_users_auth0_sub, auth_sessions drop), 008_parcels_and_versioning.sql, 009_survey_parcels.sql
    - docs/technical/data-contract-v1.md (parcels, surveys, survey_events and index sections)
  </read_first>
  <behavior>
    In a scratch schema after migrations 001-014 with seeded parcels whose centroid is {"lat":"abc","lng":2}, {}, {"lat":95,"lng":2}, {"lat":" 48.5 ","lng":" 2.25 "} and {"lat":48.1,"lng":-1.7}:
    - applying 015 succeeds; centroid_lat is NULL, NULL, NULL, 48.5, 48.1 respectively; centroid_lng is 2, NULL, 2, 2.25, -1.7
    - `information_schema.columns` reports is_generated = 'ALWAYS' for both columns
    - `UPDATE parcels SET centroid = '{"lat":47,"lng":3}'` recomputes both columns
    - pg_indexes: idx_surveys_public_submitted has the WHERE predicate on status, visibility and deleted_at; idx_parcels_centroid_lat_lng and idx_survey_events_actor_id exist; idx_users_auth0_sub, idx_surveys_parcel_id, idx_survey_parcels_survey_id do not
    - `to_regclass('auth_sessions')` is NULL; applying 015 a second time does not fail (idempotent)
  </behavior>
  <action>Per D-13, write the migration with the verified DDL from the interfaces block and a header in the 014 style (what, why, and "no PostGIS: generated columns plus btree, owner decision"). Write the spec first, modelled on the 014 spec with SCRATCH_SCHEMA "mig015_scratch". Update docs/technical/data-contract-v1.md: the two generated parcel columns (NULL when the centroid is missing, non-numeric or out of range), the new indexes, and the dropped redundant ones.</action>
  <verify>
    <automated>cd /home/user/cortege && ACCESS_TOKEN_SECRET=local-e2e-only POSTGRES_DB=ibp_p17_03_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-03 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e -- "migration-01[45]" && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GENERATED ALWAYS AS" api/migrations/015_public_indexes_centroid_columns.sql` returns 2
    - `grep -ci "postgis\|CONCURRENTLY" api/migrations/015_public_indexes_centroid_columns.sql` counts only the header comment (`grep -v '^--' api/migrations/015_public_indexes_centroid_columns.sql | grep -ci "postgis\|concurrently"` returns 0)
    - `grep -c "centroid_lat" docs/technical/data-contract-v1.md` returns at least 1
    - migration-014 and migration-015 specs pass
  </acceptance_criteria>
  <done>The schema carries the public index, the guarded generated columns and the actor index, the redundant indexes and auth_sessions are gone, and bad centroid rows cannot break the deploy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Advisory lock in the migration runner</name>
  <files>api/scripts/migrate.js, api/test/migrate-lock.e2e-spec.ts</files>
  <read_first>
    - api/scripts/migrate.js (whole file)
    - api/test/global-setup.js (maintenance client, CREATE DATABASE, runMigrations(config) call shape)
    - api/test/e2e-env.js (resolveDbConfig)
  </read_first>
  <behavior>
    - Two concurrent runMigrations on a fresh empty *_lock_test database both fulfil, and schema_migrations lists each .sql file exactly once
    - While another session holds pg_advisory_lock(MIGRATION_LOCK_KEY), runMigrations on a fresh database has not resolved after 300 ms and resolves after the lock is released
    - After runMigrations resolves, no advisory lock with that key remains in pg_locks
  </behavior>
  <action>Per D-14, add the session-level lock and unlock to migrate.js exactly as described in the interfaces block (the image CMD runs migrate.js and the VPS README documents a manual run, so the two can overlap; one file per transaction means the lock must be session-level). Keep the file's single-quote/semicolon style. Export MIGRATION_LOCK_KEY. Write the lock spec first; before adding the lock, run it once and note in the SUMMARY that the concurrent case fails on the old runner (RESEARCH reproduced "duplicate key value violates unique constraint pg_type_typname_nsp_index").</action>
  <verify>
    <automated>cd /home/user/cortege && ACCESS_TOKEN_SECRET=local-e2e-only POSTGRES_DB=ibp_p17_03_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-03 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e -- migrate-lock && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pg_advisory_lock" api/scripts/migrate.js` returns 1 and `grep -c "pg_advisory_unlock" api/scripts/migrate.js` returns 1
    - `grep -c "MIGRATION_LOCK_KEY" api/scripts/migrate.js` returns at least 2
    - migrate-lock spec passes three runs in a row, and `psql -h localhost -U ibp -d postgres -Atc "SELECT count(*) FROM pg_database WHERE datname LIKE '%lock%_test'"` returns 0 afterwards
  </acceptance_criteria>
  <done>Overlapping migration runs are serialised by PostgreSQL and both succeed.</done>
</task>

<task type="auto">
  <name>Task 3: Query-budget spec recording the baseline (written first, D-09)</name>
  <files>api/test/sync-query-budget.e2e-spec.ts</files>
  <read_first>
    - api/test/sync-validation.e2e-spec.ts:15-121 (bootstrap, payload, op shape, batch cap)
    - api/src/surveys/surveys.service.ts:143-200 and :516-564 (upsert entry, parcel id field and resolution)
  </read_first>
  <action>Per D-09, create the spec exactly as the interfaces block describes: four `it` cases in order (creates 1 parcel, updates 1 parcel, creates 3 parcels, updates 3 parcels), each counting `Client.prototype.query` calls around one POST /v1/sync of 100 upserts, asserting all 100 results are `synced` and `count <= BASELINE[case]`, and printing `query-budget <case>: <count>`. Use distinct survey id prefixes per run so a re-run on the same database never collides. Run it and record the four printed counts in the SUMMARY; if a count differs from the RESEARCH baseline by more than 2, record the measured value and the reason (the constant stays the RESEARCH figure, which is the phase's reference).</action>
  <verify>
    <automated>cd /home/user/cortege && ACCESS_TOKEN_SECRET=local-e2e-only POSTGRES_DB=ibp_p17_03_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-03 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'spyOn(Client.prototype, "query")' api/test/sync-query-budget.e2e-spec.ts` returns at least 1
    - `grep -cE "1001|901|1301|1101" api/test/sync-query-budget.e2e-spec.ts` returns at least 4
    - The spec's printed counts are recorded in 01.7-03-SUMMARY.md
    - The full E2E suite passes on ibp_p17_03_test
  </acceptance_criteria>
  <done>The phase has a committed, reproducible statement counter and a confirmed baseline before any sync-path change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy → database schema | The image runs migrations at start; the operator may run them by hand |
| Stored parcel data → migration | Existing centroid JSON of unknown quality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.7-09 | Tampering | Concurrent migration runs half-applying the schema | mitigate | Session-level pg_advisory_lock; concurrency E2E |
| T-01.7-10 | Denial of Service | Bad centroid row aborting migration 015 at deploy | mitigate | Nested CASE guard; spec seeds non-numeric, empty and out-of-range rows |
| T-01.7-11 | Denial of Service | Dropping an index still needed | mitigate | Only the three verified-redundant indexes (RESEARCH EXPLAIN proof); their covering indexes stay |
| T-01.7-12 | Information Disclosure | Leftover auth_sessions tokens table | mitigate | DROP TABLE IF EXISTS auth_sessions CASCADE with a to_regclass test |
</threat_model>

<verification>
- migration-014, migration-015, migrate-lock and the full E2E suite green on ibp_p17_03_test; lint and format green
</verification>

<success_criteria>
ROADMAP criterion 5 schema items exist (partial index, generated columns with btree, advisory lock, auth_sessions dropped) and the D-09 baseline is committed as a spec.
</success_criteria>

<output>
Create `.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-03-SUMMARY.md` when done
</output>
