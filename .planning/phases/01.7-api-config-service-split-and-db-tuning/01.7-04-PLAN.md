---
phase: 01.7-api-config-service-split-and-db-tuning
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/docker-compose.yml
  - infra/docker-compose.vps.yml
  - infra/vps/README.md
autonomous: true
requirements: [REQ-AUD-config]

must_haves:
  truths:
    - "infra/docker-compose.yml and infra/docker-compose.vps.yml run MinIO from pgsty/minio pinned by the same digest as CI (D-16)"
    - "A MinIO /data volume written by the previous MinIO release starts under the pinned pgsty/minio image with its bucket and objects intact (checked locally with Docker, D-16)"
    - "infra/vps/README.md gives the owner copy-paste commands to back up the MinIO volume before the merge, and explains that the image switch happens by itself at deploy (D-16, D-19)"
  artifacts:
    - path: infra/docker-compose.vps.yml
      provides: "Pinned pgsty/minio image for production"
      contains: "pgsty/minio:RELEASE.2026-08-04T00-00-00Z@sha256:b6bfe7239bfc83fb90d31612d9704d86039dd714f7904b3f1ad68f211e602372"
    - path: infra/vps/README.md
      provides: "MinIO volume backup and restore, image switch note"
  key_links:
    - from: infra/docker-compose.vps.yml
      to: .github/workflows/ci.yml
      via: "identical pinned digest"
      pattern: "sha256:b6bfe7239bfc83fb90d31612d9704d86039dd714f7904b3f1ad68f211e602372"
---

<objective>
Move the local and VPS compose files to the pinned `pgsty/minio` image used by CI, prove locally with Docker that an existing MinIO volume survives the switch, and document the owner's backup commands.

Purpose: STATE todo (quay.io/minio answers 401, Docker Hub minio/minio is gone, so a fresh host or an image prune breaks storage). Decisions D-16, D-19 (the switch happens at deploy, so the backup comes before the merge).
Output: two compose files, README section, and a recorded volume-compatibility check.
Plan 07 adds the config pre-flight section to the same README afterwards; keep your section self-contained.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-PATTERNS.md
@CLAUDE.md

<interfaces>
Pinned image (ci.yml:349, comment style ci.yml:341-343): `pgsty/minio:RELEASE.2026-08-04T00-00-00Z@sha256:b6bfe7239bfc83fb90d31612d9704d86039dd714f7904b3f1ad68f211e602372`.
Lines to replace: infra/docker-compose.yml:16-18 and infra/docker-compose.vps.yml:30-34 (comment + `image: quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z`). Keep `command: server /data --console-address ":9001"`, the env, volumes, ports and mem_limit untouched.
VPS facts: the stack runs from the git checkout /home/ubuntu/cortege with `docker compose -f infra/docker-compose.vps.yml --env-file /home/ubuntu/cortege.env`; the MinIO volume is declared as `cortege_minio_data` and Compose prefixes it with the project name (the directory of the compose file, so most likely `infra_cortege_minio_data`): README commands must discover it with `docker volume ls -q | grep 'cortege_minio_data$'`, never hard-code the prefix. update-stack.sh fast-forwards the checkout before `compose up -d`, so after the merge the next deploy recreates MinIO with the new image automatically (D-19); `compose up -d` pulls a missing image by default.
README style: command blocks as in infra/vps/README.md:60-66; the stop-timer / act / restart structure of the rollback and restore sections (:88-131).
Docker in the sandbox: start the daemon with `nohup dockerd > /tmp/dockerd.log 2>&1 &` and wait until `docker info` succeeds. Old image: first try `docker pull quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z` (or a cached copy in `docker image ls`); if it is not pullable, list the fork's tags (`curl -s "https://hub.docker.com/v2/repositories/pgsty/minio/tags?page_size=100"`) and use the oldest RELEASE.2025-* tag available as the writer (D-16).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin pgsty/minio in both compose files and document the owner backup</name>
  <files>infra/docker-compose.yml, infra/docker-compose.vps.yml, infra/vps/README.md</files>
  <read_first>
    - infra/docker-compose.yml, infra/docker-compose.vps.yml
    - .github/workflows/ci.yml:338-370 (pinned image and its comment)
    - infra/vps/README.md (whole file)
  </read_first>
  <action>Per D-16, replace the MinIO image line in both compose files with the pinned image from the interfaces block, with a two-line comment in the ci.yml style (upstream MinIO OSS is archived, quay.io answers 401 and Docker Hub minio/minio is gone; pgsty/minio is the maintained fork CI already uses; pinned by digest). In infra/vps/README.md add a section "MinIO: sauvegarde du volume et passage à pgsty/minio" (French headings and prose, commands unchanged) with: (1) why the image changes and that the switch happens automatically at the first deploy after the merge (D-19), so the backup must be taken before merging; (2) the backup commands: discover the volume name with `docker volume ls -q | grep 'cortege_minio_data$'`, stop MinIO with the compose command, archive the volume with `docker run --rm -v "$VOL":/data:ro -v /home/ubuntu:/backup alpine tar czf /backup/minio-data-$(date +%F).tgz -C /data .`, start MinIO again, and check the archive size with `ls -lh`; (3) a restore procedure (stop minio, clear and untar into the volume, start); (4) the post-deploy health check (`curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:9000/minio/health/live` returns 200, and `docker compose … ps minio` shows the pgsty image).</action>
  <verify>
    <automated>cd /home/user/cortege && test "$(grep -c 'pgsty/minio:RELEASE.2026-08-04T00-00-00Z@sha256:b6bfe7239bfc83fb90d31612d9704d86039dd714f7904b3f1ad68f211e602372' infra/docker-compose.yml infra/docker-compose.vps.yml | awk -F: '{s+=$2} END {print s}')" = 2 && ! grep -q "quay.io/minio" infra/docker-compose.yml infra/docker-compose.vps.yml && MINIO_SECRET_KEY=x POSTGRES_PASSWORD=x OBJECT_STORAGE_ENDPOINT=x docker compose -f infra/docker-compose.vps.yml config -q 2>/dev/null || python3 -c "import yaml;yaml.safe_load(open('infra/docker-compose.yml'));yaml.safe_load(open('infra/docker-compose.vps.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - Both compose files contain the pinned digest exactly once and no `quay.io/minio`
    - `grep -c "cortege_minio_data\$" infra/vps/README.md` returns at least 1 and `grep -c "tar czf" infra/vps/README.md` returns at least 1
    - Both YAML files parse
  </acceptance_criteria>
  <done>Both stacks use the maintained, pinned MinIO image, and the owner has the backup and restore commands.</done>
</task>

<task type="auto">
  <name>Task 2: Prove locally that an existing volume survives the switch</name>
  <files>(none: Docker check, evidence recorded in the SUMMARY)</files>
  <read_first>
    - infra/docker-compose.yml (the new minio service)
  </read_first>
  <action>Per D-16, start dockerd if needed. Create a fresh named volume `p17-minio-compat`. Run the old image (quay.io release if pullable, otherwise the oldest pgsty RELEASE.2025-* tag, per the interfaces block) on it with root user minio / minio123 on host port 19000; create a bucket and upload three objects of known content (use the `mc` client from the same image via `docker exec … mc alias set local http://127.0.0.1:9000 minio minio123`, or the AWS CLI if present), record their md5 sums, stop and remove the container. Start the pinned pgsty/minio image on the same volume, then list the bucket and download the three objects: names, sizes and md5 sums must match; also check `/minio/health/live` returns 200. Write the before and after listings with md5 sums to /tmp/p17-minio-compat.log and end it with the line `RESULT: identical` only when names, sizes and sums all match (otherwise `RESULT: MISMATCH`). Remove the containers and the volume. Copy the log into the SUMMARY and record there the exact writer image and tag, the commands and the before/after listings. If the objects do not survive, stop and report: the owner must decide before any VPS change.</action>
  <verify>
    <automated>! docker volume ls -q | grep -q '^p17-minio-compat$' && grep -q "RESULT: identical" /tmp/p17-minio-compat.log</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY shows identical object names, sizes and md5 sums before and after the image switch, with the writer image named
    - The scratch volume and containers are removed
  </acceptance_criteria>
  <done>The VPS MinIO data is known to survive the automatic image switch at deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container registry → VPS | The MinIO image pulled at deploy |
| Existing object data → new image | Volume format compatibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.7-13 | Tampering | Mutable MinIO tag replaced upstream | mitigate | Image pinned by sha256 digest, identical to CI |
| T-01.7-14 | Denial of Service | Storage down after the switch (volume incompatibility) | mitigate | Local volume-survival check (Task 2) and a mandatory owner backup before the merge (README, D-19) |
| T-01.7-15 | Repudiation | Lost objects without a restore point | mitigate | Documented tar backup and restore procedure |
</threat_model>

<verification>
- Pinned digest in both compose files; YAML valid; local Docker volume-survival check recorded
</verification>

<success_criteria>
The MinIO STATE todo is closed: both compose files use the maintained pinned image, and existing data is shown to survive the switch.
</success_criteria>

<output>
Create `.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-04-SUMMARY.md` when done
</output>
