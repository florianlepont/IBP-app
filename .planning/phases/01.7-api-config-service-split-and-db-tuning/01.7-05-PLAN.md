---
phase: 01.7-api-config-service-split-and-db-tuning
plan: 05
type: execute
wave: 2
depends_on: [01.7-01]
files_modified:
  - api/src/auth/auth.guard.ts
  - api/src/debug/test-token-secret.ts
  - api/src/debug/debug.controller.ts
  - api/src/debug/debug.service.ts
  - api/src/debug/debug-gating.ts
  - api/src/common/rate-limit.config.ts
  - api/src/app.setup.ts
  - api/src/app.module.ts
  - api/test/auth.guard.spec.ts
  - api/test/debug-surface.spec.ts
  - api/test/debug.service.spec.ts
  - api/test/app-setup.spec.ts
  - api/test/rate-limit.config.spec.ts
autonomous: true
requirements: [REQ-AUD-config]

must_haves:
  truths:
    - "ACCESS_TOKEN_SECRET is no longer read: the HS256 test path and POST /v1/debug/test-token share a random secret generated once per process, only when NODE_ENV=test (D-04)"
    - "Every E2E suite still obtains its bearer token from /v1/debug/test-token with no secret in the environment (D-04)"
    - "A failed authentication logs one Logger.warn line with the error name, message and code only: no token, no header, no stack, no error object (D-06)"
    - "Auth0 /userinfo is fetched with AbortSignal.timeout(AUTH0_HTTP_TIMEOUT_MS) and the body is read inside the timed region (D-06)"
    - "CORS_ORIGIN=none sends no Access-Control-Allow-Origin to any browser origin; an explicit list allows exactly those origins; credentials are off (D-03)"
    - "AuthGuard, DebugController, DebugService, app.setup, debug gating and throttling read configuration through ConfigService or src/config helpers, never process.env (D-01)"
  artifacts:
    - path: api/src/debug/test-token-secret.ts
      provides: "getTestTokenSecret(nodeEnv): per-process random HS256 secret, null outside test"
    - path: api/src/auth/auth.guard.ts
      provides: "Config-driven guard with message+code failure log and timed userinfo"
      contains: "AbortSignal.timeout"
  key_links:
    - from: api/src/auth/auth.guard.ts
      to: api/src/debug/test-token-secret.ts
      via: "getTestTokenSecret"
      pattern: "getTestTokenSecret\\("
    - from: api/src/debug/debug.controller.ts
      to: api/src/debug/test-token-secret.ts
      via: "same per-process secret signs the test token"
      pattern: "getTestTokenSecret\\("
    - from: api/src/app.setup.ts
      to: api/src/config/app-config.ts
      via: "appConfigOf(app.get(ConfigService)).cors"
      pattern: "appConfigOf\\("
---

<objective>
Move the authentication, debug and HTTP-setup code onto the validated configuration: remove `ACCESS_TOKEN_SECRET` in favour of a per-process test secret, log auth failures with message and code only, time out Auth0 `/userinfo`, and implement `CORS_ORIGIN=none`.

Purpose: REQ-AUD-config, ROADMAP criteria 1 (ACCESS_TOKEN_* gone, CORS required) and 2 (Logger, failure log). Decisions D-01, D-03, D-04, D-06.
Output: config-driven guard, debug surface, CORS and throttling, with their specs.
Runs in parallel with plan 06 (disjoint files). Env examples, CI and docs are plan 07.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-PATTERNS.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 01 (api/src/config/):
- `export type AppConfig` with sections nodeEnv, isProduction, http { port, trustProxy }, cors { mode: "any" | "none" | "list"; origins }, auth0 { domain, publicDomain, audience, mgmtClientId, mgmtClientSecret, appClientId, httpTimeoutMs }, debug { dataResetEnabled }, …
- `currentNodeEnv(env = process.env): NodeEnv` (NODE_ENV only, no validation: for decorator-time callers)
- `appConfigOf(config: ConfigService): AppConfig`
- ConfigModule is global (isGlobal), so ConfigService is injectable anywhere.
From api/test/config-helper.ts: `buildTestConfig(overrides)`, `buildTestConfigService(overrides)`.

Current code:
- auth.guard.ts:14-20 module-level consts from process.env (AUTH0_DOMAIN, AUTH0_PUBLIC_DOMAIN fallback, AUTH0_AUDIENCE, JWKS domains, accepted issuers); constructor(db) builds JwksClient per domain (:38-48); canActivate: `if (process.env.NODE_ENV === "test")` HS256 branch (:60-64); failure `console.error("[AuthGuard] Token validation failed:", err)` (:73); verifyTestToken reads ACCESS_TOKEN_SECRET (:78-81); fetchUserInfo (:214-227) fetches `https://${AUTH0_PUBLIC_DOMAIN}/userinfo` without timeout and returns `response.json()` un-awaited.
- debug.controller.ts:15-19: `if (process.env.NODE_ENV !== "test") throw new ForbiddenException()`, then ACCESS_TOKEN_SECRET check; jwt.sign(..., secret, { algorithm: "HS256", expiresIn: "1h" }) at :38-41 stays.
- debug.service.ts:72 `DEBUG_DATA_RESET_ENABLED` via process.env.
- debug-gating.ts: `isDebugSurfaceEnabled(env: NodeJS.ProcessEnv = process.env)` = `env.NODE_ENV !== "production"`; its doc comment lists the three defence layers (keep it, updated to the new names).
- rate-limit.config.ts: `resolveThrottleLimit(kind, env = process.env)` evaluated lazily per request from `buildThrottlerOptions()`.
- app.setup.ts: trust proxy from TRUST_PROXY; `enableCors({ origin: corsOrigin ? list : true, methods, credentials: true })`. configureApp(app) is called by main.ts and several E2E specs with an app built from AppModule: read config with `app.get(ConfigService)` inside it and keep the single-parameter signature.
- app.module.ts (after plan 01): ConfigModule first; `ThrottlerModule.forRoot(buildThrottlerOptions())` and `...(isDebugSurfaceEnabled() ? [DebugModule] : [])`.
Failure log format (RESEARCH Code Example 3): `this.logger.warn(\`Token validation failed: ${name ?? "Error"}: ${message ?? "unknown"}${code ? \` (code=${code})\` : ""}\`)` with name/message/code read defensively from the unknown error. Nothing else is passed to the logger.
Test secret module style: debug/debug-gating.ts (doc comment citing the decision, one exported function). `randomBytes` from "crypto".
Specs constructing these classes (pattern map "Specs that construct services directly"): auth.guard.spec.ts:20 and :42-48, :96-97 (ACCESS_TOKEN_SECRET set/deleted); debug-surface.spec.ts:59-75 (ACCESS_TOKEN_SECRET, console.error spy at :73 → switch to Logger.prototype.warn); debug.service.spec.ts:19. The two E2E specs that built the guard by hand (auth-provisioning, auth-profile) were already moved to `app.get(...)` by plan 01 Task 3, so this plan does not touch them and plan 06 can change DatabaseService in parallel.
Logger spy convention: `jest.spyOn(Logger.prototype, "warn").mockImplementation(() => undefined)`.
No model identifiers in code, VALIDATION.md or PR bodies; commit trailers use the attribution lines the orchestrator supplies.
Local E2E database: `ibp_p17_05_test`; after this plan the E2E needs no ACCESS_TOKEN_SECRET, so run the final E2E WITHOUT it (proof of D-04); always under `flock /tmp/ibp-e2e.lock`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-process test secret, config-driven guard, message-and-code failure log, timed userinfo</name>
  <files>api/src/debug/test-token-secret.ts, api/src/auth/auth.guard.ts, api/src/debug/debug.controller.ts, api/test/auth.guard.spec.ts, api/test/debug-surface.spec.ts</files>
  <read_first>
    - api/src/auth/auth.guard.ts (whole file)
    - api/src/debug/debug.controller.ts, api/src/debug/debug-gating.ts
    - api/test/auth.guard.spec.ts, api/test/debug-surface.spec.ts
  </read_first>
  <behavior>
    - getTestTokenSecret("test") returns the same non-empty string on every call; getTestTokenSecret("production") and ("development") return null
    - AuthGuard in test mode accepts an HS256 token signed with getTestTokenSecret("test") and rejects one signed with any other secret (401)
    - AuthGuard built with nodeEnv "production" never takes the HS256 branch: an HS256 token is rejected and db.query is not called (existing debug-surface.spec case, now without ACCESS_TOKEN_SECRET)
    - On an invalid token, Logger.prototype.warn is called once with a single string argument that contains "Token validation failed", contains the error message, contains "(code=ERR_X)" when the error has code ERR_X, and does not contain the raw token, "Bearer" or "    at " (no stack); console.error is never called
    - ForbiddenException from provisioning still propagates as 403 and is not logged as a failure
    - fetchUserInfo passes an AbortSignal to fetch and the body read is covered: with httpTimeoutMs 50 (buildTestConfigService({ AUTH0_HTTP_TIMEOUT_MS: "50" })) and a fetch mock that resolves ok headers whose `json()` returns a promise rejecting with the signal's reason when `init.signal` fires the "abort" event (a plain never-resolving json() would hang forever, because a mock ignores the signal), the call rejects within 1 s with a TimeoutError; alternatively a local http.createServer that writes headers and never ends the body, with the real global fetch. Use the real AbortSignal.timeout, no fake timers
    - JWKS domains and accepted issuers come from config: AUTH0_PUBLIC_DOMAIN empty falls back to AUTH0_DOMAIN
  </behavior>
  <action>Per D-04, create api/src/debug/test-token-secret.ts exporting `getTestTokenSecret(nodeEnv: string): string | null`, which lazily creates `randomBytes(32).toString("hex")` once per module instance and returns it only when nodeEnv is "test"; doc comment: why (no shared or guessable secret, no env var; E2E and the owner-check simulation mint through the same process), and that jest.isolateModulesAsync gets its own secret, which is fine because the app and the guard share a registry. In auth.guard.ts per D-01 inject `ConfigService` as a second constructor parameter, compute domain, public domain, audience, JWKS domains, accepted issuers, nodeEnv and httpTimeoutMs in the constructor from `appConfigOf(config)`, and delete the module-level process.env consts. The HS256 branch runs only when nodeEnv is "test" and verifies with getTestTokenSecret(nodeEnv) (throw "test token secret unavailable" if null). Per D-06 add `private readonly logger = new Logger(AuthGuard.name)` and replace the console.error with the message+code warn from the interfaces block. fetchUserInfo: `fetch(url, { headers, signal: AbortSignal.timeout(httpTimeoutMs) })`, check ok, then `return (await response.json()) as …` inside the same function. In debug.controller.ts inject ConfigService, keep the `nodeEnv !== "test"` ForbiddenException, and sign with getTestTokenSecret. Update both specs: construct `new AuthGuard(db, buildTestConfigService(...))`, sign with getTestTokenSecret("test"), replace the "ACCESS_TOKEN_SECRET is not set" case with the production-null case, switch the console.error spy to Logger.prototype.warn. Write the new cases first.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- "auth.guard|debug-surface" && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "ACCESS_TOKEN_SECRET" api/src api/test` returns nothing
    - `grep -c "process.env" api/src/auth/auth.guard.ts api/src/debug/debug.controller.ts | awk -F: '{s+=$2} END {print s}'` prints 0
    - `grep -c "console\." api/src/auth/auth.guard.ts` returns 0
    - `grep -c "AbortSignal.timeout" api/src/auth/auth.guard.ts` returns 1
    - `grep -c "randomBytes" api/src/debug/test-token-secret.ts` returns 1
    - Every behavior bullet is a passing test case
  </acceptance_criteria>
  <done>The test-token path works with no secret in the environment, auth failures leak nothing, and Auth0 calls cannot hang.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CORS none/list, trust proxy, debug gating and throttling from config</name>
  <files>api/src/app.setup.ts, api/src/app.module.ts, api/src/debug/debug-gating.ts, api/src/debug/debug.service.ts, api/src/common/rate-limit.config.ts, api/test/app-setup.spec.ts, api/test/rate-limit.config.spec.ts, api/test/debug.service.spec.ts</files>
  <read_first>
    - api/src/app.setup.ts, api/src/app.module.ts, api/src/debug/debug-gating.ts, api/src/debug/debug.service.ts, api/src/common/rate-limit.config.ts
    - api/test/app-setup.spec.ts, api/test/rate-limit.config.spec.ts, api/test/debug.service.spec.ts
  </read_first>
  <behavior>
    - app-setup: an app built from a small testing module (ConfigModule.forRoot with `load: [() => ({ app: buildTestConfig(overrides) })]` plus a dummy GET controller) and configureApp: with CORS_ORIGIN "none", a GET and an OPTIONS preflight carrying `Origin: https://evil.example` return no Access-Control-Allow-Origin header; with "https://a.example", Origin https://a.example gets ACAO https://a.example and Origin https://b.example gets none; with the test default (mode any) the origin is reflected as today; Access-Control-Allow-Credentials is never "true"
    - trust proxy equals TRUST_PROXY when set and "loopback,uniquelocal" by default
    - resolveThrottleLimit("default", "production") returns the production limit and ("default", "test") the non-production one; isDebugSurfaceEnabled("production") is false and ("test") true
    - DebugService with debug.dataResetEnabled false throws ForbiddenException; with true it proceeds (debug.service.spec)
  </behavior>
  <action>Per D-03, in configureApp read `const cfg = appConfigOf(app.get(ConfigService))`; set trust proxy from cfg.http.trustProxy; enable CORS with `origin: false` when cfg.cors.mode is "none", the origin array when "list", and `true` when "any" (only reachable outside production); `credentials: false` (no cookies anywhere, Bearer auth only; RESEARCH Code Example 2); keep methods, helmet, prefix and the ValidationPipe exactly as they are. Add a comment citing D-03 (the mobile app is native and needs no CORS). Per D-01 and pattern map C-3, change `isDebugSurfaceEnabled(nodeEnv: string = currentNodeEnv())` and `resolveThrottleLimit(kind, nodeEnv: string = currentNodeEnv())` (keep the lazy per-request evaluation in buildThrottlerOptions), keep the debug-gating doc comment's three layers with the new names (getTestTokenSecret, config nodeEnv), and in app.module call the helpers without process.env. DebugService injects ConfigService and reads debug.dataResetEnabled. Update the three specs (new cases first). This is the plan's last task: after the targeted unit specs, run the full E2E suite with `env -u ACCESS_TOKEN_SECRET`.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- "app-setup|rate-limit.config|debug.service" && npm run typecheck && npm run lint && npm run format:check && env -u ACCESS_TOKEN_SECRET POSTGRES_DB=ibp_p17_05_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-05 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "process.env" api/src/app.setup.ts api/src/app.module.ts api/src/debug/debug-gating.ts api/src/debug/debug.service.ts api/src/common/rate-limit.config.ts | awk -F: '{s+=$2} END {print s}'` prints 0
    - `grep -c "credentials: false" api/src/app.setup.ts` returns 1 and `grep -c "credentials: true" api/src/app.setup.ts` returns 0
    - The CORS cases (none, list, any, credentials) pass
    - The full E2E suite passes on ibp_p17_05_test with ACCESS_TOKEN_SECRET unset (proof of D-04: tokens come from the per-process secret)
  </acceptance_criteria>
  <done>Production CORS follows CORS_ORIGIN strictly, and all bootstrap-time readers go through src/config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → AuthGuard | Untrusted bearer tokens |
| API → Auth0 /userinfo | External HTTP dependency |
| Browser origin → API | Cross-origin requests |
| API → logs | Failure details |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.7-16 | Spoofing | Shared or guessable HS256 test secret | mitigate | Per-process randomBytes(32) secret, returned only when nodeEnv is test; DebugModule absent in production (unchanged gate) |
| T-01.7-17 | Elevation of Privilege | HS256 branch reachable outside test | mitigate | Branch keyed on config nodeEnv === "test"; unit case with nodeEnv production rejects HS256 and never queries the DB |
| T-01.7-18 | Information Disclosure | Tokens, emails from pg detail, stacks in auth failure logs | mitigate | Single warn string with name, message and code only; unit asserts no token, no "Bearer", no stack frame |
| T-01.7-19 | Denial of Service | Stalled Auth0 /userinfo body pinning requests | mitigate | AbortSignal.timeout covering headers and body; unit with a never-resolving json() |
| T-01.7-20 | Spoofing | Cross-origin credentialed requests | mitigate | origin false for `none`, explicit list otherwise, credentials false; preflight unit cases |
</threat_model>

<verification>
- Unit specs green per task; full E2E green on ibp_p17_05_test without ACCESS_TOKEN_SECRET at the end of Task 2; lint, typecheck, format green
</verification>

<success_criteria>
ROADMAP criterion 1 (ACCESS_TOKEN_* gone from code, CORS strict) and criterion 2 (auth failure logs message and code only, Logger) hold for the auth, debug and HTTP-setup surface.
</success_criteria>

<output>
Create `.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-05-SUMMARY.md` when done
</output>
