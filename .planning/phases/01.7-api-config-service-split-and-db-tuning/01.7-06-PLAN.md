---
phase: 01.7-api-config-service-split-and-db-tuning
plan: 06
type: execute
wave: 2
depends_on: [01.7-01]
files_modified:
  - api/src/database/database.service.ts
  - api/src/storage/storage.service.ts
  - api/src/auth/auth0-management.service.ts
  - api/src/users/email.service.ts
  - api/src/surveys/cadastre-provider.service.ts
  - api/src/surveys/surveys.service.ts
  - api/test/database.service.spec.ts
  - api/test/storage.service.spec.ts
  - api/test/auth0-management.service.spec.ts
  - api/test/cadastre-provider.service.spec.ts
  - api/test/database-transaction.e2e-spec.ts
autonomous: true
requirements: [REQ-AUD-config]

must_haves:
  truths:
    - "The pg pool is created with max, idleTimeoutMillis, connectionTimeoutMillis, statement_timeout (default 10 s), idle_in_transaction_session_timeout and application_name from config (D-06)"
    - "pool.on('error') is registered and logs the message and code through the Nest Logger without crashing the process (D-06)"
    - "A statement longer than PG_STATEMENT_TIMEOUT_MS fails with SQLSTATE 57014 on a real database (D-06)"
    - "DatabaseService, StorageService, Auth0ManagementService, EmailService, CadastreProviderService and SurveysService read configuration through ConfigService; none reads process.env (D-01)"
    - "Missing AUTH0_MGMT_* in production produces a startup Logger warning and account deletion degrades as today (D-02)"
  artifacts:
    - path: api/src/database/database.service.ts
      provides: "Bounded pool with an error listener"
      contains: "statement_timeout"
  key_links:
    - from: api/src/database/database.service.ts
      to: api/src/config/app-config.ts
      via: "appConfigOf(config).database"
      pattern: "appConfigOf\\("
    - from: api/src/database/database.service.ts
      to: "pg Pool error event"
      via: "this.pool.on(\"error\", …)"
      pattern: "pool\\.on\\(\"error\""
---

<objective>
Bound and harden the PostgreSQL pool, and move every remaining service-level `process.env` read onto ConfigService: database, storage, Auth0 management, email, cadastre provider and the WFS settings in SurveysService's constructor.

Purpose: REQ-AUD-config, ROADMAP criterion 2 (pool with max, idleTimeoutMillis, statement_timeout and an error listener) and the D-01 "every process.env read" rule. Decisions D-01, D-02 (MGMT warning), D-06 (pool).
Output: config-driven services, pool options and listener, a real-DB statement-timeout proof.
SurveysService: only its constructor's WFS settings change here; this plan is the first link of the strict surveys.service.ts chain (06 → 08 events → 09 parcels → 10 public map → 11 pagination → 12 upsert budget). Runs in parallel with plan 05 (disjoint files).
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-RESEARCH.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-PATTERNS.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 01: `appConfigOf(config: ConfigService): AppConfig` with sections database { host, port, user, password, database, poolMax, idleTimeoutMs, connectionTimeoutMs, statementTimeoutMs, idleInTransactionTimeoutMs, applicationName }, storage { mode, bucket, uploadsDir, endpoint, region, accessKey, secretKey }, auth0 { mgmtClientId, mgmtClientSecret, appClientId, domain, … }, smtp { … }, cadastre { provider, allowFallback, timeoutMs, reverseUrl, apiCartoParcelUrl, wfsUrl, wfsTypename, wfsCount }, isProduction. Test helper: `buildTestConfigService(overrides)` in api/test/config-helper.ts (merges process.env, forces NODE_ENV test).
Pool options (RESEARCH Pattern 2; node-postgres accepts client options on Pool): `{ host, port, user, password, database, max: poolMax, idleTimeoutMillis: idleTimeoutMs, connectionTimeoutMillis: connectionTimeoutMs, statement_timeout: statementTimeoutMs, idle_in_transaction_session_timeout: idleInTransactionTimeoutMs, application_name: applicationName }`. connectionTimeoutMillis matters: pg's default 0 waits forever on exhaustion. A 57014 is classified retryable by mapSyncError (not class 22/23), which is correct.
Error listener: `this.pool.on("error", (err) => this.logger.error(\`idle client error: ${err.message}${code ? \` (code=${code})\` : ""}\`))` with `private readonly logger = new Logger(DatabaseService.name)`. Keep `transaction()` (:40-57) untouched: it is the 01.4 D-06 primitive and plan 12's fallback path.
Current env reads to replace: database.service.ts:17-21; storage.service.ts:46-69; auth0-management.service.ts:14-16, 86, 111-112; users/email.service.ts:19-32, 85; cadastre-provider.service.ts:25-36; surveys.service.ts:54-71 (WFS url, typename, count with the 3000 cap, timeout).
D-02 warning: Auth0ManagementService constructor logs `this.logger.warn("AUTH0_MGMT_CLIENT_ID / AUTH0_MGMT_CLIENT_SECRET absent : la suppression de compte côté Auth0 est désactivée")` when isProduction and either value is empty or change-me (use `isKnownDefaultSecret` from api/src/config/production-rules.ts).
Specs constructing these services (pattern map table): database.service.spec.ts:30,45,67,86,98 (mocked pg.Pool: add `on: jest.fn()`; assert `(Pool as jest.Mock).mock.calls[0][0]`); storage.service.spec.ts:54-72 (buildService mutates env: switch to buildTestConfigService overrides); auth0-management.service.spec.ts:5; cadastre-provider.service.spec.ts:26,82,125,138,167 (env snapshot + global.fetch swap: switch to overrides). users.service.spec.ts passes mocks and needs no change.
E2E analog for statement timeout: api/test/database-transaction.e2e-spec.ts (raw DatabaseService use against the _test DB).
Local E2E database: `ibp_p17_06_test`; prefix `ACCESS_TOKEN_SECRET=local-e2e-only` (plan 05 runs in parallel); always under `flock /tmp/ibp-e2e.lock`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bounded pool with an error listener, proven on a real database</name>
  <files>api/src/database/database.service.ts, api/test/database.service.spec.ts, api/test/database-transaction.e2e-spec.ts</files>
  <read_first>
    - api/src/database/database.service.ts
    - api/test/database.service.spec.ts, api/test/database-transaction.e2e-spec.ts
  </read_first>
  <behavior>
    - Unit: Pool is constructed once with max 10, idleTimeoutMillis 30000, connectionTimeoutMillis 5000, statement_timeout 10000, idle_in_transaction_session_timeout 60000, application_name "cortege-api" by default, and with PG_POOL_MAX "4" / PG_STATEMENT_TIMEOUT_MS "250" overrides the values follow
    - Unit: an "error" listener is registered; invoking it with `Object.assign(new Error("boom"), { code: "57P01" })` calls Logger.prototype.error once with a string containing "boom" and "57P01" and does not throw
    - Unit: existing query/transaction cases still pass (transaction unchanged)
    - E2E: a DatabaseService built with buildTestConfigService({ PG_STATEMENT_TIMEOUT_MS: "300" }) answers `SHOW statement_timeout` with "300ms" and `SELECT pg_sleep(1)` rejects with code 57014; with defaults `SHOW statement_timeout` is "10s"; `SHOW application_name` is "cortege-api"; the service is closed with onModuleDestroy
  </behavior>
  <action>Per D-06 and D-01, give DatabaseService a `ConfigService` constructor parameter and build the Pool from `appConfigOf(config).database` with the options in the interfaces block; register the error listener with the Logger (message and code only). Add a comment citing D-06: why connectionTimeoutMillis and statement_timeout are bounded, and that 57014 is retryable in sync. Update the unit spec (mock gains `on`) and add the E2E describe block "pool limits" to database-transaction.e2e-spec.ts. Tests first.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- database.service && ACCESS_TOKEN_SECRET=local-e2e-only POSTGRES_DB=ibp_p17_06_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-06 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e -- database-transaction && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "statement_timeout|idleTimeoutMillis|connectionTimeoutMillis|max:" api/src/database/database.service.ts` returns at least 4
    - `grep -c 'pool.on("error"' api/src/database/database.service.ts` returns 1
    - `grep -c "process.env" api/src/database/database.service.ts` returns 0
    - The 57014 E2E case passes
  </acceptance_criteria>
  <done>The pool can no longer hang on exhaustion, runaway statements are cut at 10 s, and an idle-client error is logged instead of crashing the process.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Storage, Auth0 management, email, cadastre and SurveysService read ConfigService</name>
  <files>api/src/storage/storage.service.ts, api/src/auth/auth0-management.service.ts, api/src/users/email.service.ts, api/src/surveys/cadastre-provider.service.ts, api/src/surveys/surveys.service.ts, api/test/storage.service.spec.ts, api/test/auth0-management.service.spec.ts, api/test/cadastre-provider.service.spec.ts</files>
  <read_first>
    - api/src/storage/storage.service.ts:40-75, api/src/auth/auth0-management.service.ts, api/src/users/email.service.ts, api/src/surveys/cadastre-provider.service.ts:1-60, api/src/surveys/surveys.service.ts:40-75
    - api/test/storage.service.spec.ts, api/test/auth0-management.service.spec.ts, api/test/cadastre-provider.service.spec.ts
  </read_first>
  <behavior>
    - storage.service.spec: every existing case passes with the service built from buildTestConfigService overrides instead of env mutation (local and minio modes, bucket, endpoint)
    - auth0-management.service.spec: existing cases pass; with nodeEnv production and an empty AUTH0_MGMT_CLIENT_SECRET, construction logs one Logger warn naming AUTH0_MGMT_CLIENT_ID / AUTH0_MGMT_CLIENT_SECRET and does not throw; outside production no warning
    - cadastre-provider.service.spec: every existing case passes with overrides (CADASTRE_PROVIDER "ign", fallback, timeout) instead of env mutation
  </behavior>
  <action>Per D-01, give each of StorageService, Auth0ManagementService, EmailService, CadastreProviderService and SurveysService a `ConfigService` constructor parameter and read their values from `appConfigOf(config)` sections, deleting every `process.env` read and every inline default (defaults now live in the schema). In SurveysService change only the constructor's WFS fields (url, typename, count, timeout) to come from `cadastre`; touch nothing else in that file. Per D-02 add the production-only MGMT warning to Auth0ManagementService (the feature degrades exactly as today). EmailService is not registered in any module (deferred to 01.9); still convert it so no src file reads process.env. Update the three specs to build services from `buildTestConfigService(overrides)`; keep their assertions.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit:coverage && npm run typecheck && npm run lint && test "$(grep -rn 'process\.env' api/src --include=*.ts | grep -v '^api/src/config/' | grep -v '^api/src/main.ts' | grep -vE '^api/src/(auth/auth\.guard|debug/|app\.setup|app\.module|common/rate-limit\.config)' | wc -l)" = 0 && ACCESS_TOKEN_SECRET=local-e2e-only POSTGRES_DB=ibp_p17_06_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-06 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "process.env" api/src/storage/storage.service.ts api/src/auth/auth0-management.service.ts api/src/users/email.service.ts api/src/surveys/cadastre-provider.service.ts api/src/surveys/surveys.service.ts | awk -F: '{s+=$2} END {print s}'` prints 0
    - The unit coverage run meets every existing threshold in api/jest.unit.config.js (no threshold lowered)
    - The full E2E suite passes on ibp_p17_06_test
  </acceptance_criteria>
  <done>Every service reads validated configuration; together with plan 05, no file outside src/config and main.ts reads process.env.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → PostgreSQL | Shared pool under load |
| Environment → services | Configuration values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.7-21 | Denial of Service | Pool exhaustion hanging every request | mitigate | connectionTimeoutMillis 5 s, max explicit; unit asserts the options |
| T-01.7-22 | Denial of Service | Runaway or leaked statements and transactions | mitigate | statement_timeout 10 s and idle_in_transaction_session_timeout 60 s; E2E proves 57014 |
| T-01.7-23 | Denial of Service | Idle client error crashing the process | mitigate | pool.on("error") logs through Logger; unit invokes the handler |
| T-01.7-24 | Information Disclosure | Pool error logs carrying connection details | mitigate | Only err.message and err.code are logged |
| T-01.7-25 | Repudiation | Silent loss of Auth0 account deletion in production | accept | D-02: warning only, behaviour unchanged; warning visible in docker logs |
</threat_model>

<verification>
- Unit with coverage green (thresholds unchanged), full E2E green on ibp_p17_06_test, lint, typecheck, format green
</verification>

<success_criteria>
ROADMAP criterion 2 pool items hold (max, idleTimeoutMillis, statement_timeout, error listener), and services read configuration only through ConfigService.
</success_criteria>

<output>
Create `.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-06-SUMMARY.md` when done
</output>
