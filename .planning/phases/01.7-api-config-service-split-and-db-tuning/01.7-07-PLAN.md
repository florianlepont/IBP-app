---
phase: 01.7-api-config-service-split-and-db-tuning
plan: 07
type: execute
wave: 3
depends_on: [01.7-01, 01.7-04, 01.7-05, 01.7-06]
files_modified:
  - api/.eslintrc.json
  - .github/workflows/ci.yml
  - infra/vps/check-env.sh
  - infra/vps/update-stack.sh
  - infra/vps/README.md
  - api/test/vps-deploy-guard.spec.ts
  - api/test/check-env-parity.spec.ts
  - api/.env.example
  - api/.env.production.example
  - infra/.env.example
  - infra/vps/env.example
  - CLAUDE.md
  - README.md
  - api/README.md
autonomous: true
requirements: [REQ-AUD-config]

must_haves:
  truths:
    - "ESLint forbids process.env in api/src outside src/config/** and src/main.ts, and forbids console in api/src (D-01, D-06)"
    - "infra/vps/update-stack.sh runs `compose run --rm --no-deps api node api/dist/config/check-config.js` on the freshly pulled image before `compose up -d`; on failure it logs the report and exits without restarting, so the old stack keeps serving (D-18)"
    - "A deploy refused by the guard is retried on the next timer run once the env file is fixed: the decision compares the running container's image with the pulled image, not only before/after pull"
    - "infra/vps/check-env.sh is dependency-free, never sources the env file, applies the same production rules to /home/ubuntu/cortege.env as the API (compose mapping included) and prints plain French lines; a parity spec proves identical verdicts on a fixture matrix (D-05, D-18)"
    - "CI proves on the built image that NODE_ENV=production with default credentials exits non-zero for both main.js and check-config.js, and that check-config.js exits 0 on a valid production env (D-05)"
    - "REFRESH_TOKEN_SECRET, ACCESS_TOKEN_SECRET, ACCESS_TOKEN_EXPIRES_IN, REFRESH_TOKEN_EXPIRES_IN, AUTH_DEV_EXPOSE_EMAIL_TOKEN and AUTH_LOGIN_OR_CREATE_ENABLED appear nowhere in code, env examples, CI, README, api/README or CLAUDE.md (D-04)"
    - "The production env examples set CORS_ORIGIN=none and document the pool variables (D-03, D-06)"
  artifacts:
    - path: infra/vps/check-env.sh
      provides: "Pre-merge env check the owner runs on the VPS"
    - path: infra/vps/update-stack.sh
      provides: "Deploy guard before compose up -d"
      contains: "check-config.js"
    - path: api/test/vps-deploy-guard.spec.ts
      provides: "Stubbed run of update-stack.sh proving the guard"
  key_links:
    - from: infra/vps/update-stack.sh
      to: api/src/config/check-config.ts
      via: "compose run --rm --no-deps api node api/dist/config/check-config.js"
      pattern: "run --rm --no-deps api node api/dist/config/check-config.js"
    - from: api/test/check-env-parity.spec.ts
      to: infra/vps/check-env.sh
      via: "spawnSync bash on fixture env files"
      pattern: "check-env.sh"
---

<objective>
Close the configuration work end to end: enforce "configuration only through src/config" with ESLint, add the deploy guard to `update-stack.sh` and the dependency-free pre-merge check for the owner, prove both with tests, add the production-refusal smoke to CI, and remove the dead token variables from every env example, CI job and doc.

Purpose: REQ-AUD-config, ROADMAP criterion 1 (dead secrets gone, production refuses defaults, provable on the image). Decisions D-01 (enforcement), D-03, D-04 (removal from examples, CI, docs, CLAUDE.md), D-05 (pre-flight and CI smoke), D-06 (no console), D-17 (owner's single visit relies on check-env.sh), D-18 (deploy guard).
Output: lint rule, guard, pre-merge check, two specs, CI steps, updated examples and docs.
Owns ci.yml, update-stack.sh, env examples, CLAUDE.md, README.md and api/README.md for this phase; infra/vps/README.md already has plan 04's MinIO section, add a separate section.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-PATTERNS.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-01-SUMMARY.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-05-SUMMARY.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-06-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 01: `runConfigCheck(env, argv)` and the CLI `node api/dist/config/check-config.js [--production]` (exit 1 on any problem, French "ERREUR : <VAR> : <raison>" lines, "ATTENTION :" for AUTH0_MGMT_*); `findProductionProblems(env)`, `findProductionWarnings(env)`, `isKnownDefaultSecret(value)` in api/src/config/production-rules.ts.
VPS compose (infra/docker-compose.vps.yml): api gets `env_file: /home/ubuntu/cortege.env` plus an `environment:` block that wins over it: NODE_ENV production, PORT 3000, POSTGRES_HOST postgres, POSTGRES_PORT 5432, OBJECT_STORAGE_MODE minio, OBJECT_STORAGE_BUCKET ${OBJECT_STORAGE_BUCKET:-cortege-media}, OBJECT_STORAGE_ENDPOINT ${OBJECT_STORAGE_ENDPOINT:?}, OBJECT_STORAGE_REGION, OBJECT_STORAGE_ACCESS_KEY ${MINIO_ACCESS_KEY:-minio}, OBJECT_STORAGE_SECRET_KEY ${MINIO_SECRET_KEY:?}. check-env.sh must apply this mapping before the rules (pattern map C-1: only compose reproduces the real env).
update-stack.sh today (:31-58): fetch + `merge --ff-only`; `before` = image id of ghcr.io/florianlepont/cortege:latest; `compose pull --quiet api`; `after`; `if [ "$before" = "$after" ]; then log "image unchanged, nothing to do"; exit 0; fi`; `compose up -d`; 12 health attempts; failure path logs docker logs. Log style `log "…"`. The script is refreshed from git on every run, so the guard lands with the image that needs it (D-18).
New decision logic (fixes a retry gap the guard would otherwise create): after the pull, `running="$(docker inspect --format '{{.Image}}' cortege-api 2>/dev/null || echo none)"`; skip only when `running = after`. Then the guard: `log "checking configuration against the new image"`; `if ! compose run --rm --no-deps api node api/dist/config/check-config.js; then log "ERROR: configuration check failed; the stack was NOT restarted and the previous API keeps serving. Fix $ENV_FILE and wait for the next run (or: sudo systemctl start cortege-deploy.service)"; exit 1; fi`; then `compose up -d` as today. `compose run` prints the check's French lines into the journal.
check-env.sh contract: `bash check-env.sh [/path/to/cortege.env]` (default /home/ubuntu/cortege.env); `set -euo pipefail`; parse `KEY=VALUE` lines with a while-read loop (skip blanks and `#` comments, strip one pair of surrounding single or double quotes, never `source`/`eval`); build the effective API env with the compose mapping above; rules identical to findProductionProblems (POSTGRES_PASSWORD not default; OBJECT_STORAGE_SECRET_KEY from MINIO_SECRET_KEY not default; OBJECT_STORAGE_ENDPOINT non-empty; AUTH0_DOMAIN and AUTH0_AUDIENCE non-empty; CORS_ORIGIN present, and `none` or a list of http(s) origins without change_me); default detection: empty, ibp, minio, minio123, or case-insensitive prefix change-me/change_me/changeme; warnings for AUTH0_MGMT_CLIENT_ID/SECRET; also an "INFO : ligne inutile, peut être supprimée" line for each of the six dead variables if present (never an error). Output: one French line per problem `ERREUR : <VAR> : <raison>`, final `OK : …` or `À corriger avant la fusion : N problème(s).`; exit 0 or 1. It must never print a value.
Owner command (pre-merge, works whether the repo is public or private because the VPS clone can fetch): `git -C /home/ubuntu/cortege fetch -q origin <branch> && git -C /home/ubuntu/cortege show origin/<branch>:infra/vps/check-env.sh | bash -s -- /home/ubuntu/cortege.env`.
Deploy-guard spec design (api/test/vps-deploy-guard.spec.ts, Jest unit; bash is available on CI runners): create a temp dir with an empty `.git` directory (REPO_DIR), an env file (ENV_FILE), and a `bin/` of stub executables put first on PATH: `git` (exit 0), `curl` (prints 200), `docker` that appends its argv to a calls log and answers `image inspect` with ids from files (`before`, `after`), `inspect --format {{.Image}} cortege-api` with the running id, `compose … run …` with exit code from STUB_CHECK_EXIT, and `compose … pull|up|logs|image prune` with 0. Run `bash infra/vps/update-stack.sh` with REPO_DIR, ENV_FILE, HEALTH_URL set. Cases: (a) new image, check fails → exit 1, log contains "NOT restarted", calls log has the `run --rm --no-deps api node api/dist/config/check-config.js` call and no `up -d`; (b) new image, check passes → `up -d` called after the check, exit 0; (c) running image equals pulled image → "nothing to do", no check, no up; (d) retry: before equals after (already pulled by a refused run) but running differs → check runs again. The spec skips (with a clear message) only if bash is absent.
Parity spec design (api/test/check-env-parity.spec.ts): fixtures written to a temp dir: a valid VPS env (non-default passwords, AUTH0 set, CORS_ORIGIN=none, endpoint), then single-fault variants (POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD, MINIO_SECRET_KEY=minio123, AUTH0_AUDIENCE empty, CORS_ORIGIN empty, CORS_ORIGIN absent, CORS_ORIGIN=https://CHANGE_ME_YOUR_DOMAIN, a quoted value, a comment line, MINIO_ACCESS_KEY=minio which must pass). For each, compare `spawnSync("bash", ["infra/vps/check-env.sh", file])` exit status and the set of variables named on ERREUR lines against `findProductionProblems(composeMapped(fixture))`; both must agree. Also assert the script output never contains the fixture's secret values.
CI (ci.yml): remove the six dead variables at :212-217 (e2e), :306-311 (e2e-minio) and :509-514 (health smoke). After "Smoke test — container boots and answers health" add "Smoke test — production refuses default configuration": `set +e`; `timeout 60 docker run --rm -e NODE_ENV=production -e POSTGRES_PASSWORD=ibp -e OBJECT_STORAGE_MODE=minio -e OBJECT_STORAGE_SECRET_KEY=minio123 cortege:ci node api/dist/main.js > refusal.log 2>&1`; capture code; `set -e`; fail unless code is non-zero and not 124; `grep -q POSTGRES_PASSWORD refusal.log`; then the same env with `node api/dist/config/check-config.js` must exit 1; then a valid fake production env (non-default password and secret, AUTH0_DOMAIN, AUTH0_AUDIENCE, CORS_ORIGIN=none, OBJECT_STORAGE_ENDPOINT) with check-config.js must exit 0. Override the CMD (the default CMD runs migrate.js first and would mask the result, pattern map C-8). Keep pinned action SHAs; actionlint runs in CI.
ESLint (api/.eslintrc.json, existing `overrides` block): add to top-level rules `"no-restricted-properties": ["error", { "object": "process", "property": "env", "message": "Read configuration through ConfigService or src/config (phase 01.7 D-01)" }]` and `"no-console": "error"`; add an override for `["src/config/**/*.ts", "src/main.ts", "test/**/*.ts"]` turning `no-restricted-properties` off, and for `["test/**/*.ts"]` turning `no-console` off.
Dead-variable grep gate (RESEARCH Validation map): `! grep -rnE "REFRESH_TOKEN_SECRET|ACCESS_TOKEN_(SECRET|EXPIRES_IN)|REFRESH_TOKEN_EXPIRES_IN|AUTH_DEV_EXPOSE_EMAIL_TOKEN|AUTH_LOGIN_OR_CREATE_ENABLED" --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=coverage --exclude-dir=.planning --exclude-dir=.git --exclude-dir=audits --exclude=check-env.sh --exclude=check-env-parity.spec.ts .` (check-env.sh and its spec name the dead variables on purpose, to tell the owner the lines can go).
Docs: CLAUDE.md:286 and README.md:85 (`ACCESS_TOKEN_SECRET`, `REFRESH_TOKEN_SECRET` row) are removed; CLAUDE.md:293 and README.md:93 CORS rows say "required in production: `none` (no browser origin) or a comma-separated origin list; startup refuses otherwise"; add one row for the optional `PG_POOL_MAX`, `PG_IDLE_TIMEOUT_MS`, `PG_CONNECTION_TIMEOUT_MS`, `PG_STATEMENT_TIMEOUT_MS`, `PG_IDLE_IN_TRANSACTION_TIMEOUT_MS`, `AUTH0_HTTP_TIMEOUT_MS` with defaults; one line under CI/CD in CLAUDE.md that update-stack.sh checks the configuration before restarting. api/README.md:20 drops the AUTH_DEV_EXPOSE_EMAIL_TOKEN mention. The full docs sweep is phase 01.9: change only these rows.
Docker for local proof: `nohup dockerd > /tmp/dockerd.log 2>&1 &`, then `docker build -f api/Dockerfile -t cortege:ci .` from the repo root.
Local E2E database: `ibp_p17_07_test`; no ACCESS_TOKEN_SECRET (plan 05 removed it); always under `flock /tmp/ibp-e2e.lock`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ESLint enforcement and the CI production-refusal smoke</name>
  <files>api/.eslintrc.json, .github/workflows/ci.yml</files>
  <read_first>
    - api/.eslintrc.json, .eslintrc.json (root)
    - .github/workflows/ci.yml (e2e, e2e-minio and image-check jobs, :179-535)
    - api/Dockerfile (CMD)
  </read_first>
  <action>Per D-01 and D-06, add the ESLint rules and overrides from the interfaces block and fix anything the new rules flag (there should be nothing left after plans 05 and 06; if a src file still reads process.env or console, move it onto src/config or the Logger in that file and record it as a deviation). Per D-04 remove the six dead variables from the three ci.yml places. Per D-05 add the production-refusal smoke step exactly as described. Build the image locally with Docker and run the three smoke commands by hand to prove them (record the exit codes in the SUMMARY).</action>
  <verify>
    <automated>cd /home/user/cortege && npm run lint && python3 -c "import yaml;yaml.safe_load(open('.github/workflows/ci.yml'))" && ! grep -nE "ACCESS_TOKEN|REFRESH_TOKEN|AUTH_DEV_EXPOSE|AUTH_LOGIN_OR_CREATE" .github/workflows/ci.yml && grep -c "production refuses default configuration" .github/workflows/ci.yml && test "$(grep -rn 'process\.env' api/src --include=*.ts | grep -v '^api/src/config/' | grep -v '^api/src/main.ts' | wc -l)" = 0 && test "$(grep -rn 'console\.' api/src --include=*.ts | wc -l)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 with the new rules active (`grep -c "no-restricted-properties" api/.eslintrc.json` returns at least 2)
    - ci.yml has no dead variable and contains the new smoke step with `node api/dist/main.js` and `node api/dist/config/check-config.js`
    - The SUMMARY records the three local smoke exit codes (non-zero, 1, 0) on a locally built image
  </acceptance_criteria>
  <done>"Configuration only through src/config" and "Logger only" are mechanically enforced, and CI proves the image refuses production defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deploy guard, pre-merge env check and their tests</name>
  <files>infra/vps/check-env.sh, infra/vps/update-stack.sh, infra/vps/README.md, api/test/vps-deploy-guard.spec.ts, api/test/check-env-parity.spec.ts</files>
  <read_first>
    - infra/vps/update-stack.sh, infra/docker-compose.vps.yml, infra/vps/env.example, infra/vps/README.md (including plan 04's MinIO section)
    - api/src/config/production-rules.ts, api/src/config/check-config.ts
  </read_first>
  <behavior>
    - vps-deploy-guard.spec cases (a) to (d) from the interfaces block
    - check-env-parity.spec: for every fixture, check-env.sh's exit status and ERREUR variable set equal findProductionProblems on the compose-mapped env; MINIO_ACCESS_KEY=minio passes; no secret value appears in the output
    - check-env.sh on infra/vps/env.example (CHANGE_ME placeholders, empty CORS) exits 1 naming POSTGRES_PASSWORD, MINIO_SECRET_KEY or OBJECT_STORAGE_SECRET_KEY, and CORS_ORIGIN
  </behavior>
  <action>Per D-18, rewrite the decision block of update-stack.sh (running-image comparison) and insert the guard exactly as described in the interfaces block; keep everything else, including the health loop and prune. Per D-05/D-17/D-18, create infra/vps/check-env.sh per the contract (executable bit set with `git update-index --chmod=+x` is not needed if you `chmod +x` the file; the owner runs it through `bash -s`). In infra/vps/README.md add a section "Vérification de la configuration (avant fusion et à chaque déploiement)" in French: what the API now refuses in production, the one owner command (interfaces block), how the deploy guard behaves (old stack keeps serving, journal message, automatic retry after fixing the file: `journalctl -u cortege-deploy -n 50`). Write both specs first.</action>
  <verify>
    <automated>cd /home/user/cortege && bash -n infra/vps/check-env.sh && bash -n infra/vps/update-stack.sh && npm --workspace api run test:unit -- "vps-deploy-guard|check-env-parity" && ! grep -nE "^\s*(source|\.|eval)\s" infra/vps/check-env.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "run --rm --no-deps api node api/dist/config/check-config.js" infra/vps/update-stack.sh` returns 1, and the line number of that call is lower than the line number of `compose up -d`
    - `grep -c "NOT restarted" infra/vps/update-stack.sh` returns 1
    - `grep -c "check-env.sh" infra/vps/README.md` returns at least 1
    - Both specs pass, including the retry case (d)
  </acceptance_criteria>
  <done>A bad production env can no longer take the API down on deploy, and the owner can check the VPS env before merging with one command whose verdict matches the API's.</done>
</task>

<task type="auto">
  <name>Task 3: Remove dead variables from env examples and docs, set CORS_ORIGIN=none</name>
  <files>api/.env.example, api/.env.production.example, infra/.env.example, infra/vps/env.example, CLAUDE.md, README.md, api/README.md</files>
  <read_first>
    - api/.env.example, api/.env.production.example, infra/.env.example, infra/vps/env.example
    - CLAUDE.md (Environment variables section and CI/CD), README.md (env table), api/README.md
  </read_first>
  <action>Per D-04 delete the six dead variables and their comments from the four env examples. Per D-03 set `CORS_ORIGIN=none` in infra/vps/env.example and api/.env.production.example with a comment ("none = aucune origine navigateur ; l'app mobile n'utilise pas CORS ; vide ou absent = démarrage refusé en production"); leave dev examples empty (allowed outside production). Per D-06 add the optional PG_* and AUTH0_HTTP_TIMEOUT_MS lines, commented out with their defaults, to api/.env.example and infra/vps/env.example. Update CLAUDE.md, README.md and api/README.md exactly as the interfaces block lists (only those rows). Then run the dead-variable grep gate, the full unit suite with coverage and the full E2E suite.</action>
  <verify>
    <automated>cd /home/user/cortege && ! grep -rnE "REFRESH_TOKEN_SECRET|ACCESS_TOKEN_(SECRET|EXPIRES_IN)|REFRESH_TOKEN_EXPIRES_IN|AUTH_DEV_EXPOSE_EMAIL_TOKEN|AUTH_LOGIN_OR_CREATE_ENABLED" --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=coverage --exclude-dir=.planning --exclude-dir=.git --exclude-dir=audits --exclude=check-env.sh --exclude=check-env-parity.spec.ts . && grep -q "^CORS_ORIGIN=none" infra/vps/env.example && grep -q "^CORS_ORIGIN=none" api/.env.production.example && npm run lint && npm run typecheck && npm --workspace api run test:unit:coverage && npm run format:check && POSTGRES_DB=ibp_p17_07_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-07 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - The dead-variable grep gate finds nothing
    - Both production examples contain `CORS_ORIGIN=none`
    - `grep -c "PG_STATEMENT_TIMEOUT_MS" CLAUDE.md` returns at least 1
    - Unit (with coverage thresholds) and full E2E green on ibp_p17_07_test
  </acceptance_criteria>
  <done>No trace of the dead token settings remains outside the audit history, and every example and doc row matches the new configuration rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry image → VPS stack | Pull-based deploy restarts the stack |
| Owner shell → env file | Secrets on the VPS |
| Branch content → VPS shell | check-env.sh fetched from the phase branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.7-26 | Denial of Service | New fail-fast rules taking production down at deploy | mitigate | Guard runs check-config.js on the new image before `up -d`; refusal keeps the old stack; retry after fix (spec cases a-d) |
| T-01.7-27 | Information Disclosure | Env check printing secrets into terminals or journald | mitigate | Both checks print variable names only; parity spec asserts no value leaks |
| T-01.7-28 | Tampering / Elevation of Privilege | check-env.sh executing env-file content | mitigate | Line parser only, no source/eval (grep gate); fetched with `git show` from the owner's own clone |
| T-01.7-29 | Repudiation | Unproven production refusal | mitigate | CI smoke on the built image, three exit-code assertions |
| T-01.7-30 | Spoofing | Leftover shared token secrets in examples | mitigate | Dead variables removed everywhere; grep gate |
</threat_model>

<verification>
- Lint (new rules), typecheck, unit with coverage, format, full E2E on ibp_p17_07_test all green; local image smoke exit codes recorded
</verification>

<success_criteria>
ROADMAP criterion 1 fully holds (configuration only through @nestjs/config, production refusal proven on the image, dead secrets gone), and a bad VPS env can no longer cause an outage (D-18).
</success_criteria>

<output>
Create `.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-07-SUMMARY.md` when done
</output>
