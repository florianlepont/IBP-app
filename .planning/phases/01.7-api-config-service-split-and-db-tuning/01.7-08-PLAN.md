---
phase: 01.7-api-config-service-split-and-db-tuning
plan: 08
type: execute
wave: 3
depends_on: [01.7-05, 01.7-06]
files_modified:
  - api/src/surveys/surveys.repository.ts
  - api/src/surveys/survey-events.service.ts
  - api/src/surveys/surveys-data.module.ts
  - api/src/surveys/surveys.service.ts
  - api/src/surveys/surveys-attachments.service.ts
  - api/src/surveys/surveys.module.ts
  - api/src/surveys/surveys.controller.ts
  - api/src/reports/reports.service.ts
  - api/src/reports/reports.module.ts
  - api/test/surveys-attachments-download.spec.ts
  - api/test/survey-events.service.spec.ts
  - api/test/surveys.repository.spec.ts
autonomous: true
requirements: [REQ-AUD-surveys-split]

must_haves:
  truths:
    - "Survey events are written by exactly one method, SurveyEventsService.insert(db, surveyId, actorId: string | null, type, payload); the copies in surveys.service.ts, surveys-attachments.service.ts and the inline insert in reports.service.ts are gone (D-07)"
    - "Ownership lookups go through exactly one SurveysRepository.findOwned(db, id, userId, { activeOnly, forUpdate, columns }) whose 'ownership' mode selects id, user_id, status, visibility, sync_version, deleted_at only (D-07, ROADMAP criterion 4)"
    - "SurveysRepository and SurveyEventsService live in an exported SurveysDataModule imported by SurveysModule and ReportsModule (D-07)"
    - "Reported events still carry actor_id NULL (01.2 A-M6)"
    - "Every route and response shape is unchanged: the full E2E suite passes with no spec edits (D-07)"
  artifacts:
    - path: api/src/surveys/survey-events.service.ts
      provides: "insert, listForSurvey, SURVEY_EVENT_INSERT_SQL"
    - path: api/src/surveys/surveys.repository.ts
      provides: "findOwned, findOwnedOrThrow, getSurveyParcelIds, syncSurveyParcels"
    - path: api/src/surveys/surveys-data.module.ts
      provides: "Exported data-access module"
  key_links:
    - from: api/src/reports/reports.service.ts
      to: api/src/surveys/survey-events.service.ts
      via: "events.insert(db, surveyId, null, \"reported\", …)"
      pattern: "insert\\([^)]*null"
    - from: api/src/surveys/surveys-attachments.service.ts
      to: api/src/surveys/surveys.repository.ts
      via: "findOwned with columns ownership"
      pattern: "columns: \"ownership\""
---

<objective>
First extraction of the SurveysService split: one event writer (`SurveyEventsService`), one column-scoped ownership lookup (`SurveysRepository.findOwned`), both exported from a small `SurveysDataModule`, with every duplicate removed.

Purpose: REQ-AUD-surveys-split, ROADMAP criteria 3 (`getSurveyForUser` and `insertEvent` exist once) and 4 (column-scoped ownership checks). Decision D-07.
Output: three new files, rewired services and modules, unit specs for the new units.
Second link of the surveys.service.ts chain (after plan 06). Pagination of the events list is plan 11; this plan moves `getEvents` as is.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-RESEARCH.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-PATTERNS.md
@CLAUDE.md

<interfaces>
Duplicates to collapse (pattern map):
- `insertEvent(db: Queryable, surveyId, actorId: string, eventType, payload)` at surveys.service.ts:1615-1627 and surveys-attachments.service.ts:56-68: `INSERT INTO survey_events (id, survey_id, actor_id, event_type, payload) VALUES ($1, $2, $3, $4, $5::jsonb)` with randomUUID() and JSON.stringify(payload). Never name seq/xid8 (column defaults, migration 014).
- inline copy at reports.service.ts:57-61 with no actor_id (reported events carry no reporter identity).
- `getSurveyForUser(db, surveyId, userId, activeOnly, { forUpdate })` with `SELECT *` at surveys.service.ts:1596-1613 and surveys-attachments.service.ts:38-54; `getSurveyForUserOrThrow` (NotFoundException("Survey not found")) at :1583-1594 and :25-36.
Call sites to rewire: events at surveys.service.ts:248, 387, 500, 686, 695, 760, 951, 993, 1086; surveys-attachments.service.ts:148, 242, 293; reports.service.ts:57. Ownership at surveys.service.ts:193, 266, 347, 458 (upsert: full), :572 (patch: full), :716 (visibility: ownership), :838 (getById: full), :891 (submit: full), :1041 (delete: ownership, activeOnly false), :1112 (events: ownership); surveys-attachments.service.ts:122, 175, 264, 326, 398, 419 (ownership). Line numbers are pre-plan-06; re-locate by name.
Contract to create:
- survey-events.service.ts: `export const SURVEY_EVENT_INSERT_SQL = "INSERT INTO survey_events (id, survey_id, actor_id, event_type, payload)"` (the only place in api/src that spells this; plan 12's single-statement CTE reuses it); `@Injectable() export class SurveyEventsService { insert(db: Queryable, surveyId: string, actorId: string | null, eventType: string, payload: Record<string, unknown>): Promise<void>; listForSurvey(user: AuthenticatedUser, surveyId: string): Promise<{ items: SurveyEventRow[] }> }` where listForSurvey calls `repository.findOwnedOrThrow(db, surveyId, user.id, { activeOnly: <same as today's getEvents>, columns: "ownership" })` then runs today's SELECT (id, survey_id, actor_id, event_type, payload, created_at::text, ORDER BY created_at DESC). Keep today's response exactly (plan 11 adds the tiebreaker and next_cursor).
- surveys.repository.ts: `export type OwnershipColumns = "ownership" | "full"`; `export type SurveyOwnershipRow = Pick<SurveyRow, "id" | "user_id" | "status" | "visibility" | "sync_version" | "deleted_at">`; one generic definition (no overload list, so the grep gate counts one definition): `findOwned<C extends OwnershipColumns>(db: Queryable, id: string, userId: string, options: { activeOnly: boolean; forUpdate?: boolean; columns: C }): Promise<(C extends "full" ? SurveyRow : SurveyOwnershipRow) | null>`; `findOwnedOrThrow` with the same generic signature; the column list is a constant chosen by the enum, never interpolated from input; the `WHERE id = $1 AND user_id = $2` predicate is kept in every mode. Move `getSurveyParcelIds` and `syncSurveyParcels` (surveys.service.ts:1629-1656) here unchanged (plan 12 turns the sync into a diff).
- surveys-data.module.ts: `@Module({ providers: [SurveysRepository, SurveyEventsService], exports: [SurveysRepository, SurveyEventsService] })`, not global (analog storage/storage.module.ts). SurveysModule imports `[AuthModule, StorageModule, SurveysDataModule]`; ReportsModule adds SurveysDataModule. No cycle: SurveysModule does not import ReportsModule.
- surveys.controller.ts `events` route delegates to `SurveyEventsService.listForSurvey` (same path, params and response).
Specs: surveys-attachments-download.spec.ts:65 builds `new SurveysAttachmentsService(db, storage)`; it gains the repository and events service (use real instances over the mocked db, or light mocks).
Coverage: ./src/surveys/ thresholds in api/jest.unit.config.js (56/41/51/56) must still pass; add unit specs for the two new units so moved code stays covered.
No model identifiers in code, VALIDATION.md or PR bodies; commit trailers use the attribution lines the orchestrator supplies. Run targeted E2E per task and the full suite at the end of the plan.
Local E2E database: `ibp_p17_08_test`; always under `flock /tmp/ibp-e2e.lock`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SurveyEventsService, SurveysRepository and SurveysDataModule</name>
  <files>api/src/surveys/survey-events.service.ts, api/src/surveys/surveys.repository.ts, api/src/surveys/surveys-data.module.ts, api/test/survey-events.service.spec.ts, api/test/surveys.repository.spec.ts</files>
  <read_first>
    - api/src/surveys/surveys.service.ts (insertEvent, getSurveyForUser*, getSurveyParcelIds, syncSurveyParcels, getEvents)
    - api/src/surveys/surveys-attachments.service.ts:1-70
    - api/src/surveys/surveys.types.ts (SurveyRow, SurveyEventRow, Queryable)
    - api/src/storage/storage.module.ts
  </read_first>
  <behavior>
    - insert(db, "s1", "u1", "created", { a: 1 }) issues one query whose text starts with SURVEY_EVENT_INSERT_SQL and whose values are [uuid, "s1", "u1", "created", '{"a":1}']; insert with actorId null passes null as the third value
    - findOwned with columns "ownership" selects exactly `id, user_id, status, visibility, sync_version, deleted_at` (no `*`), with "full" selects `*`; both contain `WHERE id = $1 AND user_id = $2`; activeOnly adds `deleted_at IS NULL`; forUpdate appends `FOR UPDATE`; returns null on no row
    - findOwnedOrThrow throws NotFoundException("Survey not found") on no row
    - listForSurvey throws NotFoundException for a survey the user does not own and returns { items } ordered as today otherwise
  </behavior>
  <action>Per D-07, create the three files with the exact contract in the interfaces block (Queryable-first signatures so every call keeps running inside the caller's transaction, 01.4 D-06). Comments cite D-07 and, on the null actor, 01.2 A-M6. Write both unit specs first against a mocked `{ query: jest.fn() }`.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- "survey-events.service|surveys.repository" && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const SURVEY_EVENT_INSERT_SQL" api/src/surveys/survey-events.service.ts` returns 1
    - `grep -cE "async findOwned\(|findOwned\(" api/src/surveys/surveys.repository.ts` returns at least 1
    - `grep -n "SELECT \*" api/src/surveys/surveys.repository.ts` appears only in the "full" column constant
    - Every behavior bullet passes
  </acceptance_criteria>
  <done>The single event writer and the single ownership lookup exist with unit proof.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire surveys, attachments and reports onto the shared units and delete the duplicates</name>
  <files>api/src/surveys/surveys.service.ts, api/src/surveys/surveys-attachments.service.ts, api/src/surveys/surveys.module.ts, api/src/surveys/surveys.controller.ts, api/src/reports/reports.service.ts, api/src/reports/reports.module.ts, api/test/surveys-attachments-download.spec.ts</files>
  <read_first>
    - api/src/surveys/surveys.service.ts (every call site listed in the interfaces block)
    - api/src/surveys/surveys-attachments.service.ts
    - api/src/reports/reports.service.ts, api/src/reports/reports.module.ts, api/src/surveys/surveys.module.ts, api/src/surveys/surveys.controller.ts
    - api/test/surveys-attachments-download.spec.ts
  </read_first>
  <action>Per D-07, inject SurveysRepository and SurveyEventsService into SurveysService, SurveysAttachmentsService and ReportsService; replace every insertEvent call with `this.events.insert(db, …)` on the same Queryable (inside the same transaction as today), the reports inline insert with `this.events.insert(db, surveyId, null, "reported", { report_id: reportId })`, and every getSurveyForUser/getSurveyForUserOrThrow call with findOwned/findOwnedOrThrow using the column mode listed per call site (ownership where only id/status/visibility/sync_version/deleted_at are read; verify each by reading the code that uses the row, and use "full" whenever any other field is read). Move getEvents to SurveyEventsService.listForSurvey and point the controller at it. Delete the private duplicates, and the moved parcel-link helpers now in the repository. Wire SurveysDataModule into both modules. Update surveys-attachments-download.spec.ts's constructor call. Do not change any SQL semantics, lock order (submit's sorted FOR UPDATE, 01.4 D-08) or transaction boundary. Then run the unit suite with coverage and the full E2E suite with no E2E spec edited.</action>
  <verify>
    <automated>cd /home/user/cortege && test "$(grep -rnE '(insertEvent|getSurveyForUser|getSurveyForUserOrThrow)\(' api/src | wc -l)" = 0 && test "$(grep -rn 'INSERT INTO survey_events' api/src | wc -l)" = 1 && test "$(grep -rnE '^\s*(async )?findOwned<' api/src | wc -l)" = 1 && npm --workspace api run test:unit:coverage && npm run typecheck && npm run lint && npm run format:check && POSTGRES_DB=ibp_p17_08_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-08 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "(insertEvent|getSurveyForUser|getSurveyForUserOrThrow)\(" api/src` returns nothing; `grep -rn "INSERT INTO survey_events" api/src` returns exactly one line (the constant); exactly one `findOwned(` definition exists in api/src
    - `grep -c 'columns: "ownership"' api/src/surveys/surveys-attachments.service.ts` returns at least 6
    - `wc -l < api/src/surveys/surveys.service.ts` is lower than before this plan (record both numbers in the SUMMARY)
    - Unit coverage thresholds met; full E2E green on ibp_p17_08_test; `git log --name-only` for this plan's commits lists no `*.e2e-spec.ts` file
  </acceptance_criteria>
  <done>Each duplicate exists once, ownership checks read only the columns they need, and the black-box E2E suite proves nothing observable changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated user → survey rows | Ownership enforced per request |
| Reporter → survey event log | Reporter identity must not be stored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.7-31 | Elevation of Privilege | Refactored ownership check losing the user predicate | mitigate | `WHERE id = $1 AND user_id = $2` in every column mode (unit asserts); full E2E black-box suite unchanged |
| T-01.7-32 | Information Disclosure | Reporter identity written into survey_events | mitigate | reports pass actorId null; unit asserts null reaches the query |
| T-01.7-33 | Tampering | Event written outside the row's transaction | mitigate | insert takes the caller's Queryable; transaction boundaries unchanged; fault-injection E2E stays green |
| T-01.7-34 | Tampering | Column list built from input | mitigate | Constant column lists selected by an enum |
</threat_model>

<verification>
- Grep gates (one writer, one lookup, no old names), unit with coverage, full E2E with no spec edits, lint, typecheck, format
</verification>

<success_criteria>
ROADMAP criterion 3's "getSurveyForUser and insertEvent exist once" and criterion 4's column-scoped ownership checks hold.
</success_criteria>

<output>
Create `.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-08-SUMMARY.md` when done
</output>
