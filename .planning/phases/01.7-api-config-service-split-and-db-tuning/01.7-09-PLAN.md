---
phase: 01.7-api-config-service-split-and-db-tuning
plan: 09
type: execute
wave: 4
depends_on: [01.7-03, 01.7-08]
files_modified:
  - api/src/surveys/cadastre-provider.service.ts
  - api/src/surveys/parcels.service.ts
  - api/src/surveys/surveys.service.ts
  - api/src/surveys/surveys.module.ts
  - api/src/surveys/parcels.controller.ts
  - api/test/cadastre-provider.service.spec.ts
  - api/test/parcels.service.spec.ts
  - api/test/sync-query-budget.e2e-spec.ts
autonomous: true
requirements: [REQ-AUD-surveys-split]

must_haves:
  truths:
    - "There is one IGN HTTP client, in CadastreProviderService: geocodage, API Carto and the WFS GetFeature share one fetchJson that uses AbortSignal.timeout and awaits the body inside the timed region (D-08)"
    - "A stalled response body is aborted after the configured timeout (proved against a local HTTP server that sends headers and never ends the body) (D-08, ROADMAP criterion 4)"
    - "WFS features are cached per z15 tile in lru-cache with a 24 h TTL, bounded by count and by size; study status is never cached (D-08)"
    - "A request spanning more than 16 tiles skips IGN and uses the database path; tiles are fetched with at most 4 in flight (D-08)"
    - "The studied-parcels query in the IGN path is restricted to the commune codes of the fetched features; a failed IGN call logs a Logger warning instead of a silent catch (D-08)"
    - "ensureParcelIds writes any number of parcel ids with one INSERT … SELECT unnest … ON CONFLICT DO NOTHING; validateParcelSubmit is one GROUP BY query; computeSurveyDisplayLocation is one query on centroid_lat/centroid_lng (D-10)"
    - "ParcelsService owns resolve, history, ensureParcelIds, display location and submit validation; routes and responses are unchanged; submitSurvey's sorted FOR UPDATE is untouched (D-07, D-10)"
  artifacts:
    - path: api/src/surveys/parcels.service.ts
      provides: "ParcelsService"
      contains: "unnest("
    - path: api/src/surveys/cadastre-provider.service.ts
      provides: "fetchJson with AbortSignal.timeout, fetchParcelFeaturesInBbox with tile cache"
      contains: "AbortSignal.timeout"
  key_links:
    - from: api/src/surveys/cadastre-provider.service.ts
      to: "lru-cache"
      via: "new LRUCache({ max, maxSize, sizeCalculation, ttl })"
      pattern: "new LRUCache"
    - from: api/src/surveys/surveys.service.ts
      to: api/src/surveys/parcels.service.ts
      via: "ensureParcelIds / validateParcelSubmit / displayLocation"
      pattern: "parcels\\.(ensureParcelIds|validateParcelSubmit|displayLocation)\\("
---

<objective>
Extract `ParcelsService` from SurveysService with batched parcel writes, and merge the second IGN WFS client into `CadastreProviderService` with one fully timed-out fetch, a per-tile LRU cache, a tile cap and a commune-scoped "studied" query.

Purpose: REQ-AUD-surveys-split, ROADMAP criteria 3 (parcels service merging the internal IGN client) and 4 (parcel ids in one batched statement; IGN fetch cached per tile and timed out on the body). Decisions D-07, D-08, D-10.
Output: ParcelsService, reworked CadastreProviderService, unit proof, and a one-statement proof in the budget spec.
Third link of the surveys.service.ts chain. The public-map queries themselves move in plan 10; this plan only swaps the IGN branch of getPublicParcelStatuses onto the new client and scopes its studied query.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-RESEARCH.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-PATTERNS.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-08-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 06: CadastreProviderService and SurveysService take ConfigService; `appConfigOf(config).cadastre` = { provider, allowFallback, timeoutMs, reverseUrl, apiCartoParcelUrl, wfsUrl, wfsTypename, wfsCount }. From plan 01: lru-cache@11 is a direct api dependency (`import { LRUCache } from "lru-cache"`). From plan 03: parcels.centroid_lat / centroid_lng generated columns and idx_parcels_centroid_lat_lng. From plan 08: SurveysRepository, SurveyEventsService, SurveysDataModule.
Buggy code to replace:
- cadastre-provider.service.ts fetchJson (~179-199): AbortController + setTimeout, `return response.json()` inside try with clearTimeout in finally → body not covered.
- surveys.service.ts resolvePublicParcelStatusesFromIgnWfs (~1312-1484): builds the WFS URL (service WFS, version 2.0.0, GetFeature, typeNames, bbox `minLng,minLat,maxLng,maxLat,EPSG:4326` with toFixed(6), outputFormat application/json, count), fetch with `.finally(() => clearTimeout(timeout))`, unbounded `await response.json()`, then a "latest public survey per parcel" window query over every public survey in the country, then feature mapping, and a silent `catch { return [] }` at the end.
New CadastreProviderService surface (contract for plan 10):
- `private async fetchJson(url: URL): Promise<unknown>`: `const response = await fetch(url, { method: "GET", headers: { Accept: "application/json" }, signal: AbortSignal.timeout(this.timeoutMs) })`; non-ok → throw `cadastre provider returned HTTP <status>`; `return await response.json()`.
- `export type WfsParcelFeature = { parcel_id: string; commune_code: string; section: string; number: string; geometry?: Record<string, unknown> }` (whatever the current feature mapping extracts; no study fields).
- `async fetchParcelFeaturesInBbox(bbox: { minLng; minLat; maxLng; maxLat }): Promise<WfsParcelFeature[] | null>`: returns null when the tile count exceeds WFS_MAX_TILES_PER_REQUEST (16) or when IGN fails (after `this.logger.warn(\`IGN WFS failed: ${message}\`)`); otherwise the de-duplicated features intersecting the bbox.
- Tiles: `WFS_TILE_ZOOM = 15` (Web Mercator XYZ: x = floor((lng + 180) / 360 · 2^z); y = floor((1 − ln(tan(φ) + sec(φ)) / π) / 2 · 2^z); inverse for the tile bbox); key `z/x/y`; one WFS call per missing tile with the tile's bbox and count wfsCount; at most 4 tiles in flight (small worker loop, no new dependency).
- Cache: `new LRUCache<string, WfsParcelFeature[]>({ max: 256, maxSize: 64 * 1024 * 1024, sizeCalculation: (features) => Math.max(1, JSON.stringify(features).length), ttl: 24 * 60 * 60 * 1000 })` (Claude's discretion within D-08 bounds, RESEARCH A2). An empty tile is cached too (valid answer). A failed tile is not cached.
Studied query (moves to plan 10's PublicMapService afterwards, but written here): the latest public submitted survey per parcel, restricted with `p.commune_code = ANY($2::text[])` where $2 is the distinct commune codes of the returned features (never by centroid bbox: parcels created by ensureParcelById have centroid {} and NULL generated columns, RESEARCH Pattern 7); keep the ranking (observation_year DESC NULLS LAST, version_number DESC NULLS LAST, submitted_at DESC NULLS LAST) and the year filter.
ParcelsService (moves from surveys.service.ts, pattern map "parcels.service.ts" section): resolveParcelByCoordinates and getParcelSurveyHistory (routes in parcels.controller.ts:12-24, controller switches injection to ParcelsService, same method names and responses), computeSurveyDisplayLocation → `displayLocation(db, surveyId)` as ONE query using centroid_lat/centroid_lng (same result shape as today: read today's two queries and reproduce their semantics, including the fallback order), ensureParcelIds → ONE `INSERT INTO parcels (…) SELECT … FROM unnest($1::text[]) … ON CONFLICT (parcel_id) DO NOTHING` (keep normalizeParcelId, the JS de-duplication and the output order; derive commune_code/section/number exactly as ensureParcelById does today, e.g. with substring expressions or parallel arrays built in JS), resolveParcelFromCoordinates, ensureParcelById (single-id path still used by resolve), getDefaultVersionNumber, validateParcelSubmit → ONE `GROUP BY parcel_id` query replacing the per-parcel loop (~1877-1887) with identical outcomes. submitSurvey's sorted `FOR UPDATE` on parcels (01.4 D-08) stays in SurveysService, untouched.
Unit analogs: cadastre-provider.service.spec.ts (fetchMock with ordered responses; `fetchMock.mock.calls[0][0]` URL assertions; Logger spy). Stalled body: `http.createServer((req, res) => { res.writeHead(200, { "Content-Type": "application/json" }); res.write("{\"features\": [") })` on port 0, never ended; close the server and destroy sockets in afterAll.
Budget spec (plan 03 file): add a fifth case "parcel ids are written in one statement": one sync op with 50 parcel ids vs one op with 1 parcel id; the counts must be equal (the statement count does not grow with the number of parcels).
No model identifiers in code, VALIDATION.md or PR bodies; commit trailers use the attribution lines the orchestrator supplies. Run targeted E2E per task and the full suite at the end of the plan.
Local E2E database: `ibp_p17_09_test`; always under `flock /tmp/ibp-e2e.lock`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One IGN client with a full timeout, per-tile cache and tile cap</name>
  <files>api/src/surveys/cadastre-provider.service.ts, api/test/cadastre-provider.service.spec.ts</files>
  <read_first>
    - api/src/surveys/cadastre-provider.service.ts (whole file)
    - api/src/surveys/surveys.service.ts resolvePublicParcelStatusesFromIgnWfs (the WFS URL building and feature mapping)
    - api/test/cadastre-provider.service.spec.ts
  </read_first>
  <behavior>
    - fetchJson against the stalled-body server with CADASTRE_PROVIDER_TIMEOUT_MS "200" rejects in under 1 500 ms (existing geocodage and API Carto paths use it and fall back as today)
    - fetchParcelFeaturesInBbox over a bbox inside one z15 tile calls fetch once with a WFS URL whose bbox equals the tile bounds (6 decimals, EPSG:4326) and count wfsCount; a second call for the same or a sub-bbox calls fetch zero more times
    - a bbox covering 17 or more z15 tiles returns null and fetch is not called
    - a bbox covering 6 tiles calls fetch 6 times with never more than 4 concurrent in-flight calls (instrumented mock with deferred promises)
    - features returned from several tiles are de-duplicated by parcel_id and filtered to those intersecting the request bbox
    - an HTTP 500 or a thrown fetch returns null, logs one Logger warn containing "IGN WFS failed", and is not cached (next call fetches again)
    - the cached value type carries no study fields (compile-time: WfsParcelFeature has no study_status)
  </behavior>
  <action>Per D-08, replace fetchJson with the AbortSignal.timeout version from the interfaces block (the timer covers the body), move the WFS URL building and feature parsing from surveys.service.ts into `fetchParcelFeaturesInBbox`, and add the tile maths, the LRU cache, the 16-tile cap and the 4-way concurrency limit exactly as specified. Constants (WFS_TILE_ZOOM, WFS_MAX_TILES_PER_REQUEST, WFS_TILE_CONCURRENCY, cache bounds) are named and commented with D-08 and RESEARCH A2. Replace the silent catch with `this.logger.warn`. Write the new spec cases first.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- cadastre-provider && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AbortSignal.timeout" api/src/surveys/cadastre-provider.service.ts` returns at least 1 and `grep -cE "clearTimeout|new AbortController" api/src/surveys/cadastre-provider.service.ts` returns 0
    - `grep -c "new LRUCache" api/src/surveys/cadastre-provider.service.ts` returns 1
    - `grep -cE "WFS_MAX_TILES_PER_REQUEST = 16|WFS_TILE_ZOOM = 15" api/src/surveys/cadastre-provider.service.ts` returns 2
    - Every behavior bullet passes
  </acceptance_criteria>
  <done>IGN calls can no longer hang on a stalled body, map moves reuse cached tiles, and one request cannot fan out into thousands of outbound calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ParcelsService with batched writes, and SurveysService on the new client</name>
  <files>api/src/surveys/parcels.service.ts, api/src/surveys/surveys.service.ts, api/src/surveys/surveys.module.ts, api/src/surveys/parcels.controller.ts, api/test/parcels.service.spec.ts</files>
  <read_first>
    - api/src/surveys/surveys.service.ts (resolveSelectedParcelIds, resolveVersionInfo, getSurveyById, submitSurvey, getPublicParcelStatuses, resolvePublicParcelStatusesFromIgnWfs, resolveParcelByCoordinates, getParcelSurveyHistory, computeSurveyDisplayLocation, ensureParcelIds, resolveParcelFromCoordinates, ensureParcelById, getDefaultVersionNumber, validateParcelSubmit)
    - api/src/surveys/parcels.controller.ts, api/src/surveys/surveys.module.ts
    - api/src/surveys/surveys-normalize.utils.ts (normalizeParcelId, parcel id parsing; do not edit this file)
  </read_first>
  <behavior>
    - ensureParcelIds(db, ["75056000AB0001", " 75056000ab0001 ", "13055000CD0002", "bad"]) issues exactly one INSERT containing `unnest(` and `ON CONFLICT (parcel_id) DO NOTHING`, with the normalised, de-duplicated ids in input order, and returns them in that order (invalid ids handled as today)
    - ensureParcelIds with an empty list issues no query
    - validateParcelSubmit for 3 parcels issues exactly one query containing `GROUP BY` and yields the same accept/reject outcome as today's loop for: no prior version, a lower version, an equal version (reject)
    - displayLocation issues exactly one query referencing centroid_lat and centroid_lng
  </behavior>
  <action>Per D-07 and D-10, create ParcelsService (injects DatabaseService, CadastreProviderService, SurveysRepository as needed) with the methods and one-statement rewrites in the interfaces block; SurveysService calls it for parcel resolution, display location and submit validation, keeping the sorted FOR UPDATE in submitSurvey byte-for-byte. Point ParcelsController at ParcelsService. In getPublicParcelStatuses' IGN branch call `cadastre.fetchParcelFeaturesInBbox(bbox)`: null means "use the DB path" (as an empty IGN result does today), otherwise run the commune-restricted studied query and map exactly as today. Delete the moved methods and the WFS fields from SurveysService. Register ParcelsService in SurveysModule. Write parcels.service.spec.ts first.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- "parcels.service|cadastre-provider" && npm run typecheck && npm run lint && ! grep -nE "ignParcelWfs|resolvePublicParcelStatusesFromIgnWfs|private async ensureParcelIds|clearTimeout" api/src/surveys/surveys.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "unnest(" api/src/surveys/parcels.service.ts` returns at least 1 and `grep -c "GROUP BY" api/src/surveys/parcels.service.ts` returns at least 1
    - `grep -c "commune_code = ANY(" api/src/surveys/surveys.service.ts` returns 1
    - surveys.service.ts no longer contains the WFS client, the parcel loop or computeSurveyDisplayLocation; its line count is recorded in the SUMMARY
    - Every behavior bullet passes
  </acceptance_criteria>
  <done>Parcel logic has its own service, parcel writes are one statement, and the only IGN client is the cadastre provider.</done>
</task>

<task type="auto">
  <name>Task 3: One-statement proof in the budget spec and the full suites</name>
  <files>api/test/sync-query-budget.e2e-spec.ts</files>
  <read_first>
    - api/test/sync-query-budget.e2e-spec.ts (plan 03)
  </read_first>
  <action>Per D-10, add the fifth case from the interfaces block to the budget spec (50 parcel ids vs 1 in a single-op sync: equal statement counts; both synced) and print both counts. Keep the four existing cases and their `<= BASELINE` assertions; print their new counts too (they should already be lower thanks to the batched parcels) and record all counts in the SUMMARY. Then run the unit suite with coverage and the full E2E suite.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit:coverage && npm run format:check && POSTGRES_DB=ibp_p17_09_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-09 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "one statement" api/test/sync-query-budget.e2e-spec.ts` returns at least 1
    - Coverage thresholds met; full E2E green on ibp_p17_09_test; the five printed counts are in the SUMMARY
  </acceptance_criteria>
  <done>The statement count of a sync operation no longer depends on how many parcels it links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public client → /v1/public/parcels/status bbox | Unauthenticated, attacker-sized bbox |
| API → IGN endpoints | External HTTP, may stall |
| Process memory → tile cache | Bounded container (768 MB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.7-35 | Denial of Service | Stalled IGN body pinning requests | mitigate | AbortSignal.timeout covering the body; stalled-server unit case |
| T-01.7-36 | Denial of Service | Huge bbox fanning out into outbound calls | mitigate | 16-tile cap with DB fallback; 4 in flight |
| T-01.7-37 | Denial of Service | Cache memory exhaustion | mitigate | lru-cache max 256 entries and maxSize 64 MB with sizeCalculation; 24 h TTL |
| T-01.7-38 | Information Disclosure / Integrity | Stale study status served from cache | mitigate | Only features are cached; study status computed per request from the DB |
| T-01.7-39 | Denial of Service | Unbounded studied query over all public surveys | mitigate | Restricted to the fetched commune codes |
| T-01.7-40 | Repudiation | Silent IGN failures | mitigate | Logger warn on every failure |
</threat_model>

<verification>
- Unit (cadastre-provider, parcels.service) and coverage green; full E2E green on ibp_p17_09_test; lint, typecheck, format green
</verification>

<success_criteria>
ROADMAP criterion 3's parcels service with the merged IGN client, and criterion 4's batched parcel writes and cached, fully timed-out IGN fetch, hold.
</success_criteria>

<output>
Create `.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-09-SUMMARY.md` when done
</output>
