---
phase: 01.7-api-config-service-split-and-db-tuning
plan: 13
type: execute
wave: 8
depends_on: [01.7-01, 01.7-02, 01.7-03, 01.7-04, 01.7-05, 01.7-06, 01.7-07, 01.7-08, 01.7-09, 01.7-10, 01.7-11, 01.7-12]
files_modified:
  - api/jest.unit.config.js
  - scripts/owner-check-simulation.mjs
  - .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md
  - .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-explain-10k.txt
autonomous: true
requirements: [REQ-AUD-config, REQ-AUD-surveys-split, REQ-AUD-db-tuning]

must_haves:
  truths:
    - "The full local gate is green on the integrated phase: lint, typecheck, API unit with coverage, mobile unit, format and tracked-file prettier, and the full E2E suite in local mode (twice) and in MinIO mode against the pinned pgsty/minio container"
    - "Coverage thresholds only go up; a ./src/config/ row is added at its measured floor; the ratchet check prints `ratchet ok`"
    - "01.7-VALIDATION.md records the EXPLAIN (ANALYZE, BUFFERS) output summary on 10 000 surveys (before/after), the query-budget numbers against the baseline, and every per-task status (D-15, D-09)"
    - "scripts/owner-check-simulation.mjs is the reusable owner-check replay: base URL, port, state file and email are configurable; phases cover two-device sync, photo and picture (as in 01.6), pagination, malformed cursors, API restart, the production config refusal, the deploy guard, and read-only production probes (D-17)"
    - "Every simulation phase that can run locally has run green against the built API (MinIO mode for devices/restart), and the results are in 01.7-VALIDATION.md"
  artifacts:
    - path: scripts/owner-check-simulation.mjs
      provides: "Reusable owner-check simulation"
      contains: "SIM_BASE"
    - path: .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md
      provides: "Local gate, ratchet, EXPLAIN, budget and simulation evidence"
    - path: .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-explain-10k.txt
      provides: "Raw output of api/scripts/explain-public-routes.js"
  key_links:
    - from: scripts/owner-check-simulation.mjs
      to: infra/vps/update-stack.sh
      via: "deploy-guard phase runs the script with stubbed docker/git/curl"
      pattern: "update-stack.sh"
    - from: scripts/owner-check-simulation.mjs
      to: api/dist/config/check-config.js
      via: "config-refusal phase spawns it"
      pattern: "check-config.js"
---

<objective>
Local phase gate: prove the integrated result in both storage modes, ratchet coverage, capture the EXPLAIN and query-budget evidence, and replace the 01.6 one-off simulation with a reusable owner-check simulation that also covers pagination, the production config refusal and the deploy guard.

Purpose: all three requirements; ROADMAP criteria 1-5 demonstrated locally. Decisions D-09 (numbers), D-15 (EXPLAIN evidence), D-17 (Claude verifies everything except the VPS visit), D-19 (the deploy sequence the simulation replays).
Output: ratcheted thresholds, the reusable simulation script, the EXPLAIN output file and a filled 01.7-VALIDATION.md. Plan 14 then opens the PR, records CI and runs the owner visit.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-VALIDATION.md
@CLAUDE.md

<interfaces>
Read every 01.7-0N-SUMMARY.md (plans 01-12) for deviations, recorded counts and EXPLAIN tables before starting.
Coverage ratchet (01.6-09 pattern): `npm --workspace api run test:unit:coverage` writes api/coverage/unit/coverage-summary.json; `node scripts/coverage-by-directory.js api` prints floor values per directory including the new `./src/config/`. Raise every threshold whose floor went up, add the `./src/config/` row at its floor, never lower anything, update the date in the ratchet comment (mention phase 01.7), rerun coverage. Ratchet check: the node one-liner from 01.6-09-PLAN.md Task 1 `<verify>` (parses `git diff -U0 api/jest.unit.config.js` and fails on any lowered value); reuse it verbatim.
Tracked-file prettier: `git ls-files -z '*.ts' '*.tsx' '*.json' | xargs -0 npx prettier --check --ignore-path .prettierignore`.
Local E2E: PostgreSQL 16 (`pg_ctlcluster 16 main start`); gate database `ibp_p17_13_test`; every E2E command under `flock /tmp/ibp-e2e.lock`; no ACCESS_TOKEN_SECRET anywhere (prove with `env -u ACCESS_TOKEN_SECRET`).
MinIO mode locally (Docker works in the sandbox): `nohup dockerd > /tmp/dockerd.log 2>&1 &`, wait for `docker info`; `docker run -d --name p17-minio -p 9000:9000 -e MINIO_ROOT_USER=minio -e MINIO_ROOT_PASSWORD=minio123 pgsty/minio:RELEASE.2026-08-04T00-00-00Z@sha256:b6bfe7239bfc83fb90d31612d9704d86039dd714f7904b3f1ad68f211e602372 server /data`; wait for `curl -fsS http://localhost:9000/minio/health/live`; then the E2E with `OBJECT_STORAGE_MODE=minio OBJECT_STORAGE_BUCKET=ibp-media OBJECT_STORAGE_ENDPOINT=http://localhost:9000 OBJECT_STORAGE_REGION=us-east-1 OBJECT_STORAGE_ACCESS_KEY=minio OBJECT_STORAGE_SECRET_KEY=minio123` (same env as ci.yml e2e-minio). Expect the itMinio cases to run and the itLocal case to skip.
EXPLAIN: `npm --workspace api run build && POSTGRES_DB=ibp_p17_13_test flock /tmp/ibp-e2e.lock node api/scripts/explain-public-routes.js` after a migrated database (run the E2E first, or `POSTGRES_DB=ibp_p17_13_test npm run migrate:api`).
Budget numbers: run `sync-query-budget` alone and copy the printed `query-budget <case>: <count>` lines.
Simulation script (copy of .planning/phases/01.6-…/01.6-owner-check-simulation.mjs extended; the 01.6 file stays as the archived record of that phase, do not edit it). Pattern map C-7: no hard-coded base or state path. Config via env: `SIM_BASE` (default `http://localhost:${SIM_PORT ?? 3100}/v1`), `SIM_PORT`, `SIM_STATE` (default `path.join(os.tmpdir(), "cortege-owner-check-state.json")`), `SIM_EMAIL` (default `owner-check@ibp.local`), `SIM_API_DIR` (default `api`, for dist paths), `SIM_REPO_DIR` (default repo root from `import.meta.url`). Keep `check(name, ok, detail)`, `api(...)`, `login(email)` via POST /debug/test-token, the per-phase switch, the PASS/FAIL summary and a non-zero exit on any FAIL. Header comment: usage for every phase and what each proves.
Phases:
- `devices` (API running in test mode, MinIO mode): every 01.6 phase1 check (sync on two sessions, v2 cursor, rename, empty next pull, legacy cursor accepted, same-version rules, presigned photo PUT and download, wrong declared size 403, profile picture), plus: GET /surveys without params → `{ items, next_cursor: null }`; create 4 more surveys, then walk GET /surveys?limit=2 following next_cursor: every id once, same order as the unpaginated list; GET /surveys?limit=0 → 400; cursor=garbage → 400; GET /sync/changes?cursor=2024-02-30T00:00:00Z|x → 400 and `2024-01-01 12:00:00 junk|x` → 400; GET /surveys/:id/events without params → items present and next_cursor null; events with limit=1 page walk covers all events. Writes the state file.
- `restart` (after the executor restarts the API process): the 01.6 phase2 checks from the state file.
- `config-refusal` (no running API needed): spawnSync `node <api>/dist/main.js` with a minimal env of NODE_ENV=production, POSTGRES_PASSWORD=ibp, OBJECT_STORAGE_MODE=minio, OBJECT_STORAGE_SECRET_KEY=minio123, CORS_ORIGIN unset, PORT=<free port>, timeout 30 s → non-zero exit, output names POSTGRES_PASSWORD and CORS_ORIGIN, output never contains "minio123"; `node <api>/dist/config/check-config.js` with the same env → exit 1 with "ERREUR :" lines; a valid fake production env → check-config exit 0; then spawn main.js with that valid env on a free port (the pool connects lazily and /health does not touch the DB; if startup turns out to need a connection, point it at a local database through a temporary login role `ibp_sim` with a non-default password, created before and dropped after the phase via `SIM_PG_ADMIN_URL`, default `postgres://ibp:ibp@localhost:5432/postgres`) and check: GET /v1/health 200; POST /v1/debug/test-token 404; an OPTIONS preflight with `Origin: https://evil.example` has neither Access-Control-Allow-Origin nor `Access-Control-Allow-Credentials: true`; kill the process.
- `deploy-guard` (no API needed): write fixture VPS env files (valid; empty CORS_ORIGIN; POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD); for each, `bash infra/vps/check-env.sh <file>` and `node <api>/dist/config/check-config.js` (with the compose-mapped env: NODE_ENV production, POSTGRES_HOST postgres, OBJECT_STORAGE_MODE minio, OBJECT_STORAGE_SECRET_KEY from MINIO_SECRET_KEY, OBJECT_STORAGE_ACCESS_KEY from MINIO_ACCESS_KEY) must agree (exit status); then run `bash infra/vps/update-stack.sh` with stub `docker`/`git`/`curl` on PATH (same stub design as api/test/vps-deploy-guard.spec.ts): refused case → exit 1, "NOT restarted" logged, no `up -d`; accepted case → `up -d` after the check; retry case → the check runs again when the running image differs.
- `production` (read-only, SIM_BASE=https://cortege.algernon.ovh/v1, used by plan 14 after deploy): GET /health 200; POST /debug/test-token 404; GET /surveys without a token 401; GET /public/map-items 200 with an `items` array; OPTIONS preflight from `https://evil.example` → no Access-Control-Allow-Origin and no `Access-Control-Allow-Credentials: true` (the credentials header distinguishes the new image from the old one once CORS_ORIGIN=none is set); `https://cortege-files.algernon.ovh/minio/health/live` 200 (MinIO up after the image switch). It never writes anything.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full local gate in both storage modes and the coverage ratchet</name>
  <files>api/jest.unit.config.js, .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md</files>
  <read_first>
    - api/jest.unit.config.js, scripts/coverage-by-directory.js
    - .planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-09-PLAN.md (Task 1 verify: ratchet one-liner)
    - .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md and every 01.7-0N-SUMMARY.md
  </read_first>
  <action>From the repo root run in order: `npm run lint`, `npm run typecheck`, `npm --workspace api run test:unit:coverage`, `npm --workspace mobile run test:unit`, `npm run format:check`, tracked-file prettier; then `node scripts/coverage-by-directory.js api` and ratchet api/jest.unit.config.js (raise only, add `./src/config/`, update the comment), rerun coverage. Then with `env -u ACCESS_TOKEN_SECRET`: full E2E on ibp_p17_13_test in local mode twice, then start the pinned MinIO container and run the full E2E once in MinIO mode (interfaces block); stop and remove the container afterwards. Fix nothing silently: if a command fails, stop and report the owning plan. Record every command and result (suites, passed, skipped) in the "Local gate" section of 01.7-VALIDATION.md, the ratchet before/after table, and set each locally proven Per-Task row to ✅ (or ❌ with the failing command); set `wave_0_complete: true` when every Wave 0 file exists.</action>
  <verify>
    <automated>cd /home/user/cortege && npm run lint && npm run typecheck && npm --workspace api run test:unit:coverage && npm --workspace mobile run test:unit && npm run format:check && git ls-files -z '*.ts' '*.tsx' '*.json' | xargs -0 npx prettier --check --ignore-path .prettierignore > /dev/null && env -u ACCESS_TOKEN_SECRET POSTGRES_DB=ibp_p17_13_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-13 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e && grep -q "wave_0_complete: true" .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Every gate command exits 0; the ratchet check (01.6-09 one-liner) prints `ratchet ok`; `grep -c "./src/config/" api/jest.unit.config.js` returns 1
    - 01.7-VALIDATION.md shows two local-mode runs and one MinIO-mode run with suite/test counts, and the MinIO run lists the itMinio cases as passed
  </acceptance_criteria>
  <done>The integrated phase passes every local gate in both storage modes and coverage only went up.</done>
</task>

<task type="auto">
  <name>Task 2: EXPLAIN on 10 000 surveys and the query-budget numbers</name>
  <files>.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-explain-10k.txt, .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md</files>
  <read_first>
    - api/scripts/explain-public-routes.js
    - api/test/sync-query-budget.e2e-spec.ts
  </read_first>
  <action>Per D-15 build the API and run the EXPLAIN script against ibp_p17_13_test (migrated), writing the full output to 01.7-explain-10k.txt. In 01.7-VALIDATION.md "EXPLAIN evidence" section, add the summary table (query, before ms, after ms, seq scan before/after) for the two public queries and the three paginated lists, plus the seed sizes, PostgreSQL version and date. Per D-09 run the sync-query-budget spec alone and add the "Query budget" table: case, baseline, limit (baseline / 3), measured, reduction factor. Confirm that the database row counts are unchanged after the EXPLAIN run (rolled back).</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run build && POSTGRES_DB=ibp_p17_13_test flock /tmp/ibp-e2e.lock node api/scripts/explain-public-routes.js > .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-explain-10k.txt && grep -q "Execution Time" .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-explain-10k.txt && env -u ACCESS_TOKEN_SECRET POSTGRES_DB=ibp_p17_13_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p17-13 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e -- sync-query-budget && grep -q "Query budget" .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 01.7-explain-10k.txt contains EXPLAIN (ANALYZE, BUFFERS) output for every public and list query, before and after
    - 01.7-VALIDATION.md has the EXPLAIN summary table and the query-budget table, every measured count ≤ its limit
  </acceptance_criteria>
  <done>The EXPLAIN evidence for the PR and the 3x query reduction are recorded with numbers.</done>
</task>

<task type="auto">
  <name>Task 3: Reusable owner-check simulation and its local runs</name>
  <files>scripts/owner-check-simulation.mjs, .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-owner-check-simulation.mjs (whole file)
    - api/test/vps-deploy-guard.spec.ts (stub design)
    - infra/vps/check-env.sh, infra/vps/update-stack.sh
  </read_first>
  <action>Per D-17 create scripts/owner-check-simulation.mjs as described in the interfaces block (all five phases, configurable, no hard-coded path or port). Then: start the pinned MinIO container, create and migrate a fresh `ibp_verify17_test` database, build the API, start `node api/dist/main.js` with NODE_ENV=test, PORT=3171, the MinIO env and POSTGRES_DB=ibp_verify17_test (no ACCESS_TOKEN_SECRET) in the background; run `SIM_PORT=3171 node scripts/owner-check-simulation.mjs devices`; stop the API process and start it again; run `… restart`; run `… config-refusal` and `… deploy-guard`. Stop the API and MinIO, drop ibp_verify17_test. Record in 01.7-VALIDATION.md an "Owner check simulated by Claude" section in the 01.6 style: setup, a table mapping each owner-visible behaviour to its simulated check and result, and the PASS count per phase. The `production` phase runs in plan 14 after the deploy.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run build && node --check scripts/owner-check-simulation.mjs && ! grep -nE "localhost:3100/v1\"|/tmp/claude-0" scripts/owner-check-simulation.mjs && node scripts/owner-check-simulation.mjs config-refusal && node scripts/owner-check-simulation.mjs deploy-guard</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "SIM_BASE|SIM_PORT|SIM_STATE" scripts/owner-check-simulation.mjs` returns at least 3
    - `grep -cE "\"(devices|restart|config-refusal|deploy-guard|production)\"" scripts/owner-check-simulation.mjs` returns 5
    - The devices, restart, config-refusal and deploy-guard phases ran with 0 FAIL, and 01.7-VALIDATION.md records their PASS counts
  </acceptance_criteria>
  <done>The owner's checks are replayed end to end by a committed, reusable script, including the new production refusal and deploy-guard behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local sandbox → test databases and containers | Gate runs create and drop databases |
| Evidence → reviewers | Claims recorded in VALIDATION.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.7-53 | Tampering | Coverage thresholds lowered to pass | mitigate | Ratchet one-liner fails on any lowered value |
| T-01.7-54 | Repudiation | Unrecorded or unverifiable evidence | mitigate | Raw EXPLAIN output committed; counts, commands and PASS totals in VALIDATION.md |
| T-01.7-55 | Tampering | Gate or simulation touching a non-test database | mitigate | Only *_test databases; explain script refuses others; simulation production phase is read-only |
</threat_model>

<verification>
- Local gate green in both modes; ratchet ok; EXPLAIN and budget tables recorded; simulation phases green and recorded
</verification>

<success_criteria>
ROADMAP criteria 1-5 are demonstrated locally with recorded evidence, and the owner-check replay is a reusable script.
</success_criteria>

<output>
Create `.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-13-SUMMARY.md` when done
</output>
