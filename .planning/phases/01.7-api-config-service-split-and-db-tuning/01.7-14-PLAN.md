---
phase: 01.7-api-config-service-split-and-db-tuning
plan: 14
type: execute
wave: 9
depends_on: [01.7-13]
files_modified:
  - .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md
autonomous: false
requirements: [REQ-AUD-config, REQ-AUD-surveys-split, REQ-AUD-db-tuning]

must_haves:
  truths:
    - "On the phase PR every CI job is green, including both E2E jobs, the unit coverage thresholds and the new production-refusal smoke step; the evidence (PR URL, run URL, job table, quoted lines) is in 01.7-VALIDATION.md"
    - "The PR body carries the EXPLAIN summary on 10 000 surveys, the query-budget table and the short French owner checklist"
    - "The owner made exactly one VPS sitting, in the D-21 order: MinIO volume backup, CORS_ORIGIN=none, check-env.sh until OK, deploy timer stopped, PR merged, main CI image job finished, checkout pulled with --ff-only, timer restarted; the first deploy of this phase therefore ran the new, guarded update-stack.sh (D-17, D-19, D-21)"
    - "After the deploy, Claude confirms /v1/health answers 200 and the read-only production probes pass, including the probe that tells the new image from the old one and MinIO health (D-17, D-19, D-21)"
  artifacts:
    - path: .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md
      provides: "CI evidence, owner visit record, post-deploy checks"
      contains: "actions/runs/"
  key_links:
    - from: 01.7-VALIDATION.md
      to: "GitHub Actions run of the phase PR"
      via: "run URL, job table and quoted lines"
      pattern: "actions/runs/"
---

<objective>
Open the phase PR, record its CI evidence, hand the owner the single-visit French checklist, and after the merge verify the production deployment from outside.

Purpose: all three requirements demonstrated on CI and in production. Decisions D-17 (one owner VPS visit, no phone tests), D-19 (backup before merge; MinIO switches at deploy; Claude checks health), D-21 (the first deploy runs the new guarded script: timer stopped across the merge, checkout pulled, timer restarted).
Output: PR, CI evidence, owner visit record and post-deploy checks in 01.7-VALIDATION.md.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-13-SUMMARY.md
@.planning/phases/01.6-sync-feed-ordering-and-unified-object-storage/01.6-09-PLAN.md
@CLAUDE.md

<interfaces>
Repository github.com/florianlepont/cortege; PR base main. GitHub access: the `gh` CLI may be absent; use the GitHub MCP tools if available; if nothing can read Actions runs, surface an access gate and ask the orchestrator/owner for the run URL, job conclusions and the two E2E job logs (as in 01.6).
ci.yml jobs: "Detect changed paths", "Lint, format, typecheck", "Unit tests — API", "Unit tests — Mobile", "E2E tests — API", "E2E tests — API (MinIO mode)", "Mobile build check", "Dependency audit", "Docker image check" (now including "Smoke test — production refuses default configuration"), "CI OK", "Build & push Docker image" (main only). CodeQL: "CodeQL analysis". Mobile jobs are path-filtered: skipped is acceptable, failure is not.
No model identifiers in code, VALIDATION.md or PR bodies; commit trailers use the attribution lines the orchestrator supplies.
Deploy facts (D-21): the VPS runs update-stack.sh from its git checkout, and a running copy keeps executing the old code after its own fast-forward. The copy on the VPS today predates both the config guard and the self re-exec (plan 07), so if the timer ran the first deploy by itself, the unguarded old copy would restart the stack with the new image and a bad env would take the API down. Hence the owner stops the timer before the merge, pulls the checkout once the image exists, and restarts the timer: the new, guarded script then does the first deploy (guard → `compose up -d`, which also recreates MinIO with the pinned pgsty image, D-19; the image CMD runs migration 015 under the advisory lock before starting the API). From then on the self re-exec keeps script changes effective in the same run. After merge, CI publishes ghcr.io/florianlepont/cortege:latest from the "Build & push Docker image" job of the main run (about 3-5 minutes); give the owner https://github.com/florianlepont/cortege/actions/workflows/ci.yml?query=branch%3Amain to watch it.
Owner checklist (French, copy-paste; replace <branche> with the real phase branch name and <URL de la PR> with the PR URL before presenting). It is the single VPS sitting (D-17), in the D-21 order; the exact commands are in the checkpoint's how-to-verify below and must match infra/vps/README.md (plans 04 and 07), which is the source of truth if they differ in detail.
Post-deploy checks (Claude): start once the owner reports step 8 done; poll `curl -s -o /dev/null -w '%{http_code}' https://cortege.algernon.ovh/v1/health` every 30 s for up to 30 minutes; then `SIM_BASE=https://cortege.algernon.ovh/v1 node scripts/owner-check-simulation.mjs production`. If health is 200 but the probe still shows `Access-Control-Allow-Credentials: true` after 30 minutes, the new image is not running: most likely the deploy guard refused the env; report it to the owner with the command `journalctl -u cortege-deploy -n 50` (a second, optional visit only in that failure case).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open the phase PR and record its CI evidence</name>
  <files>.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md</files>
  <read_first>
    - .github/workflows/ci.yml (job and step names)
    - .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md (local gate, EXPLAIN, budget, simulation sections)
  </read_first>
  <action>Push the phase branch and open (or reuse) a PR against main titled "Phase 01.7: API configuration, service split and database tuning". Body (English, short): what changes for the owner (production refuses default passwords, empty AUTH0_* and a missing CORS_ORIGIN; the deploy guard keeps the old API running if the env is wrong; MinIO moves to pgsty/minio at deploy; migration 015 adds columns and indexes and runs under an advisory lock; lists accept optional limit/cursor; installed apps need no update); the EXPLAIN summary table on 10 000 surveys and a link to 01.7-explain-10k.txt; the query-budget table; then the French owner checklist (the eight steps of Task 2's how-to-verify, branch name filled in) under a heading "Checklist propriétaire (une seule séance sur le VPS)". End the body with the attribution lines from the session's system reminder. Wait for CI and CodeQL on the PR head (poll, at most 45 minutes). Verify: (1) success for Detect changed paths, Lint/format/typecheck, Unit tests — API, E2E tests — API, E2E tests — API (MinIO mode), Dependency audit, Docker image check, CI OK, CodeQL; mobile jobs success or skipped; Build & push skipped. (2) The Docker image check log shows the production-refusal step passing (quote its lines). (3) The E2E logs list sync-query-budget, list-pagination, public-routes-explain, migration-015-public-indexes, migrate-lock and surveys-upsert-cas as PASS in both modes where applicable. (4) Unit tests — API has no "threshold … not met". If a job fails, diagnose, fix on the branch in the owning plan's files, push, re-verify and record the deviation. Record PR URL, run URLs, job table and quoted lines in the "CI Evidence" section and set CI-proven rows to ✅.</action>
  <verify>
    <automated>grep -c "actions/runs/" /home/user/cortege/.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 01.7-VALIDATION.md contains the PR URL, at least one `actions/runs/` URL, a job table including "E2E tests — API (MinIO mode)" and "Docker image check", and quoted production-refusal smoke lines
    - The PR body contains "Checklist propriétaire" and the EXPLAIN and budget tables
  </acceptance_criteria>
  <done>The PR's CI proves the phase on real PostgreSQL, real MinIO and the built image, with evidence recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Owner's single VPS visit, then merge</name>
  <files>.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md (CI Evidence)
    - infra/vps/README.md (MinIO backup and configuration check sections)
  </read_first>
  <action>Present the checklist below (with <branche>, the PR URL and the CI run URL filled in), wait for the resume signal, then record the owner's answers (check-env verdict, backup size, merge time, pull output, time the timer was restarted) in the "Owner visit" section of 01.7-VALIDATION.md.</action>
  <what-built>Everything in the PR is green on CI. Only the VPS shell steps need the owner (Claude has no VPS access); no phone test is needed.</what-built>
  <how-to-verify>
    Checklist — une seule séance sur le VPS (remplacer <branche> par le nom de la branche de la PR) :

    1. Sauvegarder le volume MinIO (le stockage s'arrête quelques secondes) :
       cd /home/ubuntu/cortege
       VOL=$(docker volume ls -q | grep 'cortege_minio_data$')
       docker compose -f infra/docker-compose.vps.yml --env-file /home/ubuntu/cortege.env stop minio
       docker run --rm -v "$VOL":/data:ro -v /home/ubuntu:/backup alpine tar czf /backup/minio-data-$(date +%F).tgz -C /data .
       docker compose -f infra/docker-compose.vps.yml --env-file /home/ubuntu/cortege.env start minio
       ls -lh /home/ubuntu/minio-data-*.tgz
       → l'archive ne doit pas être vide.

    2. Désactiver CORS (l'app mobile n'en a pas besoin) :
       grep -q '^CORS_ORIGIN=' /home/ubuntu/cortege.env && sed -i 's/^CORS_ORIGIN=.*/CORS_ORIGIN=none/' /home/ubuntu/cortege.env || echo 'CORS_ORIGIN=none' >> /home/ubuntu/cortege.env
       grep '^CORS_ORIGIN=' /home/ubuntu/cortege.env
       → doit afficher CORS_ORIGIN=none.

    3. Vérifier le fichier d'environnement, et recommencer jusqu'à « OK » :
       git -C /home/ubuntu/cortege fetch -q origin <branche> && git -C /home/ubuntu/cortege show origin/<branche>:infra/vps/check-env.sh | bash -s -- /home/ubuntu/cortege.env
       → doit afficher « OK : … ». Sinon, corriger chaque ligne « ERREUR » dans /home/ubuntu/cortege.env et relancer. Les lignes « INFO » peuvent être supprimées ou laissées.

    4. Suspendre le déploiement automatique :
       sudo systemctl stop cortege-deploy.timer

    5. Fusionner la PR sur GitHub : <URL de la PR>

    6. Attendre la fin de l'image CI sur main (3 à 5 minutes, job « Build & push Docker image » en vert) :
       https://github.com/florianlepont/cortege/actions/workflows/ci.yml?query=branch%3Amain

    7. Mettre à jour le dépôt sur le VPS :
       git -C /home/ubuntu/cortege pull --ff-only

    8. Relancer le déploiement automatique (le nouveau script, avec la vérification, fait le déploiement) :
       sudo systemctl start cortege-deploy.timer
       (ou une fois à la main : sudo systemctl start cortege-deploy.service)

    C'est tout ; Claude vérifie ensuite la production.
  </how-to-verify>
  <resume-signal>Répondre « fait : sauvegarde OK (taille), check-env OK, timer arrêté, PR fusionnée, pull OK, timer relancé », ou coller la sortie de l'étape qui a échoué</resume-signal>
  <verify>
    <automated>grep -q "Owner visit" /home/user/cortege/.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The owner confirmed all eight steps (backup, CORS_ORIGIN=none, check-env OK, timer stopped, merge, CI image done, pull --ff-only, timer restarted), and 01.7-VALIDATION.md records them
  </acceptance_criteria>
  <done>The VPS env is valid for the new rules, the MinIO data is backed up, the PR is merged, and the first deploy runs the new guarded script.</done>
</task>

<task type="auto">
  <name>Task 3: Post-deploy verification from outside</name>
  <files>.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md</files>
  <read_first>
    - scripts/owner-check-simulation.mjs (production phase)
  </read_first>
  <action>Per D-17/D-19, wait for the main CI run to publish the image (record its run URL), then run the post-deploy checks from the interfaces block: poll /v1/health until 200, then run the `production` simulation phase against https://cortege.algernon.ovh/v1. Record in 01.7-VALIDATION.md a "Post-deploy checks" table (check, expected, observed) including health, test-token 404, unauthenticated 401, public map 200, the CORS/credentials probe (new image running) and MinIO health. If the new image is not detected after 30 minutes, record ❌ with the observation and tell the owner exactly what to run (interfaces block). Set the remaining Per-Task rows, `status: complete` and `nyquist_compliant: true` when every row is ✅.</action>
  <verify>
    <automated>cd /home/user/cortege && test "$(curl -s -o /dev/null -w '%{http_code}' https://cortege.algernon.ovh/v1/health)" = 200 && SIM_BASE=https://cortege.algernon.ovh/v1 node scripts/owner-check-simulation.mjs production && grep -q "Post-deploy checks" .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - /v1/health returns 200 after the deploy and the production phase reports 0 FAIL
    - 01.7-VALIDATION.md has the Post-deploy checks table and no ⬜ row left
  </acceptance_criteria>
  <done>Production runs the new image with the validated configuration, MinIO is up on the new image, and the evidence is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → GitHub | The agent pushes a branch and opens a PR; merging stays with the owner |
| Owner shell → VPS | Env file edits and backups |
| Deployed API → installed apps | New rules meet apps that were not updated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.7-56 | Elevation of Privilege | Agent merging to main | mitigate | Merge is an owner step in the blocking checkpoint |
| T-01.7-57 | Denial of Service | Deploy with an invalid env, or the first deploy run by the old unguarded script | mitigate | check-env OK required before the merge; timer stopped across the merge and the checkout pulled before restart, so the first deploy runs the guarded script (D-21); the guard keeps the old stack otherwise |
| T-01.7-58 | Repudiation | MinIO data loss at the image switch without a restore point | mitigate | Backup is step 1, before the merge (D-19); restore procedure in README |
| T-01.7-59 | Repudiation | CI or production claims without evidence | mitigate | Run URLs, job table, quoted lines and the post-deploy table recorded |
| T-01.7-60 | Denial of Service | Installed apps broken by the deploy | mitigate | Response shapes unchanged (E2E), unpaginated lists identical, production probes after deploy |
</threat_model>

<verification>
- PR CI fully green with evidence; owner visit recorded; production health and probes green
</verification>

<success_criteria>
ROADMAP criteria 1-5 hold on CI and in production, with the owner's single VPS visit done in the D-19 order.
</success_criteria>

<output>
Create `.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-14-SUMMARY.md` when done
</output>
