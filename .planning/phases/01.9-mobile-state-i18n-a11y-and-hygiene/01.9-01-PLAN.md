---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/state/render-counts.test.tsx
  - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-before.json
autonomous: true
requirements: [REQ-AUD-mobile-state]

must_haves:
  truths:
    - "A committed Jest test mounts mobile/App.tsx with fake navigators that render every tab and stack screen at once, and counts renders per screen and list rows (D-02)"
    - "The test runs on the unchanged pre-phase code and pins the measured counts for four scenarios: initial mount, one status update, one form keystroke, one-survey refresh"
    - "The before numbers are saved as JSON in the phase directory, so the phase gate can compare them with the after numbers"
    - "The harness only mocks stable seams (screen entry modules, navigation libraries, react-native, gesture-handler Swipeable, storage, API, auth session), so later plans can re-run it without rewriting it"
  artifacts:
    - path: mobile/src/state/render-counts.test.tsx
      provides: "Render-count harness with EXPECTED counts per scenario and an optional JSON writer"
      contains: "RENDER_COUNTS_OUT"
    - path: .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-before.json
      provides: "Pre-phase render counts (D-02 before)"
      contains: "statusUpdate"
  key_links:
    - from: mobile/src/state/render-counts.test.tsx
      to: mobile/App.tsx
      via: "react-test-renderer mount of the default export"
      pattern: "from \"../../App\""
---

<objective>
Build the render-count harness that stands in for the React DevTools before/after profile (D-02), run it on the pre-phase code, and record the "before" numbers.

Purpose: ROADMAP criterion 1 needs a measurable before/after. The harness is written first, on today's code, so the baseline is honest. Plans 01.9-17 (contexts in the tree) and 01.9-21 (list rows) update its expectations; the phase gate (01.9-28) records the after numbers in VALIDATION.md.
Output: `mobile/src/state/render-counts.test.tsx` (green on today's code with the measured counts pinned) and `01.9-render-counts-before.json`.
This plan changes no production file.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-RESEARCH.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@CLAUDE.md

<interfaces>
Test stack (verified): mobile unit tests are Jest 29 + ts-jest, `testEnvironment: "node"`, config `mobile/jest.unit.config.js` (`.test.tsx` is collected). Run one file with `npm --workspace mobile run test:unit -- src/state/render-counts.test.tsx`. `react-test-renderer` 19.2.3 is installed (deprecated upstream; suppress its warning as `mobile/src/screens/AuthGateScreen.test.ts:1-22` does).

Mount target: `mobile/App.tsx` default export. Today it calls useSurveyForm, useSurveyList, useSurveySync, useNearbyParcels, usePublicMapExplorer, useEditingDraft, useSurveyDraftPatcher, useGpsCapture, then renders `AuthenticatedAppNavigation` (mobile/src/app/AuthenticatedAppNavigation.tsx) with 24 props and three overlays. Every screen is mounted through a `Screen` render callback today; after plan 01.9-23 screens are mounted with `component=`. The fake navigators must support both forms.

Seams to mock (stable across the phase; keep these module paths):
- `react-native`: string host components via a `mockComponent(name)` helper (copy `AuthGateScreen.test.ts:24-80`), `StyleSheet.create: (s) => s`, `StyleSheet.absoluteFill`, `Platform` (OS "android", `select: (o) => o.android ?? o.default`), `Alert.alert: jest.fn()`, `Animated` stub with `Value`, `event: () => jest.fn()`, `View`, `ScrollView`, and `FlatList`/`Animated.FlatList` that render `data.slice(0, props.initialNumToRender ?? 10).map((item, index) => renderItem({ item, index }))` plus `ListHeaderComponent`/`ListEmptyComponent` (the list becomes a FlatList in plan 21; the mock must already exist so the harness keeps working).
- `react-native-gesture-handler` (`GestureHandlerRootView` passthrough) and `react-native-gesture-handler/Swipeable` (default export = a probe component that increments `rowRenders` and renders its children): each survey row is a Swipeable before and after the phase, so Swipeable renders = row renders.
- `react-native-safe-area-context` (`SafeAreaProvider`, `SafeAreaView` passthrough, `useSafeAreaInsets: () => ({ top: 0, bottom: 0, left: 0, right: 0 })`).
- `@react-navigation/native` (`NavigationContainer` passthrough, `useNavigation` returning a stable fake navigation object with `navigate`, `goBack`, `addListener: () => () => undefined`, `getParent`, `setOptions`, `getFocusedRouteNameFromRoute: () => undefined`, `useFocusEffect: () => undefined`, `useIsFocused: () => true`), `@react-navigation/bottom-tabs` (`createBottomTabNavigator`, `useBottomTabBarHeight: () => 0`, `BottomTabBarHeightContext` created with `React.createContext`), `@react-navigation/native-stack` (`createNativeStackNavigator`) and `@bottom-tabs/react-navigation` (`createNativeBottomTabNavigator`, same fake). A fake navigator factory returns `{ Navigator: ({ children }) => <>{children}</>, Screen: FakeScreen }` where FakeScreen renders `props.component` as an element (with `{ navigation: fakeNavigation, route: { key, name, params: props.initialParams ?? {} } }`) or calls `props.children` with the same object. Screens with required params (e.g. `surveyFactorDetail` needs `{ factor }`) get `params: { factor: "A", surveyId: "s-1", mode: "edit" }` as a union default.
- `expo-constants` (default `{ executionEnvironment: "bare", appOwnership: null }`, `ExecutionEnvironment: { StoreClient: "storeClient" }`), `expo-network` (connected), `expo-location`, `expo-image-picker`, `react-native-auth0`, `react-native-maps` (stub components), `react-native-screens` (empty) as needed until the import graph loads.
- Screen entry modules replaced by probes that count renders and return null: `../screens/HomeScreen`, `SurveyDetailScreen`, `SurveyFormScreen`, `FactorDetailScreen`, `SurveyParcelSelectionScreen`, `PublicMapScreen`, `AccountScreen`, `SettingsScreen`, `AuthGateScreen`, `ProfileSetupScreen`, `LocalDataOwnerConflictScreen` (paths relative to `mobile/src/state/`). The phase keeps these entry paths and export names (screen splits put parts in sub-folders), so the mocks stay valid. `SurveyListScreen` is NOT a probe: it is wrapped by a counting passthrough (`jest.mock` returning `{ SurveyListScreen: (props) => { counts.surveyList += 1; return React.createElement(actual.SurveyListScreen, props) } }` with `jest.requireActual`), so its rows really render through the Swipeable probe.
- Session: mock `../hooks/useAuth0Session` with a STABLE authenticated session object (pattern `mobile/src/hooks/useSurveySync.logout-purge.test.ts:78-101`: stable object, `isAuthenticated: true`, `currentUser` with first/last name set so the profile overlay stays hidden) and capture the `reportStatus` argument into a module variable: that is the status entry point before and after the phase.
- Keystroke trigger: mock `../hooks/useSurveyForm` as a passthrough to `jest.requireActual` that stores the latest returned `setSiteName` in a module variable. This works before and after the phase whatever the screens read.
- Local data owner: mock `../hooks/useLocalDataOwner` (or its storage) so `status` is "ok" and `syncAllowed` true (no conflict overlay).
- Storage: mock `../storage/db` (`initLocalDb` resolves), `../storage/attachments` (`persistLegacyAttachmentFiles` resolves, list returns []), `../storage/surveys` / `../storage` so `listLocalSurveys` resolves to a mutable in-test array of 20 `LocalSurvey` objects (drafts with `site_name`, `updated_at`, `completion_rate` 0-100) and attachment listing resolves []. The one-survey refresh scenario replaces one element with a new object (changed `site_name`) and calls the captured `refreshLocalSurveys` (capture it through a passthrough mock of `../hooks/useSurveyList`, same technique as useSurveyForm).
- API: mock `../api/ibp-api` functions to resolve empty payloads.

Scenarios and output (one `describe`, sequential `it`s sharing one mounted tree, or one `it` with four phases):
1. `initialMount`: counts after mount and all bootstrap promises flushed (`await act(async () => { await flushPromises() })` several times).
2. `statusUpdate`: reset counters, `await act(async () => capturedReportStatus("session", "idle", "x"))`.
3. `formKeystroke`: reset, `await act(async () => capturedSetSiteName("a"))`. Do not advance timers (autosave may be debounced; record what happens synchronously).
4. `oneSurveyRefresh`: reset, change one survey, `await act(async () => capturedRefreshLocalSurveys())`.
Each scenario records `{ home, surveyList, surveyDetail, surveyForm, factorDetail, parcelSelection, publicMap, account, settings, rows }` (probe counts and Swipeable row renders; screens that are not mounted count 0). The harness uses fake timers only if the tree needs them; document the choice in the file header.
A constant `EXPECTED` in the file holds the numbers for each scenario and the test asserts `toEqual(EXPECTED[scenario])`. When `process.env.RENDER_COUNTS_OUT` is set, the test also writes `{ generatedAt, gitHead?, scenarios }` JSON to that path (`fs.writeFileSync`, only in that case).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the render-count harness and make it mount the current App tree</name>
  <files>mobile/src/state/render-counts.test.tsx</files>
  <read_first>
    - mobile/App.tsx (whole file: hooks called, overlays, asset requires)
    - mobile/src/app/AuthenticatedAppNavigation.tsx:1-230 and :780-961 (navigators, native-tab lazy require, AppTabs)
    - mobile/src/screens/AuthGateScreen.test.ts:1-133 (react-test-renderer + mocked react-native recipe)
    - mobile/src/hooks/useSurveySync.logout-purge.test.ts:1-140 (stable session mock, storage/API/network mocks)
    - mobile/src/hooks/render-hook-wrapper.test.tsx (tsx test compiles under ts-jest)
    - mobile/src/screens/SurveyListScreen.tsx:1-60 (imports that the passthrough list pulls in)
    - mobile/jest.unit.config.js (moduleNameMapper already covers vector-icons, expo-sqlite, expo-haptics, expo-image, images)
  </read_first>
  <action>Create `mobile/src/state/render-counts.test.tsx` per the interfaces block (D-02). Header comment: what is measured, why it replaces a DevTools profile (no device available; the owner no longer profiles, STATE 2026-09-25), which seams are mocked and why they stay stable across the phase, and how to regenerate the JSON (`RENDER_COUNTS_OUT=<abs path> npm --workspace mobile run test:unit -- src/state/render-counts.test.tsx`). Declare every `jest.mock` factory before importing App (mock factories may only reference variables prefixed `mock`; name module-level counters and captured callbacks `mockCounts`, `mockCaptured…`). Add mocks one import error at a time until the tree mounts: the goal is that App mounts, all tab and stack screens render once through the fake navigators, and the real SurveyListScreen renders one Swipeable per seeded survey. Keep every mock in this one test file (a separate non-test helper under `src/state/` would count against coverage; test files are outside the 400-line rule, which covers `screens/` and `navigation/` only). Temporarily leave `EXPECTED` empty and print the measured counts with `console.log(JSON.stringify(...))` while developing.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/state/render-counts.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - The suite mounts App without throwing and each scenario produces a counts object with all ten keys
    - In `initialMount`, `surveyList >= 1`, `settings >= 1` and `rows === 20` (every seeded survey rendered one Swipeable)
    - `grep -c "jest.mock(\"../../App\"" mobile/src/state/render-counts.test.tsx` returns 0 (App itself is real)
    - `grep -c "RENDER_COUNTS_OUT" mobile/src/state/render-counts.test.tsx` returns at least 1
  </acceptance_criteria>
  <done>The harness mounts the real pre-phase App tree with probes and prints per-scenario counts.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the before counts and save the baseline JSON</name>
  <files>mobile/src/state/render-counts.test.tsx, .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-before.json</files>
  <read_first>
    - mobile/src/state/render-counts.test.tsx (as written in Task 1)
  </read_first>
  <action>Run the harness three times and confirm the counts are identical each run (deterministic). Fill `EXPECTED` with the measured numbers for the four scenarios, remove the development `console.log`, and add a comment above `EXPECTED`: "Pre-phase baseline (01.9-01, D-02). Plans 01.9-17 and 01.9-21 change these numbers; every change must be a decrease for statusUpdate/formKeystroke/oneSurveyRefresh." Then run once with `RENDER_COUNTS_OUT=/home/user/cortege/.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-before.json` to write the baseline (include `gitHead` from `git rev-parse --short HEAD` passed through an env var `RENDER_COUNTS_GIT_HEAD` if set). In the SUMMARY, paste the four scenario rows as a table and state the research expectation it confirms or refutes (research predicted that one status update and one keystroke each re-render every mounted screen and every row). If the measurement contradicts that prediction, say so plainly; do not tune mocks to match it. Run the full mobile unit suite and lint to make sure nothing else is affected.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/state/render-counts.test.tsx && node -e "const j=require('./.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-before.json');for(const k of ['initialMount','statusUpdate','formKeystroke','oneSurveyRefresh']){if(!j.scenarios||!j.scenarios[k])process.exit(1)}" && npm --workspace mobile run test:unit && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `01.9-render-counts-before.json` exists with the four scenarios, each holding the ten keys
    - The harness passes three consecutive runs with the pinned `EXPECTED`
    - `grep -c "console.log" mobile/src/state/render-counts.test.tsx` returns 0
    - Full mobile unit suite, lint and format check green
  </acceptance_criteria>
  <done>The before numbers are committed in the test and in the phase directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → filesystem | The harness can write a JSON file when an env var is set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-01 | Tampering | render-counts.test.tsx JSON writer | accept | Writes only when RENDER_COUNTS_OUT is set by the developer; test-only code, never bundled |
| T-01.9-02 | Repudiation | Baseline numbers tuned after the fact | mitigate | Baseline recorded on the unchanged tree with the git head in the JSON; later plans may only lower the counts (comment above EXPECTED) |
</threat_model>

<verification>
- The harness is deterministic, green on the pre-phase code, and the baseline JSON exists
</verification>

<success_criteria>
D-02 "before" half: committed, reproducible render counts for a status update, a keystroke and a one-survey refresh on the pre-phase code.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-01-SUMMARY.md` when done
</output>
