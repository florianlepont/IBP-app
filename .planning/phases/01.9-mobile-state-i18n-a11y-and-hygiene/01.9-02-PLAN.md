---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/hooks/useSurveySync.test.ts
  - mobile/src/hooks/useNearbyParcels.test.ts
  - mobile/src/hooks/useParcelStatuses.test.ts
  - mobile/src/hooks/useSurveyList.test.ts
autonomous: true
requirements: [REQ-AUD-mobile-state]

must_haves:
  truths:
    - "useSurveySync.test.ts renders the real hook with renderHook from @testing-library/react-native/pure and no longer spies on React (D-01, pattern map C-4)"
    - "useNearbyParcels.test.ts, useParcelStatuses.test.ts and useSurveyList.test.ts are rewritten the same way (D-01 lists all ten spy-based tests; the other six are rewritten in parallel by 01.9-03)"
    - "Every behaviour the old tests asserted is still asserted, and the hooks/ coverage row still passes"
    - "sync-owner-gate.test.ts and useSurveySync.logout-purge.test.ts are untouched and green (01.5 invariants)"
  artifacts:
    - path: mobile/src/hooks/useSurveySync.test.ts
      provides: "renderHook-based tests of the real useSurveySync"
      contains: "@testing-library/react-native/pure"
  key_links:
    - from: mobile/src/hooks/useSurveySync.test.ts
      to: mobile/src/hooks/useSurveySync.ts
      via: "renderHook(() => useSurveySync(PARAMS))"
      pattern: "renderHook\\("
---

<objective>
Rewrite four of the ten React-spying hook tests, starting with `useSurveySync.test.ts`, onto the `renderHook` recipe (D-01, pattern map C-4, Pitfall 1).

Purpose: plan 01.9-09 adds `useMemo`/`useContext` to `useSurveySync`; a test that calls hooks outside a renderer with `jest.spyOn(React, …)` would crash on the real dispatcher. D-01 requires these rewrites first. The other six (`useSurveySyncSurveyOperations`, `useSurveySyncProfile`, `useSurveySyncNetwork`, `useEditingDraft`, `useSurveyForm`, `usePublicMapExplorer`) are rewritten by the parallel tests-only plan 01.9-03, so all ten are on renderHook before 01.9-09.
Output: four test files on renderHook, same assertions, no production change.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@CLAUDE.md

<interfaces>
Recipe (mobile/src/hooks/render-hook-smoke.test.ts:1-26 header, useSurveySync.logout-purge.test.ts:1-140):
1. `jest.mock("react-native", () => ({ Alert: { alert: jest.fn() }, Platform: { OS: "ios" } }))` (a plain object listing only what the hook imports) BEFORE importing `@testing-library/react-native/pure`.
2. `import { act, cleanup, renderHook, waitFor } from "@testing-library/react-native/pure"`; `renderHook` and `act` are async (`await`); `afterEach(async () => { await cleanup() })`.
3. Collaborator mocks return STABLE objects (inline objects re-run effects; see logout-purge `:78-101`).
4. Frozen `PARAMS` with `noopAsync`/`noop` (logout-purge `:110-122`).
Spy sites to replace: useSurveySync.test.ts `:183-190` and `:272` (`jest.spyOn(React, "useState"|"useCallback"|"useEffect"|"useRef")`); the same pattern in the three smaller tests (grep `spyOn(React`).
Coverage row that must stay green: `./src/hooks/` statements 85, branches 69, functions 83, lines 87 (mobile/jest.unit.config.js). Run `npm --workspace mobile run test:unit:coverage` at the end.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite useSurveySync.test.ts on renderHook</name>
  <files>mobile/src/hooks/useSurveySync.test.ts</files>
  <read_first>
    - mobile/src/hooks/useSurveySync.test.ts (whole file: list every `it` and what it asserts before editing)
    - mobile/src/hooks/useSurveySync.ts (whole file)
    - mobile/src/hooks/useSurveySync.logout-purge.test.ts (whole file: the working recipe for this exact hook)
    - mobile/src/hooks/render-hook-smoke.test.ts:1-40
  </read_first>
  <action>Per D-01 and C-4, first write the list of existing test names and assertions into a comment block at the top of your working notes (not the file). Replace the React spies by rendering the real hook: stub the sub-hooks (`useAuth0Session`, `useLocalDataOwner`, `useSurveySyncNetwork`, `useSurveySyncProfile`, `useSurveySyncSurveyOperations`, `useAttachmentPreviews`) as stable plain objects the way logout-purge does, or keep them real where the old test relied on real behaviour. Keep every behaviour assertion (debug reset alerts, status text, detail/event loading, purge wiring); where an old test asserted an internal React call count, replace it with the observable effect (state value after `act`, a mock call). Do not assert exact English status strings that plan 01.9-20 will replace: assert them through a local constant so 19 only edits one place. Do not touch the production hook.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/hooks/useSurveySync && grep -c "spyOn(React" mobile/src/hooks/useSurveySync.test.ts | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "spyOn(React" mobile/src/hooks/useSurveySync.test.ts` returns 0
    - `grep -c "@testing-library/react-native/pure" mobile/src/hooks/useSurveySync.test.ts` returns 1
    - The number of `it(` cases is equal to or greater than before the rewrite (state both numbers in the SUMMARY)
    - useSurveySync.test.ts, useSurveySync.logout-purge.test.ts and sync-owner-gate.test.ts pass; the last two are unchanged (`git diff --stat` shows neither)
  </acceptance_criteria>
  <done>The central hook test renders the real hook and survives the addition of useMemo/useContext.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite useNearbyParcels, useParcelStatuses and useSurveyList tests on renderHook</name>
  <files>mobile/src/hooks/useNearbyParcels.test.ts, mobile/src/hooks/useParcelStatuses.test.ts, mobile/src/hooks/useSurveyList.test.ts</files>
  <read_first>
    - mobile/src/hooks/useNearbyParcels.ts and .test.ts
    - mobile/src/hooks/useParcelStatuses.ts and .test.ts
    - mobile/src/hooks/useSurveyList.ts and .test.ts
    - mobile/src/hooks/render-hook-smoke.test.ts:1-40
  </read_first>
  <action>Apply the same recipe to the three files (D-01 names all ten spy-based tests). Keep each existing behaviour assertion; timers (debounce, retry) use `jest.useFakeTimers()` with `await act(async () => { jest.advanceTimersByTime(n) })`. Then run the hooks coverage and the whole mobile unit suite.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/hooks/(useNearbyParcels|useParcelStatuses|useSurveyList)" && test "$(grep -c 'spyOn(React' mobile/src/hooks/useNearbyParcels.test.ts mobile/src/hooks/useParcelStatuses.test.ts mobile/src/hooks/useSurveyList.test.ts | awk -F: '{s+=$2} END {print s}')" = 0 && npm --workspace mobile run test:unit:coverage && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - No `spyOn(React` remains in the three files
    - `it(` counts equal or higher than before (listed in the SUMMARY)
    - `npm --workspace mobile run test:unit:coverage` passes its thresholds (hooks row unchanged)
  </acceptance_criteria>
  <done>Four of the ten spy-based hook tests are on renderHook; the other six are rewritten by 01.9-03 in the same wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Test-only change; no runtime input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-03 | Tampering | Loss of test assertions during rewrite (silent coverage drop on sync/purge paths) | mitigate | Case counts before/after recorded in SUMMARY; logout-purge and owner-gate suites kept unchanged; coverage thresholds enforced |
</threat_model>

<verification>
- Four rewritten suites green, no React spies left in them, coverage thresholds met
</verification>

<success_criteria>
The hook tests that plan 01.9-09 would break are on renderHook before any production change (D-01).
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-02-SUMMARY.md` when done
</output>
