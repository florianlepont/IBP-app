---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.test.ts
  - mobile/src/hooks/survey-sync/useSurveySyncProfile.test.ts
  - mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts
  - mobile/src/hooks/useEditingDraft.test.ts
  - mobile/src/hooks/useSurveyForm.test.ts
  - mobile/src/hooks/usePublicMapExplorer.test.ts
autonomous: true
requirements: [REQ-AUD-mobile-state]

must_haves:
  truths:
    - "The six remaining React-spying hook tests render the real hooks with renderHook from @testing-library/react-native/pure; together with 01.9-02, all ten spy-based tests are on renderHook before the context refactor (D-01, C-4)"
    - "Every assertion of the six files is kept unchanged in meaning: same cases, same expected calls, same expected English messages (the catalogue plans 01.9-11, 01.9-20, 01.9-21 and 01.9-23 later swap only the asserted strings)"
    - "No production file changes; after this plan `grep -rln \"spyOn(React\" mobile/src` lists nothing once 01.9-02 is merged"
  artifacts:
    - path: mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.test.ts
      provides: "renderHook-based tests of the survey operations hook"
      contains: "@testing-library/react-native/pure"
    - path: mobile/src/hooks/usePublicMapExplorer.test.ts
      provides: "renderHook-based tests of the public map explorer"
      contains: "renderHook("
  key_links:
    - from: mobile/src/hooks/useSurveyForm.test.ts
      to: mobile/src/hooks/useSurveyForm.ts
      via: "renderHook(() => useSurveyForm())"
      pattern: "renderHook\\("
---

<objective>
Move the six remaining React-spying hook tests onto `renderHook`, with their assertions unchanged, before any hook gains `useMemo`/`useContext`.

Purpose: D-01 requires all ten spy-based hook tests (C-4) to be on renderHook before the context refactor (01.9-09 depends on this plan and on 01.9-02). Tests only: no production change, no message change.
Output: six rewritten test files.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@CLAUDE.md

<interfaces>
Recipe (`mobile/src/hooks/render-hook-smoke.test.ts:1-26`, `useSurveySync.logout-purge.test.ts:1-140`): declare `jest.mock("react-native", () => ({ … }))` as a plain object listing only what the hook imports, BEFORE importing `@testing-library/react-native/pure`; `renderHook` and `act` are async (`await`); `afterEach(async () => { await cleanup() })`; collaborator mocks return STABLE objects; frozen params with `noop`/`noopAsync`; timers with `jest.useFakeTimers()` and `await act(async () => { jest.advanceTimersByTime(n) })`.
Sizes: useSurveySyncSurveyOperations.test.ts 722 lines, useSurveySyncNetwork.test.ts 406, useSurveySyncProfile.test.ts 326, useEditingDraft.test.ts 329, useSurveyForm.test.ts 532, usePublicMapExplorer.test.ts 185.
Keep the assertions as they are, English message literals included. Where a test asserted an internal React call count, replace it with the observable effect (state after `act`, a mock call) and name each such case in the SUMMARY. Record `it(` counts before/after per file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Survey operations test on renderHook</name>
  <files>mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.test.ts</files>
  <read_first>
    - mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.ts and its test (whole files)
    - mobile/src/hooks/render-hook-smoke.test.ts:1-40
  </read_first>
  <action>Per D-01/C-4, replace the React spies with a real renderHook render of the hook, keeping every case and assertion (English messages included). No production change.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/hooks/survey-sync/useSurveySyncSurveyOperations && test "$(grep -c 'spyOn(React' mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.test.ts)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - No React spy; `it(` count not lower than before; `git diff --stat -- mobile/src/hooks/survey-sync/*.ts ':!*.test.ts'` is empty
  </acceptance_criteria>
  <done>The largest spy-based test renders the real hook.</done>
</task>

<task type="auto">
  <name>Task 2: Profile and network tests on renderHook</name>
  <files>mobile/src/hooks/survey-sync/useSurveySyncProfile.test.ts, mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts</files>
  <read_first>
    - mobile/src/hooks/survey-sync/useSurveySyncProfile.ts and its test
    - mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts and its test
  </read_first>
  <action>Same method (D-01, C-4); network cases keep single-flight, retry/backoff, owner-gate refusal, reconnect auto-sync and pull.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/hooks/survey-sync/(useSurveySyncProfile|useSurveySyncNetwork)" && test "$(grep -c 'spyOn(React' mobile/src/hooks/survey-sync/useSurveySyncProfile.test.ts mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts | awk -F: '{s+=$2} END {print s}')" = 0</automated>
  </verify>
  <acceptance_criteria>
    - No React spy in either file; `it(` counts not lower
  </acceptance_criteria>
  <done>All survey-sync hook tests render the real hooks.</done>
</task>

<task type="auto">
  <name>Task 3: Editing, form and public-map explorer tests on renderHook</name>
  <files>mobile/src/hooks/useEditingDraft.test.ts, mobile/src/hooks/useSurveyForm.test.ts, mobile/src/hooks/usePublicMapExplorer.test.ts</files>
  <read_first>
    - mobile/src/hooks/useEditingDraft.ts, useSurveyForm.ts, usePublicMapExplorer.ts and their tests
    - mobile/src/hooks/useEditingDraft.autosave.test.ts (already renders the hook; reuse its setup)
  </read_first>
  <action>Same method (D-01, C-4). Then run the whole mobile suite with coverage (hooks row must hold), lint and format.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/hooks/(useEditingDraft|useSurveyForm|usePublicMapExplorer)" && test -z "$(grep -rln 'spyOn(React' mobile/src | grep -v 'useSurveySync.test.ts\|useNearbyParcels.test.ts\|useParcelStatuses.test.ts\|useSurveyList.test.ts')" && npm --workspace mobile run test:unit:coverage && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - No React spy outside the four files owned by the parallel plan 01.9-02
    - Coverage thresholds pass; no production file changed
  </acceptance_criteria>
  <done>All ten spy-based hook tests are on renderHook once 01.9-02 and this plan are merged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Test-only change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-58 | Tampering | Silent loss of assertions on sync, owner-gate and form-validation paths during the rewrite | mitigate | Assertions kept verbatim; `it(` counts before/after in SUMMARY; coverage thresholds enforced |
</threat_model>

<verification>
- Six rewritten suites green; no React spies outside 01.9-02's files; coverage met
</verification>

<success_criteria>
D-01's "rewritten with renderHook first" holds for all ten tests before 01.9-09.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-03-SUMMARY.md` when done
</output>
