---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - mobile/README-native.md
  - docs/audits/audit-2026-09-code-complet.md
autonomous: true
requirements: [REQ-AUD-hygiene]

must_haves:
  truths:
    - "CI has a native-android job (ubuntu, expo prebuild + gradle assembleRelease, unsigned or debug-keystore signed) and a native-ios job (macOS, expo prebuild + pod install + xcodebuild Release for the iOS simulator, CODE_SIGNING_ALLOWED=NO) (D-10)"
    - "Both jobs are path-filtered to mobile/** and the root package files, can be started by workflow_dispatch, and are wired into CI OK as success-or-skipped (D-10)"
    - "Both jobs ran green on a pull request's CI on the pre-hygiene tree, and the run URL is recorded in this plan's SUMMARY for the phase gate"
    - "The audit report has a Statut section linking each finding closed by phases 01.2 to 01.7 to its PR (#127 to #157); 01.8 and 01.9 rows are marked pending (D-12)"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "native-android and native-ios jobs, native path filter, ci-ok wiring"
      contains: "native-ios"
    - path: docs/audits/audit-2026-09-code-complet.md
      provides: "Statut section with PR links for 01.2-01.7"
      contains: "## 8. Statut"
  key_links:
    - from: ".github/workflows/ci.yml ci-ok"
      to: "native-android, native-ios"
      via: "needs + result loop"
      pattern: "NATIVE_ANDROID_RESULT"
---

<objective>
Add native Android and iOS build jobs to CI, prove them green on a PR before any dependency change, document them, and write the audit status rows for the phases already merged.

Purpose: REQ-AUD-hygiene and ROADMAP criterion 5 ("native iOS and Android builds still passing") without a device (D-10). The baseline run on the unchanged tree lets plan 01.9-18 show that the dependency removals did not break native builds. D-12 says the audit status rows for 01.2–01.7 are written now.
Output: two CI jobs, README-native section, audit "Statut" section, a green PR run URL in the SUMMARY.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-RESEARCH.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@CLAUDE.md

<interfaces>
ci.yml facts: triggers `push: main`, `pull_request: main` (`:3-7`); job `changes` (`:17-52`) with `dorny/paths-filter` outputs api/mobile/shared/image; `mobile-build` (`:359-404`) is the model (checkout pinned by SHA with `persist-credentials: false`, `actions/setup-node` v7 pinned SHA with node 22 + npm cache, `actions/cache` of node_modules keyed on package-lock, `npm ci` on miss, `working-directory: mobile`); `ci-ok` (`:568-614`) lists `needs` and loops over `*_RESULT` with success-or-skipped. Every action is pinned by full SHA with a version comment; new actions (`actions/setup-java`) must be pinned the same way (look up the SHA of the current v4/v5 release tag with `git ls-remote https://github.com/actions/setup-java refs/tags/v4*` or the GitHub API). actionlint runs in the `check` job, so the YAML must pass it.

Job design (Claude's discretion on names and caching, CONTEXT):
- `changes` gains output `native` with filter `mobile/**`, `package.json`, `package-lock.json`, `.github/workflows/ci.yml`. Add `workflow_dispatch:` under `on:`; for dispatch runs the native jobs run unconditionally (`if: github.event_name == 'workflow_dispatch' || needs.changes.outputs.native == 'true'`).
- `native-android` (Name "Native build — Android"): ubuntu-latest, timeout 60, setup-node 22 + npm ci (reuse the node_modules cache key), `actions/setup-java` distribution temurin java 17, `npx expo prebuild -p android --no-install --clean` in mobile, then `./gradlew assembleRelease --no-daemon` in mobile/android (RESEARCH A3: the Expo template signs release with the debug keystore; if that is not true here, use `assembleDebug` and say so in the job comment). Provide dummy public env for prebuild/config: `EXPO_PUBLIC_API_URL=https://ci.invalid/v1`, `EXPO_PUBLIC_AUTH0_DOMAIN=ci.example.auth0.com`, `EXPO_PUBLIC_AUTH0_CLIENT_ID=ci`, `EXPO_PUBLIC_AUTH0_AUDIENCE=https://ci.invalid/api` if the config or the react-native-auth0 plugin needs them (no secrets exist in this repo's CI; never add any).
- `native-ios` (Name "Native build — iOS"): macos-latest (RESEARCH A2: if the default Xcode cannot build Expo 57 / RN 0.86, pin `macos-26` or select a newer Xcode with `sudo xcode-select -s /Applications/Xcode_<v>.app` and comment why), timeout 90, setup-node 22 + npm ci (separate cache key with runner.os), `npx expo prebuild -p ios --clean` (runs pod install), then `xcodebuild -workspace ios/<Name>.xcworkspace -scheme <Name> -configuration Release -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build` (derive <Name> with `ls ios/*.xcworkspace`), same dummy env.
- `ci-ok`: add both to `needs`, add `NATIVE_ANDROID_RESULT` / `NATIVE_IOS_RESULT` env, echo lines, and include them in the success-or-skipped loop.
- Native projects are generated, never committed (`mobile/README-native.md`); `.gitignore` already ignores `mobile/ios` and `mobile/android` (verify).

Audit status (D-12): `docs/audits/audit-2026-09-code-complet.md` (French) has findings with anchors (M-C1, A-C1, ARCH-n, CI-n …) and a lot plan in `docs/audits/plan-remediation-2026-09.md`. RESEARCH Pattern 12 verified the merge-commit → phase mapping: #127–#130 = 01.2, #131–#142 = 01.3, #143–#147 = 01.4, #148–#152 = 01.5, #153–#155 = 01.6, #156–#157 = 01.7. The ROADMAP "Source" line of each phase names its lots/findings. Add "## 8. Statut" at the end: a French intro sentence, then one table row per finding: `| Réf. | Constat (court) | Phase | PR |`, PR as `[#N](https://github.com/florianlepont/cortege/pull/N)` (several PRs allowed). Findings owned by 01.8 or 01.9 get `01.8` / `01.9` and "en attente" (closing plan 01.9-29 fills them). Derive rows with git (`git log --merges --first-parent main --format='%H %s'`, then `git log --format=%s M^1..M^2` per merge) plus the ROADMAP Source lines; do not guess a PR you cannot trace, mark it "à vérifier" instead and list those in the SUMMARY.

GitHub access for the proof run: the `gh` CLI may be absent; use the GitHub MCP tools if available. If nothing can push or read Actions runs, stop at Task 3 with an access gate and ask the orchestrator for the run URL and the two job conclusions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Native build jobs in ci.yml and README-native</name>
  <files>.github/workflows/ci.yml, mobile/README-native.md</files>
  <read_first>
    - .github/workflows/ci.yml (whole file)
    - mobile/README-native.md
    - mobile/app.json and mobile/app.config.* if present (plugins that need env at prebuild)
    - .gitignore (mobile/ios, mobile/android ignored)
  </read_first>
  <action>Implement the job design above (D-10). Keep existing jobs untouched except `changes` outputs/filters, `on:` (add workflow_dispatch) and `ci-ok`. Add to `mobile/README-native.md` a short section "Native builds in CI" (English, like the file): what each job builds, that no signing or secret is used, the path filter, how to start them by hand (Actions → CI → Run workflow), and that a device check is still needed for what CI cannot see (tab bar look after login). Validate locally: YAML parse and actionlint (download the pinned actionlint the `check` job uses, or run it through docker `rhysd/actionlint`), and prove the Android prebuild part works on Linux: `cd mobile && EXPO_PUBLIC_API_URL=https://ci.invalid/v1 npx expo prebuild -p android --no-install --clean` succeeds, then delete the generated `mobile/android` directory (never commit it).</action>
  <verify>
    <automated>cd /home/user/cortege && node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8');for(const k of ['native-android:','native-ios:','workflow_dispatch','NATIVE_ANDROID_RESULT','NATIVE_IOS_RESULT','CODE_SIGNING_ALLOWED=NO','assemble']){if(!y.includes(k)){console.error('missing',k);process.exit(1)}}" && (command -v actionlint >/dev/null && actionlint .github/workflows/ci.yml || docker run --rm -v "$PWD":/repo -w /repo rhysd/actionlint:latest .github/workflows/ci.yml) && git status --porcelain mobile/android mobile/ios | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml contains both jobs, the `native` filter output, `workflow_dispatch`, and both results in ci-ok's loop
    - Every `uses:` line added is pinned by a 40-character SHA (`grep -E "uses: actions/setup-java@[0-9a-f]{40}" .github/workflows/ci.yml` matches)
    - actionlint reports no error
    - No generated native directory is tracked
  </acceptance_criteria>
  <done>CI can build both native apps without signing or secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Audit status section for phases 01.2 to 01.7</name>
  <files>docs/audits/audit-2026-09-code-complet.md</files>
  <read_first>
    - docs/audits/audit-2026-09-code-complet.md (finding ids and anchors)
    - docs/audits/plan-remediation-2026-09.md (lots → findings)
    - .planning/ROADMAP.md (Source lines of phases 01.2 to 01.9)
  </read_first>
  <action>Per D-12, generate the finding → phase → PR mapping with git and the ROADMAP Source lines as described in the interfaces block, and append the "## 8. Statut" section in French. Rows for findings assigned to 01.8/01.9 say "en attente" (plan 01.9-29 completes them). Keep the rest of the audit unchanged. In the SUMMARY, list the counts: findings linked, pending (01.8/01.9), "à vérifier".</action>
  <verify>
    <automated>cd /home/user/cortege && grep -c "^## 8. Statut" docs/audits/audit-2026-09-code-complet.md | grep -qx 1 && grep -c "cortege/pull/1[2-5][0-9]" docs/audits/audit-2026-09-code-complet.md && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - The Statut section exists once and links at least one PR in #127–#157 for each of 01.2, 01.3, 01.4, 01.5, 01.6, 01.7
    - Every finding id from the "Constats" sections appears in the table (count reported in the SUMMARY)
  </acceptance_criteria>
  <done>Merged work is traceable from the audit to its PR.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the native jobs green on a pull request (baseline before hygiene)</name>
  <files>(none: CI run evidence goes in this plan's SUMMARY)</files>
  <read_first>
    - .github/workflows/ci.yml (job names added in Task 1)
  </read_first>
  <action>Push this plan's branch and open a DRAFT pull request against main titled "01.9 native build probe (draft, do not merge)" with a two-line English body (purpose: prove the new native jobs on the unchanged tree; the phase PR supersedes it) ending with the attribution lines from the session's system reminder. Wait for the run (poll up to 120 minutes). If a native job fails, read its log, fix the job definition (Xcode selection, Java version, env, gradle task), push, and re-run; record every attempt. When both "Native build — Android" and "Native build — iOS" are green and "CI OK" is green, record in the SUMMARY: PR URL, run URL, job table with conclusions and durations, the xcodebuild "BUILD SUCCEEDED" line and the gradle "BUILD SUCCESSFUL" line quoted from the logs. Then close the draft PR without merging (its commits reach main through the phase PR). If no GitHub access exists, raise an access gate asking the orchestrator for these facts.</action>
  <verify>
    <automated>grep -E "actions/runs/[0-9]+" /home/user/cortege/.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY holds a run URL where both native jobs and CI OK concluded success, with the quoted build-success lines
    - The draft PR is closed, not merged
  </acceptance_criteria>
  <done>Native builds are proven on CI before any dependency change (baseline for 01.9-18 and the gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → third-party actions | New pinned action (setup-java), macOS runner |
| PR from repo branch → CI | Workflow runs with read-only contents permission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-08 | Tampering | actions/setup-java supply chain | mitigate | Pinned by full SHA with version comment, like every existing action |
| T-01.9-09 | Information Disclosure | Native jobs needing secrets | mitigate | No signing, no secrets: dummy EXPO_PUBLIC_* values, CODE_SIGNING_ALLOWED=NO; workflow permissions stay contents: read |
| T-01.9-10 | Denial of Service | Long macOS jobs blocking PRs | accept | Path filter to mobile/root package files; timeouts 60/90 min; public repo minutes are free (D-10) |
</threat_model>

<verification>
- actionlint clean; draft PR run green on both native jobs; audit Statut section present
</verification>

<success_criteria>
D-10 native builds exist in CI and are green on the pre-hygiene tree; D-12's "written now" audit rows exist.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-05-SUMMARY.md` when done
</output>
