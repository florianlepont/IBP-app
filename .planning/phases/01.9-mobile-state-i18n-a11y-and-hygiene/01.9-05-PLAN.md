---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/i18n/index.ts
  - mobile/src/i18n/status.ts
  - mobile/src/i18n/catalogue.test.ts
  - mobile/src/i18n/fr/index.ts
  - mobile/src/i18n/fr/common.ts
  - mobile/src/i18n/fr/sync-errors.ts
  - mobile/src/i18n/fr/navigation.ts
  - mobile/src/i18n/fr/home.ts
  - mobile/src/i18n/fr/components.ts
  - mobile/src/i18n/fr/survey-list.ts
  - mobile/src/i18n/fr/survey-detail.ts
  - mobile/src/i18n/fr/survey-form.ts
  - mobile/src/i18n/fr/factor-detail.ts
  - mobile/src/i18n/fr/parcel-selection.ts
  - mobile/src/i18n/fr/profile-setup.ts
  - mobile/src/i18n/fr/owner-conflict.ts
  - mobile/src/i18n/fr/settings.ts
  - mobile/src/i18n/fr/labels.ts
  - mobile/src/i18n/fr/auth-gate.ts
  - mobile/src/i18n/fr/account.ts
  - mobile/src/i18n/fr/public-map.ts
  - mobile/src/i18n/fr/validation.ts
  - mobile/src/i18n/fr/status/app.ts
  - mobile/src/i18n/fr/status/session.ts
  - mobile/src/i18n/fr/status/owner.ts
  - mobile/src/i18n/fr/status/sync.ts
  - mobile/src/i18n/fr/status/debug.ts
  - mobile/src/i18n/fr/status/survey-ops.ts
  - mobile/src/i18n/fr/status/profile.ts
  - mobile/src/i18n/fr/status/editing.ts
  - mobile/src/i18n/fr/status/gps.ts
  - mobile/src/i18n/fr/status/map.ts
  - mobile/src/i18n/fr/status/index.ts
  - mobile/src/app/formatters.ts
  - mobile/src/app/formatters.test.ts
  - mobile/app.json
autonomous: true
requirements: [REQ-AUD-i18n-a11y]

must_haves:
  truths:
    - "A typed French catalogue exists under mobile/src/i18n with no library and no native dependency; property access on the fr object is type-checked (D-06, PROJECT.md French only with i18n in place)"
    - "Every section of the catalogue exists from this plan on, one file per owning plan, so parallel plans each fill their own file and never edit a shared one"
    - "Status messages are a branded StatusMessage type built only by catalogue functions; raw technical detail goes to console.debug behind shouldShowDevTools() (D-06)"
    - "The per-survey sync error shown to users comes from the catalogue, keyed by last_sync_error_code when present"
    - "The iOS permission dialogs in app.json are in French (D-06)"
    - "src/i18n/ is covered at 100% statements and lines by its own tests, so it passes the global bucket until 01.9-09 adds its threshold row (pattern map C-8; this plan does not edit mobile/jest.unit.config.js, which 01.9-01 edits in the same wave)"
  artifacts:
    - path: mobile/src/i18n/fr/index.ts
      provides: "export const fr (as const), type Catalog"
      contains: "as const"
    - path: mobile/src/i18n/status.ts
      provides: "StatusMessage brand, statusText(), logStatusDetail()"
      exports: ["statusText", "logStatusDetail"]
    - path: mobile/src/i18n/catalogue.test.ts
      provides: "No empty text, no id-like text, every function entry returns text"
  key_links:
    - from: mobile/src/app/formatters.ts
      to: mobile/src/i18n/fr/sync-errors.ts
      via: "formatSyncErrorForUser reads fr.syncErrors"
      pattern: "fr\\.syncErrors"
---

<objective>
Create the French catalogue skeleton, the status message type and the sync-error texts, and translate the iOS permission strings.

Purpose: REQ-AUD-i18n-a11y, D-06. Every later plan that migrates strings fills exactly one section file created here, so screen and hook plans can run in parallel.
Output: `mobile/src/i18n/**`, `formatSyncErrorForUser` on the catalogue, French `app.json` permissions, the `./src/i18n/` threshold row.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-RESEARCH.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@CLAUDE.md

<interfaces>
Catalogue contract (other plans code against these names; keep them):
- `mobile/src/i18n/fr/index.ts`: `export const fr = { common, syncErrors, navigation, home, components, surveyList, surveyDetail, surveyForm, factorDetail, parcelSelection, profileSetup, ownerConflict, settings, labels, authGate, account, publicMap, validation, status } as const` and `export type Catalog = Widen<typeof fr>` where `Widen` maps string literal types to `string` recursively and keeps function types (so a second language would be `const en: Catalog`; none is built).
- One file per section. This plan creates `fr/common.ts` and `fr/sync-errors.ts` with content, and every other section as an empty `export const <name>Fr = {} as const` in its own file, so owners never collide. Files and owning plans:
  `fr/navigation.ts` (01.9-25), `fr/home.ts` and `fr/components.ts` (01.9-16), `fr/survey-list.ts` (01.9-22 then 01.9-27), `fr/survey-detail.ts` (01.9-12), `fr/survey-form.ts` (01.9-13), `fr/factor-detail.ts`, `fr/parcel-selection.ts`, `fr/profile-setup.ts`, `fr/owner-conflict.ts`, `fr/settings.ts`, `fr/labels.ts` (all 01.9-17), `fr/auth-gate.ts` (01.9-14), `fr/account.ts` (01.9-15), `fr/public-map.ts` (01.9-23 then 01.9-28), `fr/validation.ts` (01.9-21), and `fr/status/{app,session,owner,sync,debug,survey-ops,profile,editing,gps,map}.ts` aggregated by `fr/status/index.ts` as `status = { app, session, owner, sync, debug, surveyOps, profile, editing, gps, map }` (owners: session/owner/sync/debug 01.9-20; surveyOps/profile 01.9-11; app/editing/gps 01.9-21; map 01.9-23).
- `fr/common.ts`: shared words lifted from existing French UI (buttons Annuler, Confirmer, Supprimer, Réessayer, Fermer, Enregistrer, OK; "Relevé sans titre"; "À l'instant"; survey status labels brouillon/soumis/synchronisé/erreur/expiré as used by `mobile/src/screens/survey-screen-helpers.ts` and `mobile/src/app/survey-logic.ts`). Only this plan edits common.ts; later plans put anything else in their own section.
- `mobile/src/i18n/status.ts`: `declare const statusBrand: unique symbol; export type StatusMessage = string & { readonly [statusBrand]: true }`; `export function statusText(text: string): StatusMessage` (to be called only inside `src/i18n/`; 01.9-29 adds a lint restriction); `export function logStatusDetail(context: string, detail: unknown): void` = `console.debug` of context and detail only when `shouldShowDevTools()` (`mobile/src/app/dev-tools.ts:8-10`) is true. Status entries in the catalogue are functions returning `StatusMessage`, with parameters that carry names and counts only (never ids).
- `mobile/src/i18n/index.ts`: re-exports `fr`, `Catalog`, `StatusMessage`, `statusText`, `logStatusDetail`.
- Callers keep `setStatus(message: string)` in this phase until 01.9-29 narrows it to `StatusMessage`; a `StatusMessage` is assignable to `string`, so migrated and unmigrated callers coexist during the waves.

Sync errors today (`mobile/src/app/formatters.ts:33-53`): `SYNC_ERROR_PATTERNS` regex table + `formatSyncErrorForUser(rawError)` with French messages. New: `formatSyncErrorForUser(rawError?: string | null, code?: string | null)`: when `code` maps to a `fr.syncErrors.byCode` entry return it; otherwise keep today's pattern fallback (rows written before error codes existed) with its texts moved to `fr.syncErrors.patterns`; final fallback `fr.syncErrors.generic`. The optional second parameter keeps today's call sites compiling; the list and detail plans pass `survey.last_sync_error_code`. Find the code values written to `last_sync_error_code` in `mobile/src/storage/sync.ts` and `mobile/src/hooks/survey-sync/*` and give each a French message.

app.json (`mobile/app.json:13-25`): plugin options `photosPermission`, `cameraPermission`, `locationWhenInUsePermission` (plus any `NS*UsageDescription` under `ios.infoPlist`) become French sentences that say why the app needs access (photos of the parcel, GPS position of the survey). Plugin options only: no native code change, applied at the next prebuild.

Coverage (C-8): files outside the listed directories fall into the `global` bucket (statements 100, lines 100). Do NOT edit `mobile/jest.unit.config.js` (01.9-01 edits it in this wave; 01.9-09 adds the `./src/i18n/` row later). Instead the catalogue test must execute every statement of `src/i18n/**` (it calls every function leaf; status.ts is tested in both dev-tools states). Check with `npm --workspace mobile run test:unit:coverage` and `node scripts/coverage-by-directory.js mobile`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Catalogue skeleton, status type and catalogue test</name>
  <files>mobile/src/i18n/index.ts, mobile/src/i18n/status.ts, mobile/src/i18n/catalogue.test.ts, mobile/src/i18n/fr/index.ts, mobile/src/i18n/fr/common.ts, mobile/src/i18n/fr/status/index.ts, and the 16 section files mobile/src/i18n/fr/{navigation,home,components,survey-list,survey-detail,survey-form,factor-detail,parcel-selection,profile-setup,owner-conflict,settings,labels,auth-gate,account,public-map,validation}.ts and the 10 files mobile/src/i18n/fr/status/{app,session,owner,sync,debug,survey-ops,profile,editing,gps,map}.ts</files>
  <read_first>
    - mobile/src/app/brand-tokens.ts:1-23 (nested as const idiom)
    - mobile/src/hooks/operation-status.ts:1-9 (typed unions, export type style)
    - mobile/src/app/dev-tools.ts (shouldShowDevTools signature)
    - mobile/src/screens/survey-screen-helpers.ts and mobile/src/app/survey-logic.ts (existing French status labels to lift into common)
  </read_first>
  <behavior>
    - Every string leaf of fr is non-empty and matches no id pattern (UUID, /survey-\d+/i, /\b[0-9a-f]{8,}\b/)
    - Every function leaf, called with a Proxy argument that returns 2 for keys matching /count|total|synced|failed|n$/i and "Parcelle du Bois" otherwise, returns a non-empty string with no id pattern
    - statusText("x") is the string "x" at runtime
    - logStatusDetail calls console.debug only when shouldShowDevTools() is true (inject by mocking ../app/dev-tools)
    - fr.common.untitledSurvey is "Relevé sans titre"
  </behavior>
  <action>Write the test first, then the files, per D-06 and the interfaces contract. The walker in the test recurses over `fr` so every later section is checked automatically as plans fill it. Create every section file listed in the interfaces block (empty `as const` objects except common and sync-errors) and wire them in `fr/index.ts` and `fr/status/index.ts`. No i18n library (D-06).</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/i18n && npm --workspace mobile run typecheck && ls mobile/src/i18n/fr/*.ts mobile/src/i18n/fr/status/*.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `ls mobile/src/i18n/fr/*.ts | wc -l` is at least 19 and `ls mobile/src/i18n/fr/status/*.ts | wc -l` is 11 (10 sections + index)
    - `grep -c "unique symbol" mobile/src/i18n/status.ts` returns 1
    - `grep -rn "i18n-js\|expo-localization" mobile/package.json` returns nothing
    - Every behavior bullet is a passing test
  </acceptance_criteria>
  <done>The typed catalogue and its per-section files exist; later plans fill them without shared edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sync-error texts by code, French permission strings, full i18n coverage</name>
  <files>mobile/src/i18n/fr/sync-errors.ts, mobile/src/app/formatters.ts, mobile/src/app/formatters.test.ts, mobile/app.json</files>
  <read_first>
    - mobile/src/app/formatters.ts and mobile/src/app/formatters.test.ts
    - mobile/src/storage/sync.ts (every place writing last_sync_error_code) and mobile/src/hooks/survey-sync/utils.ts
    - mobile/app.json
  </read_first>
  <behavior>
    - formatSyncErrorForUser(null) is null; formatSyncErrorForUser("x", "<known code>") returns that code's French text; formatSyncErrorForUser("HTTP 503", null) returns the network text (legacy fallback); an unknown code with unmatched text returns fr.syncErrors.generic
    - No returned text contains the raw input
  </behavior>
  <action>Move the French texts of `SYNC_ERROR_PATTERNS` into `fr/sync-errors.ts` (`byCode`, `patterns`, `generic`), add a French message for each error code the app writes, and change `formatSyncErrorForUser` to the two-parameter form in the interfaces block (existing call sites keep compiling). Extend formatters.test.ts. Translate the app.json permission strings (D-06). Confirm `src/i18n/**` is at 100% statements and lines (C-8).</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/(i18n|app/formatters)" && node -e "const a=require('./mobile/app.json');const s=JSON.stringify(a);if(/Allow \$\(PRODUCT_NAME\)|to access your|would like to/i.test(s))process.exit(1)" && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && cd mobile && npx expo config --type public >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SYNC_ERROR_PATTERNS" mobile/src/app/formatters.ts` returns 0 or the table holds no string literal message (texts come from fr.syncErrors)
    - `git diff --stat mobile/jest.unit.config.js` is empty and `src/i18n/**` shows 100% statements and lines in the coverage summary
    - app.json permission strings are French (verify command) and `npx expo config` still parses the config
    - Coverage thresholds pass
  </acceptance_criteria>
  <done>Sync errors and permission dialogs read in French from one place, and the new folder is covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local error records → UI | Stored error text and codes from sync are displayed to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-06 | Information Disclosure | formatSyncErrorForUser echoing raw server/network text | mitigate | Output only from fr.syncErrors; test asserts the raw input never appears |
| T-01.9-07 | Information Disclosure | Technical detail in status messages | mitigate | logStatusDetail logs only behind shouldShowDevTools(); catalogue test rejects id-like text |
</threat_model>

<verification>
- i18n and formatters tests green, typecheck green, coverage thresholds met, app.json parses
</verification>

<success_criteria>
The catalogue framework of D-06 exists and is ready for per-plan filling.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-05-SUMMARY.md` when done
</output>
