---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/surveys/dtos/public-map-items-query.dto.ts
  - api/src/surveys/public-map.service.ts
  - api/src/surveys/public-map.queries.ts
  - api/test/public-map.service.spec.ts
  - api/test/public-map-bbox.e2e-spec.ts
  - docs/technical/api-contract-v1.md
  - scripts/owner-check-simulation.mjs
autonomous: true
requirements: [REQ-AUD-mobile-state]

must_haves:
  truths:
    - "GET /v1/public/map-items accepts an optional bbox=minLng,minLat,maxLng,maxLat and returns only public items whose linked parcel centroid lies inside it (D-05)"
    - "Without bbox the response is identical to today's (installed apps unaffected, D-05)"
    - "A malformed bbox returns 400 with the fixed parseBbox message and no echo; a bbox longer than 128 characters returns 400 from the ValidationPipe"
    - "The bbox predicate uses the 01.7 centroid index: EXPLAIN on the test database shows no Seq Scan on parcels for the bbox query"
    - "The new E2E lives in api/test/public-map-bbox.e2e-spec.ts; surveys-idempotency.e2e-spec.ts is not edited (01.8 splits it)"
  artifacts:
    - path: api/test/public-map-bbox.e2e-spec.ts
      provides: "HTTP cases + EXPLAIN for the bbox filter"
      contains: "bbox"
    - path: api/src/surveys/public-map.queries.ts
      provides: "buildPublicMapItemsQuery with an appended bbox EXISTS predicate"
      contains: "centroid_lat BETWEEN"
  key_links:
    - from: api/src/surveys/public-map.service.ts
      to: api/src/surveys/surveys-normalize.utils.ts
      via: "parseBbox(input?.bbox)"
      pattern: "parseBbox\\("
---

<objective>
Add the optional `bbox` filter to `GET /v1/public/map-items`, prove it with a new E2E spec and an EXPLAIN, document it, and add read-only production probes to the owner-check simulation.

Purpose: ROADMAP criterion 3 ("the map requests by bbox") needs the server to filter; today only `/public/parcels/status` takes a bbox (D-05, RESEARCH Pattern 7). The change is additive, so installed apps keep working.
Output: DTO field, service/query change, unit + E2E tests, contract doc, simulation probes.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@CLAUDE.md

<interfaces>
DTO analog `api/src/surveys/dtos/public-parcel-statuses-query.dto.ts:1-8`: `@IsOptional() @IsString() @MaxLength(128) bbox?: string` (copy verbatim into `PublicMapItemsQueryDto`). Controller `api/src/surveys/public.controller.ts:10-13` passes the DTO unchanged.
Service `public-map.service.ts`: `getPublicMapItems(input?: { from?; to?; region? })` at `:68-86`; `parseBbox` is already imported (`:21`) and used at `:89-99`. `parseBbox` (`surveys-normalize.utils.ts:342-365`) returns `{ minLng, minLat, maxLng, maxLat } | null` and throws fixed-message BadRequestException on malformed input.
Query builder `public-map.queries.ts:45-97`: `conditions`/`values` push pattern, limit-first inner subquery, `PUBLIC_SURVEY_PREDICATE` (`:11`, T-01.7-42) and `PUBLIC_MAP_ITEMS_LIMIT = 500` (`:14`) must stay. Add `bbox?: { minLng: number; minLat: number; maxLng: number; maxLat: number } | null` to `PublicMapItemsFilters` (`:19-26`) and append the predicate LAST (so from/to/region parameter numbers do not move), inside the limit-first subquery: `EXISTS (SELECT 1 FROM survey_parcels sp JOIN parcels p ON p.parcel_id = sp.parcel_id WHERE sp.survey_id = s.id AND p.centroid_lng BETWEEN $a::double precision AND $b::double precision AND p.centroid_lat BETWEEN $c::double precision AND $d::double precision)`, casts copied from `PUBLIC_PARCEL_STATUSES_BBOX_SQL` (`:136-139`). Extend the doc comment at `:28-44` with "01.9 D-05: optional bbox, index idx_parcels_centroid_lat_lng".
E2E setup analog: `api/test/list-pagination.e2e-spec.ts:1-15` (imports, header comment citing decisions); EXPLAIN helpers `api/test/public-routes-explain.e2e-spec.ts:150-188` (`explainJson`, `seqScansOn`, `indexesUsed`) and its per-filter loop `:251-264`; HTTP assertion style `surveys-idempotency.e2e-spec.ts:642-656` (read only, do not edit that file).
E2E run (01.7 practice): PostgreSQL 16 (`pg_ctlcluster 16 main start`, role ibp/ibp); this plan's database `ibp_p19_07_test`; ACCESS_TOKEN_SECRET unset; every run under `flock /tmp/ibp-e2e.lock`: `cd /home/user/cortege && env -u ACCESS_TOKEN_SECRET POSTGRES_DB=ibp_p19_07_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p19-07 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e -- <pattern>`.
Contract doc: `docs/technical/api-contract-v1.md:931-947` (map-items entry). Simulation: `scripts/owner-check-simulation.mjs:934-939` (`phaseProduction` map-items probe).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bbox in DTO, service and query builder with unit and E2E tests</name>
  <files>api/src/surveys/dtos/public-map-items-query.dto.ts, api/src/surveys/public-map.service.ts, api/src/surveys/public-map.queries.ts, api/test/public-map.service.spec.ts, api/test/public-map-bbox.e2e-spec.ts</files>
  <read_first>
    - api/src/surveys/dtos/public-map-items-query.dto.ts, api/src/surveys/dtos/public-parcel-statuses-query.dto.ts
    - api/src/surveys/public-map.service.ts, api/src/surveys/public-map.queries.ts
    - api/src/surveys/surveys-normalize.utils.ts:330-370 (parseBbox)
    - api/test/public-map.service.spec.ts, api/test/public-routes-explain.e2e-spec.ts, api/test/list-pagination.e2e-spec.ts
    - api/test/surveys-idempotency.e2e-spec.ts:600-660 (how public items are seeded and asserted; read only)
  </read_first>
  <behavior>
    - Unit (public-map.service.spec.ts): buildPublicMapItemsQuery({}) text and values are byte-identical to before the change (compare with a snapshot taken before editing); with bbox the values end with the four numbers and the text contains the EXISTS predicate after the region/date conditions
    - Unit: getPublicMapItems({ bbox: "1,2" }) rejects with BadRequestException carrying parseBbox's fixed message
    - E2E 1: without bbox, /v1/public/map-items returns the same items as the query without bbox (seed one public survey inside and one outside a small box)
    - E2E 2: bbox around the inside parcel returns the inside public survey, not the outside one, and not a private survey whose parcel is inside the box
    - E2E 3: bbox "a,b,c,d" → 400, response body does not contain "a,b,c,d"
    - E2E 4: bbox of 129 characters → 400
    - E2E 5: EXPLAIN (FORMAT JSON) of the bbox query on the seeded data has no Seq Scan on parcels (seqScansOn helper), and the spec also runs the no-bbox query to show it is unchanged
  </behavior>
  <action>Per D-05: write the unit snapshot of today's query first (red → green discipline), then the E2E spec (new file only; do NOT edit surveys-idempotency.e2e-spec.ts), then implement the DTO field, service parameter and query predicate as in the interfaces block. Seed parcels with explicit centroid_lat/centroid_lng values (check how migration 015 made them generated or plain columns and seed accordingly). If the planner's EXISTS shape shows a Seq Scan on the test data, set `enable_seqscan = off` only inside the EXPLAIN transaction as public-routes-explain does, never in production code, and record the plan in the SUMMARY.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- public-map.service && env -u ACCESS_TOKEN_SECRET POSTGRES_DB=ibp_p19_07_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p19-07 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e -- "public-map-bbox|public-routes-explain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MaxLength(128)" api/src/surveys/dtos/public-map-items-query.dto.ts` returns 1
    - `git diff --stat api/test/surveys-idempotency.e2e-spec.ts` is empty
    - `grep -c "PUBLIC_MAP_ITEMS_LIMIT" api/src/surveys/public-map.queries.ts` is unchanged from before (limit kept)
    - Every behavior bullet is a passing test
  </acceptance_criteria>
  <done>The public map endpoint filters by viewport, index-backed, with old callers unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Contract doc, simulation probes and full API suites</name>
  <files>docs/technical/api-contract-v1.md, scripts/owner-check-simulation.mjs</files>
  <read_first>
    - docs/technical/api-contract-v1.md:900-960
    - scripts/owner-check-simulation.mjs (phaseProduction and the map-items probe at :934-939; how phases are selected)
  </read_first>
  <action>Document `bbox` in the map-items entry: format `minLng,minLat,maxLng,maxLat` (WGS84 degrees), filter on the linked parcel centroid, 400 on malformed or over-128-character input with no echo, no bbox = previous behaviour, still capped at 500 and still rounded display locations (privacy rule unchanged). In `phaseProduction()` add two read-only probes after the existing map-items probe: a bbox around mainland France returns 200 with an `items` array, and a malformed bbox returns 400. Run the simulation's local phase that exercises the API against a locally started API if the script supports it (read its header), otherwise run `node --check scripts/owner-check-simulation.mjs`. Finish with the API unit suite with coverage, lint, typecheck, format, and the full E2E suite on this plan's database.</action>
  <verify>
    <automated>cd /home/user/cortege && node --check scripts/owner-check-simulation.mjs && grep -c "bbox" docs/technical/api-contract-v1.md && npm --workspace api run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && env -u ACCESS_TOKEN_SECRET POSTGRES_DB=ibp_p19_07_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p19-07 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - The contract entry for `/public/map-items` names `bbox`, its format and the 400 case
    - `grep -c "bbox" scripts/owner-check-simulation.mjs` returns at least 2
    - Full E2E suite green on ibp_p19_07_test with ACCESS_TOKEN_SECRET unset
  </acceptance_criteria>
  <done>The API change is documented and replayable against production after deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous client → /v1/public/map-items | Unauthenticated query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-11 | Tampering | bbox query parameter | mitigate | @MaxLength(128) + parseBbox (4 finite numbers, min<max) + parameterised SQL with double precision casts |
| T-01.9-12 | Denial of Service | Large or unindexed bbox scans | mitigate | LIMIT 500 kept; EXISTS on idx_parcels_centroid_lat_lng proven by EXPLAIN in the E2E |
| T-01.9-13 | Information Disclosure | Private surveys leaking through the new filter | mitigate | PUBLIC_SURVEY_PREDICATE unchanged and still first; bbox only narrows; E2E seeds a private survey inside the box and asserts it is absent |
| T-01.9-14 | Information Disclosure | Error echo of attacker input | mitigate | Fixed-message BadRequestException; E2E asserts no echo |
</threat_model>

<verification>
- Unit, targeted and full E2E green on ibp_p19_07_test; contract documented
</verification>

<success_criteria>
The server half of ROADMAP criterion 3: map items can be requested by viewport, additively.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-07-SUMMARY.md` when done
</output>
