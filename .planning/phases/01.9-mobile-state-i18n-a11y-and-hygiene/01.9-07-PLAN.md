---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/storage/db.ts
  - mobile/src/storage/surveys.ts
  - mobile/src/storage/sync.ts
  - mobile/src/storage/utils.ts
  - mobile/src/storage/db.migration.sqlite.test.ts
  - mobile/src/storage/surveys.completion.sqlite.test.ts
  - mobile/src/components/cards/DraftCard.tsx
  - mobile/src/components/cards/DraftCard.test.tsx
  - docs/technical/data-contract-v1.md
autonomous: true
requirements: [REQ-AUD-mobile-state]

must_haves:
  truths:
    - "SQLite migration 2 adds local_surveys.payload_completion (INTEGER NOT NULL DEFAULT 0), backfills it from payload_json inside the migration transaction, and bumps user_version to 2; it is additive and idempotent (D-03)"
    - "The four payload write sites store payload_completion; status-only updates are unchanged (D-03)"
    - "listLocalSurveys no longer selects or parses payload_json; completion_rate comes from SQL with submitted = 100 (D-03)"
    - "listLocalSurveys on 500 surveys runs without a single JSON.parse (D-03 500-survey proof, storage half)"
    - "DraftCard treats completion_rate as 0-100: colour threshold >= 100, factor count Math.round(rate / 10), width and label `${rate}%` (D-03, pattern map C-5)"
  artifacts:
    - path: mobile/src/storage/db.ts
      provides: "migration2, SCHEMA_VERSION = 2"
      contains: "SCHEMA_VERSION = 2"
    - path: mobile/src/storage/surveys.completion.sqlite.test.ts
      provides: "Write sites, list read, 500-row no-parse proof"
    - path: mobile/src/components/cards/DraftCard.test.tsx
      provides: "0-100 rendering regression test"
  key_links:
    - from: mobile/src/storage/surveys.ts listLocalSurveys
      to: local_surveys.payload_completion
      via: "CASE WHEN status = 'submitted' THEN 100 ELSE payload_completion END AS completion_rate"
      pattern: "payload_completion END AS completion_rate"
---

<objective>
Precompute survey completion at write time in a new SQLite column, stop parsing every payload when listing, and fix the DraftCard 0–1 vs 0–100 bugs.

Purpose: ROADMAP criterion 2 ("completion is precomputed at write time instead of parsing every payload") and D-03. This is the storage half of the fluid 500-survey list; plan 01.9-21 does the rendering half.
Output: migration 2, four write sites, list query, DraftCard fix, tests, data-contract note.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-CONTEXT.md
@docs/technical/sync-conflict-resolution-v1.md
@CLAUDE.md

<interfaces>
Migration runner (`mobile/src/storage/db.ts`): `SCHEMA_VERSION` (`:20`), `ensureColumn(tx, table, column, ddl)` (`:47-64`), `migration1` (`:66-111`, doc comment "additive only"), `MIGRATIONS = [migration1]` (`:133-135`, migration N at index N-1), runner bumps `PRAGMA user_version` atomically (`:204-221`). Fresh installs get the pre-phase baseline `CREATE TABLE` and migrate like every other install (`:146-147`): do NOT add the column to the CREATE TABLE text.
Completion (`mobile/src/storage/utils.ts:66-97`): `computeCompletionRate(status, payload)`; split out `computePayloadCompletion(payload): number` (0-100 integer, the body after the submitted short-circuit) and keep `computeCompletionRate = status === "submitted" ? 100 : computePayloadCompletion(payload)`.
Write sites (only those writing payload_json): `createLocalDraft` INSERT `surveys.ts:44-62`; `updateLocalDraft` UPDATE `surveys.ts:386-407`; `applyRemoteChanges` INSERT `sync.ts:293-306` and UPDATE `sync.ts:315-330`. Status-only updates (`surveys.ts:510,551,595`; `sync.ts:76,538,758,1246,1262,1282`) are unchanged.
List read today (`surveys.ts:428-447`) selects payload_json and parses each row. New: select every column except payload_json plus `CASE WHEN status = 'submitted' THEN 100 ELSE payload_completion END AS completion_rate`; the returned `LocalSurvey` shape is unchanged (check `mobile/src/storage/types.ts` for fields that were derived from the payload in the list mapper; if any list consumer needs a payload-derived field besides completion, add a column for it the same way and say so in the SUMMARY, or keep it out of the list mapper only if no consumer reads it: grep consumers first).
SQLite test recipe: `mobile/src/storage/db.migration.sqlite.test.ts:1-70` (`createNodeSqliteDb()`, `jest.mock("expo-sqlite", …)`, seed a schema string, `initLocalDb()`); `mobile/test/node-sqlite-db.ts`.
DraftCard (`mobile/src/components/cards/DraftCard.tsx`): `:19` `if (survey.completion_rate >= 1) return brandColors.moss`; `:35` `Math.round(survey.completion_rate * 10)`; `:36` `` `${Math.round(survey.completion_rate * 100)}%` ``. Render test: react-test-renderer with a mocked `react-native` (copy the mockComponent recipe from `mobile/src/screens/AuthGateScreen.test.ts:24-80`), mapped `@expo/vector-icons`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 2, write sites and list read</name>
  <files>mobile/src/storage/db.ts, mobile/src/storage/surveys.ts, mobile/src/storage/sync.ts, mobile/src/storage/utils.ts, mobile/src/storage/db.migration.sqlite.test.ts, mobile/src/storage/surveys.completion.sqlite.test.ts</files>
  <read_first>
    - mobile/src/storage/db.ts (whole file)
    - mobile/src/storage/utils.ts:40-110
    - mobile/src/storage/surveys.ts:30-160 and :370-460
    - mobile/src/storage/sync.ts:270-340
    - mobile/src/storage/types.ts (LocalSurvey)
    - mobile/src/storage/db.migration.sqlite.test.ts and mobile/test/node-sqlite-db.ts
  </read_first>
  <behavior>
    - Migration from a v1 fixture (schema after migration1, PRAGMA user_version = 1) with two drafts at different fill levels and one submitted survey: payload_completion equals computePayloadCompletion of each payload, user_version is 2; running initLocalDb again changes nothing (idempotent)
    - A row whose payload_json is not valid JSON gets payload_completion 0 and the migration completes
    - A v0 (pre-phase) fixture migrates through 1 and 2 in one initLocalDb call
    - createLocalDraft and updateLocalDraft write payload_completion equal to computePayloadCompletion(payload)
    - applyRemoteChanges insert and update write payload_completion
    - listLocalSurveys returns completion_rate 100 for submitted rows and payload_completion for others, equal to the pre-change computeCompletionRate result for the same rows
    - With 500 seeded surveys, listLocalSurveys returns 500 rows while a JSON.parse spy records 0 calls, and the SQL text sent to the db contains no "payload_json"
  </behavior>
  <action>Per D-03, write the tests first, then implement: `computePayloadCompletion` split, `migration2` (ensureColumn + backfill loop with `safeParseJson`/`toSurveyQueuePayload` inside the migration tx, doc comment "additive only, 01.9 D-03"), `SCHEMA_VERSION = 2`, `MIGRATIONS = [migration1, migration2]`, the four write sites (one column and one bound value each), and the list query from the interfaces block. Comments cite "01.9 D-03". Keep the returned objects' `completion_rate` computation at the write sites as it is today.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/storage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SCHEMA_VERSION = 2" mobile/src/storage/db.ts` returns 1
    - `grep -c "payload_completion" mobile/src/storage/surveys.ts` is at least 3 and in `mobile/src/storage/sync.ts` at least 2
    - The listLocalSurveys SQL contains no `payload_json` (asserted by test)
    - All storage suites green, including sync.engine/pull/retry and transaction suites unchanged
  </acceptance_criteria>
  <done>Completion is stored at write time and the list no longer parses payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DraftCard 0-100 fix and data-contract note</name>
  <files>mobile/src/components/cards/DraftCard.tsx, mobile/src/components/cards/DraftCard.test.tsx, docs/technical/data-contract-v1.md</files>
  <read_first>
    - mobile/src/components/cards/DraftCard.tsx
    - mobile/src/screens/AuthGateScreen.test.ts:1-80
    - docs/technical/data-contract-v1.md:50-100 (local diagnostic fields)
  </read_first>
  <behavior>
    - completion_rate 40 renders "40%", a width of "40%" and 4 filled factor marks, with the in-progress colour (not moss)
    - completion_rate 100 renders "100%", 10 marks and the moss colour
    - completion_rate 0 renders "0%" and 0 marks
  </behavior>
  <action>Fix the three sites per C-5 (`>= 100`, `Math.round(rate / 10)`, `` `${rate}%` `` with rate clamped to 0-100) and add the render test. Leave DraftCard's existing French literals in place (plan 01.9-15 moves them to the catalogue). In data-contract-v1.md, document the local-only `payload_completion` column, SQLite `user_version` 2 and "submitted = 100 applied at read". Read `docs/technical/sync-conflict-resolution-v1.md` and edit it only if it describes the applyRemoteChanges column list (verify only otherwise, and say which in the SUMMARY). Then run the mobile coverage suite.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/components/cards/DraftCard && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "completion_rate \* 10\|completion_rate \* 100\|completion_rate >= 1)" mobile/src/components/cards/DraftCard.tsx` returns 0
    - `grep -c "payload_completion" docs/technical/data-contract-v1.md` returns at least 1
    - Coverage thresholds pass
  </acceptance_criteria>
  <done>The Home draft card shows the right progress, and the data contract describes the new column.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device SQLite (existing installs) → new app version | Migration runs on real field data at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-15 | Tampering | migration2 on existing local_surveys (field data) | mitigate | Additive column via ensureColumn inside the runner transaction with atomic user_version bump (T-01.5-08 pattern); v0 and v1 fixtures and idempotent re-run tested; payload_json never rewritten |
| T-01.9-16 | Denial of Service | Corrupt payload_json aborting startup migration | mitigate | Backfill uses safeParseJson; unparsable payload yields completion 0, tested |
</threat_model>

<verification>
- Storage, DraftCard and full mobile unit suites green; coverage thresholds met
</verification>

<success_criteria>
D-03 storage half: completion precomputed, list without payload parsing, 500-row proof, DraftCard fixed.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-07-SUMMARY.md` when done
</output>
