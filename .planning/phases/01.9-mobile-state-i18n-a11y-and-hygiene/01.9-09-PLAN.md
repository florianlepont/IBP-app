---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 09
type: execute
wave: 2
depends_on: [01.9-05]
files_modified:
  - api/src/users/email.service.ts
  - api/src/config/env.schema.ts
  - api/src/config/config.types.ts
  - api/src/config/app-config.ts
  - api/test/env.schema.spec.ts
  - api/test/setup-env.js
  - infra/vps/check-env.sh
  - api/test/check-env-parity.spec.ts
  - api/.env.example
  - api/.env.production.example
  - infra/.env.example
  - infra/vps/env.example
  - infra/vps/README.md
  - README.md
  - api/README.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [REQ-AUD-hygiene]

must_haves:
  truths:
    - "EmailService is deleted; no API source imports nodemailer (D-09 owner decision)"
    - "SMTP_* and EMAIL_CHANGE_CONFIRM_URL_TEMPLATE are gone from the config schema, AppConfig, env examples, CI env and READMEs (D-09, pattern map C-2)"
    - "A VPS env file that still sets SMTP_* still starts the API (validateEnv ignores unknown keys, C-1) and check-env.sh prints them as obsolete INFO lines, exit 0, never echoing their values (D-09)"
    - "The API production rules are otherwise unchanged: env.schema, check-config and check-env parity specs stay green"
    - "REFRESH_TOKEN_SECRET is confirmed absent from code and examples (already removed in 01.7; verify only)"
  artifacts:
    - path: infra/vps/check-env.sh
      provides: "SMTP_* and EMAIL_CHANGE_CONFIRM_URL_TEMPLATE in DEAD_VARIABLES"
      contains: "phase 01.9 (D-09)"
    - path: api/test/check-env-parity.spec.ts
      provides: "SMTP_PASSWORD obsolete-line case without echo"
      contains: "SMTP_PASSWORD"
  key_links:
    - from: infra/vps/check-env.sh
      to: "INFO : <VAR> : ligne inutile, peut être supprimée."
      via: "DEAD_VARIABLES loop (:185-189)"
      pattern: "DEAD_VARIABLES"
---

<objective>
Remove the unused e-mail service and every SMTP setting from the API, its configuration, examples, CI and docs, and make the VPS pre-flight check report leftover SMTP lines as obsolete information.

Purpose: REQ-AUD-hygiene, ARCH-7, D-09 (owner decision). Removing the service first lets the single lockfile plan (01.9-18) drop `nodemailer` and `@types/nodemailer` without breaking the build. This plan does not touch any package.json or package-lock.json.
Output: service and config deleted, examples/CI/docs cleaned, check-env.sh + parity spec extended, CLAUDE.md left for 01.9-27.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-CONTEXT.md
@CLAUDE.md

<interfaces>
C-1: `validateEnv` = `plainToInstance(EnvironmentVariables, raw, { enableImplicitConversion: true })` + `validateSync(env, { skipMissingProperties: false })` (`api/src/config/env.schema.ts:87-88`), no whitelist/forbidNonWhitelisted, so unknown keys are ignored and deleting the SMTP fields cannot stop a production start. `infra/vps/check-env.sh`: `DEAD_VARIABLES` array (`:28-37`) printed as `INFO : X : ligne inutile, peut être supprimée.` (`:185-189`); append `SMTP_ENABLED SMTP_HOST SMTP_PORT SMTP_SECURE SMTP_USER SMTP_PASSWORD SMTP_FROM EMAIL_CHANGE_CONFIRM_URL_TEMPLATE` under a comment `# Variables supprimées en phase 01.9 (D-09)`; no new message format.
C-2: `EMAIL_CHANGE_CONFIRM_URL_TEMPLATE` lives in the smtp block (`config.types.ts:68-77`, `app-config.ts:120-130`), only read by EmailService (`email.service.ts:27`): remove it everywhere with SMTP, and drop the whole `smtp` key from AppConfig (including the app-config comment about EmailService's own rule).
Locations: `env.schema.ts:65-73`; `api/test/env.schema.spec.ts:74-75`, `:134`, `:141-146`; `api/test/setup-env.js` (grep SMTP); `api/.env.example:41-50`; `api/.env.production.example:45-53`; `infra/.env.example:33-40`; `infra/vps/env.example:39-46`; `README.md:89-90`; `api/README.md:17-18`; `.github/workflows/ci.yml:212` and `:300` (`SMTP_ENABLED: false`) and `:497` (`-e SMTP_ENABLED=false \`). CLAUDE.md's SMTP row is edited by 01.9-27, not here.
Parity test analog (`check-env-parity.spec.ts:226-233`): "flags dead variables as removable lines, never as errors" with ACCESS_TOKEN_SECRET; add the same with `SMTP_PASSWORD=<unique secret>` asserting exit 0, the INFO line, and that the secret never appears. The spec "env.example with only its CHANGE_ME placeholders replaced passes both checks" (`:275-289`) reads `infra/vps/env.example` and must stay green.
E2E (01.7 practice): database `ibp_p19_09_test`, ACCESS_TOKEN_SECRET unset, under `flock /tmp/ibp-e2e.lock`: `cd /home/user/cortege && env -u ACCESS_TOKEN_SECRET POSTGRES_DB=ibp_p19_09_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p19-09 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete EmailService and the SMTP configuration</name>
  <files>api/src/users/email.service.ts, api/src/config/env.schema.ts, api/src/config/config.types.ts, api/src/config/app-config.ts, api/test/env.schema.spec.ts, api/test/setup-env.js</files>
  <read_first>
    - api/src/users/email.service.ts, api/src/users/users.module.ts (confirm it is registered nowhere)
    - api/src/config/env.schema.ts, api/src/config/config.types.ts, api/src/config/app-config.ts
    - api/test/env.schema.spec.ts, api/test/setup-env.js
  </read_first>
  <behavior>
    - validateEnv({ NODE_ENV: "production", …valid prod env…, SMTP_HOST: "smtp.example", SMTP_PASSWORD: "x" }) does not throw (unknown keys ignored, C-1)
    - loadAppConfig({}) has no `smtp` key
    - The existing production-rule cases still pass
  </behavior>
  <action>Per D-09 and C-2: first confirm with `grep -rn "EmailService\|email.service\|nodemailer" api/src` that only email.service.ts references them, then delete the file, the SMTP/EMAIL_CHANGE fields from the schema, the smtp block from AppConfig and loadAppConfig, and their test cases; add the two behavior cases. Do not touch api/package.json (nodemailer leaves in 01.9-18). Remove the stale compiled `api/dist/users/email.service.js` by rebuilding (`npm --workspace api run build`).</action>
  <verify>
    <automated>cd /home/user/cortege && test ! -e api/src/users/email.service.ts && test -z "$(grep -rln 'nodemailer\|EmailService\|SMTP_\|smtp\b\|EMAIL_CHANGE_CONFIRM_URL_TEMPLATE' api/src api/test/setup-env.js)" && npm --workspace api run test:unit -- "env.schema|check-config" && npm --workspace api run build</automated>
  </verify>
  <acceptance_criteria>
    - api/src has no reference to nodemailer, EmailService, SMTP_ or EMAIL_CHANGE_CONFIRM_URL_TEMPLATE (verify command)
    - Behavior cases pass; build succeeds
  </acceptance_criteria>
  <done>The dead e-mail code and its settings are gone from the API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: check-env obsolete lines, examples, CI env and READMEs</name>
  <files>infra/vps/check-env.sh, api/test/check-env-parity.spec.ts, api/.env.example, api/.env.production.example, infra/.env.example, infra/vps/env.example, infra/vps/README.md, README.md, api/README.md, .github/workflows/ci.yml</files>
  <read_first>
    - infra/vps/check-env.sh (whole file)
    - api/test/check-env-parity.spec.ts:200-300
    - the four env examples (SMTP blocks)
    - README.md:80-100, api/README.md:1-30, infra/vps/README.md (owner checklist section)
    - .github/workflows/ci.yml:205-215, :295-305, :490-500
  </read_first>
  <behavior>
    - check-env.sh on an otherwise valid env plus SMTP_PASSWORD=<unique secret> exits 0, prints "INFO : SMTP_PASSWORD : ligne inutile, peut être supprimée.", and its output never contains the secret
    - The env.example "placeholders replaced passes both checks" case stays green
  </behavior>
  <action>Append the SMTP names to DEAD_VARIABLES per C-1 (comment `# Variables supprimées en phase 01.9 (D-09)`), add the parity case, delete the SMTP/EMAIL_CHANGE blocks from the four env examples, the CI `SMTP_ENABLED` lines (three places), and the README rows. In infra/vps/README.md add one French sentence to the env-check section: leftover `SMTP_*` lines are reported as INFO and can be deleted. Verify REFRESH_TOKEN_SECRET appears only in DEAD_VARIABLES, its parity test and audit/planning docs. Finish with the API unit coverage, lint, typecheck, format, actionlint and the full E2E suite.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace api run test:unit -- check-env-parity && test -z "$(grep -rln 'SMTP_\|EMAIL_CHANGE_CONFIRM_URL_TEMPLATE\|nodemailer' api/.env.example api/.env.production.example infra/.env.example infra/vps/env.example README.md api/README.md .github/workflows/ci.yml)" && npm --workspace api run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && env -u ACCESS_TOKEN_SECRET POSTGRES_DB=ibp_p19_09_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p19-09 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "phase 01.9 (D-09)" infra/vps/check-env.sh` returns 1
    - `grep -rn "SMTP_" --exclude-dir=node_modules --exclude-dir=.planning --exclude-dir=dist --exclude-dir=.git . | grep -v "check-env.sh\|check-env-parity.spec.ts\|docs/audits\|CLAUDE.md\|package-lock.json"` prints nothing
    - Full E2E suite green on ibp_p19_09_test
  </acceptance_criteria>
  <done>No SMTP setting remains except as an "obsolete" INFO line in the pre-flight check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VPS env file → API startup and check-env.sh | Operator file may still hold SMTP secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-20 | Denial of Service | Production start refusing leftover SMTP_* keys | mitigate | validateEnv ignores unknown keys (C-1); behavior test with SMTP keys in a production env |
| T-01.9-21 | Information Disclosure | check-env.sh echoing SMTP_PASSWORD | mitigate | Existing DEAD_VARIABLES path prints the name only; parity test asserts the secret never appears |
| T-01.9-22 | Information Disclosure | Stale SMTP secrets kept in examples/docs | mitigate | Examples, CI env and READMEs cleaned; owner told the INFO lines can be deleted |
</threat_model>

<verification>
- API unit (coverage), parity, full E2E green; grep gates empty
</verification>

<success_criteria>
D-09 SMTP/EmailService half: code, config and docs agree that the API sends no e-mail.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-09-SUMMARY.md` when done
</output>
