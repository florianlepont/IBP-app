---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 09
type: execute
wave: 2
depends_on: [01.9-01, 01.9-02, 01.9-03, 01.9-05]
files_modified:
  - mobile/src/state/useLatestCallback.ts
  - mobile/src/state/useLatestCallback.test.ts
  - mobile/src/state/session-context.ts
  - mobile/src/state/status-context.ts
  - mobile/src/state/sync-actions-context.ts
  - mobile/src/state/surveys-context.ts
  - mobile/src/state/survey-form-context.ts
  - mobile/src/state/AppStateProvider.tsx
  - mobile/src/state/contexts.test.tsx
  - mobile/src/hooks/useSurveySync.ts
  - mobile/src/hooks/useSurveySync.test.ts
  - mobile/App.tsx
  - mobile/src/app/AuthenticatedAppNavigation.tsx
  - mobile/jest.unit.config.js
  - mobile/src/state/render-counts.test.tsx
autonomous: true
requirements: [REQ-AUD-mobile-state]

must_haves:
  truths:
    - "Five memoised contexts exist: session, status, sync actions, surveys and form; one assembler (AppStateProvider) called once in App.tsx fills all five (D-01)"
    - "useSurveySync returns memoised slices instead of a new 50-key literal each render; every action it returns keeps its identity across renders, including handleDebugResetIbpData and handleDebugResetUserData (D-01, criterion 1)"
    - "A status update changes only the status context value; a form keystroke changes only the form context value; the action objects never change identity (proved by contexts.test.tsx)"
    - "The 01.5 invariants hold: one createSyncActivity per app, purge under purgeWhileSyncSuspended, owner gate; sync-owner-gate.test.ts and useSurveySync.logout-purge.test.ts pass unchanged"
    - "accessToken is not part of any widely read context value: it has its own narrow useAccessToken() hook (RESEARCH Pattern 1)"
    - "src/state/ and src/i18n/ have their own coverage threshold rows (C-8; the i18n row moved here from 01.9-05 to avoid a wave-1 overlap on the jest config)"
    - "Stable callbacks update their latest-implementation ref in useLayoutEffect, so an effect of a child committed in the same render already calls the new implementation (W1)"
  artifacts:
    - path: mobile/src/state/AppStateProvider.tsx
      provides: "useAppController assembler + nested providers"
      exports: ["AppStateProvider"]
    - path: mobile/src/state/useLatestCallback.ts
      provides: "useLatestCallback, useStableActions"
      exports: ["useLatestCallback", "useStableActions"]
    - path: mobile/src/state/contexts.test.tsx
      provides: "Identity/stability assertions for the five context values"
  key_links:
    - from: mobile/App.tsx
      to: mobile/src/state/AppStateProvider.tsx
      via: "<AppStateProvider> wrapping the app shell"
      pattern: "<AppStateProvider"
    - from: mobile/src/state/AppStateProvider.tsx
      to: mobile/src/hooks/useSurveySync.ts
      via: "single useSurveySync call"
      pattern: "useSurveySync\\("
---

<objective>
Introduce the five memoised contexts and the single assembler, make `useSurveySync` return memoised slices with stable actions, and mount the provider in `App.tsx`.

Purpose: REQ-AUD-mobile-state, ROADMAP criterion 1, D-01. This plan builds the state layer and wires it at the root; plan 01.9-18 then replaces the navigation prop funnel with per-screen route components so a status update or a keystroke re-renders only what reads it (measured by the harness of 01.9-01).
Output: `mobile/src/state/*`, reshaped `useSurveySync`, `App.tsx` on the provider, `AuthenticatedAppNavigation` reading contexts at its top instead of 24 props.
Boundaries: do not edit `mobile/src/hooks/survey-sync/*` (plans 01.9-11/19 migrate their strings), any screen file, and in `render-counts.test.tsx` only the `EXPECTED` constant may change, and only downwards (the navigation component still reads everything at its top, so the counts should stay the same).
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-RESEARCH.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Contract (plans 01.9-16..27 code against these names; keep them):
- `useLatestCallback<A extends unknown[], R>(fn: (...args: A) => R): (...args: A) => R`: ref updated in `useLayoutEffect` (W1: a child's `useEffect` in the same commit must already see the latest implementation; this overrides the pattern map's useEffect suggestion), returned function created once with `useCallback(…, [])`.
- `useStableActions<T extends Record<string, (...args: never[]) => unknown>>(actions: T): T`: stores the latest object in a ref (updated each render in useLayoutEffect) and returns an object created ONCE (useMemo with []) whose keys are the keys of the first render, each forwarding to `ref.current[key]`. Keys must be fixed across renders (document it; assert in dev that the key set did not change).
- `session-context.ts`: `SessionState = { apiUrl; isAuthenticated; sessionRestoring; currentUser; profile; profileUpdating; localDataOwnerStatus; foreignWork; foreignOwnerEmail }`, `SessionActions = { setApiUrl; handleLogin; handleRegister; handleForgotPassword; handleLogout; handleLoadMyProfile; handleUpdateProfile; handleChangeEmail; handlePasswordReset; handleDeleteAccount; handlePickProfilePictureFromLibrary; handleTakeProfilePictureFromCamera; handleRemoveProfilePicture; handleSwitchToOwnerAccount; handleDiscardForeignData }`; value `{ state: SessionState; actions: SessionActions }`; hooks `useSession()` and `useAccessToken(): string | null` (a second, narrow context in the same file). Each hook throws `Error("<hook> must be used inside AppStateProvider")` when the context is null (`createContext<T | null>(null)`).
- `status-context.ts`: value `{ status: string }`; hook `useStatus()`.
- `sync-actions-context.ts`: value (stable, never changes identity) `{ setStatus; handleSync; handlePullChanges; handleReportSurvey; handleDebugResetIbpData; handleDebugResetUserData; handleEnsureAttachmentPreviews; handleSimulateMissingAttachmentFile; refreshLocalSurveys; refreshLocalAttachments }`; hook `useSyncActions()`.
- `surveys-context.ts`: value `{ state: SurveysState; actions: SurveyActions }`. SurveysState = everything list/detail screens read today from `surveyList.*` and `surveySync.*` detail caches, plus `surveyStats`, `ownSurveyIds`, `surveyDetailTab`, `editingSurveyId`, `formMode` (read `AuthenticatedAppNavigation.tsx` props usage to get the full list). SurveyActions = list setters and resetFilters, `openSurvey` (today's `handleOpenSurvey` in App.tsx:143-147), `closeSurveyDetailSelection`, `setSurveyDetailTab`, the survey operations from useSurveySync (submit, retry, discard, toggleVisibility, confirmDeleteSurvey, queue attachment from library/camera, deleteAttachment, loadCanonicalDetails, loadSurveyEvents), and the editing/draft actions (`openCreateSurvey`, `startEditSurvey`, `renameSurvey`, `updateRegionVersion`, `updateVegetationStage`). Hook `useSurveys()`.
- `survey-form-context.ts`: value `{ state: SurveyFormState; actions: SurveyFormActions }` = the fields and handlers of `useSurveyForm()` plus `nearbyParcels` state, `loadNearbyParcels`, `saveSurveyEdits`, `createDraft`, `captureGpsLocation`. Hook `useSurveyFormState()`.
- `AppStateProvider({ children })`: calls `useAppController()` once, which holds what App.tsx holds today (`apiUrl` state + dev-tools stored URL effect `App.tsx:28-45`, `formMode`, `editingSurveyId`, `surveyDetailTab`, useSurveyForm, useSurveyList, useSurveySync, useNearbyParcels, useEditingDraft, useSurveyDraftPatcher, useGpsCapture with `onAlert` → `Alert.alert`, `surveyStats`, `ownSurveyIds`, `editingSurveyVisibility`, the bootstrap effect `App.tsx:129-141`), builds each value with `useMemo` over its fields and `useStableActions` for its actions, and nests the providers. `usePublicMapExplorer` is NOT in the assembler (map state stays local to the map tab): in this plan it is called at the top of AuthenticatedAppNavigation with `onStatusChange: syncActions.setStatus`; plan 01.9-18 moves it into the map route.
- `useSurveySync` return (replaces the literal at `useSurveySync.ts:521-570`): one `useMemo` object `{ sessionState, sessionActions, accessToken, status, syncActions, surveyOperations, surveyDetailsState }`, each member memoised: sessionState (useMemo over its 8 fields), sessionActions/syncActions/surveyOperations via `useStableActions`, surveyDetailsState (useMemo over surveyDetails, detailsLoadingSurveyId, surveyEvents, eventsLoadingSurveyId). `operationStatus` stays internal (no consumer, RESEARCH Pattern 1). `handleDebugResetIbpData`/`handleDebugResetUserData` become `useCallback` with their real dependencies (pattern: `runDebugReset` at `:296-331`); keep their English texts (plan 01.9-20 moves them to the catalogue). `setStatus(message: string)` keeps its signature.
- 01.5 invariants (must not regress): single `createSyncActivity()` ref (`:58-63`), `purgeWhileSyncSuspended` in `resetLocalSurveyState` (`:93-108`), `syncAllowed` passed to network hooks.
- App.tsx after this plan: gesture/safe-area shell, `<AppStateProvider>` around a small `AppShell` component that reads `useSession()` for the three overlays (`App.tsx:196-238`) and keeps `profileSetupSkipped` as local UI state; `styles.container`/`styles.appLayout` move from `src/app/styles.ts` into App.tsx's local StyleSheet (plan 01.9-24 deletes styles.ts). `AuthenticatedAppNavigation` takes no props.
- Coverage rows: add `'./src/state/'` and `'./src/i18n/'` to `mobile/jest.unit.config.js` at the measured floor (C-8), next to the `'./App.tsx'` row added by 01.9-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stable-callback helpers and the five context modules</name>
  <files>mobile/src/state/useLatestCallback.ts, mobile/src/state/useLatestCallback.test.ts, mobile/src/state/session-context.ts, mobile/src/state/status-context.ts, mobile/src/state/sync-actions-context.ts, mobile/src/state/surveys-context.ts, mobile/src/state/survey-form-context.ts</files>
  <read_first>
    - mobile/src/hooks/render-hook-wrapper.test.tsx:1-40 (context + Provider + renderHook mechanics)
    - mobile/src/app/useAppBottomTabBarHeight.ts (production createContext/useContext)
    - mobile/src/hooks/useSurveyForm.ts (return shape), mobile/src/hooks/useSurveyList.ts (return shape)
    - mobile/src/app/AuthenticatedAppNavigation.tsx:75-110 (the 24 props and their types)
  </read_first>
  <behavior>
    - useLatestCallback: returned function identity is the same across rerenders with a new fn, and calls the latest fn
    - Same-commit child effect: a parent rerenders with a new fn and passes the stable callback to a child whose useEffect calls it on that commit; the call runs the NEW fn (fails with a useEffect-based ref)
    - useStableActions: same same-commit child-effect case
    - useStableActions: returned object identity is the same across rerenders; each member calls the latest implementation; a key added on a later render is reported in dev (console.error or thrown in test)
    - Each use* hook throws a clear error outside its provider
  </behavior>
  <action>Write the tests first (renderHook recipe: `@testing-library/react-native/pure`, async, cleanup), then the helpers and the five context modules with their exported types per the contract (D-01). Types use `export type`. Context modules hold no logic beyond createContext, Provider export and the throwing hook.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/state/useLatestCallback && npm --workspace mobile run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ls mobile/src/state/*-context.ts | wc -l` returns 5
    - `grep -c "useLayoutEffect" mobile/src/state/useLatestCallback.ts` returns at least 1
    - Every behavior bullet passes
  </acceptance_criteria>
  <done>The context contract exists and is typed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useSurveySync returns memoised slices with stable actions</name>
  <files>mobile/src/hooks/useSurveySync.ts, mobile/src/hooks/useSurveySync.test.ts</files>
  <read_first>
    - mobile/src/hooks/useSurveySync.ts (whole file)
    - mobile/src/hooks/useSurveySync.test.ts (rewritten by 01.9-02)
    - mobile/src/hooks/useSurveySync.logout-purge.test.ts, mobile/src/hooks/sync-owner-gate.test.ts
  </read_first>
  <behavior>
    - Rerendering the hook with identical params returns the same top-level object and the same slices
    - After setStatus("x") only `status` changes; sessionState, the three action slices and surveyDetailsState keep identity
    - handleDebugResetIbpData and handleDebugResetUserData keep identity across a rerender where `surveys` changed
    - Existing behaviours of useSurveySync.test.ts still pass (update property paths only, e.g. result.current.syncActions.handleSync)
  </behavior>
  <action>Reshape the return per the contract (D-01, criterion 1): memoised slices, `useStableActions` for action groups, `useCallback` for the two debug resets, `operationStatus` kept internal. Update useSurveySync.test.ts property paths and add the identity tests above. Do not change sub-hooks. Run the logout-purge and owner-gate suites unchanged.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/hooks/(useSurveySync|sync-owner-gate)" && git diff --quiet -- mobile/src/hooks/useSurveySync.logout-purge.test.ts mobile/src/hooks/sync-owner-gate.test.ts mobile/src/hooks/survey-sync</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "operationStatus," mobile/src/hooks/useSurveySync.ts` in the returned object is 0 (not returned)
    - `grep -n "const handleDebugReset" mobile/src/hooks/useSurveySync.ts` shows both wrapped in useCallback
    - logout-purge, owner-gate and survey-sync/* files unchanged (verify command)
  </acceptance_criteria>
  <done>The hook no longer returns a new 50-key object each render and its actions are stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AppStateProvider assembler, App.tsx and navigation on contexts</name>
  <files>mobile/src/state/AppStateProvider.tsx, mobile/src/state/contexts.test.tsx, mobile/App.tsx, mobile/src/app/AuthenticatedAppNavigation.tsx, mobile/jest.unit.config.js, mobile/src/state/render-counts.test.tsx</files>
  <read_first>
    - mobile/App.tsx (whole file)
    - mobile/src/app/AuthenticatedAppNavigation.tsx (whole file; how each prop is used)
    - mobile/src/state/render-counts.test.tsx (the harness from 01.9-01: which modules it mocks; only EXPECTED may change, downwards)
    - mobile/src/hooks/useSurveySync.logout-purge.test.ts:1-120 (mocks to reuse in contexts.test.tsx)
  </read_first>
  <behavior>
    - Rendering AppStateProvider with mocked storage/API/session renders children and useSurveySync is called once per render of the provider (spy through a passthrough mock), never twice
    - After the session's reportStatus is called, the session, sync-actions, surveys and form values keep identity (Object.is) and the status value changes
    - After the form's setSiteName("a"), only the form value changes identity
    - The sync-actions value never changes identity across these updates
  </behavior>
  <action>Move App.tsx's state and hook calls into `useAppController` inside `AppStateProvider` per the contract (D-01), including the bootstrap effect, `handleOpenSurvey`, `closeSurveyDetailSelection` and the `onStopEditing` closure (now stable). App.tsx renders the shell with `<AppStateProvider>` and an `AppShell` that reads `useSession()`/`useSyncActions()` for the overlays; move `styles.container` and `styles.appLayout` into App.tsx's local StyleSheet and drop the `./src/app/styles` import from App.tsx. `AuthenticatedAppNavigation` becomes prop-less: at its top it reads the five contexts and `useAccessToken()`, calls `usePublicMapExplorer({ apiUrl, onStatusChange: setStatus })`, and passes the same values to its internal navigators as before (internal components unchanged in this plan; 01.9-18 replaces them). Write contexts.test.tsx first. Add the `./src/state/` and `./src/i18n/` coverage rows at the measured floor; the `./App.tsx` row from 01.9-01 must still pass (never lower it; App.tsx becomes a thin shell fully rendered by the harness). Run the render-count harness: its pinned counts should still pass. If a count goes down, update only that number in `EXPECTED` and explain why in the SUMMARY; if a count goes up, stop and fix the regression (never raise a number).</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/state" && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && cd mobile && npx expo export --platform ios --output-dir /tmp/expo-export-p19-09 >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useSurveySync(" mobile/App.tsx` returns 0 and `grep -c "useSurveySync(" mobile/src/state/AppStateProvider.tsx` returns 1
    - `grep -c "AuthenticatedAppNavigationProps" mobile/src/app/AuthenticatedAppNavigation.tsx` returns 0
    - `grep -c "from \"./src/app/styles\"" mobile/App.tsx` returns 0
    - `grep -c "'./src/state/'\|'./src/i18n/'" mobile/jest.unit.config.js` returns 2
    - render-counts.test.tsx passes and no EXPECTED number increased (`git diff` of the file touches only EXPECTED); `expo export` bundles
  </acceptance_criteria>
  <done>The app runs on the five contexts filled by one assembler; the prop funnel into navigation is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auth session → React tree | Access token and user identity distributed to components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-17 | Information Disclosure | accessToken spread through widely read contexts | mitigate | Token only in a narrow useAccessToken() context; session value holds no token (contexts.test asserts the key is absent) |
| T-01.9-18 | Tampering | Duplicate assembler call → two sync trackers, concurrent drains (01.5 WR-08) | mitigate | Single useSurveySync call in AppStateProvider asserted by test; logout-purge and owner-gate suites unchanged |
| T-01.9-19 | Elevation of Privilege | Owner gate bypass while moving handlers | mitigate | syncAllowed wiring untouched in sub-hooks; sync-owner-gate.test.ts unchanged and green |
</threat_model>

<verification>
- state, hooks and full mobile suites green with coverage; harness before-counts still pass; expo export bundles
</verification>

<success_criteria>
D-01 state layer in place: five memoised contexts, one assembler, stable actions, no 50-key object per render.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-09-SUMMARY.md` when done
</output>
