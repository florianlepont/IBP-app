---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 10
type: execute
wave: 2
depends_on: [01.9-04]
files_modified:
  - mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.ts
  - mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.test.ts
  - mobile/src/hooks/survey-sync/useSurveySyncProfile.ts
  - mobile/src/hooks/survey-sync/useSurveySyncProfile.test.ts
  - mobile/src/i18n/fr/status/survey-ops.ts
  - mobile/src/i18n/fr/status/profile.ts
autonomous: true
requirements: [REQ-AUD-i18n-a11y, REQ-AUD-mobile-state]

must_haves:
  truths:
    - "Every status message and Alert text in useSurveySyncSurveyOperations and useSurveySyncProfile comes from fr.status.surveyOps / fr.status.profile (D-06)"
    - "No status message from these hooks contains a survey id, attachment id, error code or raw error text; the raw detail goes to logStatusDetail (dev tools only) (D-06)"
    - "Both tests are rewritten on renderHook and assert catalogue messages instead of English strings (D-01, C-4, Pitfall 10)"
  artifacts:
    - path: mobile/src/i18n/fr/status/survey-ops.ts
      provides: "Survey operation status and alert texts"
    - path: mobile/src/i18n/fr/status/profile.ts
      provides: "Profile status and alert texts"
  key_links:
    - from: mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.ts
      to: mobile/src/i18n
      via: "fr.status.surveyOps.* calls"
      pattern: "fr\\.status\\.surveyOps\\."
---

<objective>
Move the status and alert texts of the survey-operations and profile sync hooks to the French catalogue, drop ids and raw errors from what users see, and rewrite both spy-based tests on renderHook.

Purpose: REQ-AUD-i18n-a11y (criterion 4: "status messages carry no ids or technical text") and D-01/C-4 for these two tests. About 70 of the 137 status literals live in these two hooks.
Output: two hooks and two tests migrated, two catalogue sections filled.
Boundaries: only these six files. `setStatus`/`reportStatus` keep accepting `string` until 01.9-27 narrows them to `StatusMessage`.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-04-SUMMARY.md
@CLAUDE.md

<interfaces>
Catalogue (from 01.9-04): `import { fr, logStatusDetail } from "../../i18n"`. Section files are `export const surveyOpsFr = { … } as const` and `export const profileFr = { … } as const`, wired as `fr.status.surveyOps` and `fr.status.profile`. Inside a section: status entries are functions returning `statusText(...)` (import `statusText` from `../status`), taking only names and counts (`{ name: survey.site_name }`, `{ count }`), never ids; Alert texts go in an `alerts` sub-object of the same section (plain strings or functions). Tone: short French sentences, "vous", no technical words (HTTP, id, payload, sync_version).
Raw errors: where a hook builds a status from `(error as Error).message` or an API code, call `logStatusDetail("<hook>.<action>", error)` and show a catalogue message chosen by the error kind the hook already distinguishes (auth required, network/timeout, conflict, validation, unknown). Keep the hook's control flow identical.
Test recipe: `mobile/src/hooks/render-hook-smoke.test.ts:1-26` (mock react-native as a plain object first, `@testing-library/react-native/pure`, async renderHook/act, cleanup). Assertions compare with the catalogue value (e.g. `expect(setStatus).toHaveBeenCalledWith(fr.status.surveyOps.submitted({ name: "Parcelle A" }))`), never with an English literal. Test data strings such as "SMTP error" (`useSurveySyncProfile.test.ts:218-225`) are inputs, not product text; they may stay if they only feed an error object.
Scanner check for these files: `node mobile/scripts/structure-report.js literals src/hooks/survey-sync/useSurveySyncSurveyOperations.ts src/hooks/survey-sync/useSurveySyncProfile.ts --max 0` and the same with `status-ids`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Survey operations hook on the catalogue, test on renderHook</name>
  <files>mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.ts, mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.test.ts, mobile/src/i18n/fr/status/survey-ops.ts</files>
  <read_first>
    - mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.ts (whole file; list every setStatus/reportStatus/Alert call)
    - mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.test.ts (whole file; list every `it`)
    - mobile/src/i18n/index.ts, mobile/src/i18n/status.ts, mobile/src/i18n/fr/common.ts
    - mobile/src/hooks/render-hook-smoke.test.ts:1-40
  </read_first>
  <behavior>
    - Every existing `it` still exists and asserts the same flow (which API called, which local write, which state), with messages compared to fr.status.surveyOps entries
    - A failure path passes the raw error to logStatusDetail and the user message contains neither the survey id nor the error text
    - No `spyOn(React` in the test
  </behavior>
  <action>Rewrite the test on renderHook first (same cases, assertions on catalogue values), then move every status and Alert text of the hook into `fr/status/survey-ops.ts` (D-06), dropping ids and raw errors (use site_name where a name helps the user). Keep behaviour identical otherwise.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/hooks/survey-sync/useSurveySyncSurveyOperations && node mobile/scripts/structure-report.js literals src/hooks/survey-sync/useSurveySyncSurveyOperations.ts --max 0 && node mobile/scripts/structure-report.js status-ids src/hooks/survey-sync/useSurveySyncSurveyOperations.ts --max 0 && test "$(grep -c 'spyOn(React' mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.test.ts)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - Both scanners report 0 for the hook file
    - `it(` count equal or higher than before (numbers in SUMMARY)
  </acceptance_criteria>
  <done>Survey operation messages are French, user-facing and id-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Profile hook on the catalogue, test on renderHook</name>
  <files>mobile/src/hooks/survey-sync/useSurveySyncProfile.ts, mobile/src/hooks/survey-sync/useSurveySyncProfile.test.ts, mobile/src/i18n/fr/status/profile.ts</files>
  <read_first>
    - mobile/src/hooks/survey-sync/useSurveySyncProfile.ts (whole file)
    - mobile/src/hooks/survey-sync/useSurveySyncProfile.test.ts (whole file)
  </read_first>
  <behavior>
    - Same cases as before, assertions on fr.status.profile values; profile-picture and e-mail-change failures show catalogue text and log the detail
    - No `spyOn(React` in the test
  </behavior>
  <action>Same method as Task 1 for the profile hook (D-06, C-4). Then run the whole mobile suite with coverage, lint (the hooks folder is outside the JSX gates but must stay clean), typecheck and format.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/hooks/survey-sync && node mobile/scripts/structure-report.js literals src/hooks/survey-sync/useSurveySyncProfile.ts --max 0 && node mobile/scripts/structure-report.js status-ids src/hooks/survey-sync/useSurveySyncProfile.ts --max 0 && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - Both scanners report 0 for the profile hook
    - `grep -c 'spyOn(React' mobile/src/hooks/survey-sync/useSurveySyncProfile.test.ts` returns 0
    - Coverage thresholds pass (hooks row)
  </acceptance_criteria>
  <done>Profile messages are French and id-free; two more spy tests are on renderHook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/network errors → status bar | Server and network error text reaching the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-23 | Information Disclosure | Survey/attachment ids and raw errors in status messages | mitigate | Catalogue messages with name/count params only; raw detail to logStatusDetail (dev tools); status-ids scanner at 0 for both files |
</threat_model>

<verification>
- Both hook suites green on renderHook; scanners at 0 for both files; coverage thresholds met
</verification>

<success_criteria>
Criterion 4 for the survey-operations and profile status messages.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-10-SUMMARY.md` when done
</output>
