---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 13
type: execute
wave: 2
depends_on: [01.9-03, 01.9-04]
files_modified:
  - mobile/src/screens/AuthGateScreen.tsx
  - mobile/src/screens/AuthGateScreen.test.ts
  - mobile/src/screens/auth-gate/HeroSection.tsx
  - mobile/src/screens/auth-gate/AuthPanel.tsx
  - mobile/src/screens/auth-gate/styles.ts
  - mobile/src/i18n/fr/auth-gate.ts
autonomous: true
requirements: [REQ-AUD-mobile-state, REQ-AUD-i18n-a11y]

must_haves:
  truths:
    - "No file of the sign-in screen exceeds 400 lines; its parts and styles live in mobile/src/screens/auth-gate/ (D-04)"
    - "The sign-in screen has 0 unused style keys and every user-facing string comes from fr.authGate or fr.common (D-04, D-06)"
    - "AuthGateScreen keeps its path, export name and props; AuthGateScreen.test.ts passes with catalogue-based assertions"
  artifacts:
    - path: mobile/src/i18n/fr/auth-gate.ts
      provides: "Sign-in screen texts and a11y labels"
  key_links:
    - from: mobile/src/screens/AuthGateScreen.tsx
      to: mobile/src/screens/auth-gate/HeroSection.tsx
      via: "import"
      pattern: "auth-gate/HeroSection"
---

<objective>
Split the sign-in screen (752 lines) under 400 lines per file, delete unused styles, and move its text to the catalogue.

Purpose: criterion 2 (no screen over 400 lines, dead styles gone) and criterion 4 (every string from the catalogue) for the sign-in screen (D-04, D-06).
Output: slim `AuthGateScreen.tsx`, `screens/auth-gate/*`, filled `fr/auth-gate.ts`, updated test.
Layout decision: same as 01.9-11 (entry path/name/props unchanged; parts in the feature folder; part names adjustable inside `screens/auth-gate/`, listed in the SUMMARY).
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-04-SUMMARY.md
@docs/specs/epic-a-access-and-security.md
@CLAUDE.md

<interfaces>
Facts: `AuthGateScreen.tsx` has layout constants (`:47-61`), `HeroSection` (`:93`), main component (`:250`), 13 JSX Text nodes; `AuthGateScreen.test.ts` (133 lines) renders it with a mocked react-native.
Catalogue: fill only `mobile/src/i18n/fr/auth-gate.ts` (`export const authGateFr = { … } as const`), shared words from `fr.common`. The dev-only API URL field (if shown behind dev tools) still takes its label from the catalogue.
Per-folder gates at 0 with paths `src/screens/AuthGateScreen.tsx src/screens/auth-gate`: `structure-report.js long-files|unused-styles|literals --max 0` and `npx eslint … --max-warnings 0`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split the sign-in screen and delete unused styles</name>
  <files>mobile/src/screens/AuthGateScreen.tsx, mobile/src/screens/auth-gate/HeroSection.tsx, mobile/src/screens/auth-gate/AuthPanel.tsx, mobile/src/screens/auth-gate/styles.ts</files>
  <read_first>
    - mobile/src/screens/AuthGateScreen.tsx (whole file)
    - mobile/src/screens/AuthGateScreen.test.ts
  </read_first>
  <action>Per D-04, pure structural move: `HeroSection` and its animation constants to `auth-gate/HeroSection.tsx`, the sign-in/register panel to `auth-gate/AuthPanel.tsx`, styles next to them, dead keys deleted. Entry keeps name and props.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run typecheck && npm --workspace mobile run test:unit -- src/screens/AuthGateScreen && node mobile/scripts/structure-report.js long-files src/screens/AuthGateScreen.tsx src/screens/auth-gate --max 0 && node mobile/scripts/structure-report.js unused-styles src/screens/AuthGateScreen.tsx src/screens/auth-gate --max 0</automated>
  </verify>
  <acceptance_criteria>
    - Both scanners at 0; existing AuthGateScreen test green unchanged
  </acceptance_criteria>
  <done>The sign-in screen is split with no dead styles.</done>
</task>

<task type="auto">
  <name>Task 2: French catalogue for the sign-in screen</name>
  <files>mobile/src/screens/AuthGateScreen.tsx, mobile/src/screens/auth-gate/HeroSection.tsx, mobile/src/screens/auth-gate/AuthPanel.tsx, mobile/src/screens/AuthGateScreen.test.ts, mobile/src/i18n/fr/auth-gate.ts</files>
  <read_first>
    - the files produced by Task 1; mobile/src/i18n/fr/common.ts
  </read_first>
  <action>Per D-06 move every JSX text, text prop and Alert text to `fr/auth-gate.ts`; Pressables that lack them get `accessibilityRole` and a catalogue `accessibilityLabel` too (not required by D-07 here, but keeps the screen consistent). Update AuthGateScreen.test.ts to assert catalogue values where it asserted literal text.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/screens/AuthGateScreen|src/i18n" && node mobile/scripts/structure-report.js literals src/screens/AuthGateScreen.tsx src/screens/auth-gate --max 0 && node mobile/scripts/structure-report.js long-files src/screens/AuthGateScreen.tsx src/screens/auth-gate --max 0 && cd mobile && npx eslint src/screens/AuthGateScreen.tsx src/screens/auth-gate --max-warnings 0 && cd .. && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - Literal scanner and eslint at 0 for the sign-in paths; tests and coverage green
  </acceptance_criteria>
  <done>The sign-in screen reads entirely from the catalogue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Presentational refactor of the pre-login screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-27 | Spoofing | Sign-in flow wiring broken during the split (wrong handler bound) | mitigate | Pure move first with the existing AuthGateScreen test green; props unchanged |
</threat_model>

<verification>
- Per-folder gates at 0; AuthGateScreen test and full suite green
</verification>

<success_criteria>
Criteria 2 and 4 satisfied for the sign-in screen.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-13-SUMMARY.md` when done
</output>
