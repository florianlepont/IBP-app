---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 14
type: execute
wave: 2
depends_on: [01.9-03, 01.9-04]
files_modified:
  - mobile/src/screens/AccountScreen.tsx
  - mobile/src/screens/account/IdentityCard.tsx
  - mobile/src/screens/account/ProfileCard.tsx
  - mobile/src/screens/account/AccountSettingsRows.tsx
  - mobile/src/screens/account/styles.ts
  - mobile/src/screens/account/IdentityCard.test.tsx
  - mobile/src/i18n/fr/account.ts
autonomous: true
requirements: [REQ-AUD-mobile-state, REQ-AUD-i18n-a11y]

must_haves:
  truths:
    - "No file of the account screen exceeds 400 lines; its parts and styles live in mobile/src/screens/account/ (D-04)"
    - "The account screen has 0 unused style keys and every user-facing string, including its three Alert dialogs, comes from fr.account or fr.common (D-04, D-06)"
    - "Only the avatar part uses the access token (Bearer header for the profile picture), so a token refresh can re-render the avatar alone once 01.9-17 feeds it from useAccessToken() (RESEARCH Pattern 1)"
  artifacts:
    - path: mobile/src/i18n/fr/account.ts
      provides: "Account screen texts, alerts and a11y labels"
    - path: mobile/src/screens/account/IdentityCard.test.tsx
      provides: "Avatar image request carries the token; labels from catalogue"
  key_links:
    - from: mobile/src/screens/account/IdentityCard.tsx
      to: "profile picture request"
      via: "Authorization Bearer header from the accessToken prop"
      pattern: "Bearer"
---

<objective>
Split the account screen (596 lines) under 400 lines per file, delete unused styles, move its text and alerts to the catalogue, and isolate the only use of the access token in the avatar part.

Purpose: criteria 2 and 4 for the account screen (D-04, D-06) and the narrow-token rule from RESEARCH Pattern 1.
Output: slim `AccountScreen.tsx`, `screens/account/*`, filled `fr/account.ts`, one test.
Layout decision: same as 01.9-11 (entry path/name/props unchanged; part names adjustable inside `screens/account/`).
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-04-SUMMARY.md
@docs/specs/epic-a-access-and-security.md
@CLAUDE.md

<interfaces>
Facts: `AccountScreen.tsx` (596 lines, one component from `:76`), sections marked ACC-xx (identity hero `:241`, avatar with camera badge `:248-287`, profile card `:295-350`, e-mail/password rows `:368-430`, save button `:433`, logout `:452`); the token is used only for the profile picture request (`:262`); 3 Alert.alert calls; 5 JSX Text nodes.
Catalogue: fill only `mobile/src/i18n/fr/account.ts` (`export const accountFr = { … } as const`, with `alerts` and `a11y`).
Per-folder gates at 0 with paths `src/screens/AccountScreen.tsx src/screens/account`: `structure-report.js long-files|unused-styles|literals --max 0` and `npx eslint … --max-warnings 0`.
Render test recipe: `mobile/src/screens/AuthGateScreen.test.ts:1-80`; expo-image is mapped to `mobile/test/expo-image.mock.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split the account screen and delete unused styles</name>
  <files>mobile/src/screens/AccountScreen.tsx, mobile/src/screens/account/IdentityCard.tsx, mobile/src/screens/account/ProfileCard.tsx, mobile/src/screens/account/AccountSettingsRows.tsx, mobile/src/screens/account/styles.ts</files>
  <read_first>
    - mobile/src/screens/AccountScreen.tsx (whole file)
  </read_first>
  <action>Per D-04, pure structural move: identity card with avatar (the only receiver of `accessToken`) to `IdentityCard.tsx`, profile fields to `ProfileCard.tsx`, e-mail/password/logout rows to `AccountSettingsRows.tsx`, styles next to them, dead keys deleted. Entry keeps name and props.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run typecheck && node mobile/scripts/structure-report.js long-files src/screens/AccountScreen.tsx src/screens/account --max 0 && node mobile/scripts/structure-report.js unused-styles src/screens/AccountScreen.tsx src/screens/account --max 0 && test "$(grep -l accessToken mobile/src/screens/account/*.tsx | grep -v IdentityCard | wc -l)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - Both scanners at 0; `accessToken` referenced in `screens/account/` only by IdentityCard.tsx
  </acceptance_criteria>
  <done>The account screen is split and the token has one reader.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: French catalogue and accessibility for the account screen</name>
  <files>mobile/src/screens/AccountScreen.tsx, mobile/src/screens/account/IdentityCard.tsx, mobile/src/screens/account/ProfileCard.tsx, mobile/src/screens/account/AccountSettingsRows.tsx, mobile/src/screens/account/IdentityCard.test.tsx, mobile/src/i18n/fr/account.ts</files>
  <read_first>
    - the files produced by Task 1; mobile/src/i18n/fr/common.ts
  </read_first>
  <behavior>
    - IdentityCard with accessToken "t" requests the picture with header Authorization "Bearer t"; without a picture it renders initials
    - The camera badge Pressable has accessibilityRole "button" and the catalogue label
  </behavior>
  <action>Per D-06 move every text, text prop and the three Alert texts to `fr/account.ts`; add role/label to Pressables lacking them. Write the test first.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/screens/account|src/i18n" && node mobile/scripts/structure-report.js literals src/screens/AccountScreen.tsx src/screens/account --max 0 && node mobile/scripts/structure-report.js long-files src/screens/AccountScreen.tsx src/screens/account --max 0 && cd mobile && npx eslint src/screens/AccountScreen.tsx src/screens/account --max-warnings 0 && cd .. && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - Literal scanner and eslint at 0 for the account paths; behavior tests and coverage pass
  </acceptance_criteria>
  <done>The account screen reads in French with accessible controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Access token → UI component | Bearer token used to fetch the profile picture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-28 | Information Disclosure | Token passed to more components than needed | mitigate | Only IdentityCard receives accessToken (grep gate); 01.9-17 feeds it from the narrow useAccessToken() |
</threat_model>

<verification>
- Per-folder gates at 0; account tests and full suite green
</verification>

<success_criteria>
Criteria 2 and 4 satisfied for the account screen.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-14-SUMMARY.md` when done
</output>
