---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 17
type: execute
wave: 3
depends_on: [01.9-01, 01.9-08, 01.9-12]
files_modified:
  - mobile/src/navigation/types.ts
  - mobile/src/navigation/routes/HomeRoute.tsx
  - mobile/src/navigation/routes/SurveyListRoute.tsx
  - mobile/src/navigation/routes/SurveyDetailRoute.tsx
  - mobile/src/navigation/routes/SurveyFormRoute.tsx
  - mobile/src/navigation/routes/FactorDetailRoute.tsx
  - mobile/src/navigation/routes/ParcelSelectionRoute.tsx
  - mobile/src/navigation/routes/PublicMapRoute.tsx
  - mobile/src/navigation/routes/AccountRoute.tsx
  - mobile/src/navigation/routes/SettingsRoute.tsx
  - mobile/src/app/AuthenticatedAppNavigation.tsx
  - mobile/src/screens/SurveyFormScreen.tsx
  - mobile/src/state/AppStateProvider.tsx
  - mobile/src/state/render-counts.test.tsx
  - mobile/jest.unit.config.js
autonomous: true
requirements: [REQ-AUD-mobile-state]

must_haves:
  truths:
    - "Each routed screen has a memoised route component that reads only the contexts it needs; the navigation tree passes no data props and consumes no frequently changing context (D-01)"
    - "A status update re-renders the Settings screen only: every other screen probe and every list row count 0 in the harness (criterion 1, D-02)"
    - "A form keystroke re-renders only the form screens (survey form, factor detail, parcel selection) and no list row (criterion 1, D-02)"
    - "The public map explorer state lives in the map route; pressing the Explorer tab still closes the survey detail selection and reloads the map"
    - "The account avatar reads the token through useAccessToken() only"
    - "src/navigation/ has its own coverage threshold row (C-8)"
  artifacts:
    - path: mobile/src/navigation/routes/SettingsRoute.tsx
      provides: "The only consumer of useStatus()"
      contains: "useStatus"
    - path: mobile/src/state/render-counts.test.tsx
      provides: "EXPECTED updated to the after counts for statusUpdate and formKeystroke"
  key_links:
    - from: mobile/src/app/AuthenticatedAppNavigation.tsx
      to: mobile/src/navigation/routes/
      via: "Screen render callbacks returning route components without data props"
      pattern: "routes/"
---

<objective>
Replace the navigation prop funnel with one memoised route component per screen that reads its own contexts, move the map explorer into the map route, and update the render-count harness to the target numbers.

Purpose: ROADMAP criterion 1 ("a status update no longer re-renders every mounted tab") and D-01 ("screens read the context they need directly instead of receiving 24 props"). The harness from 01.9-01 proves it.
Output: `mobile/src/navigation/routes/*`, `navigation/types.ts` (param lists moved out of AuthenticatedAppNavigation), a data-free navigation tree, harness EXPECTED for statusUpdate and formKeystroke at the target, coverage row for `src/navigation/`.
Interpretation of D-01 (recorded for the verifier): each screen's route component is the part of the screen feature that reads contexts; the presentational screen keeps a typed props interface so it stays testable and so the harness can count its renders. 01.9-23 moves the tree itself into `src/navigation/` and switches render callbacks to `component=`.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-RESEARCH.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-01-SUMMARY.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-08-SUMMARY.md
@CLAUDE.md

<interfaces>
From 01.9-08 (read its SUMMARY for the final names): `useSession()` → `{ state, actions }`, `useAccessToken()`, `useStatus()` → `{ status }`, `useSyncActions()` (stable), `useSurveys()` → `{ state, actions }`, `useSurveyFormState()` → `{ state, actions }`, `useLatestCallback`, `useStableActions` in `mobile/src/state/`.
Route component rules:
- `export const XRoute = memo(function XRoute(props: { navigation; route }) { … })` in `mobile/src/navigation/routes/XRoute.tsx`; reads only the contexts whose data the screen shows; every callback passed to the screen is stable (context actions, or `useLatestCallback` wrappers for callbacks that also call `navigation.navigate`); returns `<XScreen …props />` with the screen's existing props (the screen entry files are unchanged, except SurveyFormScreen below).
- Which contexts: Settings → status + sync actions + session actions + surveys actions (refresh); Home → session state + surveys state; SurveyList → surveys + sync actions (pull, delete, previews); SurveyDetail → surveys + sync actions + `apiUrl` from session state; SurveyForm/FactorDetail/ParcelSelection → form context (+ surveys actions for navigation after save); PublicMap → `usePublicMapExplorer({ apiUrl, onStatusChange: setStatus })` inside the route, `ownSurveyIds` from surveys state, `handleReportSurvey`; Account → session + `useAccessToken()`.
- `SurveyFormScreen` drops its unused `status` prop (`status: _status`), since no route passes it any more.
- AuthenticatedAppNavigation keeps its navigators, options and listeners, but its components read only stable action objects (and session `isAuthenticated` where a listener needs it); it no longer reads surveys, form or status state. The Mes Relevés header search sync (`searchBarRef` effect and `setSurveyQuery`) moves into a tiny component or into SurveyListRoute's `navigation.setOptions` so the stack navigator does not subscribe to the surveys context.
- The Explorer tab listener (`makePublicMapTabListeners`, `:264-274`) called `publicMapExplorer.loadPublicMap()`; the explorer now lives in PublicMapRoute, which subscribes with `navigation.getParent()?.addListener("tabPress", …)` (unsubscribe on unmount) to close the survey detail selection and call `loadPublicMap()`. The Explorer tab keeps its own listener only for closing the detail selection if that is simpler; behaviour must be identical.
- Param list types (`AuthenticatedAppNavigation.tsx:48-73`) move to `mobile/src/navigation/types.ts` (exported), so routes can type `navigation`/`route` with `NativeStackScreenProps`.
Harness: `mobile/src/state/render-counts.test.tsx` mocks screen entries (still valid), navigation libraries and seams listed in 01.9-01. Update only `EXPECTED` and, if needed, the fake navigator so it passes `navigation`/`route` to render callbacks. Target: statusUpdate `{ settings: 1, others: 0, rows: 0 }`; formKeystroke `{ surveyForm: 1, factorDetail: 0 or 1, parcelSelection: 0 or 1, others: 0, rows: 0 }` (record the actual values; any non-form screen above 0 is a failure to fix, not to pin). oneSurveyRefresh may already drop; 01.9-21 finishes rows.
Coverage (C-8): add `'./src/navigation/'` at the measured floor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route components and navigation types</name>
  <files>mobile/src/navigation/types.ts, mobile/src/navigation/routes/HomeRoute.tsx, mobile/src/navigation/routes/SurveyListRoute.tsx, mobile/src/navigation/routes/SurveyDetailRoute.tsx, mobile/src/navigation/routes/SurveyFormRoute.tsx, mobile/src/navigation/routes/FactorDetailRoute.tsx, mobile/src/navigation/routes/ParcelSelectionRoute.tsx, mobile/src/navigation/routes/PublicMapRoute.tsx, mobile/src/navigation/routes/AccountRoute.tsx, mobile/src/navigation/routes/SettingsRoute.tsx, mobile/src/screens/SurveyFormScreen.tsx</files>
  <read_first>
    - mobile/src/app/AuthenticatedAppNavigation.tsx (whole file: every Screen render callback and the props it passes)
    - mobile/src/state/*.ts(x) (context contract from 01.9-08)
    - the props types of each screen entry (mobile/src/screens/*Screen.tsx, top of file)
  </read_first>
  <action>Per D-01, create `navigation/types.ts` (param lists moved, exported with `export type`) and the nine memoised route components per the rules above, each passing exactly the props its screen takes today, with stable callbacks. Remove the `status` prop from SurveyFormScreen's props type and destructuring.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run typecheck && test "$(ls mobile/src/navigation/routes/*Route.tsx | wc -l)" -ge 9 && test "$(grep -l "useStatus" mobile/src/navigation/routes/*.tsx | wc -l)" = 1</automated>
  </verify>
  <acceptance_criteria>
    - Nine route files exist, each exporting a `memo` component
    - Only SettingsRoute.tsx calls `useStatus`
    - `grep -c "status" mobile/src/screens/SurveyFormScreen.tsx` shows no `status` prop in the props type
  </acceptance_criteria>
  <done>Every screen has a route component that reads only what it shows.</done>
</task>

<task type="auto">
  <name>Task 2: Data-free navigation tree, map explorer in its route, harness after-counts</name>
  <files>mobile/src/app/AuthenticatedAppNavigation.tsx, mobile/src/state/AppStateProvider.tsx, mobile/src/state/render-counts.test.tsx, mobile/jest.unit.config.js</files>
  <read_first>
    - mobile/src/app/AuthenticatedAppNavigation.tsx
    - mobile/src/state/render-counts.test.tsx and mobile/src/state/AppStateProvider.tsx
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-before.json
  </read_first>
  <action>Per D-01 and the rules above: render callbacks return route components with only `navigation`/`route`; remove the top-of-component context reads that 01.9-08 added (keep only stable actions and session `isAuthenticated` for listeners); move `usePublicMapExplorer` into PublicMapRoute with the tabPress subscription; move the header search sync out of the stack navigator. Adjust AppStateProvider only if a route needs a value the contexts do not yet expose (list additions in the SUMMARY). Run the harness, confirm the statusUpdate and formKeystroke targets, update `EXPECTED` (only decreases), and write the current counts with `RENDER_COUNTS_OUT=/tmp/render-counts-p19-17.json` into the SUMMARY next to the before JSON. Add the `./src/navigation/` coverage row. Run expo export to prove the bundle.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/state && node -e "const b=require('./.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-before.json').scenarios" && RENDER_COUNTS_OUT=/tmp/render-counts-p19-17.json npm --workspace mobile run test:unit -- src/state/render-counts.test.tsx && node -e "const s=require('/tmp/render-counts-p19-17.json').scenarios.statusUpdate;const bad=Object.entries(s).filter(([k,v])=>k!=='settings'&&v!==0);if(bad.length||s.settings<1){console.error(bad);process.exit(1)}" && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && cd mobile && npx expo export --platform ios --output-dir /tmp/expo-export-p19-17 >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - statusUpdate: settings ≥ 1 and every other key 0 (verify command)
    - formKeystroke: surveyList, surveyDetail, home, publicMap, account, settings and rows are 0
    - `grep -c "useSurveys()\|useSurveyFormState()\|useStatus()" mobile/src/app/AuthenticatedAppNavigation.tsx` returns 0
    - `grep -c "'./src/navigation/'" mobile/jest.unit.config.js` returns 1
    - Full suite, lint, typecheck, format, expo export green
  </acceptance_criteria>
  <done>A status update or a keystroke re-renders only the screens that show it, measured and pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session/token contexts → route components | Token and identity distributed per route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-31 | Information Disclosure | Token reaching routes other than Account | mitigate | Only AccountRoute calls useAccessToken() (grep in SUMMARY) |
| T-01.9-32 | Tampering | Sync triggered twice by duplicated listeners after the move | mitigate | Tab listeners call the same stable actions; single assembler unchanged; owner-gate and logout-purge suites run in the full suite |
</threat_model>

<verification>
- Harness after-counts at target; full suite, typecheck, lint, expo export green
</verification>

<success_criteria>
ROADMAP criterion 1 demonstrated by committed render counts.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-17-SUMMARY.md` when done
</output>
