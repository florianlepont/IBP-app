---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 18
type: execute
wave: 3
depends_on: [01.9-05, 01.9-09]
files_modified:
  - package.json
  - package-lock.json
  - App.tsx
  - tsconfig.json
  - .dockerignore
  - api/package.json
  - api/Dockerfile
  - mobile/package.json
  - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-autolinking-before.json
  - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-autolinking-after.json
autonomous: true
requirements: [REQ-AUD-hygiene, REQ-AUD-mobile-state]

must_haves:
  truths:
    - "The root App.tsx, the root runtime dependencies (expo, react, react-native) and the Expo root tsconfig.json are gone; root overrides and devDependencies.react-test-renderer stay (D-09, criterion 5)"
    - "bcryptjs, @nestjs/schedule, nodemailer and @types/nodemailer are removed from the API; @expo/ngrok is a mobile devDependency (D-09)"
    - "supercluster@9.1.0 is a mobile dependency, installed only after npm view confirms repository github.com/mapbox/supercluster (D-05)"
    - "This is the only plan of the phase that changes package-lock.json (D-09)"
    - "The set of linked native modules is identical before and after: expo-modules-autolinking resolve and react-native-config outputs are saved and diffed; npm ls shows one copy of react, react-native and expo (D-09, Pitfall 13)"
    - "Both tab libraries stay (D-08): the criterion's 'unused tab library' has no target, and the SUMMARY records the evidence"
    - "The API image still builds, carries no mobile dependency and boots"
  artifacts:
    - path: .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-autolinking-before.json
      provides: "Linked native modules before the removals"
    - path: .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-autolinking-after.json
      provides: "Linked native modules after the removals (identical module set)"
  key_links:
    - from: mobile/package.json
      to: supercluster
      via: "dependencies entry pinned 9.1.0"
      pattern: "\"supercluster\": \"9.1.0\""
---

<objective>
Do every dependency change of the phase in one plan: remove the root leftovers and the unused API packages, move `@expo/ngrok` to devDependencies, install `supercluster@9.1.0`, and prove the native module set and the API image are unchanged.

Purpose: ROADMAP criterion 5, D-09 (removals, one lockfile plan, autolinking before/after), D-05 (supercluster), D-08 (both tab libraries kept). Native builds are re-proven on the phase PR by the jobs from 01.9-05 (baseline run recorded in 01.9-05-SUMMARY).
Output: cleaned package manifests and lockfile, deleted root files, two autolinking JSON snapshots.
No other plan of the phase touches any package.json or package-lock.json.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-RESEARCH.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-09-SUMMARY.md
@CLAUDE.md

<interfaces>
Root today: `App.tsx` (3 lines, `import App from "./mobile/App"`, referenced only by `.dockerignore:13`); `tsconfig.json` (`"extends": "expo/tsconfig.base"`; nothing extends it, api/ and mobile/ have their own); root `package.json` `dependencies` { expo 57.0.24, react 19.2.3, react-native 0.86.3 }, `overrides` (keep) and `devDependencies.react-test-renderer` (keep). The CI `shared` filter lists `tsconfig*.json` (`ci.yml:45`); harmless once the file is gone.
API: `api/package.json:27` `@types/nodemailer`, `:48` `@nestjs/schedule`, `:50` `bcryptjs`, `:58` `nodemailer`. EmailService was deleted by 01.9-09; `grep -rn "ScheduleModule\|bcrypt\|nodemailer" api/src` must be empty before removing. `api/Dockerfile:5-6` and `:22-23` carry a stale comment about the root package.json's Expo/React Native dependencies: rewrite it for the new root (workspaces + overrides only).
Mobile: `mobile/package.json:23` `@expo/ngrok` → devDependencies (no native code; `expo start --tunnel` still finds it in dev installs).
supercluster (D-05, owner-approved 2026-09-26, no further checkpoint): before installing run `npm view supercluster@9.1.0 name version repository.url maintainers scripts dependencies`; STOP and report if the repository is not github.com/mapbox/supercluster, if the maintainers are not Mapbox people, or if an install/preinstall/postinstall script exists. Then `npm install --workspace mobile --save-exact supercluster@9.1.0`. Afterwards run `ls -d node_modules/supercluster mobile/node_modules/supercluster` and record where it was hoisted (C-9) for 01.9-22's Jest mapper. Never run `slopcheck install`.
Tab libraries (D-08): keep `react-native-bottom-tabs`, `@bottom-tabs/react-navigation` (native iOS bar) and `@react-navigation/bottom-tabs` (Android and JS fallback). Record in the SUMMARY that neither is unused (RESEARCH Pitfall 12), so criterion 5's "unused tab library" has nothing to remove, by owner decision.
Autolinking proof (D-09, RESEARCH Pattern 10), from `mobile/`: `npx expo-modules-autolinking resolve -p android --json`, `npx expo-modules-autolinking resolve -p ios --json`, `npx expo-modules-autolinking react-native-config -p android --json`; from the root `npm ls react react-native expo --all`. Save a combined JSON `{ capturedAt, moduleNames, expoModules: { android, ios }, communityModules, npmLs }` where `moduleNames` is the sorted, de-duplicated list of every Expo module name (both platforms) and community module name. Compare before and after on `moduleNames` (versions and absolute paths may differ). Today's community list: expo, react-native-auth0, react-native-bottom-tabs, react-native-gesture-handler, react-native-maps, react-native-safe-area-context, react-native-screens, react-native-svg.
Bundle check: `cd mobile && npx expo export --platform ios --source-maps --output-dir /tmp/expo-export-p19-18`, then the single-copy source-map check used in phase 01.3 (find it in `.planning/phases/01.3-ci-and-test-safety-net/*SUMMARY.md`; it asserts one copy of react and react-native in the map's sources).
Image check (like ci.yml image-check): start Docker if needed (`nohup dockerd >/tmp/dockerd.log 2>&1 &`), `docker build -t cortege-api:p19-18 -f api/Dockerfile .`, then the "no mobile deps in image" smoke (`ci.yml:473-477`) and the boot + /v1/health smoke (`ci.yml:484-520`), reusing their commands.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autolinking baseline, then the removals and the supercluster install</name>
  <files>package.json, package-lock.json, App.tsx, tsconfig.json, .dockerignore, api/package.json, api/Dockerfile, mobile/package.json, .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-autolinking-before.json</files>
  <read_first>
    - package.json, api/package.json, mobile/package.json
    - App.tsx, tsconfig.json, .dockerignore, api/Dockerfile
    - .eslintrc.json and .prettierignore (confirm nothing references the root tsconfig or App.tsx)
  </read_first>
  <action>First save the before snapshot (interfaces block) to `01.9-autolinking-before.json`, before touching any manifest. Then, per D-09: delete root `App.tsx` and `tsconfig.json` and the `App.tsx` line of `.dockerignore`; delete the root `dependencies` block; remove the four API packages; move `@expo/ngrok` to mobile devDependencies; rewrite the Dockerfile comment. Per D-05 run the `npm view` check and install supercluster with `--save-exact`. Regenerate the lockfile with `npm install` from the root (never hand-edit it). Confirm no JSON config references the deleted root tsconfig.</action>
  <verify>
    <automated>cd /home/user/cortege && test ! -e App.tsx && test ! -e tsconfig.json && test -s .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-autolinking-before.json && node -e "const r=require('./package.json');if(r.dependencies)process.exit(1);if(!r.overrides||!r.devDependencies['react-test-renderer'])process.exit(2);const a=require('./api/package.json');for(const p of ['bcryptjs','@nestjs/schedule','nodemailer','@types/nodemailer']){if((a.dependencies||{})[p]||(a.devDependencies||{})[p])process.exit(3)}const m=require('./mobile/package.json');if(m.dependencies['@expo/ngrok']||!m.devDependencies['@expo/ngrok'])process.exit(4);if(m.dependencies.supercluster!=='9.1.0')process.exit(5)" && npm ls react react-native expo --all >/dev/null && npm ci --dry-run >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Root App.tsx and tsconfig.json deleted; root package.json has no `dependencies`, keeps `overrides` and `devDependencies.react-test-renderer`
    - The four API packages are gone; `@expo/ngrok` is a devDependency; `"supercluster": "9.1.0"` is a mobile dependency
    - The npm view output (repository, maintainers, scripts) is quoted in the SUMMARY
    - `npm ls react react-native expo --all` exits 0 with one version of each
  </acceptance_criteria>
  <done>All dependency changes of the phase are in one lockfile update.</done>
</task>

<task type="auto">
  <name>Task 2: After snapshot, native module diff, bundle and image checks</name>
  <files>.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-autolinking-after.json</files>
  <read_first>
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-autolinking-before.json
    - .github/workflows/ci.yml:422-520 (image-check smoke commands)
  </read_first>
  <action>Save the after snapshot, compare module names with the before snapshot and require equality (D-09). Run the bundle and single-copy source-map check, the Docker image build and both smokes, then the full local gate (lint, typecheck, API and mobile unit suites, format). Record in the SUMMARY: the diff result, `npm ls` output, the source-map check result, the image smoke lines, the supercluster hoisting location, and the D-08 tab-library evidence. The native iOS/Android builds are re-run on the phase PR by 01.9-28 (jobs from 01.9-05).</action>
  <verify>
    <automated>cd /home/user/cortege && node -e "const d='./.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/';const b=require(d+'01.9-autolinking-before.json').moduleNames,a=require(d+'01.9-autolinking-after.json').moduleNames;if(!Array.isArray(b)||!b.length||JSON.stringify(a)!==JSON.stringify(b)){console.error('native module set changed or missing');process.exit(1)}" && cd mobile && npx expo export --platform ios --output-dir /tmp/expo-export-p19-18 >/dev/null && cd .. && npm run lint && npm run typecheck && npm run test:unit && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - The native module name sets before and after are identical (verify command)
    - `docker run --rm cortege-api:p19-18 sh -c 'ls node_modules | grep -E "^(react-native|expo)$"'` prints nothing (no mobile deps in the image) and the boot smoke gets 200 from /v1/health
    - expo export bundles; full local gate green
  </acceptance_criteria>
  <done>The dependency cleanup is proven not to change native linking, the bundle or the API image.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repository | One new package installed; lockfile regenerated |
| Build → API image | Image content changes with the root manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-SC | Tampering | supercluster@9.1.0 install | mitigate | Owner approved 2026-09-26 (D-05); npm view gate on repository/maintainers/install scripts before install; exact version pin; slopcheck [OK] in RESEARCH; never `slopcheck install` |
| T-01.9-33 | Tampering | Lockfile regeneration pulling unexpected versions | mitigate | Root overrides kept; npm ls single copies; autolinking module-set diff; npm audit runs in CI |
| T-01.9-34 | Denial of Service | Two React copies after hoisting change (runtime crash) | mitigate | npm ls + source-map single-copy check after expo export |
| T-01.9-35 | Elevation of Privilege | Smaller attack surface expected, but image regressions possible | mitigate | Image build + no-mobile-deps + boot smokes locally; CI image-check on the PR |
</threat_model>

<verification>
- Module sets identical, single copies, bundle, image smokes and full local gate green
</verification>

<success_criteria>
ROADMAP criterion 5 (removals half) and D-09, with native linking proven unchanged locally; native builds re-proven on the PR.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-18-SUMMARY.md` when done
</output>
