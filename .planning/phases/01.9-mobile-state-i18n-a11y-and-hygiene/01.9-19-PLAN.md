---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 19
type: execute
wave: 3
depends_on: [01.9-04, 01.9-08]
files_modified:
  - mobile/src/hooks/useAuth0Session.ts
  - mobile/src/hooks/useAuth0Session.test.ts
  - mobile/src/hooks/useLocalDataOwner.ts
  - mobile/src/hooks/useLocalDataOwner.test.ts
  - mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts
  - mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts
  - mobile/src/hooks/useSurveySync.ts
  - mobile/src/hooks/useSurveySync.test.ts
  - mobile/src/hooks/useSurveySync.logout-purge.test.ts
  - mobile/src/hooks/sync-owner-gate.test.ts
  - mobile/src/i18n/fr/status/session.ts
  - mobile/src/i18n/fr/status/owner.ts
  - mobile/src/i18n/fr/status/sync.ts
  - mobile/src/i18n/fr/status/debug.ts
autonomous: true
requirements: [REQ-AUD-i18n-a11y, REQ-AUD-mobile-state]

must_haves:
  truths:
    - "Every status message and Alert text of useAuth0Session, useLocalDataOwner, useSurveySyncNetwork and useSurveySync (including the debug-reset dialogs and the initial 'Ready') comes from fr.status.session/owner/sync/debug (D-06)"
    - "None of these messages carries a survey id, error code or raw error text; the detail goes to logStatusDetail (D-06)"
    - "useSurveySyncNetwork.test.ts is rewritten on renderHook (D-01, C-4)"
    - "Sync behaviour is unchanged: single-flight drain, owner gate, purge suspension; logout-purge and owner-gate suites change at most in message literals"
  artifacts:
    - path: mobile/src/i18n/fr/status/sync.ts
      provides: "Sync and pull status texts (counts only)"
    - path: mobile/src/i18n/fr/status/debug.ts
      provides: "Debug reset dialog and status texts"
  key_links:
    - from: mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts
      to: mobile/src/i18n/fr/status/sync.ts
      via: "fr.status.sync.* calls"
      pattern: "fr\\.status\\.sync\\."
---

<objective>
Move the status and dialog texts of the session, owner, network and central sync hooks to the catalogue, removing ids and raw errors from what users see, and rewrite the network hook test on renderHook.

Purpose: criterion 4 for the remaining sync/session status messages (D-06) and C-4 for `useSurveySyncNetwork.test.ts`. The hotspot order for `useSurveySync.ts` is STATE (01.9-08) then I18N (this plan).
Output: four hooks on the catalogue, four sections filled, tests updated.
Boundaries: behaviour of the sync engine must not change (01.5 invariants). `setStatus` keeps accepting `string` until 01.9-27.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-10-PLAN.md
@.planning/phases/01.5-mobile-sync-engine-reliability/01.5-CONTEXT.md
@CLAUDE.md

<interfaces>
Same conventions as 01.9-10 (read its interfaces block): section files `sessionFr`, `ownerFr`, `syncFr`, `debugFr` wired as `fr.status.session|owner|sync|debug`; status entries are functions returning `statusText(...)` with names/counts only; Alert texts in an `alerts` sub-object; raw errors → `logStatusDetail("<hook>.<action>", error)` plus a catalogue message chosen by the error kind the hook already distinguishes (AUTH_REQUIRED_ERROR / AUTH_TEMPORARILY_UNAVAILABLE, network, conflict, validation, unknown).
Counts per file (grep): useSurveySyncNetwork 27 status calls; useAuth0Session 25; useLocalDataOwner 8; useSurveySync 18 status + 5 Alert (debug reset title/message/Cancel/Reset `:296-362`, initial `"Ready"` at `:51-52`). `reportStatus(scope, state, message)` is passed into useAuth0Session; keep its signature.
useSurveySync.test.ts (from 01.9-02) asserts status strings through one local constant block: replace those constants with catalogue values. `useSurveySync.logout-purge.test.ts` and `sync-owner-gate.test.ts` are 01.5 safety nets: edit them only to replace an asserted English message with its catalogue value; no other change (state any edit in the SUMMARY).
Scanner gates at 0 for each hook file: `node mobile/scripts/structure-report.js literals <file> --max 0` and `status-ids <file> --max 0`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session and owner hooks on the catalogue</name>
  <files>mobile/src/hooks/useAuth0Session.ts, mobile/src/hooks/useAuth0Session.test.ts, mobile/src/hooks/useLocalDataOwner.ts, mobile/src/hooks/useLocalDataOwner.test.ts, mobile/src/i18n/fr/status/session.ts, mobile/src/i18n/fr/status/owner.ts</files>
  <read_first>
    - mobile/src/hooks/useAuth0Session.ts and its test
    - mobile/src/hooks/useLocalDataOwner.ts and its test
    - mobile/src/i18n/status.ts, mobile/src/i18n/fr/common.ts
  </read_first>
  <action>Per D-06 move every status text to `fr/status/session.ts` and `fr/status/owner.ts` (no e-mail addresses of other accounts in plain status text unless the owner-conflict flow shows it by design; keep what the owner screen needs as a param), route raw errors to logStatusDetail, and update the tests to assert catalogue values.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/hooks/(useAuth0Session|useLocalDataOwner)" && for f in src/hooks/useAuth0Session.ts src/hooks/useLocalDataOwner.ts; do node mobile/scripts/structure-report.js literals $f --max 0 && node mobile/scripts/structure-report.js status-ids $f --max 0 || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Both scanners at 0 for both hooks; their tests green
  </acceptance_criteria>
  <done>Session and owner messages are French and id-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Network hook on the catalogue, test on renderHook</name>
  <files>mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts, mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts, mobile/src/i18n/fr/status/sync.ts</files>
  <read_first>
    - mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts and its test (whole files)
    - mobile/src/hooks/render-hook-smoke.test.ts:1-40
  </read_first>
  <behavior>
    - Every existing case is kept (single-flight, retry/backoff, owner gate refusal, auto-sync on reconnect, pull), asserting catalogue messages
    - A finished sync reports counts only (e.g. fr.status.sync.done({ synced: 2, failed: 1 }))
    - No `spyOn(React` in the test
  </behavior>
  <action>Rewrite the test on renderHook first (C-4), then move the texts (D-06). Control flow unchanged.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/hooks/survey-sync/useSurveySyncNetwork && node mobile/scripts/structure-report.js literals src/hooks/survey-sync/useSurveySyncNetwork.ts --max 0 && node mobile/scripts/structure-report.js status-ids src/hooks/survey-sync/useSurveySyncNetwork.ts --max 0 && test "$(grep -c 'spyOn(React' mobile/src/hooks/survey-sync/useSurveySyncNetwork.test.ts)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - Scanners at 0; no React spies; `it(` count equal or higher than before
  </acceptance_criteria>
  <done>Network sync messages are French and the last survey-sync spy test is on renderHook.</done>
</task>

<task type="auto">
  <name>Task 3: useSurveySync status and debug dialogs on the catalogue</name>
  <files>mobile/src/hooks/useSurveySync.ts, mobile/src/hooks/useSurveySync.test.ts, mobile/src/hooks/useSurveySync.logout-purge.test.ts, mobile/src/hooks/sync-owner-gate.test.ts, mobile/src/i18n/fr/status/debug.ts, mobile/src/i18n/fr/status/sync.ts</files>
  <read_first>
    - mobile/src/hooks/useSurveySync.ts (after 01.9-08)
    - mobile/src/hooks/useSurveySync.test.ts, useSurveySync.logout-purge.test.ts, sync-owner-gate.test.ts
  </read_first>
  <action>Per D-06 move the initial status, the detail/event loading messages (`Loading canonical details for ${id}` and similar lose the id), the logout/delete-account messages and the debug-reset dialogs and results to `fr/status/sync.ts` and `fr/status/debug.ts` (debug texts are French too; they only appear in dev builds). Update tests per the interfaces rule. Finish with the full mobile suite with coverage, lint, typecheck, format.</action>
  <verify>
    <automated>cd /home/user/cortege && node mobile/scripts/structure-report.js literals src/hooks/useSurveySync.ts --max 0 && node mobile/scripts/structure-report.js status-ids src/hooks/useSurveySync.ts --max 0 && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - Scanners at 0 for useSurveySync.ts
    - `git diff mobile/src/hooks/useSurveySync.logout-purge.test.ts mobile/src/hooks/sync-owner-gate.test.ts` changes only string literals (reviewed and stated in SUMMARY)
    - Full suite and coverage green
  </acceptance_criteria>
  <done>All session and sync status messages come from the catalogue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auth0 / API / network errors → status text | Technical error content reaching the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-36 | Information Disclosure | Auth errors, survey ids, error codes in status | mitigate | Catalogue messages only; logStatusDetail behind dev tools; status-ids scanner at 0 per file |
| T-01.9-37 | Tampering | Sync engine behaviour changed while editing messages (01.5 regressions) | mitigate | Safety-net suites may change only literals (diff review); full suite with coverage |
</threat_model>

<verification>
- Scanners at 0 for the four hooks; full suite and coverage green
</verification>

<success_criteria>
Criterion 4 for session, owner and sync status messages; C-4 completed for the network hook.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-19-SUMMARY.md` when done
</output>
