---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 21
type: execute
wave: 4
depends_on: [01.9-03, 01.9-04, 01.9-05, 01.9-09, 01.9-18]
files_modified:
  - mobile/src/hooks/useEditingDraft.ts
  - mobile/src/hooks/useEditingDraft.test.ts
  - mobile/src/hooks/useEditingDraft.autosave.test.ts
  - mobile/src/hooks/useSurveyDraftPatcher.ts
  - mobile/src/hooks/useSurveyDraftPatcher.test.ts
  - mobile/src/hooks/useGpsCapture.ts
  - mobile/src/hooks/useGpsCapture.test.ts
  - mobile/src/hooks/useSurveyForm.ts
  - mobile/src/hooks/useSurveyForm.test.ts
  - mobile/src/state/AppStateProvider.tsx
  - mobile/src/i18n/fr/status/app.ts
  - mobile/src/i18n/fr/status/editing.ts
  - mobile/src/i18n/fr/status/gps.ts
  - mobile/src/i18n/fr/validation.ts
autonomous: true
requirements: [REQ-AUD-i18n-a11y, REQ-AUD-mobile-state]

must_haves:
  truths:
    - "Every status message of useEditingDraft, useSurveyDraftPatcher, useGpsCapture and the app bootstrap (init error, survey opened) comes from the catalogue and carries no id or raw error (D-06)"
    - "The form validation messages of useSurveyForm (requiredError, numberError, oneOfError and the field labels) come from fr.validation, keyed on today's field and rule names (D-06, D-12 item 3: re-checked after 01.8 in 01.9-32)"
    - "useEditingDraft.test.ts and useSurveyForm.test.ts (on renderHook since 01.9-03) keep every case; only asserted strings become catalogue values; no test in mobile/src spies on React"
  artifacts:
    - path: mobile/src/i18n/fr/validation.ts
      provides: "Field labels and validation messages keyed by field and rule"
    - path: mobile/src/i18n/fr/status/app.ts
      provides: "Bootstrap and navigation status texts"
  key_links:
    - from: mobile/src/hooks/useSurveyForm.ts
      to: mobile/src/i18n/fr/validation.ts
      via: "fr.validation.* in requiredError/numberError/oneOfError"
      pattern: "fr\\.validation\\."
---

<objective>
Move the remaining hook status messages (editing, draft patching, GPS, app bootstrap) and the form validation messages to the catalogue. Tests only swap asserted strings (renderHook rewrites were done by 01.9-03).

Purpose: criterion 4 for these messages (D-06); D-12 item 3 says the validation catalogue is built now on today's names and re-checked after 01.8.
Output: four hooks and the provider on the catalogue, four sections filled, two tests rewritten.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-11-PLAN.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-18-SUMMARY.md
@docs/technical/ibp-validation-matrix-v1.md
@CLAUDE.md

<interfaces>
Conventions: same as 01.9-11 (sections `appFr`, `editingFr`, `gpsFr` under `fr.status.app|editing|gps`; `validationFr` as `fr.validation`; status entries return `statusText(...)` with names/counts only; raw errors to logStatusDetail).
Facts: useEditingDraft 14 status calls; useSurveyDraftPatcher 4; useGpsCapture 8 plus an `onAlert(title, message)` callback (the provider wires it to `Alert.alert`; the texts come from `fr.status.gps.alerts`); useSurveyForm validation: `type FieldError = string | null`, `requiredError(value, label)` (`:68`), `numberError(...)` (`:71-86`), `oneOfError(value, label, allowed)` (`:88`), labels such as `"Site name"` (`:296`). AppStateProvider (from 01.9-09) holds the bootstrap `Init error: ${message}` and `Survey ${surveyId} opened` messages (formerly `App.tsx:140,146`).
Validation catalogue shape (keep names stable for 01.9-32): `fr.validation.fields` (label per form field key, French), `fr.validation.rules.required(label)`, `.number(label)`, `.min(label, min)`, `.max(label, max)`, `.oneOf(label, allowed: string)`; FieldError stays `string | null`.
Tests are already on renderHook (01.9-03); do not restructure them.
Gates at 0 per hook file: `structure-report.js literals <file> --max 0` and `status-ids <file> --max 0`; also for `src/state/AppStateProvider.tsx`. End state: `grep -rln "spyOn(React" mobile/src` lists nothing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Editing and draft-patcher hooks on the catalogue</name>
  <files>mobile/src/hooks/useEditingDraft.ts, mobile/src/hooks/useEditingDraft.test.ts, mobile/src/hooks/useEditingDraft.autosave.test.ts, mobile/src/hooks/useSurveyDraftPatcher.ts, mobile/src/hooks/useSurveyDraftPatcher.test.ts, mobile/src/i18n/fr/status/editing.ts</files>
  <read_first>
    - mobile/src/hooks/useEditingDraft.ts and both tests
    - mobile/src/hooks/useSurveyDraftPatcher.ts and its test
  </read_first>
  <behavior>
    - useEditingDraft.test.ts keeps every case; only its asserted strings become catalogue values
    - Autosave and draft-patch failures show catalogue text; the survey id never appears in the message
  </behavior>
  <action>Swap the asserted strings in useEditingDraft.test.ts for catalogue values first, then move the texts of both hooks to `fr/status/editing.ts` (D-06) and update the autosave and patcher tests.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/hooks/(useEditingDraft|useSurveyDraftPatcher)" && for f in src/hooks/useEditingDraft.ts src/hooks/useSurveyDraftPatcher.ts; do node mobile/scripts/structure-report.js literals $f --max 0 && node mobile/scripts/structure-report.js status-ids $f --max 0 || exit 1; done && test "$(grep -c 'spyOn(React' mobile/src/hooks/useEditingDraft.test.ts)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - Scanners at 0 for both hooks; no React spy in useEditingDraft.test.ts; `it(` count not lower
  </acceptance_criteria>
  <done>Editing messages are French and id-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validation messages on the catalogue</name>
  <files>mobile/src/hooks/useSurveyForm.ts, mobile/src/hooks/useSurveyForm.test.ts, mobile/src/i18n/fr/validation.ts</files>
  <read_first>
    - mobile/src/hooks/useSurveyForm.ts (whole file) and its test
    - docs/technical/ibp-validation-matrix-v1.md (French wording of rules)
  </read_first>
  <behavior>
    - An empty site name yields fr.validation.rules.required(fr.validation.fields.siteName)
    - A non-number and an out-of-range factor value yield the number/min/max messages with the French field label
    - A value outside an allowed set yields the oneOf message listing allowed values
    - Test cases unchanged except asserted strings
  </behavior>
  <action>Swap the test's asserted validation strings for catalogue values first, then build `fr/validation.ts` keyed on today's field keys and rule names (D-12 item 3) and make the three error builders read it. Do not change which values are valid (the rules themselves move to the shared domain package in 01.8).</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/hooks/useSurveyForm && node mobile/scripts/structure-report.js literals src/hooks/useSurveyForm.ts --max 0 && test "$(grep -c 'spyOn(React' mobile/src/hooks/useSurveyForm.test.ts)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - Literal scanner at 0 for useSurveyForm.ts; behavior tests pass
  </acceptance_criteria>
  <done>Form validation messages are French, from one catalogue section.</done>
</task>

<task type="auto">
  <name>Task 3: GPS hook and app bootstrap messages on the catalogue</name>
  <files>mobile/src/hooks/useGpsCapture.ts, mobile/src/hooks/useGpsCapture.test.ts, mobile/src/state/AppStateProvider.tsx, mobile/src/i18n/fr/status/gps.ts, mobile/src/i18n/fr/status/app.ts</files>
  <read_first>
    - mobile/src/hooks/useGpsCapture.ts and its test
    - mobile/src/state/AppStateProvider.tsx
  </read_first>
  <action>Per D-06 move the GPS status and alert texts to `fr/status/gps.ts` and the provider's bootstrap/opened messages to `fr/status/app.ts` (the "survey opened" message uses the survey's name, not its id; the init error shows a generic French text and logs the detail). Update the GPS test. Confirm no React spy remains in mobile tests. Finish with the full mobile suite with coverage, the render harness, lint, typecheck and format.</action>
  <verify>
    <automated>cd /home/user/cortege && for f in src/hooks/useGpsCapture.ts src/state/AppStateProvider.tsx; do node mobile/scripts/structure-report.js literals $f --max 0 && node mobile/scripts/structure-report.js status-ids $f --max 0 || exit 1; done && test -z "$(grep -rln 'spyOn(React' mobile/src)" && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - Scanners at 0 for both files; no `spyOn(React` in mobile/src; full suite and coverage green
  </acceptance_criteria>
  <done>Every hook-level status message of the app comes from the catalogue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device location and local errors → status text | GPS errors and init failures reaching the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-38 | Information Disclosure | Survey ids and raw init/GPS errors in status | mitigate | Catalogue messages with names only; logStatusDetail for detail; status-ids scanner at 0 |
| T-01.9-39 | Tampering | Validation rules changed while moving texts | mitigate | Only message builders read the catalogue; valid-value logic untouched; behavior tests on each rule |
</threat_model>

<verification>
- Scanners at 0 for the listed files; no React spies left in mobile tests; full suite green
</verification>

<success_criteria>
Criterion 4 for the remaining hook messages and D-12 item 3's validation catalogue (built on today's names).
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-21-SUMMARY.md` when done
</output>
