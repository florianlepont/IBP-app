---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 21
type: execute
wave: 4
depends_on: [01.9-07, 01.9-17]
files_modified:
  - mobile/src/screens/SurveyListScreen.tsx
  - mobile/src/screens/survey-list/SurveyRow.tsx
  - mobile/src/screens/survey-list/row-styles.ts
  - mobile/src/screens/survey-list/survey-list-500.test.tsx
  - mobile/src/navigation/routes/SurveyListRoute.tsx
  - mobile/src/state/render-counts.test.tsx
  - mobile/src/i18n/fr/survey-list.ts
autonomous: true
requirements: [REQ-AUD-mobile-state, REQ-AUD-i18n-a11y]

must_haves:
  truths:
    - "The survey list is an Animated.FlatList with keyExtractor, a stable renderItem, a memoised ListHeaderComponent and tuned initialNumToRender/windowSize/removeClippedSubviews (D-03)"
    - "Each row is memo(SurveyRow) taking survey, preview, selected, onOpen(id) and onDelete(id); the Swipeable ref is local to the row (D-03)"
    - "With 500 surveys only the initial window of rows mounts, and changing one survey re-renders exactly one row (D-03 500-survey proof, rendering half)"
    - "In the render harness, a status update and a keystroke re-render 0 rows and a one-survey refresh re-renders 1 row (criterion 1, D-02)"
    - "Row texts and the two row accessibility labels come from fr.surveyList (D-06, C-10)"
  artifacts:
    - path: mobile/src/screens/survey-list/SurveyRow.tsx
      provides: "Memoised list row"
      contains: "memo("
    - path: mobile/src/screens/survey-list/survey-list-500.test.tsx
      provides: "500-survey windowing and single-row re-render proof"
  key_links:
    - from: mobile/src/screens/SurveyListScreen.tsx
      to: mobile/src/screens/survey-list/SurveyRow.tsx
      via: "renderItem returning <SurveyRow>"
      pattern: "SurveyRow"
---

<objective>
Turn the survey list into a virtualised `Animated.FlatList` with memoised rows, prove it stays fluid with 500 surveys, and pin the row numbers in the render harness.

Purpose: ROADMAP criterion 2 ("the survey list is a FlatList with memoised rows and stays fluid with 500 surveys"), D-03, and the row part of D-02. Completion is already a column (01.9-07).
Output: `SurveyRow`, FlatList in `SurveyListScreen`, 500-survey test, harness rows pinned, row texts in `fr/survey-list.ts`.
Hotspot order for `SurveyListScreen.tsx`: this plan (LIST) → 01.9-24 (NAV props) → 01.9-25 (split and remaining text). This plan changes only the list body and the row; the screen is split later.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-RESEARCH.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-17-SUMMARY.md
@CLAUDE.md

<interfaces>
Today (`SurveyListScreen.tsx`): `Animated.ScrollView` from `:678` to `:1199`, rows `mainListSurveys.map(...)` at `:1019-1161` with inline derivations (`attachments`, `firstPhotoAttachment`, `isSelected`), a `let swipeableRef` closure (`:1031`), delete action label `` `Supprimer le relevé ${survey.site_name}` `` (`:1042`) and row label `` `${survey.site_name}, ${uiStatusLabel}, mis à jour …` `` (`:1065`) (C-10), `import Swipeable from "react-native-gesture-handler/Swipeable"` (`:18`, keep: reanimated is not installed), collapsing hero reading `scrollY` (~`:320`), attention section and continue-draft card (`:912-1000`).
Target (RESEARCH Pattern 3, Pitfall 5): `Animated.FlatList` (the hero reads scrollY) with `data={mainListSurveys}`, `keyExtractor={(s) => s.id}`, `renderItem` from `useCallback`, `ListHeaderComponent` = a memoised element holding hero, filters bar, attention section and continue card, `ListEmptyComponent`, `onScroll={Animated.event([{ nativeEvent: { contentOffset: { y: scrollY } } }], { useNativeDriver: true })}`, keep `keyboardShouldPersistTaps` and the refresh control, `initialNumToRender={10}`, `windowSize={7}`, `removeClippedSubviews` (Claude's discretion on numbers, CONTEXT). `previewById` derived once with `useMemo` from `attachmentsBySurvey`, so each row gets one preview value. `onOpen(id)`/`onDelete(id)` are stable (from SurveyListRoute via context actions or useLatestCallback; fix the route if it passes inline closures). Row sync error text: `formatSyncErrorForUser(survey.last_sync_error, survey.last_sync_error_code)`.
Catalogue: fill `mobile/src/i18n/fr/survey-list.ts` with a `row` and an `a11y` sub-object (`deleteSurvey(name)`, `openSurvey({ name, status, updatedAt })`) now; 01.9-25 adds the rest of the screen's text to the same file later (sequential).
Harness (`mobile/src/state/render-counts.test.tsx`): its react-native mock already renders `FlatList`/`Animated.FlatList` as `data.slice(0, initialNumToRender ?? 10).map(renderItem)` and counts Swipeable renders as rows. With 20 seeded surveys and initialNumToRender 10, initialMount rows becomes 10. Update only `EXPECTED` (all decreases): statusUpdate rows 0, formKeystroke rows 0, oneSurveyRefresh rows 1 (only if the changed survey is inside the first 10; seed it so).
500 test (`survey-list-500.test.tsx`): react-test-renderer + mocked react-native (the harness mock shape) whose FlatList honours `initialNumToRender`; render `SurveyListScreen` with 500 surveys and stable callbacks; assert mounted rows ≤ 10, then rerender with one survey object replaced (same id) and assert exactly one SurveyRow render (count through a Swipeable probe), then rerender with an unrelated prop change (e.g. `selectedSurveyId` of an off-screen survey) and assert 0 row renders for untouched rows.
Gates for the new row files at 0: `structure-report.js literals|unused-styles src/screens/survey-list --max 0`, `npx eslint src/screens/survey-list --max-warnings 0`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Memoised SurveyRow and the FlatList list body</name>
  <files>mobile/src/screens/SurveyListScreen.tsx, mobile/src/screens/survey-list/SurveyRow.tsx, mobile/src/screens/survey-list/row-styles.ts, mobile/src/screens/survey-list/survey-list-500.test.tsx, mobile/src/navigation/routes/SurveyListRoute.tsx, mobile/src/i18n/fr/survey-list.ts</files>
  <read_first>
    - mobile/src/screens/SurveyListScreen.tsx (whole file)
    - mobile/src/navigation/routes/SurveyListRoute.tsx
    - mobile/src/state/render-counts.test.tsx (react-native mock to copy)
    - mobile/src/app/formatters.ts (formatSyncErrorForUser signature)
  </read_first>
  <behavior>
    - 500 surveys: mounted rows ≤ 10
    - One survey replaced: exactly one row re-renders
    - Parent rerender with unchanged list props: 0 row re-renders; renderItem identity unchanged
    - Pressing a row's delete action calls onDelete with that row's own survey id
    - Row accessibility labels equal fr.surveyList.a11y.deleteSurvey(name) and fr.surveyList.a11y.openSurvey(...) and contain no id
  </behavior>
  <action>Write the 500 test first. Then per D-03 extract the row into `memo(SurveyRow)` with its styles in `row-styles.ts` (moved from the screen's styles; delete the moved keys from the screen), replace the ScrollView/map with `Animated.FlatList` per the interfaces block, derive `previewById` once, make row callbacks id-based and stable, and move the row's texts and the two C-10 labels into `fr/survey-list.ts` (D-06).</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/screens/survey-list && node mobile/scripts/structure-report.js literals src/screens/survey-list --max 0 && node mobile/scripts/structure-report.js unused-styles src/screens/SurveyListScreen.tsx src/screens/survey-list --max 0 && cd mobile && npx eslint src/screens/survey-list --max-warnings 0 && grep -c "Animated.FlatList" src/screens/SurveyListScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mainListSurveys.map" mobile/src/screens/SurveyListScreen.tsx` returns 0
    - `grep -c "memo(" mobile/src/screens/survey-list/SurveyRow.tsx` returns at least 1
    - `grep -c "let swipeableRef" mobile/src/screens/SurveyListScreen.tsx` returns 0
    - Behavior tests pass; gates at 0 for survey-list/
  </acceptance_criteria>
  <done>The list is virtualised with memoised rows and proven at 500 surveys.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the row counts in the render harness</name>
  <files>mobile/src/state/render-counts.test.tsx</files>
  <read_first>
    - mobile/src/state/render-counts.test.tsx
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-before.json
  </read_first>
  <action>Run the harness, confirm statusUpdate rows 0, formKeystroke rows 0 and oneSurveyRefresh rows 1, and update `EXPECTED` (decreases only; if oneSurveyRefresh re-renders other screens, check they read the surveys context and record why). Write `RENDER_COUNTS_OUT=/tmp/render-counts-p19-21.json` and put the four scenarios next to the before numbers in the SUMMARY. Finish with the full mobile suite with coverage, lint, typecheck, format and an expo export.</action>
  <verify>
    <automated>cd /home/user/cortege && RENDER_COUNTS_OUT=/tmp/render-counts-p19-21.json npm --workspace mobile run test:unit -- src/state/render-counts.test.tsx && node -e "const s=require('/tmp/render-counts-p19-21.json').scenarios;if(s.statusUpdate.rows!==0||s.formKeystroke.rows!==0||s.oneSurveyRefresh.rows!==1)process.exit(1)" && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && cd mobile && npx expo export --platform android --output-dir /tmp/expo-export-p19-21 >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - statusUpdate.rows 0, formKeystroke.rows 0, oneSurveyRefresh.rows 1 (verify command)
    - No EXPECTED number increased compared with 01.9-17
  </acceptance_criteria>
  <done>Row re-renders are measured and pinned at their target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Rendering change over local data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-40 | Denial of Service | UI freeze with many local surveys in the field | mitigate | Virtualised FlatList, memo rows, 500-survey test; completion column removes JSON parsing (01.9-07) |
| T-01.9-41 | Tampering | Delete action bound to the wrong survey after the row refactor | mitigate | onDelete(id) receives the row's own survey id; row test asserts the id passed to onDelete |
</threat_model>

<verification>
- 500 test, harness rows, full suite and expo export green
</verification>

<success_criteria>
Criterion 2 list half: virtualised, memoised, fluid at 500 surveys; criterion 1 rows pinned.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-21-SUMMARY.md` when done
</output>
