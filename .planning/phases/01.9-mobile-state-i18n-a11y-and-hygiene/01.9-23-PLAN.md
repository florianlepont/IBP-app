---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 23
type: execute
wave: 4
depends_on: [01.9-03, 01.9-04, 01.9-07, 01.9-18, 01.9-19]
files_modified:
  - mobile/src/api/ibp-api.ts
  - mobile/src/api/ibp-api.test.ts
  - mobile/src/hooks/usePublicMapExplorer.ts
  - mobile/src/hooks/usePublicMapExplorer.test.ts
  - mobile/src/hooks/useDebouncedValue.ts
  - mobile/src/hooks/useDebouncedValue.test.ts
  - mobile/src/screens/public-map/useMapClusters.ts
  - mobile/src/screens/public-map/useMapClusters.test.ts
  - mobile/src/i18n/fr/status/map.ts
  - mobile/jest.unit.config.js
autonomous: true
requirements: [REQ-AUD-mobile-state, REQ-AUD-i18n-a11y]

must_haves:
  truths:
    - "fetchPublicMapItems sends bbox when given; loadPublicMap(bbox) ignores stale responses and skips a request whose key equals the last one (D-05)"
    - "useDebouncedValue delays a value by the given delay (about 400 ms for the map) and cleans its timer on unmount (D-05)"
    - "useMapClusters builds one supercluster index per items array and derives clusters from the region without rebuilding the index; a cluster at max zoom yields its leaves instead of zooming (D-05, Pitfall 7)"
    - "supercluster resolves in Jest through a moduleNameMapper to its UMD dist file at the hoisting location recorded by 01.9-19 (D-05, C-9)"
    - "usePublicMapExplorer.test.ts (on renderHook since 01.9-03) keeps its existing cases with asserted strings swapped for fr.status.map values, and gains the bbox/stale/dedupe cases (D-06)"
  artifacts:
    - path: mobile/src/screens/public-map/useMapClusters.ts
      provides: "Supercluster index + clusters for the current region"
      contains: "supercluster"
    - path: mobile/src/hooks/useDebouncedValue.ts
      provides: "Generic debounced value hook"
  key_links:
    - from: mobile/src/hooks/usePublicMapExplorer.ts
      to: mobile/src/api/ibp-api.ts
      via: "fetchPublicMapItems(apiUrl, { bbox, … })"
      pattern: "fetchPublicMapItems\\("
---

<objective>
Build the map's data layer: bbox-aware API client and explorer hook with stale-response and duplicate-request guards, a debounced value hook, and a supercluster-based clustering hook, all unit-tested.

Purpose: ROADMAP criterion 3 (requests by bbox, clusters, debounces) and D-05, mobile side. 01.9-28 wires these into the split map screen (memoised markers, no auto-refit loop, accessibility).
Hotspot order for `usePublicMapExplorer.ts`: STATE (01.9-18 moved its call into PublicMapRoute) → MAP (this plan, including its three status strings, so no later I18N plan edits it). This plan does not touch `PublicMapScreen.tsx` or any route file (`loadPublicMap`'s new bbox parameter is optional, so current callers compile).
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-RESEARCH.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-07-SUMMARY.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-19-SUMMARY.md
@CLAUDE.md

<interfaces>
API client (`mobile/src/api/ibp-api.ts:174-190` fetchPublicMapItems; `:192-199` fetchPublicParcelStatuses as the bbox analog): add `bbox?: string` to the input and `if (input?.bbox?.trim()) queryParts.push(\`bbox=${encodeURIComponent(input.bbox)}\`)`.
Explorer (`usePublicMapExplorer.ts`): `loadPublicMap` at `:25-41` (two English status strings `:35`, `:37`); parcels path already has the stale guard `requestRef` (`:43-76`). New: `loadPublicMap(options?: { bbox?: string })` with its own `itemsRequestRef` stale guard and a `lastItemsKeyRef` that skips a request whose key (`bbox ?? "all"`) equals the last completed one unless `options.force` is true (the Explorer tab press forces a reload). Status texts to `fr/status/map.ts` (`mapFr` under `fr.status.map`), no raw errors (logStatusDetail).
`useDebouncedValue<T>(value: T, delayMs: number): T` (RESEARCH Code Examples) in `mobile/src/hooks/`.
`useMapClusters({ items, region })` in `mobile/src/screens/public-map/`: `const index = useMemo(() => { const sc = new Supercluster({ radius: 60, maxZoom: 16 }); sc.load(items.map(toPointFeature)); return sc }, [items])`; `clusters = useMemo(() => index.getClusters(bboxArray(region), Math.floor(computeRegionZoom(region))), [index, region])`; helpers `expansionZoom(clusterId)` and `leaves(clusterId)`; `computeRegionBbox`/`computeRegionZoom` exist in `mobile/src/app/map-viewport.ts:14,32` (reuse; add a numeric bbox helper next to them only if needed, in the hook file). Item coordinates come from `display_location` (rounded to 2 decimals by the API, so identical points are common: at max zoom a cluster tap lists its surveys via leaves, Pitfall 7). Radius 60 and maxZoom 16 are Claude's discretion (CONTEXT).
supercluster 9.1.0 is ESM-only (Pitfall 8): add to `mobile/jest.unit.config.js` `moduleNameMapper` `'^supercluster$': '<rootDir>/<path from 01.9-19 SUMMARY>/supercluster/dist/supercluster.js'` (`<rootDir>/node_modules/...` if hoisted in mobile, `<rootDir>/../node_modules/...` if at the root). Metro resolves the ESM entry normally.
Tests: renderHook recipe (`render-hook-smoke.test.ts:1-26`) with fake timers for the debounce; usePublicMapExplorer.test.ts is already on renderHook (01.9-03): extend it, do not restructure it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bbox client, explorer guards and status texts</name>
  <files>mobile/src/api/ibp-api.ts, mobile/src/api/ibp-api.test.ts, mobile/src/hooks/usePublicMapExplorer.ts, mobile/src/hooks/usePublicMapExplorer.test.ts, mobile/src/i18n/fr/status/map.ts</files>
  <read_first>
    - mobile/src/api/ibp-api.ts:160-210 and mobile/src/api/ibp-api.test.ts
    - mobile/src/hooks/usePublicMapExplorer.ts and its test (whole files)
    - mobile/src/navigation/routes/PublicMapRoute.tsx (caller; do not edit)
  </read_first>
  <behavior>
    - fetchPublicMapItems(url, { bbox: "1,2,3,4" }) requests a URL containing "bbox=1%2C2%2C3%2C4"; without bbox the URL is unchanged
    - Two overlapping loadPublicMap calls: only the later response updates items
    - loadPublicMap with the same bbox twice: one request; with { force: true }: two
    - A failed load shows fr.status.map text and logs the detail; no URL or error text in the status
    - Existing explorer cases kept; only their asserted strings change
  </behavior>
  <action>Write the new test cases first (in the existing renderHook test), then implement per the interfaces block (D-05, D-06).</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/api/ibp-api|src/hooks/usePublicMapExplorer" && node mobile/scripts/structure-report.js literals src/hooks/usePublicMapExplorer.ts --max 0 && node mobile/scripts/structure-report.js status-ids src/hooks/usePublicMapExplorer.ts --max 0 && test -z "$(grep -rln 'spyOn(React' mobile/src)"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior tests pass; scanners at 0 for the explorer; no React spy left in mobile/src
  </acceptance_criteria>
  <done>Map items can be loaded by viewport without stale or duplicate requests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Debounced value, clustering hook and the supercluster Jest mapper</name>
  <files>mobile/src/hooks/useDebouncedValue.ts, mobile/src/hooks/useDebouncedValue.test.ts, mobile/src/screens/public-map/useMapClusters.ts, mobile/src/screens/public-map/useMapClusters.test.ts, mobile/jest.unit.config.js</files>
  <read_first>
    - mobile/src/app/map-viewport.ts and its test
    - mobile/jest.unit.config.js
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-19-SUMMARY.md (supercluster location)
    - node_modules/supercluster/index.d.ts (or mobile/node_modules/…)
  </read_first>
  <behavior>
    - useDebouncedValue returns the old value before the delay and the new one after it; a change within the delay restarts the timer; unmount clears the timer
    - useMapClusters with 3 items at identical coordinates and a France-wide region returns one cluster of 3; with a region zoomed on them at max zoom, leaves(clusterId) returns the 3 items
    - Changing only the region does not rebuild the index (constructor spy called once); changing items rebuilds it
  </behavior>
  <action>Add the mapper (C-9 path) first so the test can import supercluster, then write the tests and implement both hooks per the interfaces block (D-05). Finish with the mobile suite with coverage, lint, typecheck and format.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/hooks/useDebouncedValue|src/screens/public-map" && grep -c "'^supercluster\$'" mobile/jest.unit.config.js && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "supercluster/dist/supercluster.js" mobile/jest.unit.config.js` returns 1
    - Behavior tests pass; coverage thresholds pass
  </acceptance_criteria>
  <done>Debounce and clustering are ready for the map screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → public API | Viewport sent as a query parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-42 | Denial of Service | Request storms from region changes | mitigate | 400 ms debounce, key dedupe and stale guard, unit-tested |
| T-01.9-43 | Tampering | Query injection through bbox | mitigate | encodeURIComponent on the client; server validation from 01.9-07 |
</threat_model>

<verification>
- Map data-layer tests green; no React spies left; coverage thresholds met
</verification>

<success_criteria>
Criterion 3 data layer: bbox requests, debounce and clustering exist and are tested.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-23-SUMMARY.md` when done
</output>
