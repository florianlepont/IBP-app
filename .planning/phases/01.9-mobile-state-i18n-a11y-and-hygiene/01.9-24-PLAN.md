---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 24
type: execute
wave: 5
depends_on: [01.9-18, 01.9-22]
files_modified:
  - mobile/src/app/AuthenticatedAppNavigation.tsx
  - mobile/src/app/styles.ts
  - mobile/App.tsx
  - mobile/src/navigation/AppNavigation.tsx
  - mobile/src/navigation/types.ts
  - mobile/src/navigation/styles.ts
  - mobile/src/navigation/tab-config.tsx
  - mobile/src/navigation/tabs/NativeRootTabs.tsx
  - mobile/src/navigation/tabs/JsRootTabs.tsx
  - mobile/src/navigation/stacks/HomeStack.tsx
  - mobile/src/navigation/stacks/SurveysStack.tsx
  - mobile/src/navigation/stacks/PublicMapStack.tsx
  - mobile/src/navigation/stacks/AccountStack.tsx
  - mobile/src/navigation/routes/HomeRoute.tsx
  - mobile/src/navigation/routes/SurveyListRoute.tsx
  - mobile/src/navigation/routes/SurveyDetailRoute.tsx
  - mobile/src/navigation/routes/SurveyFormRoute.tsx
  - mobile/src/navigation/routes/FactorDetailRoute.tsx
  - mobile/src/navigation/routes/ParcelSelectionRoute.tsx
  - mobile/src/navigation/routes/PublicMapRoute.tsx
  - mobile/src/navigation/routes/AccountRoute.tsx
  - mobile/src/navigation/routes/SettingsRoute.tsx
  - mobile/src/state/render-counts.test.tsx
autonomous: true
requirements: [REQ-AUD-mobile-state]

must_haves:
  truths:
    - "AuthenticatedAppNavigation.tsx (961 lines) is replaced by mobile/src/navigation/ with no file over 400 lines (D-04)"
    - "Navigation is typed through the global ReactNavigation.RootParamList; there is no `useNavigation() as any` and no `as any` in src/navigation (D-04, criterion 2)"
    - "Screens are mounted with component= route components instead of render callbacks (RESEARCH State of the Art)"
    - "mobile/src/app/styles.ts is deleted; its last live keys live in src/navigation/styles.ts (D-04, hotspot rule: styles.ts is deleted in NAV)"
    - "Behaviour is unchanged: same tabs, stacks, options, listeners, lazy native-tab require; the render harness keeps its after-counts"
  artifacts:
    - path: mobile/src/navigation/types.ts
      provides: "Param lists + global ReactNavigation.RootParamList declaration"
      contains: "namespace ReactNavigation"
    - path: mobile/src/navigation/AppNavigation.tsx
      provides: "NavigationContainer + AppTabs (native or JS)"
      exports: ["AppNavigation"]
  key_links:
    - from: mobile/App.tsx
      to: mobile/src/navigation/AppNavigation.tsx
      via: "import { AppNavigation }"
      pattern: "navigation/AppNavigation"
---

<objective>
Split the 961-line navigation file into `mobile/src/navigation/`, type navigation globally, mount screens with `component=`, and delete `app/styles.ts`.

Purpose: criterion 2 ("no screen file exceeds 400 lines", "navigation is typed (no useNavigation() as any)", "unused style keys gone") and D-04. Pure restructuring: tab behaviour changes (search tab, hiding, fallback reason) are 01.9-25.
Hotspot order for the navigation file: STATE (01.9-09, 01.9-18) → NAV (this plan, then 01.9-25) → I18N/MAP (01.9-25 strings, 01.9-28 map route).
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-RESEARCH.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-18-SUMMARY.md
@CLAUDE.md

<interfaces>
Global typing (RESEARCH Pattern 6, `@react-navigation/core` 7.22 types.d.ts:103-110): in `navigation/types.ts`, `declare global { namespace ReactNavigation { interface RootParamList extends RootTabParamList {} } }` (add an eslint-disable comment for `@typescript-eslint/no-empty-interface` only if the rule fires). Then `useNavigation()` needs no cast and `navigation.navigate("publicMap")` / `navigate("surveys", { screen: "surveyForm" })` are checked. Stack screens type props with `NativeStackScreenProps<SurveysStackParamList, "surveyDetail">` or `CompositeScreenProps<…, BottomTabScreenProps<RootTabParamList>>`. The native tab navigator (`@bottom-tabs/react-navigation`) is not a BottomTabNavigationProp, so rely on the global declaration, not on BottomTab types, for cross-tab navigation.
The one cast (`AuthenticatedAppNavigation.tsx:606-607`, used at `:628` for `onNavigateToExplorer`) goes away.
Keep (`:126-145`, `:936-961`): `isNativeBottomTabViewAvailable()` logic unchanged here (01.9-25 changes it), `getNativeTabNavigator()` lazy `require` (so unsupported runtimes still boot; do not turn it into a static import), one NavigationContainer, AppTabs choosing NativeRootTabs or JsRootTabs, the `console.warn` fallback log (01.9-25 replaces it).
Module layout (Claude's discretion; each file ≤ 400 lines): `AppNavigation.tsx` (container + AppTabs + availability check), `tab-config.tsx` (icons, titles, `jsTabScreenOptions`, listener factories), `tabs/NativeRootTabs.tsx`, `tabs/JsRootTabs.tsx`, `stacks/*Stack.tsx` (stack navigators with `component={XRoute}` and typed options/listeners), `styles.ts` (the live keys from `app/styles.ts`: `tabScreenContainer` and whatever the tree reads; App.tsx's two keys were moved by 01.9-09), `types.ts`. Route components (from 01.9-18) switch from render-callback props to typed screen props (`NativeStackScreenProps<…>`) and stay `memo`.
Harness: `render-counts.test.tsx`'s fake Screen already renders `component=`; edit only the fake navigator if it needs to pass typed props, never `EXPECTED`.
Gates: `node mobile/scripts/structure-report.js long-files src/navigation --max 0`; `grep -rn "as any" mobile/src/navigation` empty; `test ! -e mobile/src/app/styles.ts`; `node mobile/scripts/structure-report.js unused-styles src/navigation --max 0`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split the navigation file into src/navigation with global typing</name>
  <files>mobile/src/app/AuthenticatedAppNavigation.tsx, mobile/src/navigation/AppNavigation.tsx, mobile/src/navigation/types.ts, mobile/src/navigation/tab-config.tsx, mobile/src/navigation/tabs/NativeRootTabs.tsx, mobile/src/navigation/tabs/JsRootTabs.tsx, mobile/src/navigation/stacks/HomeStack.tsx, mobile/src/navigation/stacks/SurveysStack.tsx, mobile/src/navigation/stacks/PublicMapStack.tsx, mobile/src/navigation/stacks/AccountStack.tsx, mobile/App.tsx</files>
  <read_first>
    - mobile/src/app/AuthenticatedAppNavigation.tsx (whole file)
    - mobile/src/navigation/types.ts and mobile/src/navigation/routes/*.tsx (from 01.9-18)
    - node_modules/@react-navigation/core/lib/typescript/src/types.d.ts:80-115 (global RootParamList)
  </read_first>
  <action>Per D-04, move the code into the module layout above without behaviour change, add the global `ReactNavigation.RootParamList` declaration, remove the `useNavigation() as any` cast (typed `useNavigation()` now), and point App.tsx at `AppNavigation`. Delete `AuthenticatedAppNavigation.tsx`.</action>
  <verify>
    <automated>cd /home/user/cortege && test ! -e mobile/src/app/AuthenticatedAppNavigation.tsx && npm --workspace mobile run typecheck && test -z "$(grep -rn 'as any' mobile/src/navigation)" && grep -c "namespace ReactNavigation" mobile/src/navigation/types.ts && node mobile/scripts/structure-report.js long-files src/navigation --max 0</automated>
  </verify>
  <acceptance_criteria>
    - Old file deleted; typecheck green; no `as any` in src/navigation; every navigation file ≤ 400 lines
    - `grep -rn "useNavigation() as" mobile/src` prints nothing
  </acceptance_criteria>
  <done>Navigation lives in its own typed folder.</done>
</task>

<task type="auto">
  <name>Task 2: component= routes, delete app/styles.ts, prove behaviour unchanged</name>
  <files>mobile/src/navigation/routes/HomeRoute.tsx, mobile/src/navigation/routes/SurveyListRoute.tsx, mobile/src/navigation/routes/SurveyDetailRoute.tsx, mobile/src/navigation/routes/SurveyFormRoute.tsx, mobile/src/navigation/routes/FactorDetailRoute.tsx, mobile/src/navigation/routes/ParcelSelectionRoute.tsx, mobile/src/navigation/routes/PublicMapRoute.tsx, mobile/src/navigation/routes/AccountRoute.tsx, mobile/src/navigation/routes/SettingsRoute.tsx, mobile/src/navigation/stacks/HomeStack.tsx, mobile/src/navigation/stacks/SurveysStack.tsx, mobile/src/navigation/stacks/PublicMapStack.tsx, mobile/src/navigation/stacks/AccountStack.tsx, mobile/src/navigation/styles.ts, mobile/src/app/styles.ts, mobile/src/state/render-counts.test.tsx</files>
  <read_first>
    - the files produced by Task 1
    - mobile/src/app/styles.ts (find the keys still read: `grep -rn "from \"./styles\"\|app/styles" mobile/src mobile/App.tsx`)
    - mobile/src/state/render-counts.test.tsx
  </read_first>
  <action>Mount every screen with `component={XRoute}` and type each route's props from the param lists. Move the live keys of `app/styles.ts` to `navigation/styles.ts` and delete `app/styles.ts` (its ~261 dead keys go with it). Run the harness: EXPECTED must pass unchanged (a change means behaviour moved; fix it). Finish with the full mobile suite with coverage, lint, typecheck, format and an expo export for both platforms.</action>
  <verify>
    <automated>cd /home/user/cortege && test ! -e mobile/src/app/styles.ts && node mobile/scripts/structure-report.js unused-styles src/navigation --max 0 && npm --workspace mobile run test:unit -- src/state/render-counts.test.tsx && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && cd mobile && npx expo export --platform ios --output-dir /tmp/expo-export-p19-24-ios >/dev/null && npx expo export --platform android --output-dir /tmp/expo-export-p19-24-android >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `app/styles.ts` deleted; `grep -rn "app/styles" mobile/src mobile/App.tsx` prints nothing
    - `grep -rn "children={\|{({ navigation" mobile/src/navigation/stacks` shows no render-callback screens
    - Harness EXPECTED unchanged; full suite, lint, typecheck, format, both exports green
  </acceptance_criteria>
  <done>The navigation tree is split, typed and mounts routes as components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Structural refactor of navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-44 | Denial of Service | Native module init crash if the lazy native-tab require becomes static (#119 class) | mitigate | getNativeTabNavigator keeps its lazy require (acceptance grep in SUMMARY); CI native-ios build on the PR |
| T-01.9-45 | Tampering | Behaviour change hidden in the split | mitigate | Harness EXPECTED must pass unchanged; both platform exports bundle |
</threat_model>

<verification>
- Old file and app/styles.ts gone; typed navigation; harness unchanged; full suite and exports green
</verification>

<success_criteria>
Criterion 2's navigation items: typed, split under 400 lines, dead global styles removed.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-24-SUMMARY.md` when done
</output>
