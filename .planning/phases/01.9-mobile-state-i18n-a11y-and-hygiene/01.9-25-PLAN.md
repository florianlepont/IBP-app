---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 25
type: execute
wave: 6
depends_on: [01.9-24]
files_modified:
  - mobile/src/navigation/AppNavigation.tsx
  - mobile/src/navigation/types.ts
  - mobile/src/navigation/tab-config.tsx
  - mobile/src/navigation/tabs/NativeRootTabs.tsx
  - mobile/src/navigation/tabs/JsRootTabs.tsx
  - mobile/src/navigation/stacks/SurveysStack.tsx
  - mobile/src/navigation/stacks/AccountStack.tsx
  - mobile/src/navigation/stacks/HomeStack.tsx
  - mobile/src/navigation/stacks/PublicMapStack.tsx
  - mobile/src/navigation/routes/SurveyListRoute.tsx
  - mobile/src/navigation/tab-bar.ts
  - mobile/src/navigation/tab-bar.test.ts
  - mobile/src/navigation/native-tabs-availability.ts
  - mobile/src/navigation/native-tabs-availability.test.ts
  - mobile/src/navigation/tabs.test.tsx
  - mobile/src/screens/SurveyListScreen.tsx
  - mobile/src/i18n/fr/navigation.ts
autonomous: true
requirements: [REQ-AUD-mobile-state, REQ-AUD-i18n-a11y]

must_haves:
  truths:
    - "The separate Recherche tab is gone: 4 tabs (Accueil, Mes Relevés, Explorer, Compte) and one survey stack; search is the native iOS header search bar of Mes Relevés, and the inline search stays on Android and the JS fallback (D-08, C-6)"
    - "One pure, unit-tested rule shouldHideTabBar(focusedRouteName) returns true on surveyParcels; the JS tree uses it in tabBarStyle and the native tree sets tabBarHidden from the focused route, so both trees hide the bar on parcel selection (D-08, D-13, C-3)"
    - "Native-tabs availability returns a reason code (ok, platform, expo-go, env-opt-out) and logs it once; in Release builds the env opt-out is ignored so iPhone Release always uses the native bar, and the JS fallback remains only for Expo Go / unsupported runtimes (D-08, D-13, C-7)"
    - "The cause of the owner's Release JS-bar fallback is investigated from the code and recorded (D-08)"
    - "Tab titles, header titles and the search placeholder come from fr.navigation (D-06)"
  artifacts:
    - path: mobile/src/navigation/tab-bar.ts
      provides: "shouldHideTabBar"
      exports: ["shouldHideTabBar"]
    - path: mobile/src/navigation/native-tabs-availability.ts
      provides: "getNativeTabsAvailability with reason codes"
      exports: ["getNativeTabsAvailability"]
    - path: mobile/src/navigation/tabs.test.tsx
      provides: "Both trees register exactly the 4 tabs and hide the bar on surveyParcels"
  key_links:
    - from: mobile/src/navigation/tabs/NativeRootTabs.tsx
      to: mobile/src/navigation/tab-bar.ts
      via: "tabBarHidden={shouldHideTabBar(focusedRouteName)}"
      pattern: "shouldHideTabBar\\("
    - from: mobile/src/navigation/tabs/JsRootTabs.tsx
      to: mobile/src/navigation/tab-bar.ts
      via: "tabBarStyle display none when shouldHideTabBar"
      pattern: "shouldHideTabBar\\("
---

<objective>
Apply the owner's tab decisions: remove the Recherche tab in favour of the native header search in Mes Relevés, hide the bar on parcel selection in both trees through one shared rule, make the native-tabs choice explicit and logged with Release always native on iPhone, and move navigation text to the catalogue.

Purpose: D-08 and D-13 (owner decisions), C-3, C-6, C-7; criterion 4 for navigation text; the STATE todo "Investigate iOS Release build navigation".
Output: tab-bar rule, availability function, 4-tab trees, tests, French navigation texts. The docs (README-native, US-E2) are updated by 01.9-26 (W6 split).
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-24-SUMMARY.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
C-6 (line numbers refer to the pre-split file; find the moved code in src/navigation): remove `search` from `RootTabParamList`, `IOS_TAB_ICONS`, `ANDROID_TAB_ICONS`, `TAB_TITLES`, `JS_TAB_ICONS`; delete the `NativeTab.Screen name="search"` block; in the surveys stack `searchEntry`/`hasDedicatedSearchTab`/`nativeSearchEnabled` collapse to `useNativeNav && Platform.OS === "ios"`; `visibleSurveys` no longer branches (always the filtered list); `showInlineSearch` stays true on Android and the JS fallback; header search `placement` for Mes Relevés is `"automatic"` (or `"integratedButton"`, whichever shows a visible search field in the header; record the choice); keep the `searchBarRef` sync. `SurveyListScreen` props `useNativeSearchUI`/`showInlineSearch`/`visibleSurveys` are adjusted to the collapsed logic (hotspot: LIST 01.9-22 → NAV this plan → split 01.9-27; change only these props and their uses).
C-3 / D-13: `shouldHideTabBar(focusedRouteName: string | undefined): boolean` returns true only for `"surveyParcels"`. JS tree: the surveys tab options use `tabBarStyle: shouldHideTabBar(getFocusedRouteNameFromRoute(route)) ? { display: "none" } : <normal style>`. Native tree: `@bottom-tabs/react-navigation` 1.4 has no per-screen hide (`tabBarItemHidden` hides an item), but the navigator accepts `tabBarHidden` (`types.d.ts:97`, `TabView.d.ts:209`): track the focused leaf route with `NavigationContainer onStateChange` (walk `state.routes[state.index]` down to the leaf) in AppNavigation and pass `tabBarHidden={shouldHideTabBar(focusedLeaf)}` to `NativeTab.Navigator`. No modal redesign.
C-7 / D-08: `getNativeTabsAvailability({ platformOS, envOptOut, executionEnvironment, appOwnership, isDev }): { native: boolean; reason: "ok" | "platform" | "expo-go" | "env-opt-out" }` with defaults read from `Platform.OS`, `process.env.EXPO_PUBLIC_ENABLE_NATIVE_TABS === "false"`, `Constants.executionEnvironment`, `Constants.appOwnership`, `isDev = __DEV__` (injectable like `app/dev-tools.ts:8`). Order: not iOS → platform; Expo Go (StoreClient or appOwnership "expo") → expo-go; env opt-out AND isDev → env-opt-out; otherwise ok (Release ignores the env opt-out, so "always native in Release" holds). AppNavigation logs once `console.warn("[tabs] native=<bool> reason=<code>")` when native is false, and `console.info` when the env opt-out was ignored in Release. The lazy `getNativeTabNavigator()` require stays.
Investigation (STATE todo, RESEARCH A1): read the git history of the native-tabs code (`git log -p -S EXPO_PUBLIC_ENABLE_NATIVE_TABS`, commit fb22c7b / #119), `mobile/.env.example`, `mobile/README-native.md` and app config; record the most likely cause of the Release JS bar and of "Mes relevés ne marche pas" (e.g. the dedicated search tab reusing the surveys stack) and how this plan removes it. The owner confirms on device in 01.9-31 (D-11).
Catalogue: fill `mobile/src/i18n/fr/navigation.ts` (`navigationFr`: `tabs` {home, surveys, map, account}, `headers` {surveys, detail: "Détail", settings: "Paramètres", …}, `search.placeholder`), and use it in tab-config and stacks.
Tests: `tab-bar.test.ts`, `native-tabs-availability.test.ts` (every branch, including Release ignoring the opt-out), `tabs.test.tsx` (mock both tab libraries with navigators that record `Screen` names and the native navigator's `tabBarHidden` prop; assert names `["home","surveys","publicMap","account"]` in both trees and `tabBarHidden` true when the focused leaf is surveyParcels). Keep the render harness green (it runs the JS tree).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared hide rule and native-tabs availability with reason codes</name>
  <files>mobile/src/navigation/tab-bar.ts, mobile/src/navigation/tab-bar.test.ts, mobile/src/navigation/native-tabs-availability.ts, mobile/src/navigation/native-tabs-availability.test.ts, mobile/src/navigation/AppNavigation.tsx</files>
  <read_first>
    - mobile/src/navigation/AppNavigation.tsx and tab-config.tsx (from 01.9-24)
    - mobile/src/app/dev-tools.ts (injectable isDev pattern)
    - node_modules/@bottom-tabs/react-navigation/lib/typescript/src/types.d.ts (or mobile/node_modules/…)
  </read_first>
  <behavior>
    - shouldHideTabBar("surveyParcels") true; "surveysHome", "surveyDetail", undefined false
    - availability: android → { native:false, reason:"platform" }; iOS + StoreClient → expo-go; iOS + appOwnership "expo" → expo-go; iOS + opt-out + isDev → env-opt-out; iOS + opt-out + !isDev → { native:true, reason:"ok" }; iOS otherwise → ok
  </behavior>
  <action>Write the tests first, then implement both modules per the interfaces block (D-13, C-7, D-08) and use the availability result in AppNavigation with the one-time log. Record the investigation findings in the SUMMARY.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/navigation/(tab-bar|native-tabs-availability)" && npm --workspace mobile run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior bullet passes
    - `grep -c "getNativeTabNavigator" mobile/src/navigation/AppNavigation.tsx` ≥ 1 and it still uses `require(` lazily
  </acceptance_criteria>
  <done>The tab rules are pure, tested and explain their decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four tabs, native header search, identical hiding in both trees, French titles</name>
  <files>mobile/src/navigation/types.ts, mobile/src/navigation/tab-config.tsx, mobile/src/navigation/tabs/NativeRootTabs.tsx, mobile/src/navigation/tabs/JsRootTabs.tsx, mobile/src/navigation/stacks/SurveysStack.tsx, mobile/src/navigation/stacks/AccountStack.tsx, mobile/src/navigation/stacks/HomeStack.tsx, mobile/src/navigation/stacks/PublicMapStack.tsx, mobile/src/navigation/routes/SurveyListRoute.tsx, mobile/src/navigation/AppNavigation.tsx, mobile/src/navigation/tabs.test.tsx, mobile/src/screens/SurveyListScreen.tsx, mobile/src/i18n/fr/navigation.ts</files>
  <read_first>
    - the navigation files from 01.9-24 and Task 1
    - mobile/src/screens/SurveyListScreen.tsx (props useNativeSearchUI, showInlineSearch, visibleSurveys only)
    - mobile/src/state/render-counts.test.tsx (must stay green)
  </read_first>
  <behavior>
    - Both trees register exactly home, surveys, publicMap, account
    - JS tree: surveys tab style is display none when the focused leaf is surveyParcels
    - Native tree: tabBarHidden is true when the focused leaf is surveyParcels and false on surveysHome
    - Mes Relevés on native iOS has headerSearchBarOptions with the catalogue placeholder; on the JS tree showInlineSearch is true
  </behavior>
  <action>Per D-08/C-6 remove the search tab and collapse the surveys-stack search logic; per D-13/C-3 wire shouldHideTabBar in both trees (onStateChange-tracked focused leaf for the native tree); per D-06 read tab and header titles and the search placeholder from `fr/navigation.ts`. Adjust only the three SurveyListScreen search props. Write tabs.test.tsx first.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- "src/navigation|src/state/render-counts" && test -z "$(grep -rn '"search"\|search:' mobile/src/navigation/types.ts)" && node mobile/scripts/structure-report.js literals src/navigation --max 0 && node mobile/scripts/structure-report.js long-files src/navigation --max 0 && cd mobile && npx eslint src/navigation --max-warnings 0 && cd .. && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && cd mobile && npx expo export --platform ios --output-dir /tmp/expo-export-p19-25 >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - No `search` key in RootTabParamList or the tab maps; behavior tests pass; harness green
    - Literal scanner and eslint at 0 for src/navigation
  </acceptance_criteria>
  <done>The app has four tabs with native search and the same hiding rule in both trees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time env → runtime UI choice | EXPO_PUBLIC_ENABLE_NATIVE_TABS baked into the bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-46 | Denial of Service | A leftover dev env flag degrading Release builds | mitigate | Release ignores the opt-out; reason logged; owner removes the leftover line before the D-11 check |
| T-01.9-47 | Repudiation | Silent fallback without a trace | mitigate | Reason code logged once at startup, tested per branch |
</threat_model>

<verification>
- Navigation tests, harness, full suite and export green; gates at 0 for src/navigation
</verification>

<success_criteria>
D-08 and D-13 implemented and tested; navigation text in French.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-25-SUMMARY.md` when done
</output>
