---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 26
type: execute
wave: 7
depends_on: [01.9-06, 01.9-25]
files_modified:
  - mobile/README-native.md
  - docs/specs/epic-e-data-quality-and-trust.md
autonomous: true
requirements: [REQ-AUD-hygiene]

must_haves:
  truths:
    - "mobile/README-native.md explains the native-tabs reason codes (ok, platform, expo-go, env-opt-out), where the log appears, that EXPO_PUBLIC_ENABLE_NATIVE_TABS=false now only applies to dev builds, the shared hide rule and the 4-tab layout (D-08, D-13)"
    - "User story US-E2 says search is the header search bar of Mes Relevés, with no separate tab (D-08)"
  artifacts:
    - path: mobile/README-native.md
      provides: "Native tabs section"
      contains: "env-opt-out"
    - path: docs/specs/epic-e-data-quality-and-trust.md
      provides: "US-E2 updated"
  key_links:
    - from: mobile/README-native.md
      to: mobile/src/navigation/native-tabs-availability.ts
      via: "documents its reason codes"
      pattern: "native-tabs-availability"
---

<objective>
Document the tab changes of 01.9-25 in the native README and the search user story.

Purpose: keeps docs true for D-08/D-13 (split out of the tab plan per the checker's W6). Docs only.
Output: README-native section and US-E2 text.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-25-SUMMARY.md
@CLAUDE.md

<interfaces>
From 01.9-25: `getNativeTabsAvailability` in `mobile/src/navigation/native-tabs-availability.ts` returns reason `ok | platform | expo-go | env-opt-out`; Release ignores the env opt-out; AppNavigation logs `[tabs] native=<bool> reason=<code>` once; `shouldHideTabBar` in `mobile/src/navigation/tab-bar.ts`; tabs Accueil, Mes Relevés, Explorer, Compte; the investigation findings are in 01.9-25-SUMMARY.
README-native is English (section "Native builds in CI" added by 01.9-06); `docs/specs/epic-e-data-quality-and-trust.md:27-37` (US-E2) is French.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README-native native-tabs section</name>
  <files>mobile/README-native.md</files>
  <read_first>
    - mobile/README-native.md
    - mobile/src/navigation/native-tabs-availability.ts, mobile/src/navigation/tab-bar.ts
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-25-SUMMARY.md
  </read_first>
  <action>Add a "Native tabs" section (English): the four reason codes and when each applies, where the one-time log appears (Metro/Xcode console), that `EXPO_PUBLIC_ENABLE_NATIVE_TABS=false` only applies to dev builds (Release on iPhone always uses the native bar, D-08), the shared `shouldHideTabBar` rule (bar hidden on parcel selection in both trees, D-13), the 4-tab layout, and the likely cause of the old Release fallback from 01.9-25's investigation.</action>
  <verify>
    <automated>cd /home/user/cortege && for c in platform expo-go env-opt-out shouldHideTabBar; do grep -q "$c" mobile/README-native.md || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - README-native names the four reason codes, the dev-only opt-out and the hide rule
  </acceptance_criteria>
  <done>Native tab behaviour is documented for the next Release build.</done>
</task>

<task type="auto">
  <name>Task 2: US-E2 search story</name>
  <files>docs/specs/epic-e-data-quality-and-trust.md</files>
  <read_first>
    - docs/specs/epic-e-data-quality-and-trust.md:20-45
  </read_first>
  <action>Update US-E2 in French: search is the header search bar of « Mes Relevés » on iPhone (inline search field on Android), with no separate tab. Keep the acceptance criteria that still hold. Run format check.</action>
  <verify>
    <automated>cd /home/user/cortege && grep -c "Mes Relevés" docs/specs/epic-e-data-quality-and-trust.md && ! sed -n '/US-E2/,/US-E3/p' docs/specs/epic-e-data-quality-and-trust.md | grep -qi "onglet Recherche" && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - US-E2 describes the header search in Mes Relevés and no longer mentions a search tab
  </acceptance_criteria>
  <done>The spec matches the 4-tab app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Documentation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-59 | Repudiation | Undocumented env flag behaviour leading to another silent Release fallback | mitigate | README documents the dev-only opt-out and the logged reason code |
</threat_model>

<verification>
- Both docs updated; format check green
</verification>

<success_criteria>
D-08/D-13 behaviour is documented.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-26-SUMMARY.md` when done
</output>
