---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 26
type: execute
wave: 7
depends_on: [01.9-22, 01.9-24]
files_modified:
  - mobile/src/screens/PublicMapScreen.tsx
  - mobile/src/screens/public-map/MapCanvas.tsx
  - mobile/src/screens/public-map/SurveyMarker.tsx
  - mobile/src/screens/public-map/ClusterMarker.tsx
  - mobile/src/screens/public-map/ClusterListSheet.tsx
  - mobile/src/screens/public-map/SelectedSurveyCard.tsx
  - mobile/src/screens/public-map/MapControls.tsx
  - mobile/src/screens/public-map/useMapViewport.ts
  - mobile/src/screens/public-map/useMapViewport.test.ts
  - mobile/src/screens/public-map/markers.test.tsx
  - mobile/src/screens/public-map/styles.ts
  - mobile/src/navigation/routes/PublicMapRoute.tsx
  - mobile/src/i18n/fr/public-map.ts
  - docs/technical/technical-architecture-v1.md
  - docs/specs/user-stories.md
autonomous: true
requirements: [REQ-AUD-mobile-state, REQ-AUD-i18n-a11y]

must_haves:
  truths:
    - "The map requests items by the visible bbox after region changes settle (about 400 ms debounce), with no load/move/load loop: the camera no longer re-fits on every load, only on the first load or an explicit filter change (D-05, Pitfall 6)"
    - "Markers are clustered with supercluster; SurveyMarker and ClusterMarker are memoised, ClusterMarker uses tracksViewChanges={false}, and marker callbacks take ids (no inline closures per marker) (D-05)"
    - "A cluster tap zooms to its expansion zoom, and at max zoom opens the list of its surveys (Pitfall 7)"
    - "No map file exceeds 400 lines, unused map styles are gone, every string comes from fr.publicMap, and every Pressable has a role and a catalogue label (D-04, D-06, D-07)"
    - "The Explorer tab press still closes the survey detail selection and forces a reload"
  artifacts:
    - path: mobile/src/screens/public-map/useMapViewport.ts
      provides: "Debounced region → bbox → loadPublicMap, first-fit only"
    - path: mobile/src/screens/public-map/markers.test.tsx
      provides: "Memo markers do not re-render on unrelated updates; ids passed to callbacks"
  key_links:
    - from: mobile/src/screens/public-map/useMapViewport.ts
      to: mobile/src/hooks/usePublicMapExplorer.ts
      via: "loadPublicMap({ bbox }) after useDebouncedValue(region, 400)"
      pattern: "loadPublicMap\\("
    - from: mobile/src/screens/public-map/MapCanvas.tsx
      to: mobile/src/screens/public-map/useMapClusters.ts
      via: "clusters for the current region"
      pattern: "useMapClusters\\("
---

<objective>
Rebuild the public map screen on the 01.9-22 data layer: viewport loading with debounce and no auto-refit loop, clustered memoised markers, a split under 400 lines, French text and accessible controls.

Purpose: ROADMAP criterion 3 (bbox, clusters, memoised markers, debounced region changes) and criteria 2/4 for the map screen (the audit counted 5/5 Pressables without role or label). Hotspot: `PublicMapScreen.tsx` is MAP first, split in the same plan.
Output: `screens/public-map/*`, slim `PublicMapScreen.tsx`, route tab-press reload, filled `fr/public-map.ts`, architecture and user-story docs updated.
Layout decision: as in 01.9-11 (entry path/name kept so the harness mock stays valid; props may change here because this plan also owns PublicMapRoute).
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-RESEARCH.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-PATTERNS.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-22-SUMMARY.md
@CLAUDE.md

<interfaces>
Today (`PublicMapScreen.tsx`, 754 lines): `DEFAULT_REGION` (`:36`), auto-refit effect `useEffect(() => { setMapRegion(targetRegion); mapRef.current?.animateToRegion(targetRegion, 520) }, [targetRegion])` (`:130-133`), `onLoadParcelsRef` latest-callback (`:135-137`), parcels 400 ms debounce with key dedupe (`:139-155`), `MapView` with `onRegionChangeComplete={setMapRegion}` (`:203`), inline `screenStyles` (`:199-201`), one `Marker` per item with inline `onPress` (`:218-237`), 2 Alert.alert, 6 Text, 5 Pressables without role/label.
From 01.9-22: `useDebouncedValue(value, ms)` (`mobile/src/hooks/`), `useMapClusters({ items, region })` with `expansionZoom(id)` and `leaves(id)` (`screens/public-map/`), `loadPublicMap({ bbox, force })` with stale guard and key dedupe (`usePublicMapExplorer`). `computeRegionBbox`, `computeRegionZoom` in `mobile/src/app/map-viewport.ts`.
`useMapViewport` (new, testable without MapView): holds `region`, exposes `onRegionChangeComplete(region, details)`, derives `debouncedRegion = useDebouncedValue(region, 400)`, calls `loadPublicMap({ bbox: computeRegionBbox(debouncedRegion) })` when it changes, and exposes `fitOnce(targetRegion)` that animates only on the first items load or after an explicit filter change (`details.isGesture` from react-native-maps 1.27 distinguishes user moves). The parcels loading keeps its existing debounce through the same hook.
Markers: `memo(SurveyMarker)` props `{ id, coordinate, selected, onSelect(id) }`; `memo(ClusterMarker)` props `{ clusterId, coordinate, count, onPress(clusterId) }` with `tracksViewChanges={false}`; accessibility labels from `fr.publicMap.a11y` (count, never ids).
PublicMapRoute (`mobile/src/navigation/routes/PublicMapRoute.tsx`): the tab-press subscription calls `loadPublicMap({ force: true })` after closing the detail selection.
Catalogue: fill `mobile/src/i18n/fr/public-map.ts` (`publicMapFr`: texts, alerts, a11y, cluster list).
Docs: `docs/technical/technical-architecture-v1.md:118` (viewport-driven loading, client clustering), `docs/specs/user-stories.md:36,65-73` (explore map: viewport loading and clustering; anonymisation and rounding unchanged).
Gates at 0 for `src/screens/PublicMapScreen.tsx src/screens/public-map`: `structure-report.js long-files|unused-styles|literals --max 0` and `npx eslint … --max-warnings 0` (includes the D-07 Pressable rule).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Viewport hook and memoised markers</name>
  <files>mobile/src/screens/public-map/useMapViewport.ts, mobile/src/screens/public-map/useMapViewport.test.ts, mobile/src/screens/public-map/SurveyMarker.tsx, mobile/src/screens/public-map/ClusterMarker.tsx, mobile/src/screens/public-map/markers.test.tsx</files>
  <read_first>
    - mobile/src/screens/PublicMapScreen.tsx (whole file)
    - mobile/src/screens/public-map/useMapClusters.ts, mobile/src/hooks/useDebouncedValue.ts, mobile/src/hooks/usePublicMapExplorer.ts
    - mobile/src/app/map-viewport.ts
  </read_first>
  <behavior>
    - Three region changes within 400 ms produce one loadPublicMap call with the last region's bbox (fake timers)
    - A programmatic fit (isGesture false) right after a load does not trigger another load with the same bbox (dedupe) and fitOnce animates only on the first load and after a filter change
    - SurveyMarker re-renders 0 times when its parent re-renders with identical props; onSelect receives the marker id
    - ClusterMarker renders tracksViewChanges false and an accessibility label with the count
  </behavior>
  <action>Write the tests first, then implement `useMapViewport` and the two memo markers per the interfaces block (D-05, Pitfall 6, D-07).</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run test:unit -- src/screens/public-map</automated>
  </verify>
  <acceptance_criteria>
    - Behavior tests pass; `grep -c "memo(" mobile/src/screens/public-map/SurveyMarker.tsx mobile/src/screens/public-map/ClusterMarker.tsx` ≥ 1 each
  </acceptance_criteria>
  <done>Viewport loading and marker memoisation are proven in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Split the map screen onto the new parts, French text and accessibility</name>
  <files>mobile/src/screens/PublicMapScreen.tsx, mobile/src/screens/public-map/MapCanvas.tsx, mobile/src/screens/public-map/ClusterListSheet.tsx, mobile/src/screens/public-map/SelectedSurveyCard.tsx, mobile/src/screens/public-map/MapControls.tsx, mobile/src/screens/public-map/styles.ts, mobile/src/navigation/routes/PublicMapRoute.tsx, mobile/src/i18n/fr/public-map.ts</files>
  <read_first>
    - mobile/src/screens/PublicMapScreen.tsx; mobile/src/navigation/routes/PublicMapRoute.tsx
    - the Task 1 files
  </read_first>
  <action>Rebuild the screen from parts: `MapCanvas` (MapView, clusters from useMapClusters, memo markers, parcel overlay), `ClusterListSheet` (leaves at max zoom), `SelectedSurveyCard` (report action), `MapControls` (filters/recentre), styles next to them (move `screenStyles`). Remove the auto-refit effect in favour of `fitOnce` (D-05, Pitfall 6). Move all text and alerts to `fr/public-map.ts` (D-06) and give every Pressable role and label (D-07). Update PublicMapRoute's tab-press handler to force a reload.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run typecheck && for c in long-files unused-styles literals; do node mobile/scripts/structure-report.js $c src/screens/PublicMapScreen.tsx src/screens/public-map --max 0 || exit 1; done && cd mobile && npx eslint src/screens/PublicMapScreen.tsx src/screens/public-map src/navigation/routes/PublicMapRoute.tsx --max-warnings 0 && cd .. && npm --workspace mobile run test:unit -- "src/screens/public-map|src/state/render-counts|src/hooks/usePublicMapExplorer"</automated>
  </verify>
  <acceptance_criteria>
    - All four per-folder gates at 0 (the D-07 Pressable rule included)
    - `grep -rn "animateToRegion" mobile/src/screens/public-map mobile/src/screens/PublicMapScreen.tsx` appears only inside fitOnce or the explicit recentre control
    - Harness green
  </acceptance_criteria>
  <done>The map screen is split, clustered, debounced, French and accessible.</done>
</task>

<task type="auto">
  <name>Task 3: Architecture and user-story docs, full checks</name>
  <files>docs/technical/technical-architecture-v1.md, docs/specs/user-stories.md</files>
  <read_first>
    - docs/technical/technical-architecture-v1.md:100-130; docs/specs/user-stories.md:30-75
  </read_first>
  <action>Document viewport-driven loading (bbox param, 400 ms debounce, first-fit only), client clustering with supercluster, and that anonymisation/rounding rules are unchanged. Run the full mobile suite with coverage, lint, typecheck, format and both expo exports.</action>
  <verify>
    <automated>cd /home/user/cortege && grep -c "bbox" docs/technical/technical-architecture-v1.md && npm --workspace mobile run test:unit:coverage && npm run lint && npm run typecheck && npm run format:check && cd mobile && npx expo export --platform ios --output-dir /tmp/expo-export-p19-26 >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Both docs describe viewport loading and clustering; full checks green
  </acceptance_criteria>
  <done>Map docs match the new behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public API data → map UI | Anonymised public items rendered as markers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-49 | Denial of Service | Load/move/load loop draining battery and API quota | mitigate | Auto-refit removed except first load/filter change; debounce + dedupe tested |
| T-01.9-50 | Information Disclosure | Survey ids in marker or cluster accessibility labels | mitigate | Labels from catalogue with counts/names only; D-07 lint and literal scanner at 0 |
</threat_model>

<verification>
- Map tests, harness, full suite, exports green; per-folder gates at 0
</verification>

<success_criteria>
ROADMAP criterion 3 complete on mobile; criteria 2 and 4 for the map screen.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-26-SUMMARY.md` when done
</output>
