---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 27
type: execute
wave: 8
depends_on: [01.9-10, 01.9-11, 01.9-13, 01.9-14, 01.9-15, 01.9-16, 01.9-18, 01.9-19, 01.9-20, 01.9-25, 01.9-26]
files_modified:
  - mobile/src/i18n/status.ts
  - mobile/src/state/sync-actions-context.ts
  - mobile/src/state/status-context.ts
  - mobile/src/state/AppStateProvider.tsx
  - mobile/src/hooks/operation-status.ts
  - mobile/src/hooks/useSurveySync.ts
  - mobile/src/hooks/useAuth0Session.ts
  - mobile/src/hooks/useLocalDataOwner.ts
  - mobile/src/hooks/useEditingDraft.ts
  - mobile/src/hooks/useSurveyDraftPatcher.ts
  - mobile/src/hooks/useGpsCapture.ts
  - mobile/src/hooks/usePublicMapExplorer.ts
  - mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts
  - mobile/src/hooks/survey-sync/useSurveySyncProfile.ts
  - mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.ts
  - mobile/src/hooks/survey-sync/useAttachmentPreviews.ts
  - mobile/src/state/render-counts.test.tsx
  - mobile/.eslintrc.json
  - mobile/src/__checks__/structure.test.ts
  - CLAUDE.md
autonomous: true
requirements: [REQ-AUD-i18n-a11y, REQ-AUD-mobile-state, REQ-AUD-hygiene]

must_haves:
  truths:
    - "setStatus, reportStatus and every onStatusChange parameter accept only StatusMessage, so a status can no longer be built outside the catalogue (D-06)"
    - "statusText may only be imported inside src/i18n (lint rule)"
    - "The D-06 text gate and the D-07 Pressable gate are errors, not warnings, and the whole mobile tree passes them (D-06, D-07)"
    - "The structure test asserts exact zeros: 0 unused style keys, 0 files over 400 lines under screens/ and navigation/, 0 user-facing literals outside src/i18n, 0 status id leaks (D-04, D-06)"
    - "CLAUDE.md describes this phase's changes: contexts in src/state, the catalogue in src/i18n, src/navigation, the 4 tabs, SMTP settings removed, no root App.tsx, native CI jobs (the full sweep waits for 01.9-29, D-12)"
  artifacts:
    - path: mobile/.eslintrc.json
      provides: "D-06/D-07 rules at error; statusText import restriction"
      contains: "\"error\""
    - path: mobile/src/__checks__/structure.test.ts
      provides: "Exact-zero assertions"
  key_links:
    - from: mobile/src/state/sync-actions-context.ts
      to: mobile/src/i18n/status.ts
      via: "setStatus(message: StatusMessage)"
      pattern: "StatusMessage"
---

<objective>
Lock the phase's text, accessibility and structure rules: narrow status setters to the catalogue type, turn the lint gates into errors, set every structure ratchet to zero, fix any leftover, and record this phase's facts in CLAUDE.md.

Purpose: turns criteria 2 and 4 from "migrated" into "enforced" (D-04, D-06, D-07), and keeps CLAUDE.md true for what changed in this phase (the final sweep is D-12's 01.9-29).
Output: typed status pipeline, strict ESLint, exact-zero structure test, CLAUDE.md edits.
This plan runs alone in its wave because it edits files owned earlier by many plans.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-03-SUMMARY.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-04-SUMMARY.md
@CLAUDE.md

<interfaces>
Type narrowing: `StatusMessage` from `mobile/src/i18n/status.ts`. Change `setStatus(message: string)` → `(message: StatusMessage)` in useSurveySync and the sync-actions context type, `reportStatus(scope, state, message: StatusMessage)`, `onStatusChange: (message: StatusMessage) => void` in every hook parameter, and the status context value type. `operation-status.ts` stores the message: widen only where it stores, not where it accepts. Any caller that still passes a plain string is a missed migration: fix it with the right catalogue entry (add the entry to the section owned by that caller's plan; every section file is free to edit now). Tests that pass plain strings to typed setters use `statusText("…")` from `src/i18n/status` (tests may import it; the lint restriction exempts `*.test.*`).
Harness: `render-counts.test.tsx` calls the captured `reportStatus("session","idle","x")`; change the third argument to a catalogue value (e.g. `fr.status.sync.ready()`), EXPECTED unchanged.
ESLint (`mobile/.eslintrc.json`, overrides from 01.9-03): set `react/jsx-no-literals` and both `no-restricted-syntax` overrides to "error"; add, for `src/**/*.{ts,tsx}` excluding `src/i18n/**` and tests, a `no-restricted-syntax` entry `ImportSpecifier[imported.name='statusText']` with message "Build status messages in src/i18n only (phase 01.9 D-06)" (merge it into each override's selector list where overrides overlap, since later overrides replace the rule's array).
Structure test: set BASELINE to 0 for the four counts (or assert `toEqual([])` on each finder over `mobile/src` plus `mobile/App.tsx`), and update the comment. D-04's script check: `node mobile/scripts/structure-report.js unused-styles --max 0` must exit 0.
CLAUDE.md phase facts only (D-12 leaves the rest to 01.9-29): Architecture → replace "State management — custom hooks only, no Redux or Context API" with the five contexts in `src/state/` filled by `AppStateProvider` (one `useSurveySync` call) and the per-screen route components in `src/navigation/routes/`; add `src/i18n/` (typed French catalogue, StatusMessage) and `src/navigation/`; mobile key files (`App.tsx` mounts AppStateProvider, `src/state/AppStateProvider.tsx`, `src/navigation/AppNavigation.tsx`, `src/i18n/fr/index.ts`); remove the `SMTP_ENABLED, SMTP_HOST/…` env row; mention the four tabs and the D-08 native-tab rule briefly; CI list gains the two native build jobs; the repo root has no App.tsx and no root runtime dependencies.
Leftovers: run each scanner over the whole tree; any finding in a file no plan owned (e.g. `src/app/auth0-config.ts`, `src/app/dev-tools.ts`, `mobile/App.tsx`) is fixed here with a catalogue entry (list them in the SUMMARY).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow status setters to StatusMessage and fix every missed call</name>
  <files>mobile/src/i18n/status.ts, mobile/src/state/sync-actions-context.ts, mobile/src/state/status-context.ts, mobile/src/state/AppStateProvider.tsx, mobile/src/hooks/operation-status.ts, mobile/src/hooks/useSurveySync.ts, mobile/src/hooks/useAuth0Session.ts, mobile/src/hooks/useLocalDataOwner.ts, mobile/src/hooks/useEditingDraft.ts, mobile/src/hooks/useSurveyDraftPatcher.ts, mobile/src/hooks/useGpsCapture.ts, mobile/src/hooks/usePublicMapExplorer.ts, mobile/src/hooks/survey-sync/useSurveySyncNetwork.ts, mobile/src/hooks/survey-sync/useSurveySyncProfile.ts, mobile/src/hooks/survey-sync/useSurveySyncSurveyOperations.ts, mobile/src/hooks/survey-sync/useAttachmentPreviews.ts, mobile/src/state/render-counts.test.tsx</files>
  <read_first>
    - mobile/src/i18n/status.ts, mobile/src/state/sync-actions-context.ts, mobile/src/hooks/useSurveySync.ts
    - `grep -rn "setStatus\|reportStatus\|onStatusChange" mobile/src --include=*.ts --include=*.tsx` (every signature and call)
  </read_first>
  <action>Per D-06, narrow the types as the interfaces block says, run typecheck, and fix each error at the call site with a catalogue entry (never with a cast). Update tests that break for type reasons only (list them in the SUMMARY) and the harness call.</action>
  <verify>
    <automated>cd /home/user/cortege && npm --workspace mobile run typecheck && test -z "$(grep -rn 'as StatusMessage' mobile/src --include=*.ts --include=*.tsx | grep -v '^mobile/src/i18n/')" && npm --workspace mobile run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - Typecheck green with the narrowed signatures; no `as StatusMessage` cast outside src/i18n
    - `grep -n "message: string" mobile/src/state/sync-actions-context.ts mobile/src/hooks/useSurveySync.ts` shows no status setter taking string
    - Harness green, EXPECTED unchanged
  </acceptance_criteria>
  <done>A status message can only come from the catalogue.</done>
</task>

<task type="auto">
  <name>Task 2: Lint gates to error, structure ratchets to zero, leftovers fixed</name>
  <files>mobile/.eslintrc.json, mobile/src/__checks__/structure.test.ts</files>
  <read_first>
    - mobile/.eslintrc.json, mobile/src/__checks__/structure.test.ts, mobile/scripts/structure-report.js
  </read_first>
  <action>Per D-06/D-07 set the rule levels to error and add the statusText import restriction; per D-04 set the structure assertions to exact zero. Run every scanner on `src` and `App.tsx` and ESLint on the whole mobile tree; fix leftovers (a catalogue entry in the owning section, a role/label, a deleted key, a split) and list each in the SUMMARY. Record the final four counts and the ESLint summary.</action>
  <verify>
    <automated>cd /home/user/cortege && for c in unused-styles long-files literals status-ids; do node mobile/scripts/structure-report.js $c src App.tsx --max 0 || exit 1; done && npm --workspace mobile run test:unit -- src/__checks__ && cd mobile && npx eslint "src/**/*.{ts,tsx}" App.tsx --max-warnings 0 && grep -c "\"warn\"" .eslintrc.json | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - The four scanner counts are 0 over the whole tree; `node mobile/scripts/structure-report.js unused-styles --max 0` exits 0 (D-04 script at 0)
    - ESLint passes with 0 warnings; no D-06/D-07 rule is at warn
  </acceptance_criteria>
  <done>The text, accessibility and size rules are enforced for good.</done>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md facts for this phase</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (Architecture, Key files, Environment variables, CI/CD sections)
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-08-SUMMARY.md, 01.9-17-SUMMARY.md, 01.9-23-SUMMARY.md, 01.9-24-SUMMARY.md, 01.9-05-SUMMARY.md, 01.9-09-SUMMARY.md, 01.9-18-SUMMARY.md (final names)
  </read_first>
  <action>Edit only the facts listed in the interfaces block (English, matching CLAUDE.md's style). Leave versions, routes, local_meta, test conventions, the API module table and ibp-domain for 01.9-29 (D-12). Then run the full local gate (lint, typecheck, unit tests for both workspaces with mobile coverage, format).</action>
  <verify>
    <automated>cd /home/user/cortege && ! grep -n "no Redux or Context API" CLAUDE.md && ! grep -n "SMTP_ENABLED" CLAUDE.md && grep -c "src/state\|src/i18n\|src/navigation" CLAUDE.md && npm run lint && npm run typecheck && npm run test:unit && npm --workspace mobile run test:unit:coverage && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md no longer says "no Redux or Context API" nor lists SMTP variables, and names src/state, src/i18n and src/navigation
    - Full local gate green
  </acceptance_criteria>
  <done>CLAUDE.md is true about this phase's changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer code → user-visible text | Future changes could reintroduce raw text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-51 | Information Disclosure | Regression reintroducing ids or raw errors in status text | mitigate | StatusMessage type + statusText import restriction + status-ids scanner at 0 in the unit suite (CI) |
| T-01.9-52 | Tampering | Gates silently relaxed later | accept | Rules live in versioned config reviewed in PRs; CI runs lint and the structure test |
</threat_model>

<verification>
- Scanners 0, ESLint 0 warnings, typecheck and full local gate green
</verification>

<success_criteria>
Criteria 2 and 4 enforced by type, lint and tests; CLAUDE.md true for this phase.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-27-SUMMARY.md` when done
</output>
