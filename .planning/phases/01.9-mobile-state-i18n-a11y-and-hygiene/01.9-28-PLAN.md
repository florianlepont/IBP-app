---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 28
type: execute
wave: 9
depends_on: [01.9-27]
files_modified:
  - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md
  - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-after.json
  - mobile/jest.unit.config.js
  - api/jest.unit.config.js
autonomous: false
requirements: [REQ-AUD-mobile-state, REQ-AUD-i18n-a11y, REQ-AUD-hygiene]

must_haves:
  truths:
    - "The full local gate is green on the integrated phase: lint, typecheck, format, API and mobile unit suites with coverage (ratchet only raised), full API E2E in local and MinIO modes on ibp_p19_28_test"
    - "01.9-VALIDATION.md records render counts before/after (D-02), the unused-style-key script at 0 and no screen/navigation file over 400 lines (D-04), the 500-survey proof (D-03), the ESLint gates at 0 (D-06, D-07), the autolinking before/after comparison (D-09), and expo-doctor + expo export"
    - "On the phase PR every CI job is green, including Native build — Android and Native build — iOS; run URL, job table and build-success lines are recorded (D-10)"
    - "The owner ran the single device check in French, at most 6 steps (D-11): 4-tab liquid-glass bar in Release, each tab opens, search in Mes Relevés, offline cold start keeps the surveys; the answers are recorded"
    - "After the owner merges, the read-only production probes (bbox and malformed bbox) pass against the deployed API"
  artifacts:
    - path: .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md
      provides: "Phase evidence: local gate, measurements, CI evidence, owner check, post-deploy probe"
      contains: "actions/runs/"
    - path: .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-after.json
      provides: "D-02 after numbers"
  key_links:
    - from: 01.9-VALIDATION.md
      to: "GitHub Actions run of the phase PR"
      via: "run URL and job table including the native build jobs"
      pattern: "Native build"
---

<objective>
Prove the phase: run and record the full local gate and every measurement the criteria ask for, open the phase PR and record its CI (native builds included), give the owner the one short device check in French, and after the merge probe production.

Purpose: all three requirements demonstrated. D-02 (render counts before/after instead of a DevTools profile), D-03 (500 surveys), D-04 (script at 0, 400 lines), D-06/D-07 (lint gates), D-09 (autolinking diff), D-10 (native builds on CI), D-11 (the only owner step besides merging PRs). The D-12 items that wait for 01.8 are in 01.9-29 and are not part of this gate.
Output: completed 01.9-VALIDATION.md, after-counts JSON, coverage ratchet rows, the phase PR.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-05-SUMMARY.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-18-SUMMARY.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-24-SUMMARY.md
@CLAUDE.md

<interfaces>
Local gate (01.7-13 practice, adapted): PostgreSQL 16 (`pg_ctlcluster 16 main start`, role ibp/ibp), database `ibp_p19_28_test`, ACCESS_TOKEN_SECRET unset, every E2E run under `flock /tmp/ibp-e2e.lock`, `ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p19-28`. MinIO mode: start Docker if needed and the pinned `pgsty/minio:RELEASE.2026-08-04T00-00-00Z@sha256:b6bfe7239bfc83fb90d31612d9704d86039dd714f7904b3f1ad68f211e602372` on a free port with the ci.yml e2e-minio env (01.7-VALIDATION "Local gate" row 11 shows the exact command shape); stop and remove the container afterwards.
Coverage ratchet: `npm --workspace mobile run test:unit:coverage` and `npm --workspace api run test:unit:coverage`, then `node scripts/coverage-by-directory.js mobile` / `api`; raise each row to the floor of the measurement, never lower one; rows for `./src/state/`, `./src/i18n/`, `./src/navigation/` exist (C-8); record a before/after table like 01.7-VALIDATION.
Measurements to record in VALIDATION.md:
- Render counts: `RENDER_COUNTS_OUT=<phase dir>/01.9-render-counts-after.json npm --workspace mobile run test:unit -- src/state/render-counts.test.tsx`; table of the four scenarios before (01.9-render-counts-before.json) vs after; state explicitly that this committed test replaces the React DevTools profile of criterion 1 (D-02).
- `node mobile/scripts/structure-report.js unused-styles --max 0` (D-04 script output: `unused-styles: 0`), `long-files --max 0`, `literals src App.tsx --max 0`, `status-ids --max 0`; `npx eslint "src/**/*.{ts,tsx}" App.tsx --max-warnings 0` from mobile/.
- 500 surveys: `npm --workspace mobile run test:unit -- "survey-list-500|surveys.completion"` (D-03, both halves).
- No React spy: `grep -rln "spyOn(React" mobile/src` empty (D-01).
- `grep -rn "useNavigation() as" mobile/src` empty; `grep -rn "as any" mobile/src/navigation` empty.
- Autolinking: re-run the 01.9-18 capture on the final tree and compare `moduleNames` with `01.9-autolinking-before.json` (D-09).
- `cd mobile && npx --yes expo-doctor@1.20.4` (with `EXPO_DOCTOR_SKIP_DEPENDENCY_VERSION_CHECK=1`, as CI) and `npx expo export --platform ios` / `android`.
- Owner-check simulation: run the phases of `scripts/owner-check-simulation.mjs` that apply to a local API (read its header) against the built API in MinIO mode, including the bbox probes added by 01.9-06.
CI (01.7-14 practice): repository github.com/florianlepont/cortege, base main; `gh` may be absent → GitHub MCP tools; if nothing can read Actions runs, raise an access gate asking the orchestrator for the run URL, job conclusions and native build logs. Job names: "Detect changed paths", "Lint, format, typecheck", "Unit tests — API", "Unit tests — Mobile", "E2E tests — API", "E2E tests — API (MinIO mode)", "Mobile build check", "Native build — Android", "Native build — iOS", "Dependency audit", "Docker image check", "CI OK", CodeQL. Build & push is main-only (skipped on the PR).
No model identifiers in code, VALIDATION.md or PR bodies; PR bodies end with the attribution lines from the session's system reminder.
Post-merge probe: after the owner says the PR is merged, poll `https://cortege.algernon.ovh/v1/health` every 30 s for up to 30 minutes (the VPS timer pulls the new image), then `SIM_BASE=https://cortege.algernon.ovh/v1 node scripts/owner-check-simulation.mjs production`; the bbox probe returning 200 with items proves the new image; a 400-for-malformed-bbox probe proves validation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full local gate, measurements and coverage ratchet into VALIDATION.md</name>
  <files>.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md, .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-after.json, mobile/jest.unit.config.js, api/jest.unit.config.js</files>
  <read_first>
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md (skeleton sections)
    - .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md ("Local gate" and "Coverage ratchet" sections: the format to follow)
    - every 01.9-*-SUMMARY.md (deviations to carry into VALIDATION)
  </read_first>
  <action>Run each command of the interfaces block on the integrated phase branch, in order, and record each as a numbered row (command, result, counts, duration) in the "Local gate" section; fill "Measurements" (render counts before/after table with the D-02 statement, D-04 script output, 400-line check, 500-survey tests, ESLint, no React spy, typed navigation greps, autolinking comparison, expo-doctor, exports, simulation results) and the coverage ratchet table; update the per-task map statuses from the SUMMARYs. If a command fails, fix it in the owning plan's files on the branch, re-run, and record the deviation.</action>
  <verify>
    <automated>cd /home/user/cortege && npm run lint && npm run typecheck && npm run format:check && npm --workspace api run test:unit:coverage && npm --workspace mobile run test:unit:coverage && node mobile/scripts/structure-report.js unused-styles --max 0 && node mobile/scripts/structure-report.js long-files --max 0 && test -z "$(grep -rln 'spyOn(React' mobile/src)" && test -s .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-after.json && env -u ACCESS_TOKEN_SECRET POSTGRES_DB=ibp_p19_28_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p19-28 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e && grep -c "unused-styles: 0" .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION.md "Local gate" lists every command with its result, including full E2E in local and MinIO modes
    - "Measurements" holds the before/after render table, `unused-styles: 0`, 0 files over 400 lines, the 500-survey results, ESLint 0, the autolinking comparison and expo-doctor/export results
    - The ratchet table shows no lowered threshold
  </acceptance_criteria>
  <done>The phase is proven locally with every number recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Phase PR and CI evidence, native builds included</name>
  <files>.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md</files>
  <read_first>
    - .github/workflows/ci.yml (job names)
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-05-SUMMARY.md (baseline native run)
  </read_first>
  <action>Push the phase branch and open (or reuse) a PR against main titled "Phase 01.9: mobile state, French catalogue, accessibility and hygiene". Body (English, short): what changes for users (faster app, French everywhere, accessible buttons, 4 tabs with search in Mes Relevés, map loads by area with clusters), what changes for the owner (SMTP lines on the VPS become INFO, one device check), installed apps keep working (the map API change is additive; SQLite migration 2 is additive), then the render-count table, then the French owner checklist of Task 3 under "Contrôle propriétaire (un seul passage sur l'iPhone)", then the attribution lines. Wait for CI and CodeQL (poll up to 150 minutes, the iOS job is long). Require success for every job listed in the interfaces block (Build & push skipped); on failure, diagnose, fix in the owning plan's files, push and re-verify, recording the deviation. Record PR URL, run URL, job table with durations, and quote the gradle "BUILD SUCCESSFUL" and xcodebuild "BUILD SUCCEEDED" lines in "CI Evidence"; compare native job durations with the 01.9-05 baseline.</action>
  <verify>
    <automated>grep -c "actions/runs/" /home/user/cortege/.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md && grep -c "Native build — iOS" /home/user/cortege/.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION.md "CI Evidence" has the PR URL, a run URL, a job table where both native jobs, both E2E jobs, Docker image check and CI OK are success, and the two quoted build lines
  </acceptance_criteria>
  <done>CI proves the phase, native builds included (D-10).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner device check on iPhone (D-11), merge, then production probe</name>
  <files>.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md (CI Evidence)
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-24-SUMMARY.md (tab investigation findings)
  </read_first>
  <action>Present the checklist below with <branche> and <URL de la PR> filled in. Justification to state in one line: CI cannot log in or look at the native tab bar, and the offline cold start (phase 01.5 criterion 7) needs a real phone. Wait for the resume signal. Record each answer in "Owner device check" of VALIDATION.md. If a step fails, fix it on the branch (01.9-24 owns tabs, 01.9-08/17 own state), re-run the affected CI jobs, and ask the owner to repeat only the failed step. Once the owner reports the merge, run the post-merge production probe from the interfaces block and record health, the bbox probe and the malformed-bbox probe in "Post-deploy".</action>
  <what-built>Everything is green on CI, native builds included. Only what CI cannot see remains: the native tab bar after login, and the offline cold start pending since phase 01.5.</what-built>
  <how-to-verify>
    Contrôle sur l'iPhone (6 étapes, environ 15 minutes) :

    1. Sur le Mac, dans le dossier cortege : git fetch origin && git checkout <branche> && git pull && npm install
    2. Lancer grep NATIVE_TABS mobile/.env — si une ligne EXPO_PUBLIC_ENABLE_NATIVE_TABS=false s'affiche, la supprimer du fichier.
    3. Brancher l'iPhone, puis : cd mobile && npx expo run:ios --device --configuration Release
    4. Se connecter. En bas, la barre « verre liquide » montre 4 onglets : Accueil, Mes Relevés, Explorer, Compte. Toucher chaque onglet : chacun s'ouvre.
    5. Dans Mes Relevés, toucher la recherche en haut et taper le nom d'un relevé : la liste se filtre.
    6. Mettre l'iPhone en mode avion, fermer complètement l'app (glisser vers le haut), la rouvrir : vos relevés sont toujours là.

    Si tout est bon, répondre « approuvé », puis fusionner la PR : <URL de la PR>. Sinon, indiquer le numéro de l'étape qui ne va pas et ce que vous voyez.
  </how-to-verify>
  <resume-signal>Type "approuvé" (then say when the PR is merged), or give the failing step number and what you see</resume-signal>
  <verify>
    <automated>V=/home/user/cortege/.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md; ! sed -n "/^## Owner device check/,/^## /p" $V | grep -q "⬜" && ! sed -n "/^## Post-deploy/,/^## /p" $V | grep -q "⬜"</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION.md records the owner's answer for each of the 6 steps and the merge time
    - VALIDATION.md "Post-deploy" records /v1/health 200 and the bbox and malformed-bbox production probes passing
  </acceptance_criteria>
  <done>The owner confirmed the native tabs, search and offline cold start on a Release build; the merged API answers the bbox probes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase branch → production (pull-based deploy on main) | API image with the bbox change and SMTP removal |
| Owner device → evidence | Manual observations recorded as validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-53 | Denial of Service | Deployed API refusing to start after the SMTP schema change | mitigate | Unknown keys ignored (01.9-09 test); image boot smoke in CI; post-deploy health poll and probe |
| T-01.9-54 | Repudiation | Device check claimed without evidence | mitigate | Each step's answer recorded with date in VALIDATION.md; failures re-tested step by step |
| T-01.9-55 | Tampering | Merge with a red native job | mitigate | CI OK requires both native jobs success-or-skipped; Task 2 requires success on the phase PR |
</threat_model>

<verification>
- VALIDATION.md complete: local gate, measurements, CI evidence with native jobs, owner check, post-deploy
</verification>

<success_criteria>
ROADMAP criteria 1-5 proven for phase 01.9; criterion 6 partly (this phase's docs), completed by 01.9-29 after 01.8.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-28-SUMMARY.md` when done
</output>
