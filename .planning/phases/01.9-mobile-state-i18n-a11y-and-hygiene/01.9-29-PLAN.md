---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 29
type: execute
wave: 10
depends_on: [01.9-28]
files_modified:
  - CLAUDE.md
  - docs/technical/technical-architecture-v1.md
  - docs/technical/api-contract-v1.md
  - docs/technical/data-contract-v1.md
  - docs/technical/sync-conflict-resolution-v1.md
  - docs/audits/audit-2026-09-code-complet.md
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - mobile/src/i18n/fr/validation.ts
  - mobile/src/hooks/useSurveyForm.ts
  - mobile/src/hooks/useSurveyForm.test.ts
  - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md
autonomous: false
requirements: [REQ-AUD-hygiene, REQ-AUD-i18n-a11y]

must_haves:
  truths:
    - "This plan starts only after phase 01.8 is complete and merged; a blocking checkpoint enforces it (D-12)"
    - "CLAUDE.md and docs/technical/* match the final code: versions (RN 0.86.3, Expo 57.0.24, react-native-maps 1.27.2, Node >= 22.5), POST /v1/sync and GET /v1/sync/changes, local_meta and SQLite user_version migrations, test conventions, the CI job list, the hooks and contexts, the API module table and packages/ibp-domain (D-12 item 1, criterion 6)"
    - "The audit Statut section links every finding closed by 01.8 and 01.9 to its PR; no row is left 'en attente' (D-12 item 2, criterion 6)"
    - "The French IBP validation messages match the error shape 01.8 introduced (re-keyed if it changed, confirmed unchanged otherwise) (D-12 item 3)"
  artifacts:
    - path: docs/audits/audit-2026-09-code-complet.md
      provides: "Complete Statut table 01.2 → 01.9"
      contains: "## 8. Statut"
    - path: CLAUDE.md
      provides: "Accurate project guide after 01.8 and 01.9"
      contains: "ibp-domain"
  key_links:
    - from: docs/audits/audit-2026-09-code-complet.md
      to: "GitHub PRs of phases 01.8 and 01.9"
      via: "Statut rows"
      pattern: "cortege/pull/"
---

<objective>
Close phase 01.9 after phase 01.8: final documentation sweep, complete audit status links, and the French validation messages aligned with 01.8's error shape.

Purpose: D-12 (the three items that depend on 01.8) and ROADMAP criterion 6. Runs in its own wave, after the phase gate 01.9-28 and after 01.8 is merged. The phase is verified after this plan.
Output: accurate CLAUDE.md and technical docs, refreshed `.planning/codebase/` maps, a complete audit Statut table, validation messages checked or re-keyed, a short PR recorded in VALIDATION.md.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-RESEARCH.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-28-SUMMARY.md
@CLAUDE.md

<interfaces>
Precondition (checked in Task 1): ROADMAP.md marks Phase 01.8 complete; `.planning/phases/01.8-*/01.8-VERIFICATION.md` exists with a passed status; `git log origin/main --merges --oneline` contains the 01.8 PR(s); `packages/ibp-domain/package.json` exists on main. The 01.9 phase PR (01.9-28) is merged.
Docs facts to correct (RESEARCH Summary "Docs" and Pattern 12): RN 0.86.3, Expo 57.0.24, react-native-maps 1.27.2, Node ≥ 22.5 (engines); `POST /v1/sync` and `GET /v1/sync/changes` (not `POST /surveys/sync`); `local_meta` (not `app_metadata`) and the `PRAGMA user_version` migrations (now 2, `payload_completion`); API tests `api/test/*.spec.ts` + `*.e2e-spec.ts` (and the 01.8 split of the idempotency spec), mobile `*.sqlite.test.ts` on node:sqlite; CI jobs `changes`, `check` (lint, format, typecheck, actionlint), `unit-api`, `unit-mobile`, `e2e`, `e2e-minio`, `mobile-build` (expo-doctor, expo export), `native-android`, `native-ios`, `audit`, `image-check`, `ci-ok`, `build`, plus `codeql.yml`; hooks list (add useLocalDataOwner, useAttachmentPreviews, useNearbyParcels, useParcelStatuses, useDebouncedValue) and the contexts; API module table (`config/`, `surveys-sync`, `survey-events`, `surveys.repository`, `parcels`, `public-map`, `storage`); env table (TRUST_PROXY, CADASTRE_*, DEBUG_DATA_RESET_ENABLED, PG_* timeouts; no SMTP); `packages/ibp-domain` and whatever 01.8 added (read its SUMMARYs). Read the code for each fact; do not copy this list blindly.
Audit (D-12 item 2): complete the rows marked "en attente" by 01.9-05 with the 01.8 and 01.9 PR links, using the same git method (merge commits → phase ids).
Validation messages (D-12 item 3): `mobile/src/i18n/fr/validation.ts` (01.9-20) is keyed on today's field and rule names; if 01.8's `validateDraft`/`validateSubmit` return codes or a different field naming, re-key the catalogue to them and make the form read messages by code; otherwise record "unchanged" with the evidence (the 01.8 function signatures).
PR: short English body ending with the attribution lines from the session's system reminder; CI must be green; the owner merges (no device check in this plan).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Blocking gate — run only after phase 01.8 is complete</name>
  <files>(none: precondition check)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 01.8 status)
    - .planning/STATE.md
  </read_first>
  <action>Run the precondition checks from the interfaces block and show their output. If any fails, stop here and report "01.9-29 is blocked until phase 01.8 is complete and merged" to the orchestrator; do not start Task 2. This plan must not run in the same wave as anything else. The orchestrator resumes it when the checks pass; no owner action is needed beyond having merged 01.8's PR.</action>
  <what-built>Nothing yet: this gate protects the D-12 items that depend on phase 01.8 (docs sweep, audit links for 01.8/01.9, validation message keys).</what-built>
  <how-to-verify>
    1. ROADMAP.md shows Phase 01.8 complete and 01.8-VERIFICATION.md reports passed.
    2. origin/main contains packages/ibp-domain and the 01.8 merge commit(s).
    3. The 01.9 phase PR from 01.9-28 is merged.
  </how-to-verify>
  <resume-signal>Type "01.8 merged" when all three checks pass</resume-signal>
  <verify>
    <automated>cd /home/user/cortege && git fetch -q origin main && git show origin/main:packages/ibp-domain/package.json >/dev/null && ls .planning/phases/01.8-*/01.8-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - All three precondition checks pass before Task 2 starts
  </acceptance_criteria>
  <done>Phase 01.8 is merged; the closing work can start.</done>
</task>

<task type="auto">
  <name>Task 2: Final documentation sweep and codebase maps</name>
  <files>CLAUDE.md, docs/technical/technical-architecture-v1.md, docs/technical/api-contract-v1.md, docs/technical/data-contract-v1.md, docs/technical/sync-conflict-resolution-v1.md, .planning/codebase/STACK.md, .planning/codebase/STRUCTURE.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/INTEGRATIONS.md</files>
  <read_first>
    - CLAUDE.md and the four docs/technical files
    - package.json (engines), mobile/package.json, api/package.json, packages/ibp-domain/package.json
    - api/src/**/*.module.ts and controllers (routes), mobile/src/storage/db.ts, .github/workflows/ci.yml
    - .planning/phases/01.8-*/01.8-*-SUMMARY.md
  </read_first>
  <action>Per D-12 item 1, check every fact of the interfaces list against the code and correct CLAUDE.md, the four technical docs and the four `.planning/codebase` maps (remove SMTP/nodemailer and the old structure there). Keep each file's language and style. Record in the SUMMARY a fact table (fact, where checked, file:line fixed).</action>
  <verify>
    <automated>cd /home/user/cortege && ! grep -n "POST /surveys/sync\|app_metadata\|Node.js 20+\|React Native 0.81\|Expo 54\|Maps 1.20\|no Redux or Context API" CLAUDE.md docs/technical/*.md && grep -c "ibp-domain" CLAUDE.md && grep -c "local_meta" CLAUDE.md && grep -c "/v1/sync" CLAUDE.md && ! grep -rn "nodemailer\|SMTP_" .planning/codebase/*.md && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - None of the stale facts remain (verify command); CLAUDE.md names ibp-domain, local_meta and /v1/sync
    - The SUMMARY fact table lists each correction with file:line
  </acceptance_criteria>
  <done>Docs describe what is actually there (criterion 6, docs half).</done>
</task>

<task type="auto">
  <name>Task 3: Audit status links, validation message keys, PR and VALIDATION</name>
  <files>docs/audits/audit-2026-09-code-complet.md, mobile/src/i18n/fr/validation.ts, mobile/src/hooks/useSurveyForm.ts, mobile/src/hooks/useSurveyForm.test.ts, .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md</files>
  <read_first>
    - docs/audits/audit-2026-09-code-complet.md (section 8)
    - mobile/src/i18n/fr/validation.ts, mobile/src/hooks/useSurveyForm.ts
    - packages/ibp-domain validateDraft/validateSubmit signatures
  </read_first>
  <action>Per D-12 item 2 fill every "en attente" row with its PR link. Per D-12 item 3 compare 01.8's validation error shape with `fr/validation.ts`: re-key and adapt useSurveyForm (+ test) if it changed, or record "unchanged" with evidence. Run the mobile suite, lint, typecheck, format. Open a PR "Phase 01.9 closing: docs, audit status, validation messages", wait for CI, record the PR, run URL and job table in a "Closing plan (01.9-29)" section of VALIDATION.md, and ask the owner to merge it.</action>
  <verify>
    <automated>cd /home/user/cortege && ! grep -n "en attente\|à vérifier" docs/audits/audit-2026-09-code-complet.md && npm --workspace mobile run test:unit -- "src/hooks/useSurveyForm|src/i18n" && npm run lint && npm run typecheck && npm run format:check && grep -c "Closing plan (01.9-29)" .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - No "en attente" or "à vérifier" row remains; every finding links a PR
    - Validation messages re-keyed or confirmed unchanged with evidence; tests green
    - VALIDATION.md records the closing PR's CI run
  </acceptance_criteria>
  <done>Every audit finding links to the PR that closed it, and phase 01.9 is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → future contributors and AI agents | CLAUDE.md drives future changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-56 | Tampering | Stale docs leading future work to wrong routes/tables (e.g. writing to app_metadata) | mitigate | Every fact checked against code; grep gate for the known stale facts |
| T-01.9-57 | Repudiation | Audit findings marked closed without traceable PR | mitigate | PR links derived from merge commits; no "en attente" or unverified row left |
</threat_model>

<verification>
- Precondition passed; stale-fact greps empty; audit table complete; tests and CI green
</verification>

<success_criteria>
ROADMAP criterion 6 and D-12 complete; phase 01.9 ready for verification.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-29-SUMMARY.md` when done
</output>
