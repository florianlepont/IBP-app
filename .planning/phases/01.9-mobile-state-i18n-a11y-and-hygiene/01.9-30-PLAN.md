---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 30
type: execute
wave: 7
depends_on: [01.9-06, 01.9-09, 01.9-10, 01.9-18, 01.9-19, 01.9-24, 01.9-25]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [REQ-AUD-hygiene]

must_haves:
  truths:
    - "CLAUDE.md describes this phase's changes: the five contexts in src/state filled by AppStateProvider (one useSurveySync call), per-screen route components in src/navigation/routes, the typed French catalogue in src/i18n, src/navigation, the 4 tabs and the D-08 native-tab rule, SMTP settings removed, no root App.tsx or root runtime dependencies, and the two native CI build jobs"
    - "CLAUDE.md no longer says 'no Redux or Context API' and lists no SMTP variable"
    - "Facts outside this phase (versions, /v1/sync, local_meta, test conventions, API module table, ibp-domain) are left for the final sweep in 01.9-32 (D-12)"
  artifacts:
    - path: CLAUDE.md
      provides: "Phase 01.9 facts"
      contains: "src/state"
  key_links:
    - from: CLAUDE.md
      to: mobile/src/state/AppStateProvider.tsx
      via: "Key files table"
      pattern: "AppStateProvider"
---

<objective>
Record this phase's facts in CLAUDE.md, in the same PR as the code (RESEARCH "two passes"; split out of the strict-gates plan per the checker's W6).

Purpose: REQ-AUD-hygiene, criterion 6 for what this phase changed. The full sweep that depends on 01.8 is 01.9-32 (D-12).
Output: CLAUDE.md edits only.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@CLAUDE.md

<interfaces>
Facts to write (read the final names in the SUMMARYs of 01.9-06, 01.9-09, 01.9-10, 01.9-18, 01.9-19, 01.9-24, 01.9-25 first):
- Architecture → Mobile: replace "State management — custom hooks only, no Redux or Context API" with the five contexts in `mobile/src/state/` (session, status, sync actions, surveys, form) filled by `AppStateProvider`, which calls `useSurveySync` once (01.5 single-sync guarantees), plus memoised route components in `mobile/src/navigation/routes/` that read only the contexts their screen shows; add `mobile/src/i18n/` (typed French catalogue, `StatusMessage`) and `mobile/src/navigation/` (typed with the global `ReactNavigation.RootParamList`).
- Key files: `mobile/App.tsx` (mounts AppStateProvider and the overlays), `mobile/src/state/AppStateProvider.tsx`, `mobile/src/navigation/AppNavigation.tsx`, `mobile/src/i18n/fr/index.ts`; remove `app/styles.ts` and `AuthenticatedAppNavigation` if listed.
- Tabs: 4 tabs (Accueil, Mes Relevés, Explorer, Compte), search in the Mes Relevés header; native iOS bar always in Release, JS bar on Android and Expo Go.
- Environment variables: remove the `SMTP_ENABLED, SMTP_HOST/PORT/USER/PASSWORD/FROM` row.
- CI/CD: add the `native-android` and `native-ios` jobs (path-filtered, wired into CI OK).
- Repo layout: no root `App.tsx`, no root runtime dependencies (root keeps workspaces and overrides).
Style: English, the file's existing table and bullet style. Do not touch facts reserved for 01.9-32.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Architecture, key files and tabs</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (Architecture and Key files sections)
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-09-SUMMARY.md, 01.9-18-SUMMARY.md, 01.9-24-SUMMARY.md, 01.9-25-SUMMARY.md
  </read_first>
  <action>Write the architecture, key-file and tab facts from the interfaces block.</action>
  <verify>
    <automated>cd /home/user/cortege && ! grep -n "no Redux or Context API" CLAUDE.md && grep -c "src/state" CLAUDE.md && grep -c "src/i18n" CLAUDE.md && grep -c "src/navigation" CLAUDE.md && grep -c "AppStateProvider" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - The four greps find the new facts; the "no Context API" sentence is gone
  </acceptance_criteria>
  <done>CLAUDE.md describes the new mobile architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Environment, CI and repository layout</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (Environment variables, CI/CD sections)
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-06-SUMMARY.md, 01.9-10-SUMMARY.md, 01.9-19-SUMMARY.md
  </read_first>
  <action>Remove the SMTP row, add the two native CI jobs, and state that the root has no App.tsx or runtime dependencies. Run format check.</action>
  <verify>
    <automated>cd /home/user/cortege && ! grep -n "SMTP_ENABLED" CLAUDE.md && grep -c "native-ios\|native-android" CLAUDE.md && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - No SMTP variable in CLAUDE.md; both native jobs named
  </acceptance_criteria>
  <done>CLAUDE.md is true for this phase's changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → future contributors and agents | CLAUDE.md drives future changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-60 | Tampering | A future agent re-adding SMTP config or a second assembler call because the guide is stale | mitigate | CLAUDE.md states the single-assembler rule and the SMTP removal |
</threat_model>

<verification>
- Greps pass; format check green
</verification>

<success_criteria>
Criterion 6 for this phase's own changes.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-30-SUMMARY.md` when done
</output>
