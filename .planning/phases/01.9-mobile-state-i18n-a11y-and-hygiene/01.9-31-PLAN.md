---
phase: 01.9-mobile-state-i18n-a11y-and-hygiene
plan: 31
type: execute
wave: 9
depends_on: [01.9-26, 01.9-29, 01.9-30]
files_modified:
  - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md
  - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-after.json
  - mobile/jest.unit.config.js
  - api/jest.unit.config.js
autonomous: false
requirements: [REQ-AUD-mobile-state, REQ-AUD-i18n-a11y, REQ-AUD-hygiene]

must_haves:
  truths:
    - "The full local gate is green on the integrated phase: lint, typecheck, format, API and mobile unit suites with coverage (ratchet only raised, the './App.tsx' row included), full API E2E in local and MinIO modes on ibp_p19_31_test"
    - "01.9-VALIDATION.md records render counts before/after for all five scenarios (D-02), the unused-style-key script at 0 and no screen/navigation file over 400 lines (D-04), the 500-survey proof (D-03), the ESLint gates at 0 (D-06, D-07), the autolinking comparison (D-09), expo-doctor and expo export"
    - "VALIDATION.md has a criterion → substitute-evidence table: D-02 render counts replace the React DevTools profile of criterion 1; for criterion 5's 'unused tab library', both libraries are kept by owner decision D-08 with 01.9-19's evidence; native builds are proven on CI (D-10) instead of on a device (W8)"
    - "PR #158's CI is green on every job, including Native build — Android and Native build — iOS; the orchestrator supplies the run evidence and the executor records it (D-10, B2). Executors never push, open PRs or poll CI"
    - "After PR #158 is merged and the VPS has deployed, the read-only production probes (bbox and malformed bbox) pass, and the owner runs the single device check from main against production, in French, in at most 6 steps (D-11, W4)"
  artifacts:
    - path: .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md
      provides: "Phase evidence: local gate, measurements, substitute evidence, CI evidence, post-deploy, owner check"
      contains: "actions/runs/"
    - path: .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-after.json
      provides: "D-02 after numbers"
  key_links:
    - from: 01.9-VALIDATION.md
      to: "GitHub Actions run of PR #158"
      via: "orchestrator-supplied run URL and job table including the native build jobs"
      pattern: "Native build"
---

<objective>
Prove the phase: run and record the full local gate and every measurement, record PR #158's CI evidence supplied by the orchestrator (native builds included), and after the merge and the VPS deploy, probe production and run the owner's short device check from main.

Purpose: all three requirements demonstrated. D-02 (render counts instead of a DevTools profile), D-03 (500 surveys), D-04 (script at 0, 400 lines), D-06/D-07 (lint gates), D-09 (autolinking), D-10 (native builds on CI), D-11 (the only owner step besides merging PRs). The D-12 items that wait for 01.8 are in 01.9-32.
Output: completed 01.9-VALIDATION.md, after-counts JSON, raised coverage rows, a PR #158 body draft in the SUMMARY.
Executors never push, open or edit PRs, or poll CI (B2): the orchestrator does, and hands evidence back through the checkpoints.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-CONTEXT.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md
@.planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-06-SUMMARY.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-19-SUMMARY.md
@.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-25-SUMMARY.md
@CLAUDE.md

<interfaces>
Local gate (01.7-13 practice): PostgreSQL 16 (`pg_ctlcluster 16 main start`, role ibp/ibp), database `ibp_p19_31_test`, ACCESS_TOKEN_SECRET unset, every E2E run under `flock /tmp/ibp-e2e.lock`, `ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p19-31`. MinIO mode: start Docker if needed and the pinned `pgsty/minio:RELEASE.2026-08-04T00-00-00Z@sha256:b6bfe7239bfc83fb90d31612d9704d86039dd714f7904b3f1ad68f211e602372` on a free port with the ci.yml e2e-minio env (01.7-VALIDATION "Local gate" row 11 has the command shape); stop and remove the container afterwards.
Coverage ratchet: `npm --workspace mobile run test:unit:coverage` and `npm --workspace api run test:unit:coverage`, then `node scripts/coverage-by-directory.js mobile` / `api`; raise each row to the floor of the measurement, never lower one. Rows that must exist: `./App.tsx` (01.9-01, raised here, B5), `./src/state/`, `./src/i18n/` (01.9-09), `./src/navigation/` (01.9-18). Record a before/after table like 01.7-VALIDATION.
Measurements for VALIDATION.md:
- Render counts: `RENDER_COUNTS_OUT=<phase dir>/01.9-render-counts-after.json npm --workspace mobile run test:unit -- src/state/render-counts.test.tsx`; five scenarios before vs after.
- `node mobile/scripts/structure-report.js unused-styles --max 0` (script output line `unused-styles: 0`), `long-files --max 0`, `literals src App.tsx --max 0` (object labels included), `status-ids --max 0`; `npx eslint "src/**/*.{ts,tsx}" App.tsx --max-warnings 0` from mobile/.
- 500 surveys: `npm --workspace mobile run test:unit -- "survey-list-500|surveys.completion"` (D-03).
- `grep -rln "spyOn(React" mobile/src` empty (D-01); `grep -rn "useNavigation() as" mobile/src` and `grep -rn "as any" mobile/src/navigation` empty.
- Autolinking: re-run the 01.9-19 capture on the final tree, compare `moduleNames` with `01.9-autolinking-before.json` (D-09).
- `cd mobile && EXPO_DOCTOR_SKIP_DEPENDENCY_VERSION_CHECK=1 npx --yes expo-doctor@1.20.4`, `npx expo export --platform ios` and `android`.
- Owner-check simulation: the phases of `scripts/owner-check-simulation.mjs` that apply to a local API (read its header), against the built API in MinIO mode, including the bbox probes from 01.9-07.
Substitute-evidence table (W8), in a VALIDATION section "Criterion evidence": criterion 1 "React DevTools profile before/after" → committed render-count harness before/after (D-02); criterion 2 "stays fluid with 500 surveys" → 500-survey render test + no-parse storage test (D-03); criterion 2 "158 unused style keys gone" → script reports 0 (research re-measured 301) (D-04); criterion 5 "unused tab library removed" → neither library is unused; both kept by owner decision D-08, evidence in 01.9-19-SUMMARY (usage and autolinking); criterion 5 "native builds still passing" → CI jobs on PR #158 (D-10) plus the owner's Release build (D-11); criterion 6 → split between 01.9-30 (this phase's facts) and 01.9-32 (after 01.8, D-12).
PR and CI (B2): the phase branch is `claude/code-audit-complete-3sn99m`, PR #158. The orchestrator pushes it, updates PR #158's body from the draft in this plan's SUMMARY, waits for CI and CodeQL, and supplies the run URL, the conclusion of every job and the gradle `BUILD SUCCESSFUL` / xcodebuild `BUILD SUCCEEDED` lines. Job names: "Detect changed paths", "Lint, format, typecheck", "Unit tests — API", "Unit tests — Mobile", "E2E tests — API", "E2E tests — API (MinIO mode)", "Mobile build check", "Native build — Android", "Native build — iOS", "Dependency audit", "Docker image check", "CI OK", CodeQL; "Build & push" is main-only. No model identifiers and no commit attribution trailers in code, VALIDATION.md or the PR body draft.
Post-deploy (W4, after the owner merges PR #158): the VPS timer pulls the new image; poll `https://cortege.algernon.ovh/v1/health` every 30 s for up to 30 minutes, then `SIM_BASE=https://cortege.algernon.ovh/v1 node scripts/owner-check-simulation.mjs production`; the bbox probe returning 200 with items proves the new image, the malformed-bbox 400 proves validation. The owner's device check then runs from main against production.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full local gate, measurements, substitute evidence and coverage ratchet into VALIDATION.md; PR body draft</name>
  <files>.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md, .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-after.json, mobile/jest.unit.config.js, api/jest.unit.config.js</files>
  <read_first>
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md (skeleton sections)
    - .planning/phases/01.7-api-config-service-split-and-db-tuning/01.7-VALIDATION.md ("Local gate" and "Coverage ratchet" sections)
    - every 01.9-*-SUMMARY.md (deviations to carry into VALIDATION)
  </read_first>
  <action>Run each command of the interfaces block on the integrated phase branch, in order, and record each as a numbered row (command, result, counts, duration) in "Local gate"; fill "Measurements", "Criterion evidence" (W8 table) and the coverage ratchet table (raise `./App.tsx` and every other row to its measured floor, never lower one); update the per-task map statuses from the SUMMARYs. If a command fails, fix it in the owning plan's files on the branch, re-run and record the deviation. Commit locally. Then write the PR #158 body draft in this plan's SUMMARY (English, short): what changes for users (faster app, French everywhere, accessible buttons, 4 tabs with search in Mes Relevés, map loads by area with clusters), for the owner (leftover SMTP lines on the VPS become INFO; one device check after the deploy), compatibility (map API change additive, SQLite migration 2 additive), the render-count table, and the French owner checklist of Task 3. No attribution trailers in it.</action>
  <verify>
    <automated>cd /home/user/cortege && npm run lint && npm run typecheck && npm run format:check && npm --workspace api run test:unit:coverage && npm --workspace mobile run test:unit:coverage && node mobile/scripts/structure-report.js unused-styles --max 0 && node mobile/scripts/structure-report.js long-files --max 0 && test -z "$(grep -rln 'spyOn(React' mobile/src)" && test -s .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-render-counts-after.json && env -u ACCESS_TOKEN_SECRET POSTGRES_DB=ibp_p19_31_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p19-31 flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e && grep -c "unused-styles: 0" .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md && ! sed -n "/^## Criterion evidence/,/^## /p" .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md | grep -q "⬜"</automated>
  </verify>
  <acceptance_criteria>
    - "Local gate" lists every command with its result, including full E2E in local and MinIO modes
    - "Measurements" holds the five-scenario render table, `unused-styles: 0`, 0 files over 400 lines, the 500-survey results, ESLint 0, the autolinking comparison and expo-doctor/export results
    - "Criterion evidence" is filled (no pending cell); the ratchet table shows no lowered threshold and a raised or equal `./App.tsx` row
    - The SUMMARY holds the PR #158 body draft with the French checklist
  </acceptance_criteria>
  <done>The phase is proven locally with every number recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PR #158 CI evidence (orchestrator), then the owner merges</name>
  <files>.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-06-SUMMARY.md (baseline native run)
  </read_first>
  <action>Stop at this checkpoint and ask the orchestrator to push `claude/code-audit-complete-3sn99m`, update PR #158's body from the draft in this plan's SUMMARY, and supply the CI evidence. Record the PR URL, the run URL, the job table with durations and the two quoted build lines in "CI Evidence", and compare the native job durations with the 01.9-06 baseline. If a job failed, read the log the orchestrator supplies, fix it in the owning plan's files, commit locally, and return to this checkpoint (the orchestrator re-pushes). When every job is green, the owner merges PR #158; wait for the orchestrator to confirm the merge.</action>
  <what-built>The integrated phase, committed locally, with a full local gate and a PR body draft.</what-built>
  <how-to-verify>
    1. Orchestrator: push `claude/code-audit-complete-3sn99m` and update PR #158's body from the SUMMARY draft.
    2. When CI and CodeQL finish, supply the run URL, every job's conclusion (both "Native build" jobs included) and the gradle/xcodebuild success lines, or the failing log.
    3. When everything is green, the owner merges PR #158; confirm the merge time.
  </how-to-verify>
  <resume-signal>Provide the run URL, job conclusions and build lines (or the failing log), then "merged at <time>"</resume-signal>
  <verify>
    <automated>grep -c "actions/runs/" /home/user/cortege/.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md && grep -c "Native build — iOS" /home/user/cortege/.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - "CI Evidence" has the PR URL, a run URL, a job table where both native jobs, both E2E jobs, Docker image check and CI OK are success, the two quoted build lines, and the merge time
  </acceptance_criteria>
  <done>CI proves the phase, native builds included (D-10), and PR #158 is merged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Production probe after the deploy, then the owner device check from main (D-11)</name>
  <files>.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-25-SUMMARY.md (tab investigation findings)
  </read_first>
  <action>First run the post-deploy checks of the interfaces block (health poll, production simulation with the bbox and malformed-bbox probes; read-only) and record them in "Post-deploy". Only when the bbox probe shows the new image is live, present the checklist below. Justification to state in one line: CI cannot log in or look at the native tab bar, and the offline cold start (phase 01.5 criterion 7) needs a real phone. Record each answer in "Owner device check". If a step fails, fix it locally on a branch from main (01.9-25 owns tabs, 01.9-09/18 own state), commit locally, and tell the orchestrator, which pushes a follow-up PR; after its merge and deploy, ask the owner to repeat only the failed step.</action>
  <what-built>PR #158 is merged and deployed; production answers the new map query. Only what CI cannot see remains.</what-built>
  <how-to-verify>
    Contrôle sur l'iPhone, après la mise en production (6 étapes, environ 15 minutes) :

    1. Sur le Mac, dans le dossier cortege : git checkout main && git pull && npm install
    2. Lancer grep NATIVE_TABS mobile/.env — si une ligne EXPO_PUBLIC_ENABLE_NATIVE_TABS=false s'affiche, la supprimer du fichier.
    3. Brancher l'iPhone, puis : cd mobile && npx expo run:ios --device --configuration Release
    4. Se connecter. En bas, la barre « verre liquide » montre 4 onglets : Accueil, Mes Relevés, Explorer, Compte. Toucher chaque onglet : chacun s'ouvre, et la carte d'Explorer affiche des relevés.
    5. Dans Mes Relevés, toucher la recherche en haut et taper le nom d'un relevé : la liste se filtre.
    6. Mettre l'iPhone en mode avion, fermer complètement l'app (glisser vers le haut), la rouvrir : vos relevés sont toujours là.

    Si tout est bon, répondre « approuvé ». Sinon, indiquer le numéro de l'étape qui ne va pas et ce que vous voyez.
  </how-to-verify>
  <resume-signal>Type "approuvé", or give the failing step number and what you see</resume-signal>
  <verify>
    <automated>V=/home/user/cortege/.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-VALIDATION.md; ! sed -n "/^## Owner device check/,/^## /p" $V | grep -q "⬜" && ! sed -n "/^## Post-deploy/,/^## /p" $V | grep -q "⬜"</automated>
  </verify>
  <acceptance_criteria>
    - "Post-deploy" records /v1/health 200 and the bbox and malformed-bbox production probes passing
    - "Owner device check" records the owner's answer for each of the 6 steps
  </acceptance_criteria>
  <done>Production serves the phase, and the owner confirmed the native tabs, search and offline cold start on a Release build from main.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase branch → production (pull-based deploy on main) | API image with the bbox change and SMTP removal |
| Owner device → evidence | Manual observations recorded as validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-53 | Denial of Service | Deployed API refusing to start after the SMTP schema change | mitigate | Unknown keys ignored (01.9-10 test); image boot smoke in CI; post-deploy health poll and probe before the device check |
| T-01.9-54 | Repudiation | Device check claimed without evidence | mitigate | Each step's answer recorded in VALIDATION.md; failures re-tested step by step |
| T-01.9-55 | Tampering | Merge with a red native job | mitigate | CI OK requires both native jobs success-or-skipped; Task 2 requires success before the owner merges |
</threat_model>

<verification>
- VALIDATION.md complete: local gate, measurements, criterion evidence, CI evidence with native jobs, post-deploy, owner check
</verification>

<success_criteria>
ROADMAP criteria 1-5 proven for phase 01.9; criterion 6 for this phase's facts (01.9-30), completed by 01.9-32 after 01.8.
</success_criteria>

<output>
Create `.planning/phases/01.9-mobile-state-i18n-a11y-and-hygiene/01.9-31-SUMMARY.md` when done
</output>
